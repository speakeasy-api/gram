// Code generated by goa v3.25.3, DO NOT EDIT.
//
// emaResources service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/dev-idp/design -o .

package emaresources

import (
	"context"
)

// Cross-app access resource apps. Each is one resource authorization server
// mounted at /resource-as/<slug>. Permanently unauthenticated.
type Service interface {
	// Register a resource. Its authorization server becomes reachable immediately.
	Create(context.Context, *CreatePayload) (res *EmaResource, err error)
	// Patch a resource. Changing the slug moves its authorization server, and
	// invalidates any ID-JAG already minted for the old issuer.
	Update(context.Context, *UpdatePayload) (res *EmaResource, err error)
	// List resources.
	List(context.Context, *ListPayload) (res *ListEmaResourcesResult, err error)
	// Hard-delete a resource. Cascades to its trust rules, assignments, and issued
	// tokens.
	Delete(context.Context, *DeletePayload) (err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram-dev-idp"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "emaResources"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [4]string{"create", "update", "list", "delete"}

// CreatePayload is the payload type of the emaResources service create method.
type CreatePayload struct {
	// Path segment for the authorization server. Must be unique.
	Slug string
	// Display name; defaults to the slug.
	Name *string
	// The MCP server URL this resource guards.
	ResourceIdentifier string
}

// DeletePayload is the payload type of the emaResources service delete method.
type DeletePayload struct {
	// Resource UUID.
	ID string
}

// EmaResource is the result type of the emaResources service create method.
type EmaResource struct {
	// Resource UUID.
	ID string
	// Path segment this resource's authorization server is served at (unique).
	Slug string
	// Display name.
	Name string
	// The MCP server URL this resource guards; lands in an ID-JAG's `resource`
	// claim.
	ResourceIdentifier string
	// Issuer identifier of this resource's authorization server. Derived from the
	// slug; the value an ID-JAG must carry in `aud`.
	Issuer    string
	CreatedAt string
	UpdatedAt string
}

// ListEmaResourcesResult is the result type of the emaResources service list
// method.
type ListEmaResourcesResult struct {
	// Resources on this page.
	Items []*EmaResource
	// Cursor for the next page, empty when exhausted.
	NextCursor string
}

// ListPayload is the payload type of the emaResources service list method.
type ListPayload struct {
	// Opaque cursor returned by a previous list call.
	Cursor *string
	// Maximum items to return.
	Limit int
}

// UpdatePayload is the payload type of the emaResources service update method.
type UpdatePayload struct {
	// Resource UUID.
	ID string
	// Path segment.
	Slug *string
	// Display name.
	Name *string
	// The MCP server URL this resource guards.
	ResourceIdentifier *string
}
