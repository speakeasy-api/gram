// Code generated by goa v3.25.3, DO NOT EDIT.
//
// memberships service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/dev-idp/design -o .

package memberships

import (
	"context"
)

// Dev-idp memberships CRUD. Idempotent on (user_id, organization_id) for
// create. Permanently unauthenticated.
type Service interface {
	// Idempotently create a membership for (user_id, organization_id).
	Create(context.Context, *CreatePayload) (res *Membership, err error)
	// Patch a membership's role.
	Update(context.Context, *UpdatePayload) (res *Membership, err error)
	// List memberships. Optional `user_id` and/or `organization_id` filters
	// (idp-design.md §6.1).
	List(context.Context, *ListPayload) (res *ListMembershipsResult, err error)
	// Hard-delete a membership.
	Delete(context.Context, *DeletePayload) (err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram-dev-idp"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "memberships"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [4]string{"create", "update", "list", "delete"}

// CreatePayload is the payload type of the memberships service create method.
type CreatePayload struct {
	// User UUID.
	UserID string
	// Organization UUID.
	OrganizationID string
	// Role within the organization; defaults to `member`.
	Role *string
}

// DeletePayload is the payload type of the memberships service delete method.
type DeletePayload struct {
	// Membership UUID.
	ID string
}

// ListMembershipsResult is the result type of the memberships service list
// method.
type ListMembershipsResult struct {
	// Memberships on this page.
	Items []*Membership
	// Cursor for the next page, empty when exhausted.
	NextCursor string
}

// ListPayload is the payload type of the memberships service list method.
type ListPayload struct {
	// Opaque cursor returned by a previous list call.
	Cursor *string
	// Maximum items to return.
	Limit int
	// Optional user filter.
	UserID *string
	// Optional organization filter.
	OrganizationID *string
}

// Membership is the result type of the memberships service create method.
type Membership struct {
	// Membership UUID.
	ID string
	// User UUID.
	UserID string
	// Organization UUID.
	OrganizationID string
	// Role within the organization (default `member`).
	Role      string
	CreatedAt string
	UpdatedAt string
}

// UpdatePayload is the payload type of the memberships service update method.
type UpdatePayload struct {
	// Membership UUID.
	ID string
	// New role.
	Role string
}
