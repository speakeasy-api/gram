/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 8e05e7418a1d
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { packagesPublish } from "../funcs/packagesPublish.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type PublishPackageMutationVariables = {
  request: operations.PublishRequest;
  security?: operations.PublishSecurity | undefined;
  options?: RequestOptions;
};

export type PublishPackageMutationData = components.PublishPackageResult;

/**
 * publish packages
 *
 * @remarks
 * Publish a new version of a package.
 */
export function usePublishPackageMutation(
  options?: MutationHookOptions<
    PublishPackageMutationData,
    Error,
    PublishPackageMutationVariables
  >,
): UseMutationResult<
  PublishPackageMutationData,
  Error,
  PublishPackageMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildPublishPackageMutation(client, options),
    ...options,
  });
}

export function mutationKeyPublishPackage(): MutationKey {
  return ["@gram/client", "packages", "publish"];
}

export function buildPublishPackageMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: PublishPackageMutationVariables,
  ) => Promise<PublishPackageMutationData>;
} {
  return {
    mutationKey: mutationKeyPublishPackage(),
    mutationFn: function publishPackageMutationFn({
      request,
      security,
      options,
    }): Promise<PublishPackageMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(packagesPublish(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
