procs:
  server:
    cmd: ["mise", "run", "start:server"]
    stop: SIGTERM
  proxy:
    cmd: ["mise", "run", "start:proxy"]
    stop: SIGTERM
  worker:
    cmd: ["mise", "run", "start:worker"]
    stop: SIGTERM
  dashboard:
    cmd: ["mise", "run", "start:dashboard"]
    stop:
      send-keys: ["<C-c>"]