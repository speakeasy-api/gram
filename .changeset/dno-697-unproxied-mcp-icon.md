---
"server": minor
"dashboard": patch
---

Unproxied MCP servers now default to the vendor's favicon as their icon on creation (best-effort, never blocks creation), and the icon can be uploaded or replaced later from the server's Branding settings.
