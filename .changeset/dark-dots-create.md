---
"admin": minor
---

Platform operators can store an organization's directory endpoint and bearer token from the admin dashboard, both on the organization overview and as the step straight after creating one, so that organization's own onboarding can set up directory sync from stored values. The token is write-only: never prefilled, never read back, and shown afterwards only as a fingerprint.
  