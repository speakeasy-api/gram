---
"server": minor
"dashboard": minor
---

Let organization administrators map Slack memberships to existing personnel from an inline picker, reassign or remove mappings, and review directory changes without granting new permissions. Syncs map members whose email matches exactly one person.

The shared demo shows synthetic mappings with read-only controls. Disconnecting a workspace removes its mappings.
