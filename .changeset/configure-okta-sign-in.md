---
"server": minor
---

Create and verify Okta OIDC sign-in applications through guided setup. WorkOS connection creation remains in Admin Portal because authenticated `POST /connections` requests return `404` unless Connections API migration capabilities are enabled.
