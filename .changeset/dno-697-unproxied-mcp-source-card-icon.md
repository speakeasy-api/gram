---
"dashboard": patch
---

MCP server icons (set via Branding settings, or the vendor favicon auto-fetched for unproxied servers) now render on the Connect Sources cards/table, and on the Distribute → MCP tab, instead of a generic placeholder icon.
