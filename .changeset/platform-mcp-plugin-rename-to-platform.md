---
"server": patch
"dashboard": patch
---

Rename the Platform MCP plugin from `speakeasy` to `platform`, so the install command becomes `/plugin install platform@speakeasy` and tool calls render as `plugin:platform:platform`. The Cursor and Codex packages become `platform-cursor` and `platform-codex`.
