---
"dashboard": minor
---

Explore filters pick their values from what the dimension holds inside the window, with a count beside each, instead of being typed blind. A value that is not listed can still be typed.
