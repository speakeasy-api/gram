---
"server": patch
"dashboard": patch
---

Align the Platform MCP server name with the plugin name (`speakeasy`), so the install command is `/plugin install speakeasy@speakeasy` and tool calls render as `plugin:speakeasy:speakeasy` — a clear brand signal in Claude Code's installed-plugin display.
