---
"dashboard": minor
---

Product surfaces that are not set up now say so and point at the fix. The Security overview, Shadow AI, MCP, Tool Logs and Insights empty states, and the Costs explorer, show onboarding guidance for their use case while the `gram-new-onboarding` flag is on and the surface has no evidence in the last 30 days, the same test the onboarding wizard verifies against. An organization that picked that use case sees its computed next step with a link to where the work happens; any other organization is sent into onboarding with the use case already chosen. Admins get the calls to action; members see the explanation only.
