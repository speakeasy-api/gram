---
"dashboard": patch
---

Onboarding now asks for Slack credentials in the order users encounter them in Slack's UI: Signing Secret first (Basic Information), then Bot User OAuth Token, then User OAuth Token (both on the Install App / OAuth & Permissions page). No more pasting `xoxb-` into the signing-secret field.
