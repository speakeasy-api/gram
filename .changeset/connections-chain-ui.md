---
"dashboard": minor
---

MCP Connections now shows each brokered connection as one chain — the client an agent connects through, the Gram server it reaches, and the upstream provider Gram holds credentials for on that person's behalf — grouped by person, provider, or client. The same list renders on the MCP server detail tab and the employee page, replacing the drill-down that filtered a separate table.
