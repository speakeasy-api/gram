---
"server": patch
---

Add the projection from normalized OTLP log records and spans into agent_events rows, and teach the OTLP dialects to classify what a record is in agent vocabulary. Nothing consumes it yet.
