---
"server": minor
---

Add read-only staff Admin MCP tools for global issuers (list, detail, duplicate and migration preflights, convergence candidates), support matrix facts, organization onboarding configuration, project MCP server inventory, and bounded organization inference key state, spend history and metered usage totals. Each tool requires live verified staff context and returns typed, bounded projections that omit credentials, provider identifiers, server URLs and customer-authored text.
