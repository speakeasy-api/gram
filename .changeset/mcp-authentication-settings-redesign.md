---
"dashboard": patch
---

Reorganize the MCP server Authentication settings into separate rows for session length, client access and connected services, and move the verified client catalog and your own allowed client URLs into one table.
