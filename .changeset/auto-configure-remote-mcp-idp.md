---
"dashboard": patch
---

Auto-configure remote identity providers for custom remote MCP servers when OAuth
metadata can be discovered, including reusing existing issuers and deriving a
display name from the issuer URL.
