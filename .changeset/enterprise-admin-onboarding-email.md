---
"server": minor
"dashboard": minor
---

Add `organizations.sendEnterpriseAdminOnboardingEmail` endpoint and matching super-admin "Onboarding" tab that lets operators paste a comma-separated list of emails and dispatch the enterprise admin onboarding email (Loops template `cmpqyxnzl00hj0jwtkibhyjdz`), which links each recipient to the active organization's setup wizard.
