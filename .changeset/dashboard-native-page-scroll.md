---
"dashboard": patch
---

Dashboard pages scroll with the browser's own scrollbar. The page header, mode switcher, and assistant composer stay pinned with `position: sticky` / `fixed` instead of living inside an inner `overflow-y: auto` container.
