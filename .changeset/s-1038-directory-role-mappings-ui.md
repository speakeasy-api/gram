---
"dashboard": minor
---

Manage directory role mappings on the IDP and SSO page. Every directory group is listed with its own role picker, unmapped groups first, and picking a role saves it. Attribute values are available as a collapsed fallback, and "Create role…" opens the role editor and maps the new role on return. The role editor no longer fails when the agents service is unavailable, the active navigation item drops its box border, and Team rows no longer show the killswitch status icon.
