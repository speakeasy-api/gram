---
"server": minor
---

Platform tools can be marked hidden so they no longer appear in tool listings by default. Pass `include_hidden=true` to `tools.list` to include them. Execution is unaffected — hidden tools remain invocable.
