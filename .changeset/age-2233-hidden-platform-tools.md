---
"server": minor
---

Platform tools can be marked hidden so they no longer appear in dashboard tool listings by default. Pass `include_hidden=true` to `tools.list` to include them. MCP clients and execution paths are unaffected — hidden tools remain visible to assistants and invocable.
