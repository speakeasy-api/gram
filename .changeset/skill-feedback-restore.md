---
"server": minor
---

Review suggested skill edits as a diff with comments on the changed lines. Suggestions are now stored as diffs, so they survive unrelated edits to the skill and are retired only when the change no longer applies. Each suggestion shows how many sessions asked for it and can expand the agent reports it was built from.
