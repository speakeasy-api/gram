---
"dashboard": patch
"server": minor
---

Gateway server instructions are editable: the gateway settings tab gains an Instructions section, and the metaMcp update endpoint accepts `instructions`. Gateways without custom text keep serving Gram's built-in drill-down guidance.
