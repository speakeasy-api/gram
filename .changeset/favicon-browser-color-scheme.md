---
"dashboard": patch
---

The browser tab icon now follows the browser's own light/dark setting instead of the theme selected inside the dashboard, so it no longer disappears when the two disagree.
