---
"server": patch
---

Platform MCP gains two Shadow AI tools. `list_shadow_ai_tools` returns the AI tools an organization's devices have been seen running, with each tool's access decision; `list_ai_scan_library` returns the scan target library those detections are matched against. Both require an org admin on every call.
