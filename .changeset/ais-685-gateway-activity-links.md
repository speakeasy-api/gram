---
"server": patch
"dashboard": patch
---

Gateway activity now hides members no longer on the gateway, hook-observed calls on a gateway URL classify as the gateway instead of shadow MCP, and the activity charts link to the tool logs and insights pages filtered to that gateway.
