---
"server": minor
"dashboard": patch
---

Add a guided Observability plugin credential rotation flow that mints a replacement hooks key, makes the previous key's fate explicit, and updates marketplace installs when eligible.
