---
"dashboard": minor
---

Jump into a project from Cmd+K. The palette now carries a Projects group that matches on a project's name, slug, or id — offered from the moment the palette opens at the organization level, where picking a project is its main job, and once you start typing inside a project, where it doubles as the fastest way to switch.
