---
"dashboard": minor
---

OpenCode setup instructions now point at the `speakeasy-hooks install --provider=opencode` flow instead of the standalone npm observability plugin, which is discontinued in favor of shipping OpenCode through the same Go relay binary used by Claude Code, Cursor, and Codex.
