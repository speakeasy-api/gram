---
"dashboard": minor
---

Add OpenCode as a supported provider. `speakeasy-hooks install --provider=opencode` renders the OpenCode observability plugin directly into a repo, shipping OpenCode through the same Go relay binary used by Claude Code, Cursor, and Codex.
