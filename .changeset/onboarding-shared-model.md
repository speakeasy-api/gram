---
"server": minor
"dashboard": patch
---

Expose whether each setup task counts toward onboarding progress, and drive the onboarding board and wizard from one shared model, so both show the same task order, progress, and save recovery. Tasks from a newer server now appear with an explicit "needs a newer dashboard" state instead of disappearing.
