---
"dashboard": minor
"server": patch
---

Reshape organization onboarding around outcomes. "Connect identity provider" and "Directory sync" merge into one "Set up identity provider" card, publishing the plugin marketplace becomes a section at the front of the cards that need it instead of a card of its own, and confirming traffic becomes the last section of both the new "Set up Anthropic Enterprise" card (Claude Cowork, which the device agent cannot reach) and "Instrument agents".
