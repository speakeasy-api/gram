---
"server": minor
---

Add an unproxied MCP server backend for `mcp_servers`. Staff-only for now: lets Speakeasy staff register a vendor's MCP server that Gram lists and can attach to a plugin without ever proxying it, so there's no OAuth callback or upstream allowlisting involved.
