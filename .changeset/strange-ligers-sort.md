---
"cli": minor
---

`gram update` command to provide a self-update mechanism for the Gram CLI.
`--check` flag - Check for updates without installing (dry-run)
`--force` flag - Force update even if already on latest version
