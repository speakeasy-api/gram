---
"dashboard": patch
---

Enterprise setup now includes an Enable logs step that uses the same Enable Logs switch as Organization → Logging & Telemetry, so admins opt in to product logging during onboarding instead of only from org settings.
