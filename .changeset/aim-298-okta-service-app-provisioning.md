---
"server": patch
---

Adds internal provisioning for identity provider connection credentials: a per-connection KMS signing key published through the existing key set and remote session client path. Rows it creates are read-only through the organization APIs.
