---
"server": patch
---

Count agent_events rows the dialects could not classify, by producer surface and raw event name, so a producer changing its vocabulary shows up as a signal rather than a dataset quietly going thin.
