---
"server": patch
---

Grant demo organization sessions every user-visible scope so enforcement matches the grant set the dashboard is given, and make the demo write guard fail closed on unrecognized RPC verbs.
