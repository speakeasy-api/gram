---
"server": minor
"dashboard": patch
---

The Overview tab now shows a best-effort "tool calls over time" chart for unproxied MCP servers, sourced from Shadow MCP's hook-reported activity matched by URL, since Gram's own proxy telemetry never sees traffic to these servers.
