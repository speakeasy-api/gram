---
"server": minor
"admin": minor
---

Record support matrix account eligibility as a field on each integration method and platform mapping rather than reading it out of the method's plan notes, so methods whose notes never described a plan stop reporting every account type as unknown. Adds Cursor and OpenAI admin plugin distribution, a Microsoft Copilot platform, and Claude Mobile and Claude Tag as platform families of their own.
