---
"dashboard": patch
"server": patch
---

feat(risk): add a non-assistant filter to risk events so findings from chats not linked to an assistant (the ones most likely missing user attribution) can be surfaced on their own.
