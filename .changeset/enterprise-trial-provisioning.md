---
"server": minor
---

Behind the `enterprise-trials` flag, public self-signup (`Register`) provisions a whitelisted enterprise organization with a 14-day trial window, the enterprise feature bundle (minus SSO/SCIM), and the first member as org-admin — all in one atomic transaction. With the flag off, signup behavior is unchanged.
