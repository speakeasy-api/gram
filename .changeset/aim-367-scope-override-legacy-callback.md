---
"server": minor
"dashboard": minor
---

Show and edit a remote identity provider's scope override, warn wherever a client's scopes are edited that the override makes them inert, and flag remote session clients that use the legacy callback URLs.
