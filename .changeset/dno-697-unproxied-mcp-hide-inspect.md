---
"dashboard": patch
---

Hide the Inspect tab for unproxied MCP servers for now — there's no reliable way to list their tools yet (the vendor's own auth blocks an anonymous probe), so the tab had nothing useful to show. Direct links to it now redirect to Overview.
