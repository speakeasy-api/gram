---
"server": minor
---

Add organization-level user session issuer management APIs with org-scoped issuer and CIMD client CRUD, deletion impact preflight, dependency guards, and project mutation isolation.
