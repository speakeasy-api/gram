---
"dashboard": minor
---

Create a private MCP server for each Okta application picked during onboarding, backed by a remote MCP server at the matched catalog endpoint, with each card reporting its own outcome and the way on to finishing setup.
