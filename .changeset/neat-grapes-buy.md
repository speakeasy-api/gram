---
"dashboard": minor
---

Create a private MCP server for each Okta application picked during onboarding, backed by a remote MCP server at the matched catalog endpoint, with each card reporting its own outcome and the way on to finishing setup. Each draft then carries on to the sign-in its upstream asks for, configuring the server's sign-in provider and registering a client automatically where the provider supports it, and saying on the card when that has to be finished by hand.
