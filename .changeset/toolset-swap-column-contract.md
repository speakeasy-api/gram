---
"server": minor
"dashboard": patch
---

MCP publishing settings now live exclusively on MCP servers and their endpoints. The toolset-level publishing fields (mcp_slug, mcp_is_public, mcp_enabled, custom_domain_id) are removed from the API, and custom-domain server addresses no longer respond on the platform domain.
