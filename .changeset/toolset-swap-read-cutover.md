---
"server": minor
"dashboard": minor
---

MCP server listings now show every server in one place — hosted toolset-backed servers alongside remote and tunneled ones — with tool counts, catalog badges, and unified search and filters. Install links prefer your custom domain's root URL when a server is set as the domain default.
