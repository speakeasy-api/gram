---
"server": minor
---

Remote Session issuer metadata is now refreshed on a schedule, so capability columns fill in and upstream endpoint changes are picked up without an operator clicking refresh.
