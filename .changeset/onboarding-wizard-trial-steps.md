---
"dashboard": patch
---

The onboarding setup wizard now derives its visible steps from the org's product-feature entitlement: identity steps (Connect IdP, Directory Sync) are hidden for trial orgs without SSO/SCIM, and a persistent upsell banner is shown. Fully-entitled enterprise orgs continue to see all steps.
