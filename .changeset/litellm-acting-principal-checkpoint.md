---
"server": minor
---

Enforce the internal `ai_access` kill switch before managed LiteLLM inference. Authenticated Gram users can mint a short-lived, invocation-bound acting-principal assertion for an active project integration; the pinned fail-closed LiteLLM Generic Guardrail forwards it to Gram, where active membership and the current managed instance key are revalidated before content processing. Matched prescriptions return their selected external note, while identity, resource, and evaluator failures block without presenting a false match.
