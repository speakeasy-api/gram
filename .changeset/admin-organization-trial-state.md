---
"admin": patch
---

Report each organization's real trial state on the admin organizations and organization detail endpoints. An operator can now tell a running trial from one that is ending soon, expired, demoted or converted, and can see an organization that never trialled at all, together with the trial's real end date. The previous fields defaulted to fourteen days after signup for every organization, so every row looked like it was trialling.
