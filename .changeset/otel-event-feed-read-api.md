---
"server": minor
---

New org-scoped read endpoints on the otel service for the Event Feed: otel.listEventLog (time-keyset-paginated merged log/span feed), otel.getEventVolume (logs-vs-spans time buckets), and otel.getEventFacets (available sources and names).
