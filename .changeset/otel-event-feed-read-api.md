---
"server": minor
---

New org-scoped telemetry endpoints for the Event Feed: telemetry.listEventLog (cursor-paginated merged log/span feed), telemetry.getEventVolume (logs-vs-spans time buckets), and telemetry.getEventFacets (available sources and names).
