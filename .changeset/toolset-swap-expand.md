---
"server": minor
"dashboard": patch
---

Custom domain root routing now works with every MCP server type, including toolsets published before dedicated MCP servers existed. Selecting a default MCP server for a domain root recognizes these servers automatically, and the selector no longer lists disabled servers or loses its selection after saving.
