---
"server": minor
"@gram/client": minor
"dashboard": minor
---

Added RBAC roles and permissions management via WorkOS integration. CRUD endpoints for roles and member role assignment backed by WorkOS Authorization APIs. UI for managing roles with permission picker, scope-based resource access, and member assignment.
