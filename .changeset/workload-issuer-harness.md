---
"server": patch
---

Test-only: stand a real OIDC issuer up as a workload platform in the oauthtest harness, so workload assertion verification is exercised against a served key set fetched over the network rather than one handed to the verifier inline
