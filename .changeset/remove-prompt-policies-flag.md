---
"server": patch
"dashboard": patch
---

Make prompt-based (LLM-judge) risk policies available to every organization by removing the `gram-prompt-policies` feature flag. Creating and updating prompt-based policies, realtime and batch evaluation, the Policy Center listing, and the Platform MCP risk policy mutations are no longer gated.
