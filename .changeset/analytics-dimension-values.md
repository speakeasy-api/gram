---
"server": minor
---

Add `analytics.dimensionValues`: the values a dimension holds inside a window, most frequent first, resolved after the dataset collapses its observations. What Explore's filter pickers read.
