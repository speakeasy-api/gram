---
"dashboard": minor
---

feat: add the AI Scan Targets page under Platform Admin

Platform administrators can list, add, edit, enable, disable, and delete the Shadow AI scan targets served to every enrolled device agent, and see the catalog's recent revisions. The strict platform-admin gate used by OpenRouter Keys is now a shared component.
