---
"dashboard": patch
---

Improve notification persistence and UI placement

- Fix SDK call to correctly wrap notification data in createNotificationForm
- Change toast persistence to opt-in model (persist: true)
- Move notification bell from sidebar to page header
- Add persist: true to important user action toasts across the dashboard
