---
"dashboard": patch
---

Watchdog evidence cards can now show the message behind a finding. In the signal drawer, the session title on each card expands in place to the session's full opening message, opens the session transcript at the flagged message, and links to the session in Agent Sessions. Flagged values stay masked in the expanded message. These controls only appear for users with the `chat:read` scope.
