---
"dashboard": patch
---

Name the sign-in provider portal for what it does rather than whose product it is. The identity provider setup card and the organization identity settings page now say "the sign-in provider portal" and "the sign-in provider tab" in their setup notes and in the toasts that launch and verify a connection, matching the way the guided Okta path already read. The banner over an impersonated session now says what the session is rather than which vendor issued it.
