---
"server": patch
---

Recheck live authorization before delegated credential refresh, scope claim cleanup to its issuer, and quarantine rotated credentials when identity verification is unavailable without replaying them. Clear terminal configuration secrets and preserve retryable provider dependency failures.
