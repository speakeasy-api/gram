---
"server": minor
"dashboard": minor
---

Add a tool call logs data export source that delivers the tool-call records Gram writes for hosted, proxied, and platform MCP servers to a project's OTLP destination, with tool IO and caller identity redacted unless the destination includes sensitive data.
