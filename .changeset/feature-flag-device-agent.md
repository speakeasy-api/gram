---
"dashboard": patch
---

Gate the Device Agent pages and sidebar nav item behind the `gram-device-agent` feature flag (defaults off). The nav link is hidden entirely when the flag is off, and the page redirects away so it can't be reached by direct URL.
