---
"dashboard": minor
---

Adds agent identity setup for the Device Agent. Pick or create an agent, grant it plugin sync, hook ingestion, and project read access, mint a key, and copy an install snippet that needs no email, version, or checksum. The page shows when the agent last checked in, and agent API keys now list when each was last used.
