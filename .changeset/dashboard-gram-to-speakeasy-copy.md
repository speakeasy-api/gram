---
"dashboard": patch
---

Replace the remaining user-visible "Gram" product mentions in dashboard UI copy with "Speakeasy" (auth descriptions, onboarding steps, insights tooltips, detection rule description, tunneled MCP setup). Rename the `GramLogo` component to `SpeakeasyLogo` — its SVG already rendered the Speakeasy wordmark/icon — and add an accessible `aria-label` to the brand mark.
