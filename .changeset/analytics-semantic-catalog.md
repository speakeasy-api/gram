---
"server": patch
---

Add the semantic catalog for agent session data: the sessions and tool_calls datasets, their typed fields, guardrail constants, and the source queries that de-duplicate and collapse agent_events to each dataset's grain. Nothing serves it yet.
