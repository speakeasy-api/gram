---
"server": patch
"dashboard": patch
---

Add an optional Claude Code handoff in Platform MCP setup for bringing existing remote MCP servers into Speakeasy management. The reviewed workflow verifies the agent's connection, requests informed discovery consent or a sanitized manual inventory, confirms the destination and selected servers, and reports each registration separately. Authentication stays in Speakeasy without migrating local credentials.
