---
"server": patch
---

Tightens DPoP nonce challenge parsing to the DPoP scheme, canonicalizes the Okta org URL so it matches the proof, and reuses the verification token when its grant covers the default scopes.
