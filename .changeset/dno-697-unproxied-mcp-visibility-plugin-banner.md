---
"dashboard": patch
---

Unproxied MCP servers now show a fixed "Public" visibility indicator instead of a Disabled/Private/Public dropdown that gated nothing (Gram has no hosted endpoint to disable for these), and now show the plugin publish-status banner on their Overview tab, same as every other server type.
