---
"server": minor
"dashboard": patch
---

Speed up the dashboard's project assistant: move it to Sonnet, warm its runtime when the dock opens instead of on the first message, hold the runtime warm for five minutes, ask for extended prompt-cache retention, and tell the model to plan a turn as one compose script rather than a chain of them.
