---
"server": patch
---

Give the project's managed assistant (Project Assistant) a new `platform_get_changelog` tool that reads the public Speakeasy changelog feed, so it can answer questions about what recently shipped on the platform and dashboard.
