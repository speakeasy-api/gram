---
"server": minor
---

Add a Verify action to the remote-session consent page that presents a stored credential to its upstream MCP server in a dry run of dispatch (connect and list tools) and records the verdict (valid, rejected by the member, or unknown) on the session, shown on the card.
