---
"server": minor
"dashboard": patch
---

Automatically disable custom domains after 14 consecutive actionable health-check failures, remove their Kubernetes routing resources, notify organization admins when available, and offer a dashboard re-verification path. Gram-side check failures are excluded from the threshold.
