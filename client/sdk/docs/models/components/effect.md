# Effect

Whether this grant allows or denies the scope. Defaults to 'allow' when omitted.

## Example Usage

```typescript
import { Effect } from "@gram/client/models/components";

let value: Effect = "deny";
```

## Values

```typescript
"allow" | "deny"
```