/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery } from "@tanstack/react-query";
import { notificationsList } from "../funcs/notificationsList.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
/**
 * listNotifications notifications
 *
 * @remarks
 * List notifications for the current project
 */
export function useListNotifications(request, security, options) {
  const client = useGramContext();
  return useQuery({
    ...buildListNotificationsQuery(client, request, security, options),
    ...options,
  });
}
/**
 * listNotifications notifications
 *
 * @remarks
 * List notifications for the current project
 */
export function useListNotificationsSuspense(request, security, options) {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildListNotificationsQuery(client, request, security, options),
    ...options,
  });
}
export function prefetchListNotifications(
  queryClient,
  client$,
  request,
  security,
) {
  return queryClient.prefetchQuery({
    ...buildListNotificationsQuery(client$, request, security),
  });
}
export function setListNotificationsData(client, queryKeyBase, data) {
  const key = queryKeyListNotifications(...queryKeyBase);
  return client.setQueryData(key, data);
}
export function invalidateListNotifications(client, queryKeyBase, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "notifications", "list", ...queryKeyBase],
  });
}
export function invalidateAllListNotifications(client, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "notifications", "list"],
  });
}
export function buildListNotificationsQuery(
  client$,
  request,
  security,
  options,
) {
  return {
    queryKey: queryKeyListNotifications({
      archived: request?.archived,
      limit: request?.limit,
      cursor: request?.cursor,
      gramSession: request?.gramSession,
      gramProject: request?.gramProject,
    }),
    queryFn: async function listNotificationsQueryFn(ctx) {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };
      return unwrapAsync(
        notificationsList(client$, request, security, mergedOptions),
      );
    },
  };
}
export function queryKeyListNotifications(parameters) {
  return ["@gram/client", "notifications", "list", parameters];
}
//# sourceMappingURL=listNotifications.js.map
