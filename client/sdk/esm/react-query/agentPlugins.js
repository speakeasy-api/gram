/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildAgentPluginsQuery, prefetchAgentPlugins, queryKeyAgentPlugins, } from "./agentPlugins.core.js";
export { buildAgentPluginsQuery, prefetchAgentPlugins, queryKeyAgentPlugins, };
/**
 * getPlugins agent
 *
 * @remarks
 * Resolve the marketplaces and plugins assigned to the enrolled user. The device agent reconciles these into whichever AI developer tools it manages (Claude Code today), so each tool's own plugin manager fetches and installs the bundles. The response is tool-agnostic: it names what to install, and each tool's syncer decides how to render it into that tool's native configuration.
 */
export function useAgentPlugins(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildAgentPluginsQuery(client, request, security, options),
        ...options,
    });
}
/**
 * getPlugins agent
 *
 * @remarks
 * Resolve the marketplaces and plugins assigned to the enrolled user. The device agent reconciles these into whichever AI developer tools it manages (Claude Code today), so each tool's own plugin manager fetches and installs the bundles. The response is tool-agnostic: it names what to install, and each tool's syncer decides how to render it into that tool's native configuration.
 */
export function useAgentPluginsSuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildAgentPluginsQuery(client, request, security, options),
        ...options,
    });
}
export function setAgentPluginsData(client, queryKeyBase, data) {
    const key = queryKeyAgentPlugins(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateAgentPlugins(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "agent", "getPlugins", ...queryKeyBase],
    });
}
export function invalidateAllAgentPlugins(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "agent", "getPlugins"],
    });
}
//# sourceMappingURL=agentPlugins.js.map