/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildListSessionsQuery, prefetchListSessions, queryKeyListSessions, } from "./listSessions.core.js";
export { buildListSessionsQuery, prefetchListSessions, queryKeyListSessions, };
/**
 * listSessions telemetry
 *
 * @remarks
 * Org-scoped list of individual chat sessions for a slice of usage, filtered by the same allowlisted dimensions as telemetry.query. Returns per-session cost, token, and tool metrics with cursor pagination.
 */
export function useListSessions(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildListSessionsQuery(client, request, security, options),
        ...options,
    });
}
/**
 * listSessions telemetry
 *
 * @remarks
 * Org-scoped list of individual chat sessions for a slice of usage, filtered by the same allowlisted dimensions as telemetry.query. Returns per-session cost, token, and tool metrics with cursor pagination.
 */
export function useListSessionsSuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildListSessionsQuery(client, request, security, options),
        ...options,
    });
}
export function setListSessionsData(client, queryKeyBase, data) {
    const key = queryKeyListSessions(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateListSessions(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "listSessions", ...queryKeyBase],
    });
}
export function invalidateAllListSessions(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "listSessions"],
    });
}
//# sourceMappingURL=listSessions.js.map