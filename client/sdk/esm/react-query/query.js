/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildQueryQuery, prefetchQuery, queryKeyQuery, } from "./query.core.js";
export { buildQueryQuery, prefetchQuery, queryKeyQuery };
/**
 * query telemetry
 *
 * @remarks
 * Generic, org-scoped analytics query over pre-aggregated usage metrics. Returns both a grouped table and a per-group hourly timeseries for the same slice of data, supporting arbitrary allowlisted group-by dimensions and filters (e.g. group by department_name, then drill in by filtering department_name and grouping by role).
 */
export function useQuery(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildQueryQuery(client, request, security, options),
        ...options,
    });
}
/**
 * query telemetry
 *
 * @remarks
 * Generic, org-scoped analytics query over pre-aggregated usage metrics. Returns both a grouped table and a per-group hourly timeseries for the same slice of data, supporting arbitrary allowlisted group-by dimensions and filters (e.g. group by department_name, then drill in by filtering department_name and grouping by role).
 */
export function useQuerySuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildQueryQuery(client, request, security, options),
        ...options,
    });
}
export function setQueryData(client, queryKeyBase, data) {
    const key = queryKeyQuery(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateQuery(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "query", ...queryKeyBase],
    });
}
export function invalidateAllQuery(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "query"],
    });
}
//# sourceMappingURL=query.js.map