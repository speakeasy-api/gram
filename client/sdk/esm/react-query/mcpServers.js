/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildMcpServersQuery, prefetchMcpServers, queryKeyMcpServers, } from "./mcpServers.core.js";
export { buildMcpServersQuery, prefetchMcpServers, queryKeyMcpServers, };
/**
 * listMcpServers mcpServers
 *
 * @remarks
 * List MCP servers for a project. Accepts optional remote_mcp_server_id, tunneled_mcp_server_id, or toolset_id filters to scope the result to a single backend; at most one filter may be supplied since the backends are mutually exclusive.
 */
export function useMcpServers(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildMcpServersQuery(client, request, security, options),
        ...options,
    });
}
/**
 * listMcpServers mcpServers
 *
 * @remarks
 * List MCP servers for a project. Accepts optional remote_mcp_server_id, tunneled_mcp_server_id, or toolset_id filters to scope the result to a single backend; at most one filter may be supplied since the backends are mutually exclusive.
 */
export function useMcpServersSuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildMcpServersQuery(client, request, security, options),
        ...options,
    });
}
export function setMcpServersData(client, queryKeyBase, data) {
    const key = queryKeyMcpServers(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateMcpServers(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "mcpServers", "list", ...queryKeyBase],
    });
}
export function invalidateAllMcpServers(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "mcpServers", "list"],
    });
}
//# sourceMappingURL=mcpServers.js.map