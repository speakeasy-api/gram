/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { userSessionsList } from "../funcs/userSessionsList.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { unwrapResultIterator } from "../types/operations.js";
import { pageIteratorToJSON } from "./_types.js";
export function prefetchUserSessions(
  queryClient,
  client$,
  request,
  security,
  options,
) {
  return queryClient.prefetchQuery({
    ...buildUserSessionsQuery(client$, request, security, options),
  });
}
export function prefetchUserSessionsInfinite(
  queryClient,
  client$,
  request,
  security,
  options,
) {
  return queryClient.prefetchInfiniteQuery({
    ...buildUserSessionsInfiniteQuery(client$, request, security, options),
    initialPageParam: undefined,
    getNextPageParam: (previousPage) => previousPage["~next"],
  });
}
export function buildUserSessionsQuery(client$, request, security, options) {
  return {
    queryKey: queryKeyUserSessions({
      subjectUrn: request?.subjectUrn,
      userSessionIssuerId: request?.userSessionIssuerId,
      status: request?.status,
      clientId: request?.clientId,
      cursor: request?.cursor,
      limit: request?.limit,
      gramSession: request?.gramSession,
      gramKey: request?.gramKey,
      gramProject: request?.gramProject,
    }),
    queryFn: async function userSessionsQueryFn(ctx) {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };
      return unwrapAsync(
        userSessionsList(client$, request, security, mergedOptions),
      );
    },
  };
}
export function buildUserSessionsInfiniteQuery(
  client$,
  request,
  security,
  options,
) {
  return {
    queryKey: queryKeyUserSessionsInfinite({
      subjectUrn: request?.subjectUrn,
      userSessionIssuerId: request?.userSessionIssuerId,
      status: request?.status,
      clientId: request?.clientId,
      cursor: request?.cursor,
      limit: request?.limit,
      gramSession: request?.gramSession,
      gramKey: request?.gramKey,
      gramProject: request?.gramProject,
    }),
    queryFn: async function userSessionsQuery(ctx) {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };
      if (!ctx.pageParam) {
        const pageResult = await unwrapResultIterator(
          userSessionsList(client$, request, security, mergedOptions),
        );
        return pageIteratorToJSON(pageResult);
      }
      const pageResult = await unwrapResultIterator(
        userSessionsList(
          client$,
          {
            ...request,
            cursor: ctx.pageParam.cursor,
          },
          security,
          mergedOptions,
        ),
      );
      return pageIteratorToJSON(pageResult);
    },
  };
}
export function queryKeyUserSessions(parameters) {
  return ["@gram/client", "userSessions", "list", parameters];
}
export function queryKeyUserSessionsInfinite(parameters) {
  return ["@gram/client", "userSessions", "list", "infinite", parameters];
}
//# sourceMappingURL=userSessions.core.js.map
