/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildTelemetryQueryQuery, prefetchTelemetryQuery, queryKeyTelemetryQuery, } from "./telemetryQuery.core.js";
export { buildTelemetryQueryQuery, prefetchTelemetryQuery, queryKeyTelemetryQuery, };
/**
 * query telemetry
 *
 * @remarks
 * Generic, org-scoped analytics query over pre-aggregated usage metrics. Returns both a grouped table and a per-group hourly timeseries for the same slice of data, supporting arbitrary allowlisted group-by dimensions and filters (e.g. group by department_name, then drill in by filtering department_name and grouping by role).
 */
export function useTelemetryQuery(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildTelemetryQueryQuery(client, request, security, options),
        ...options,
    });
}
/**
 * query telemetry
 *
 * @remarks
 * Generic, org-scoped analytics query over pre-aggregated usage metrics. Returns both a grouped table and a per-group hourly timeseries for the same slice of data, supporting arbitrary allowlisted group-by dimensions and filters (e.g. group by department_name, then drill in by filtering department_name and grouping by role).
 */
export function useTelemetryQuerySuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildTelemetryQueryQuery(client, request, security, options),
        ...options,
    });
}
export function setTelemetryQueryData(client, queryKeyBase, data) {
    const key = queryKeyTelemetryQuery(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateTelemetryQuery(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "query", ...queryKeyBase],
    });
}
export function invalidateAllTelemetryQuery(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "telemetry", "query"],
    });
}
//# sourceMappingURL=telemetryQuery.js.map