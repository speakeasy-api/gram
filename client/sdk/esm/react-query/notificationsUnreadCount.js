/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery } from "@tanstack/react-query";
import { notificationsUnreadCount } from "../funcs/notificationsUnreadCount.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
/**
 * getUnreadCount notifications
 *
 * @remarks
 * Get the count of notifications created since a given timestamp
 */
export function useNotificationsUnreadCount(request, security, options) {
  const client = useGramContext();
  return useQuery({
    ...buildNotificationsUnreadCountQuery(client, request, security, options),
    ...options,
  });
}
/**
 * getUnreadCount notifications
 *
 * @remarks
 * Get the count of notifications created since a given timestamp
 */
export function useNotificationsUnreadCountSuspense(
  request,
  security,
  options,
) {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildNotificationsUnreadCountQuery(client, request, security, options),
    ...options,
  });
}
export function prefetchNotificationsUnreadCount(
  queryClient,
  client$,
  request,
  security,
) {
  return queryClient.prefetchQuery({
    ...buildNotificationsUnreadCountQuery(client$, request, security),
  });
}
export function setNotificationsUnreadCountData(client, queryKeyBase, data) {
  const key = queryKeyNotificationsUnreadCount(...queryKeyBase);
  return client.setQueryData(key, data);
}
export function invalidateNotificationsUnreadCount(
  client,
  queryKeyBase,
  filters,
) {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "notifications", "unreadCount", ...queryKeyBase],
  });
}
export function invalidateAllNotificationsUnreadCount(client, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "notifications", "unreadCount"],
  });
}
export function buildNotificationsUnreadCountQuery(
  client$,
  request,
  security,
  options,
) {
  return {
    queryKey: queryKeyNotificationsUnreadCount({
      since: request?.since,
      gramSession: request?.gramSession,
      gramProject: request?.gramProject,
    }),
    queryFn: async function notificationsUnreadCountQueryFn(ctx) {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };
      return unwrapAsync(
        notificationsUnreadCount(client$, request, security, mergedOptions),
      );
    },
  };
}
export function queryKeyNotificationsUnreadCount(parameters) {
  return ["@gram/client", "notifications", "unreadCount", parameters];
}
//# sourceMappingURL=notificationsUnreadCount.js.map
