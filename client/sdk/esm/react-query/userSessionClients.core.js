/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { userSessionClientsList } from "../funcs/userSessionClientsList.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { unwrapResultIterator } from "../types/operations.js";
import { pageIteratorToJSON } from "./_types.js";
export function prefetchUserSessionClients(
  queryClient,
  client$,
  request,
  security,
  options,
) {
  return queryClient.prefetchQuery({
    ...buildUserSessionClientsQuery(client$, request, security, options),
  });
}
export function prefetchUserSessionClientsInfinite(
  queryClient,
  client$,
  request,
  security,
  options,
) {
  return queryClient.prefetchInfiniteQuery({
    ...buildUserSessionClientsInfiniteQuery(
      client$,
      request,
      security,
      options,
    ),
    initialPageParam: undefined,
    getNextPageParam: (previousPage) => previousPage["~next"],
  });
}
export function buildUserSessionClientsQuery(
  client$,
  request,
  security,
  options,
) {
  return {
    queryKey: queryKeyUserSessionClients({
      userSessionIssuerId: request?.userSessionIssuerId,
      cursor: request?.cursor,
      limit: request?.limit,
      gramSession: request?.gramSession,
      gramKey: request?.gramKey,
      gramProject: request?.gramProject,
    }),
    queryFn: async function userSessionClientsQueryFn(ctx) {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };
      return unwrapAsync(
        userSessionClientsList(client$, request, security, mergedOptions),
      );
    },
  };
}
export function buildUserSessionClientsInfiniteQuery(
  client$,
  request,
  security,
  options,
) {
  return {
    queryKey: queryKeyUserSessionClientsInfinite({
      userSessionIssuerId: request?.userSessionIssuerId,
      cursor: request?.cursor,
      limit: request?.limit,
      gramSession: request?.gramSession,
      gramKey: request?.gramKey,
      gramProject: request?.gramProject,
    }),
    queryFn: async function userSessionClientsQuery(ctx) {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };
      if (!ctx.pageParam) {
        const pageResult = await unwrapResultIterator(
          userSessionClientsList(client$, request, security, mergedOptions),
        );
        return pageIteratorToJSON(pageResult);
      }
      const pageResult = await unwrapResultIterator(
        userSessionClientsList(
          client$,
          {
            ...request,
            cursor: ctx.pageParam.cursor,
          },
          security,
          mergedOptions,
        ),
      );
      return pageIteratorToJSON(pageResult);
    },
  };
}
export function queryKeyUserSessionClients(parameters) {
  return ["@gram/client", "userSessionClients", "list", parameters];
}
export function queryKeyUserSessionClientsInfinite(parameters) {
  return ["@gram/client", "userSessionClients", "list", "infinite", parameters];
}
//# sourceMappingURL=userSessionClients.core.js.map
