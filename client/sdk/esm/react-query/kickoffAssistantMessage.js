/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation } from "@tanstack/react-query";
import { assistantsKickoffMessage } from "../funcs/assistantsKickoffMessage.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
/**
 * kickoffMessage assistants
 *
 * @remarks
 * Nudge the assistant to proactively greet a returning user. Enqueues a hidden turn (the prompt is server-owned and never shown in the conversation log) so the assistant emits a short welcome-back recap as the next reply. Poll the returned chat to read it.
 */
export function useKickoffAssistantMessageMutation(options) {
  const client = useGramContext();
  return useMutation({
    ...buildKickoffAssistantMessageMutation(client, options),
    ...options,
  });
}
export function mutationKeyKickoffAssistantMessage() {
  return ["@gram/client", "assistants", "kickoffMessage"];
}
export function buildKickoffAssistantMessageMutation(client$, hookOptions) {
  return {
    mutationKey: mutationKeyKickoffAssistantMessage(),
    mutationFn: function kickoffAssistantMessageMutationFn({
      request,
      security,
      options,
    }) {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(
        assistantsKickoffMessage(client$, request, security, mergedOptions),
      );
    },
  };
}
//# sourceMappingURL=kickoffAssistantMessage.js.map
