/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation } from "@tanstack/react-query";
import { tokenExchangeExchange } from "../funcs/tokenExchangeExchange.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
/**
 * exchange tokenExchange
 *
 * @remarks
 * Exchange the org-scoped device-agent install credential plus a vouched user email for a long-lived, per-user API key carrying the 'agent_user' scope. Authenticated with an org-scoped API key carrying the 'agent' scope — deliberately broader than the 'agent_user' scope the minted per-user keys carry, so a leaked per-user key cannot re-enter this endpoint to forge another user's key. The raw key is returned exactly once.
 */
export function useTokenExchangeExchangeMutation(options) {
  const client = useGramContext();
  return useMutation({
    ...buildTokenExchangeExchangeMutation(client, options),
    ...options,
  });
}
export function mutationKeyTokenExchangeExchange() {
  return ["@gram/client", "tokenExchange", "exchange"];
}
export function buildTokenExchangeExchangeMutation(client$, hookOptions) {
  return {
    mutationKey: mutationKeyTokenExchangeExchange(),
    mutationFn: function tokenExchangeExchangeMutationFn({
      request,
      security,
      options,
    }) {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(
        tokenExchangeExchange(client$, request, security, mergedOptions),
      );
    },
  };
}
//# sourceMappingURL=tokenExchangeExchange.js.map
