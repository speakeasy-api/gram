/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { userSessionsRevoke } from "../funcs/userSessionsRevoke.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
/**
 * revokeUserSession userSessions
 *
 * @remarks
 * Push the session's jti into the revocation cache and soft-delete the row.
 */
export function useRevokeUserSessionMutation(options) {
    const client = useGramContext();
    return useMutation({
        ...buildRevokeUserSessionMutation(client, options),
        ...options,
    });
}
export function mutationKeyRevokeUserSession() {
    return ["@gram/client", "userSessions", "revoke"];
}
export function buildRevokeUserSessionMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyRevokeUserSession(),
        mutationFn: function revokeUserSessionMutationFn({ request, security, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(userSessionsRevoke(client$, request, security, mergedOptions));
        },
    };
}
//# sourceMappingURL=revokeUserSession.js.map