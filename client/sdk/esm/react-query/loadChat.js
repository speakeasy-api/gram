/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildLoadChatQuery, prefetchLoadChat, queryKeyLoadChat, } from "./loadChat.core.js";
export { buildLoadChatQuery, prefetchLoadChat, queryKeyLoadChat, };
/**
 * loadChat chat
 *
 * @remarks
 * Load a chat by its ID. Messages within a generation are paginated by `seq` keyset: omit cursors to receive the newest page, pass `before_seq` to load older messages (scroll up) or `after_seq` to load newer ones (scroll down). Set `from_start` to receive the oldest page (the start of the thread) instead of the newest. Omit `generation` to receive the latest generation. Set `risk_only` to return only messages with risk findings plus a few messages of surrounding context per finding. Set `query` to instead return only messages whose text matches a search query plus surrounding context (mutually exclusive with `risk_only`).
 */
export function useLoadChat(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildLoadChatQuery(client, request, security, options),
        ...options,
    });
}
/**
 * loadChat chat
 *
 * @remarks
 * Load a chat by its ID. Messages within a generation are paginated by `seq` keyset: omit cursors to receive the newest page, pass `before_seq` to load older messages (scroll up) or `after_seq` to load newer ones (scroll down). Set `from_start` to receive the oldest page (the start of the thread) instead of the newest. Omit `generation` to receive the latest generation. Set `risk_only` to return only messages with risk findings plus a few messages of surrounding context per finding. Set `query` to instead return only messages whose text matches a search query plus surrounding context (mutually exclusive with `risk_only`).
 */
export function useLoadChatSuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildLoadChatQuery(client, request, security, options),
        ...options,
    });
}
export function setLoadChatData(client, queryKeyBase, data) {
    const key = queryKeyLoadChat(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateLoadChat(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "chat", "load", ...queryKeyBase],
    });
}
export function invalidateAllLoadChat(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "chat", "load"],
    });
}
//# sourceMappingURL=loadChat.js.map