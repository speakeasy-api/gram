/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGramContext } from "./_context.js";
import { buildListToolsetToolFiltersQuery, prefetchListToolsetToolFilters, queryKeyListToolsetToolFilters, } from "./listToolsetToolFilters.core.js";
export { buildListToolsetToolFiltersQuery, prefetchListToolsetToolFilters, queryKeyListToolsetToolFilters, };
/**
 * listToolFilters toolsets
 *
 * @remarks
 * List the tool filter scopes (tags) available on a toolset-backed MCP server and the tools under each, including tools excluded from all filters. Read-only; reflects the explicit tool variations group configured on the toolset, deriving effective tags with the same logic as the runtime ?tags= filter. Returns filtering disabled when no explicit group is set.
 */
export function useListToolsetToolFilters(request, security, options) {
    const client = useGramContext();
    return useQuery({
        ...buildListToolsetToolFiltersQuery(client, request, security, options),
        ...options,
    });
}
/**
 * listToolFilters toolsets
 *
 * @remarks
 * List the tool filter scopes (tags) available on a toolset-backed MCP server and the tools under each, including tools excluded from all filters. Read-only; reflects the explicit tool variations group configured on the toolset, deriving effective tags with the same logic as the runtime ?tags= filter. Returns filtering disabled when no explicit group is set.
 */
export function useListToolsetToolFiltersSuspense(request, security, options) {
    const client = useGramContext();
    return useSuspenseQuery({
        ...buildListToolsetToolFiltersQuery(client, request, security, options),
        ...options,
    });
}
export function setListToolsetToolFiltersData(client, queryKeyBase, data) {
    const key = queryKeyListToolsetToolFilters(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateListToolsetToolFilters(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "toolsets", "listToolFilters", ...queryKeyBase],
    });
}
export function invalidateAllListToolsetToolFilters(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gram/client", "toolsets", "listToolFilters"],
    });
}
//# sourceMappingURL=listToolsetToolFilters.js.map