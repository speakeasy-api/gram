/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsEnvironmentsNumberDeleteEnvironment } from "../../funcs/environmentsEnvironmentsNumberDeleteEnvironment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvironmentsNumberDeleteEnvironmentRequest$inboundSchema,
};

export const tool$environmentsEnvironmentsNumberDeleteEnvironment:
  ToolDefinition<typeof args> = {
    name: "environments-environments-number-delete-environment",
    description: `deleteEnvironment environments

Delete an environment`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await environmentsEnvironmentsNumberDeleteEnvironment(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
