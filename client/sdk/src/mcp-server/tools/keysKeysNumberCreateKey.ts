/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { keysKeysNumberCreateKey } from "../../funcs/keysKeysNumberCreateKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.KeysNumberCreateKeyRequest$inboundSchema,
};

export const tool$keysKeysNumberCreateKey: ToolDefinition<typeof args> = {
  name: "keys-keys-number-create-key",
  description: `createKey keys

Create a new api key`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await keysKeysNumberCreateKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
