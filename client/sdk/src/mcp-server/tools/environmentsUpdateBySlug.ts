/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsUpdateBySlug } from "../../funcs/environmentsUpdateBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvironmentsNumberUpdateEnvironmentRequest$inboundSchema,
};

export const tool$environmentsUpdateBySlug: ToolDefinition<typeof args> = {
  name: "environments-update-by-slug",
  description: `updateEnvironment environments

Update an environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsUpdateBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
