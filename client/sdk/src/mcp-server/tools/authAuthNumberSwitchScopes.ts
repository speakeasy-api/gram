/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authAuthNumberSwitchScopes } from "../../funcs/authAuthNumberSwitchScopes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AuthNumberSwitchScopesRequest$inboundSchema.optional(),
};

export const tool$authAuthNumberSwitchScopes: ToolDefinition<typeof args> = {
  name: "auth-auth-number-switch-scopes",
  description: `switchScopes auth

Switches the authentication scope to a different organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authAuthNumberSwitchScopes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
