/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsToolsetsNumberUpdateToolset } from "../../funcs/toolsetsToolsetsNumberUpdateToolset.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberUpdateToolsetRequest$inboundSchema,
};

export const tool$toolsetsToolsetsNumberUpdateToolset: ToolDefinition<
  typeof args
> = {
  name: "toolsets-toolsets-number-update-toolset",
  description: `updateToolset toolsets

Update a toolset's properties including name, description, and HTTP tools`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsToolsetsNumberUpdateToolset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
