/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsToolsetsNumberGetToolsetDetails } from "../../funcs/toolsetsToolsetsNumberGetToolsetDetails.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberGetToolsetDetailsRequest$inboundSchema,
};

export const tool$toolsetsToolsetsNumberGetToolsetDetails: ToolDefinition<
  typeof args
> = {
  name: "toolsets-toolsets-number-get-toolset-details",
  description: `getToolsetDetails toolsets

Get detailed information about a toolset including full HTTP tool definitions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsToolsetsNumberGetToolsetDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
