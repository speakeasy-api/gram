/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsUpdateBySlug } from "../../funcs/toolsetsUpdateBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateToolsetRequest$inboundSchema,
};

export const tool$toolsetsUpdateBySlug: ToolDefinition<typeof args> = {
  name: "toolsets-update-by-slug",
  description: `updateToolset toolsets

Update a toolset's properties including name, description, and HTTP tools`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsUpdateBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
