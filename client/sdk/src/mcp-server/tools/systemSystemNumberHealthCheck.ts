/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { systemSystemNumberHealthCheck } from "../../funcs/systemSystemNumberHealthCheck.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$systemSystemNumberHealthCheck: ToolDefinition = {
  name: "system-system-number-health-check",
  description: `healthCheck system

Check the health of the service.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await systemSystemNumberHealthCheck(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
