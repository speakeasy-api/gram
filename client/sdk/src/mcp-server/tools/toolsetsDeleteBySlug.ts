/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsDeleteBySlug } from "../../funcs/toolsetsDeleteBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteToolsetRequest$inboundSchema,
};

export const tool$toolsetsDeleteBySlug: ToolDefinition<typeof args> = {
  name: "toolsets-delete-by-slug",
  description: `deleteToolset toolsets

Delete a toolset by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsDeleteBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
