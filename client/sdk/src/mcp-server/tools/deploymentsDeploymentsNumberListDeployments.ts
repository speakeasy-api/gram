/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsDeploymentsNumberListDeployments } from "../../funcs/deploymentsDeploymentsNumberListDeployments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeploymentsNumberListDeploymentsRequest$inboundSchema
    .optional(),
};

export const tool$deploymentsDeploymentsNumberListDeployments: ToolDefinition<
  typeof args
> = {
  name: "deployments-deployments-number-list-deployments",
  description: `listDeployments deployments

List all deployments in descending order of creation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsDeploymentsNumberListDeployments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
