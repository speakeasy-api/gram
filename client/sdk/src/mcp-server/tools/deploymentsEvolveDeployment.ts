/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsEvolveDeployment } from "../../funcs/deploymentsEvolveDeployment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvolveDeploymentRequest$inboundSchema,
};

export const tool$deploymentsEvolveDeployment: ToolDefinition<typeof args> = {
  name: "deployments-evolve-deployment",
  description: `evolve deployments

Create a new deployment with an additional tool sources.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsEvolveDeployment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
