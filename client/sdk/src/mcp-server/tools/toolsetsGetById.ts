/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsGetById } from "../../funcs/toolsetsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberGetToolsetDetailsRequest$inboundSchema,
};

export const tool$toolsetsGetById: ToolDefinition<typeof args> = {
  name: "toolsets-get-by-id",
  description: `getToolsetDetails toolsets

Get detailed information about a toolset including full HTTP tool definitions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
