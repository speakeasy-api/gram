/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsGetById } from "../../funcs/deploymentsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDeploymentRequest$inboundSchema,
};

export const tool$deploymentsGetById: ToolDefinition<typeof args> = {
  name: "deployments-get-by-id",
  description: `getDeployment deployments

Create a deployment to load tool definitions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
