/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authAuthNumberLogout } from "../../funcs/authAuthNumberLogout.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AuthNumberLogoutRequest$inboundSchema.optional(),
};

export const tool$authAuthNumberLogout: ToolDefinition<typeof args> = {
  name: "auth-auth-number-logout",
  description: `logout auth

Logs out the current user by clearing their session.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authAuthNumberLogout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
