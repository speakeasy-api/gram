/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsDeleteBySlug } from "../../funcs/environmentsDeleteBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvironmentsNumberDeleteEnvironmentRequest$inboundSchema,
};

export const tool$environmentsDeleteBySlug: ToolDefinition<typeof args> = {
  name: "environments-delete-by-slug",
  description: `deleteEnvironment environments

Delete an environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsDeleteBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
