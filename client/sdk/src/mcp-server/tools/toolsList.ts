/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsList } from "../../funcs/toolsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListToolsRequest$inboundSchema.optional(),
};

export const tool$toolsList: ToolDefinition<typeof args> = {
  name: "tools-list",
  description: `listTools tools

List all tools for a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
