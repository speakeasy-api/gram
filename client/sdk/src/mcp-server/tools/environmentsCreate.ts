/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsCreate } from "../../funcs/environmentsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvironmentsNumberCreateEnvironmentRequest$inboundSchema,
};

export const tool$environmentsCreate: ToolDefinition<typeof args> = {
  name: "environments-create",
  description: `createEnvironment environments

Create a new environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
