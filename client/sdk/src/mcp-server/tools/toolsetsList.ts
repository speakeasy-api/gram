/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsList } from "../../funcs/toolsetsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberListToolsetsRequest$inboundSchema
    .optional(),
};

export const tool$toolsetsList: ToolDefinition<typeof args> = {
  name: "toolsets-list",
  description: `listToolsets toolsets

List all toolsets for a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
