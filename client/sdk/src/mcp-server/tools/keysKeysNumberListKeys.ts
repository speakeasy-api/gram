/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { keysKeysNumberListKeys } from "../../funcs/keysKeysNumberListKeys.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.KeysNumberListKeysRequest$inboundSchema.optional(),
};

export const tool$keysKeysNumberListKeys: ToolDefinition<typeof args> = {
  name: "keys-keys-number-list-keys",
  description: `listKeys keys

List all api keys for an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await keysKeysNumberListKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
