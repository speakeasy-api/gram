/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authAuthNumberCallback } from "../../funcs/authAuthNumberCallback.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AuthNumberCallbackRequest$inboundSchema,
};

export const tool$authAuthNumberCallback: ToolDefinition<typeof args> = {
  name: "auth-auth-number-callback",
  description: `callback auth

Handles the authentication callback.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authAuthNumberCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
