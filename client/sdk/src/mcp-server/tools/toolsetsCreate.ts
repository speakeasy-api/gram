/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsCreate } from "../../funcs/toolsetsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberCreateToolsetRequest$inboundSchema,
};

export const tool$toolsetsCreate: ToolDefinition<typeof args> = {
  name: "toolsets-create",
  description: `createToolset toolsets

Create a new toolset with associated tools`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
