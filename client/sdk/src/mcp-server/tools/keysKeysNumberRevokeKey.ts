/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { keysKeysNumberRevokeKey } from "../../funcs/keysKeysNumberRevokeKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.KeysNumberRevokeKeyRequest$inboundSchema,
};

export const tool$keysKeysNumberRevokeKey: ToolDefinition<typeof args> = {
  name: "keys-keys-number-revoke-key",
  description: `revokeKey keys

Revoke a api key`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await keysKeysNumberRevokeKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
