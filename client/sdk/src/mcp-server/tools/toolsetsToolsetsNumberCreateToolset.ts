/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsToolsetsNumberCreateToolset } from "../../funcs/toolsetsToolsetsNumberCreateToolset.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsetsNumberCreateToolsetRequest$inboundSchema,
};

export const tool$toolsetsToolsetsNumberCreateToolset: ToolDefinition<
  typeof args
> = {
  name: "toolsets-toolsets-number-create-toolset",
  description: `createToolset toolsets

Create a new toolset with associated tools`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsToolsetsNumberCreateToolset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
