/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authAuthNumberInfo } from "../../funcs/authAuthNumberInfo.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AuthNumberInfoRequest$inboundSchema.optional(),
};

export const tool$authAuthNumberInfo: ToolDefinition<typeof args> = {
  name: "auth-auth-number-info",
  description: `info auth

Provides information about the current authentication status.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authAuthNumberInfo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
