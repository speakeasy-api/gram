/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetsGetBySlug } from "../../funcs/toolsetsGetBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetToolsetRequest$inboundSchema,
};

export const tool$toolsetsGetBySlug: ToolDefinition<typeof args> = {
  name: "toolsets-get-by-slug",
  description: `getToolset toolsets

Get detailed information about a toolset including full HTTP tool definitions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsetsGetBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
