/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chatChatNumberCompletion } from "../../funcs/chatChatNumberCompletion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChatNumberCompletionRequest$inboundSchema,
};

export const tool$chatChatNumberCompletion: ToolDefinition<typeof args> = {
  name: "chat-chat-number-completion",
  description: `completion chat

Create a completion for a given prompt`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chatChatNumberCompletion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
