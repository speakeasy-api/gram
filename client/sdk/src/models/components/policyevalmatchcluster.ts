/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PolicyEvalMatchCluster = {
  /**
   * Number of findings sharing this matched value.
   */
  count: number;
  /**
   * Number of distinct chat sessions this matched value appeared in.
   */
  distinctSessions: number;
  /**
   * Opaque, stable hash of the matched value used to collapse duplicates. Never the raw value.
   */
  matchHash: string;
  /**
   * Redacted/masked representative of the matched value, safe to display (e.g. `<redacted len=N sha=XXXX>`). For non-sensitive sources (e.g. shadow_mcp) this may be the value verbatim, mirroring the rest of the risk UI.
   */
  matchRedacted: string;
  /**
   * Canonical rule id for this cluster.
   */
  ruleId?: string | undefined;
  /**
   * Detection source for this cluster (e.g. gitleaks, presidio, custom).
   */
  source: string;
};

/** @internal */
export const PolicyEvalMatchCluster$inboundSchema: z.ZodMiniType<
  PolicyEvalMatchCluster,
  unknown
> = z.pipe(
  z.object({
    count: z.int(),
    distinct_sessions: z.int(),
    match_hash: z.string(),
    match_redacted: z.string(),
    rule_id: z.optional(z.string()),
    source: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "distinct_sessions": "distinctSessions",
      "match_hash": "matchHash",
      "match_redacted": "matchRedacted",
      "rule_id": "ruleId",
    });
  }),
);

export function policyEvalMatchClusterFromJSON(
  jsonString: string,
): SafeParseResult<PolicyEvalMatchCluster, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyEvalMatchCluster$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyEvalMatchCluster' from JSON`,
  );
}
