/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const TeamInviteStatus = {
  Pending: "pending",
  Accepted: "accepted",
  Expired: "expired",
  Cancelled: "cancelled",
} as const;
export type TeamInviteStatus = ClosedEnum<typeof TeamInviteStatus>;

export type TeamInvite = {
  /**
   * When the invite was created
   */
  createdAt: Date;
  /**
   * The invited email address
   */
  email: string;
  /**
   * When the invite expires
   */
  expiresAt: Date;
  /**
   * The invite ID
   */
  id: string;
  /**
   * Name of the user who sent the invite
   */
  invitedBy: string;
  status: TeamInviteStatus;
};

/** @internal */
export const TeamInviteStatus$inboundSchema: z.ZodNativeEnum<
  typeof TeamInviteStatus
> = z.nativeEnum(TeamInviteStatus);

/** @internal */
export const TeamInvite$inboundSchema: z.ZodType<
  TeamInvite,
  z.ZodTypeDef,
  unknown
> = z.object({
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  email: z.string(),
  expires_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  id: z.string(),
  invited_by: z.string(),
  status: TeamInviteStatus$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "expires_at": "expiresAt",
    "invited_by": "invitedBy",
  });
});

export function teamInviteFromJSON(
  jsonString: string,
): SafeParseResult<TeamInvite, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TeamInvite$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TeamInvite' from JSON`,
  );
}
