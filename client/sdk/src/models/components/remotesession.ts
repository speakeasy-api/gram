/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A remote_session record — Gram's upstream OAuth session for a (principal, remote_session_client) pair. access_token_encrypted and refresh_token_encrypted are never returned.
 */
export type RemoteSession = {
  /**
   * Upstream access-token expiry. Independent of refresh_expires_at.
   */
  accessExpiresAt: Date;
  createdAt: Date;
  /**
   * The remote_session id.
   */
  id: string;
  /**
   * The session's principal URN (user:<id> | apikey:<uuid> | anonymous:<mcp-session-id>).
   */
  subjectUrn: string;
  /**
   * Upstream refresh-token expiry. Null when the session has no refresh token.
   */
  refreshExpiresAt?: Date | undefined;
  /**
   * The remote_session_client this session was minted against.
   */
  remoteSessionClientId: string;
  /**
   * Scopes held by this session.
   */
  scopes: Array<string>;
  updatedAt: Date;
  /**
   * The user_session_issuer this session is bound to.
   */
  userSessionIssuerId: string;
};

/** @internal */
export const RemoteSession$inboundSchema: z.ZodMiniType<
  RemoteSession,
  unknown
> = z.pipe(
  z.object({
    access_expires_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    subject_urn: z.string(),
    refresh_expires_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    remote_session_client_id: z.string(),
    scopes: z.array(z.string()),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    user_session_issuer_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_expires_at": "accessExpiresAt",
      "created_at": "createdAt",
      "subject_urn": "subjectUrn",
      "refresh_expires_at": "refreshExpiresAt",
      "remote_session_client_id": "remoteSessionClientId",
      "updated_at": "updatedAt",
      "user_session_issuer_id": "userSessionIssuerId",
    });
  }),
);

export function remoteSessionFromJSON(
  jsonString: string,
): SafeParseResult<RemoteSession, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RemoteSession$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RemoteSession' from JSON`,
  );
}
