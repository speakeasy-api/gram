/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DetectionDescriptorField,
  DetectionDescriptorField$inboundSchema,
} from "./detectiondescriptorfield.js";

/**
 * One CEL type. Registered identically in the Go engine and the client checker under 'name'.
 */
export type DetectionDescriptorType = {
  /**
   * Plain-English description of the type.
   */
  description: string;
  /**
   * Short human label for the editor (e.g. 'field', 'tool').
   */
  displayName: string;
  /**
   * Typed members of a non-opaque object type. Empty when opaque.
   */
  fields: Array<DetectionDescriptorField>;
  /**
   * Engine type name (e.g. 'field', 'celenv.celTool').
   */
  name: string;
  /**
   * True when the type has no readable members, only receiver methods (e.g. 'field').
   */
  opaque: boolean;
};

/** @internal */
export const DetectionDescriptorType$inboundSchema: z.ZodMiniType<
  DetectionDescriptorType,
  unknown
> = z.object({
  description: z.string(),
  displayName: z.string(),
  fields: z.array(DetectionDescriptorField$inboundSchema),
  name: z.string(),
  opaque: z.boolean(),
});

export function detectionDescriptorTypeFromJSON(
  jsonString: string,
): SafeParseResult<DetectionDescriptorType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DetectionDescriptorType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DetectionDescriptorType' from JSON`,
  );
}
