/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One CEL macro. Macros are built into both engines (not declared), listed here for completion and documentation only.
 */
export type DetectionDescriptorMacro = {
  /**
   * Plain-English description of the macro's behaviour.
   */
  description: string;
  /**
   * Macro name (e.g. 'exists', 'has', 'map').
   */
  name: string;
  /**
   * True when the macro yields a boolean verdict; false when it yields a list.
   */
  returnsBool: boolean;
  /**
   * Human-readable call signature.
   */
  signature: string;
};

/** @internal */
export const DetectionDescriptorMacro$inboundSchema: z.ZodMiniType<
  DetectionDescriptorMacro,
  unknown
> = z.object({
  description: z.string(),
  name: z.string(),
  returnsBool: z.boolean(),
  signature: z.string(),
});

export function detectionDescriptorMacroFromJSON(
  jsonString: string,
): SafeParseResult<DetectionDescriptorMacro, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DetectionDescriptorMacro$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DetectionDescriptorMacro' from JSON`,
  );
}
