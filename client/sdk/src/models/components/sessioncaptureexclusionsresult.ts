/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * List of users excluded from session capture for the active organization.
 */
export type SessionCaptureExclusionsResult = {
  /**
   * Gram user IDs (matching the Team roster IDs) excluded from session capture.
   */
  userIds: Array<string>;
};

/** @internal */
export const SessionCaptureExclusionsResult$inboundSchema: z.ZodMiniType<
  SessionCaptureExclusionsResult,
  unknown
> = z.pipe(
  z.object({
    user_ids: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "user_ids": "userIds",
    });
  }),
);

export function sessionCaptureExclusionsResultFromJSON(
  jsonString: string,
): SafeParseResult<SessionCaptureExclusionsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionCaptureExclusionsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionCaptureExclusionsResult' from JSON`,
  );
}
