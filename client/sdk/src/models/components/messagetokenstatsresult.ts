/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MessageTokenStatsPoint,
  MessageTokenStatsPoint$inboundSchema,
} from "./messagetokenstatspoint.js";

/**
 * Result of the message-level token stats query
 */
export type MessageTokenStatsResult = {
  /**
   * Timeseries bucket width in seconds. Always 86400 — the stats are bucketed daily.
   */
  intervalSeconds: number;
  /**
   * Gap-filled daily buckets in ascending time order
   */
  points: Array<MessageTokenStatsPoint>;
};

/** @internal */
export const MessageTokenStatsResult$inboundSchema: z.ZodMiniType<
  MessageTokenStatsResult,
  unknown
> = z.pipe(
  z.object({
    interval_seconds: z.int(),
    points: z.array(MessageTokenStatsPoint$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_seconds": "intervalSeconds",
    });
  }),
);

export function messageTokenStatsResultFromJSON(
  jsonString: string,
): SafeParseResult<MessageTokenStatsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MessageTokenStatsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MessageTokenStatsResult' from JSON`,
  );
}
