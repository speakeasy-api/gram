/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Form for creating or updating a principal grant.
 */
export type UpsertGrantForm = {
  /**
   * The principal URN (e.g. "user:user_abc", "role:admin").
   */
  principalUrn: string;
  /**
   * The resource this grant applies to. Use "*" for unrestricted access.
   */
  resource: string;
  /**
   * The scope to grant (e.g. "build:read", "mcp:connect").
   */
  scope: string;
};

/** @internal */
export type UpsertGrantForm$Outbound = {
  principal_urn: string;
  resource: string;
  scope: string;
};

/** @internal */
export const UpsertGrantForm$outboundSchema: z.ZodMiniType<
  UpsertGrantForm$Outbound,
  UpsertGrantForm
> = z.pipe(
  z.object({
    principalUrn: z.string(),
    resource: z.string(),
    scope: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      principalUrn: "principal_urn",
    });
  }),
);

export function upsertGrantFormToJSON(
  upsertGrantForm: UpsertGrantForm,
): string {
  return JSON.stringify(UpsertGrantForm$outboundSchema.parse(upsertGrantForm));
}
