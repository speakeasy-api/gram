/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RiskCapabilitiesResult = {
  /**
   * Whether the prompt-injection ML classifier is configured on this server.
   */
  piClassifierEnabled: boolean;
};

/** @internal */
export const RiskCapabilitiesResult$inboundSchema: z.ZodMiniType<
  RiskCapabilitiesResult,
  unknown
> = z.pipe(
  z.object({
    pi_classifier_enabled: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "pi_classifier_enabled": "piClassifierEnabled",
    });
  }),
);

export function riskCapabilitiesResultFromJSON(
  jsonString: string,
): SafeParseResult<RiskCapabilitiesResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskCapabilitiesResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskCapabilitiesResult' from JSON`,
  );
}
