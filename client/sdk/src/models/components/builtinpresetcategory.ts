/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BuiltinPresetEntry,
  BuiltinPresetEntry$inboundSchema,
} from "./builtinpresetentry.js";

/**
 * A named group of built-in preset rules.
 */
export type BuiltinPresetCategory = {
  /**
   * The rules in this category.
   */
  entries: Array<BuiltinPresetEntry>;
  /**
   * Human category label, e.g. "Test credit cards".
   */
  label: string;
};

/** @internal */
export const BuiltinPresetCategory$inboundSchema: z.ZodMiniType<
  BuiltinPresetCategory,
  unknown
> = z.object({
  entries: z.array(BuiltinPresetEntry$inboundSchema),
  label: z.string(),
});

export function builtinPresetCategoryFromJSON(
  jsonString: string,
): SafeParseResult<BuiltinPresetCategory, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BuiltinPresetCategory$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BuiltinPresetCategory' from JSON`,
  );
}
