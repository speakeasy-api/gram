/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { PolicyEvalRun, PolicyEvalRun$inboundSchema } from "./policyevalrun.js";

export type ListPolicyEvalRunsResult = {
  /**
   * Cursor for the next page of runs.
   */
  nextCursor?: string | undefined;
  /**
   * The list of eval runs.
   */
  runs: Array<PolicyEvalRun>;
};

/** @internal */
export const ListPolicyEvalRunsResult$inboundSchema: z.ZodMiniType<
  ListPolicyEvalRunsResult,
  unknown
> = z.pipe(
  z.object({
    next_cursor: z.optional(z.string()),
    runs: z.array(PolicyEvalRun$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function listPolicyEvalRunsResultFromJSON(
  jsonString: string,
): SafeParseResult<ListPolicyEvalRunsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPolicyEvalRunsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPolicyEvalRunsResult' from JSON`,
  );
}
