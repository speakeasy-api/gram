/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PolicyAccessTarget,
  PolicyAccessTarget$inboundSchema,
} from "./policyaccesstarget.js";

/**
 * An active risk-policy bypass grant.
 */
export type PolicyBypassGrant = {
  createdAt: Date;
  /**
   * Principal grant ID.
   */
  id: string;
  /**
   * Risk policy ID.
   */
  policyId: string;
  /**
   * Policy display name.
   */
  policyName?: string | undefined;
  /**
   * Principal type, e.g. user or role.
   */
  principalType: string;
  /**
   * Principal holding the bypass.
   */
  principalUrn: string;
  /**
   * A generic target narrowing for a policy bypass. Empty fields mean the whole policy.
   */
  target: PolicyAccessTarget;
  updatedAt: Date;
};

/** @internal */
export const PolicyBypassGrant$inboundSchema: z.ZodMiniType<
  PolicyBypassGrant,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    policy_id: z.string(),
    policy_name: z.optional(z.string()),
    principal_type: z.string(),
    principal_urn: z.string(),
    target: PolicyAccessTarget$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "policy_id": "policyId",
      "policy_name": "policyName",
      "principal_type": "principalType",
      "principal_urn": "principalUrn",
      "updated_at": "updatedAt",
    });
  }),
);

export function policyBypassGrantFromJSON(
  jsonString: string,
): SafeParseResult<PolicyBypassGrant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyBypassGrant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyBypassGrant' from JSON`,
  );
}
