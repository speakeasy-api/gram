/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Skill = {
  activeVersionId?: string | undefined;
  createdAt: Date;
  description?: string | undefined;
  id: string;
  name: string;
  skillUuid?: string | undefined;
  slug: string;
  updatedAt: Date;
};

/** @internal */
export const Skill$inboundSchema: z.ZodMiniType<Skill, unknown> = z.pipe(
  z.object({
    active_version_id: z.optional(z.string()),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    description: z.optional(z.string()),
    id: z.string(),
    name: z.string(),
    skill_uuid: z.optional(z.string()),
    slug: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "active_version_id": "activeVersionId",
      "created_at": "createdAt",
      "skill_uuid": "skillUuid",
      "updated_at": "updatedAt",
    });
  }),
);

export function skillFromJSON(
  jsonString: string,
): SafeParseResult<Skill, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Skill$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Skill' from JSON`,
  );
}
