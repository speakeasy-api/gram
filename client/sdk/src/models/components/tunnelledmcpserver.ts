/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TunnelledMcpConnection,
  TunnelledMcpConnection$inboundSchema,
} from "./tunnelledmcpconnection.js";

/**
 * Derived live connection status for a tunnelled MCP server source
 */
export const ConnectionStatus = {
  Connected: "connected",
  Inactive: "inactive",
  NeverConnected: "never_connected",
} as const;
/**
 * Derived live connection status for a tunnelled MCP server source
 */
export type ConnectionStatus = ClosedEnum<typeof ConnectionStatus>;

/**
 * Stored lifecycle status for a tunnelled MCP server source
 */
export const TunnelledMcpServerStatus = {
  Created: "created",
  Active: "active",
  Revoked: "revoked",
} as const;
/**
 * Stored lifecycle status for a tunnelled MCP server source
 */
export type TunnelledMcpServerStatus = ClosedEnum<
  typeof TunnelledMcpServerStatus
>;

/**
 * A customer-hosted MCP server connected through a tunnel
 */
export type TunnelledMcpServer = {
  /**
   * Number of active tunnel connections currently visible in Redis
   */
  activeConnectionCount: number;
  /**
   * Total MCP consumer sessions currently pinned across active tunnel connections
   */
  activeConsumerSessionCount: number;
  /**
   * Most recent agent version reported by the tunnel
   */
  agentVersion?: string | undefined;
  /**
   * Derived live connection status for a tunnelled MCP server source
   */
  connectionStatus: ConnectionStatus;
  /**
   * Live tunnel connections currently visible in Redis
   */
  connections: Array<TunnelledMcpConnection>;
  /**
   * When the tunnelled MCP server source was created
   */
  createdAt: Date;
  /**
   * The ID of the tunnelled MCP server
   */
  id: string;
  /**
   * Non-secret prefix of the tunnel key
   */
  keyPrefix: string;
  /**
   * Most recent persisted heartbeat timestamp
   */
  lastSeenAt?: Date | undefined;
  /**
   * Human-readable name for the tunnelled MCP server
   */
  name: string;
  /**
   * The project ID this tunnelled MCP server belongs to
   */
  projectId: string;
  /**
   * Stored lifecycle status for a tunnelled MCP server source
   */
  status: TunnelledMcpServerStatus;
  /**
   * When the tunnelled MCP server source was last updated
   */
  updatedAt: Date;
};

/** @internal */
export const ConnectionStatus$inboundSchema: z.ZodMiniEnum<
  typeof ConnectionStatus
> = z.enum(ConnectionStatus);

/** @internal */
export const TunnelledMcpServerStatus$inboundSchema: z.ZodMiniEnum<
  typeof TunnelledMcpServerStatus
> = z.enum(TunnelledMcpServerStatus);

/** @internal */
export const TunnelledMcpServer$inboundSchema: z.ZodMiniType<
  TunnelledMcpServer,
  unknown
> = z.pipe(
  z.object({
    active_connection_count: z.int(),
    active_consumer_session_count: z.int(),
    agent_version: z.optional(z.string()),
    connection_status: ConnectionStatus$inboundSchema,
    connections: z.array(TunnelledMcpConnection$inboundSchema),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    key_prefix: z.string(),
    last_seen_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    name: z.string(),
    project_id: z.string(),
    status: TunnelledMcpServerStatus$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "active_connection_count": "activeConnectionCount",
      "active_consumer_session_count": "activeConsumerSessionCount",
      "agent_version": "agentVersion",
      "connection_status": "connectionStatus",
      "created_at": "createdAt",
      "key_prefix": "keyPrefix",
      "last_seen_at": "lastSeenAt",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function tunnelledMcpServerFromJSON(
  jsonString: string,
): SafeParseResult<TunnelledMcpServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TunnelledMcpServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TunnelledMcpServer' from JSON`,
  );
}
