/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { RiskResult, RiskResult$inboundSchema } from "./riskresult.js";

export type RiskFindingCluster = {
  /**
   * Distinct deterministic source|rule|span keys among the cluster's members: how many flat-list groups this one semantic cluster collapses.
   */
  baselineGroupCount?: number | undefined;
  /**
   * Number of findings in the cluster.
   */
  count: number;
  /**
   * One-sentence description of what the cluster represents.
   */
  description?: string | undefined;
  /**
   * Number of distinct chats the cluster's findings span.
   */
  distinctChats: number;
  /**
   * Synthetic cluster identifier within this response (e.g. 'c0').
   */
  id: string;
  /**
   * Short human label for the cluster (LLM-generated, best-effort).
   */
  label?: string | undefined;
  /**
   * The findings in the cluster (raw match; mask client-side).
   */
  members: Array<RiskResult>;
  /**
   * Distinct rule ids present in the cluster.
   */
  ruleIds?: Array<string> | undefined;
  /**
   * Distinct detection sources present in the cluster.
   */
  sources?: Array<string> | undefined;
};

/** @internal */
export const RiskFindingCluster$inboundSchema: z.ZodMiniType<
  RiskFindingCluster,
  unknown
> = z.pipe(
  z.object({
    baseline_group_count: z.optional(z.int()),
    count: z.int(),
    description: z.optional(z.string()),
    distinct_chats: z.int(),
    id: z.string(),
    label: z.optional(z.string()),
    members: z.array(RiskResult$inboundSchema),
    rule_ids: z.optional(z.array(z.string())),
    sources: z.optional(z.array(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "baseline_group_count": "baselineGroupCount",
      "distinct_chats": "distinctChats",
      "rule_ids": "ruleIds",
    });
  }),
);

export function riskFindingClusterFromJSON(
  jsonString: string,
): SafeParseResult<RiskFindingCluster, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskFindingCluster$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskFindingCluster' from JSON`,
  );
}
