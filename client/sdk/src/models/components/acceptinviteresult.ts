/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AcceptInviteResult = {
  /**
   * The slug of the organization the user was added to
   */
  organizationSlug: string;
};

/** @internal */
export const AcceptInviteResult$inboundSchema: z.ZodType<
  AcceptInviteResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  organization_slug: z.string(),
}).transform((v) => {
  return remap$(v, {
    "organization_slug": "organizationSlug",
  });
});

export function acceptInviteResultFromJSON(
  jsonString: string,
): SafeParseResult<AcceptInviteResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInviteResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInviteResult' from JSON`,
  );
}
