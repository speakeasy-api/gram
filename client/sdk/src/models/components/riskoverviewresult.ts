/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RiskOverviewCategory,
  RiskOverviewCategory$inboundSchema,
} from "./riskoverviewcategory.js";
import {
  RiskOverviewTimeSeriesFinding,
  RiskOverviewTimeSeriesFinding$inboundSchema,
} from "./riskoverviewtimeseriesfinding.js";
import {
  RiskOverviewUser,
  RiskOverviewUser$inboundSchema,
} from "./riskoverviewuser.js";

export type RiskOverviewResult = {
  /**
   * Enabled risk policies for the current project.
   */
  activePolicies: number;
  /**
   * Policy findings in the window.
   */
  findings: number;
  /**
   * Chat sessions with at least one finding in the window.
   */
  flaggedSessions: number;
  /**
   * Inclusive start of the overview window.
   */
  from: Date;
  /**
   * Messages analyzed by risk policies in the window.
   */
  messagesScanned: number;
  /**
   * Time-series finding counts by category in the window.
   */
  timeSeriesFindings: Array<RiskOverviewTimeSeriesFinding>;
  /**
   * Exclusive end of the overview window.
   */
  to: Date;
  /**
   * Top policy categories by finding count.
   */
  topCategories: Array<RiskOverviewCategory>;
  /**
   * Top users by finding count.
   */
  topUsers: Array<RiskOverviewUser>;
};

/** @internal */
export const RiskOverviewResult$inboundSchema: z.ZodMiniType<
  RiskOverviewResult,
  unknown
> = z.pipe(
  z.object({
    active_policies: z.int(),
    findings: z.int(),
    flagged_sessions: z.int(),
    from: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    messages_scanned: z.int(),
    time_series_findings: z.array(RiskOverviewTimeSeriesFinding$inboundSchema),
    to: z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    top_categories: z.array(RiskOverviewCategory$inboundSchema),
    top_users: z.array(RiskOverviewUser$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "active_policies": "activePolicies",
      "flagged_sessions": "flaggedSessions",
      "messages_scanned": "messagesScanned",
      "time_series_findings": "timeSeriesFindings",
      "top_categories": "topCategories",
      "top_users": "topUsers",
    });
  }),
);

export function riskOverviewResultFromJSON(
  jsonString: string,
): SafeParseResult<RiskOverviewResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskOverviewResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskOverviewResult' from JSON`,
  );
}
