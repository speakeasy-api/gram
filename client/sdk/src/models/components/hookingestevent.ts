/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Canonical Gram hook event type.
 */
export const HookIngestEventType = {
  SessionStarted: "session.started",
  SessionUpdated: "session.updated",
  SessionEnded: "session.ended",
  PromptSubmitted: "prompt.submitted",
  ToolRequested: "tool.requested",
  ToolCompleted: "tool.completed",
  ToolFailed: "tool.failed",
  AssistantResponded: "assistant.responded",
  UsageReported: "usage.reported",
  SkillActivated: "skill.activated",
  NotificationReported: "notification.reported",
} as const;
/**
 * Canonical Gram hook event type.
 */
export type HookIngestEventType = ClosedEnum<typeof HookIngestEventType>;

/**
 * Canonical Gram feature event.
 */
export type HookIngestEvent = {
  /**
   * RFC3339 timestamp from the local agent. Defaults to receive time when absent.
   */
  occurredAt?: string | undefined;
  /**
   * Canonical Gram hook event type.
   */
  type: HookIngestEventType;
};

/** @internal */
export const HookIngestEventType$outboundSchema: z.ZodMiniEnum<
  typeof HookIngestEventType
> = z.enum(HookIngestEventType);

/** @internal */
export type HookIngestEvent$Outbound = {
  occurred_at?: string | undefined;
  type: string;
};

/** @internal */
export const HookIngestEvent$outboundSchema: z.ZodMiniType<
  HookIngestEvent$Outbound,
  HookIngestEvent
> = z.pipe(
  z.object({
    occurredAt: z.optional(z.string()),
    type: HookIngestEventType$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      occurredAt: "occurred_at",
    });
  }),
);

export function hookIngestEventToJSON(
  hookIngestEvent: HookIngestEvent,
): string {
  return JSON.stringify(HookIngestEvent$outboundSchema.parse(hookIngestEvent));
}
