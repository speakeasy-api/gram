/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Per-project config that controls polling Cursor Admin API usage events. The API key is write-only; reads only expose whether a key is configured.
 */
export type CursorIntegrationConfig = {
  /**
   * ISO 8601 timestamp when the config was created. Omitted when no config is set.
   */
  createdAt?: Date | undefined;
  /**
   * Whether Cursor usage polling is active.
   */
  enabled: boolean;
  /**
   * Whether a Cursor API key is currently stored. The key itself is never returned.
   */
  hasApiKey: boolean;
  /**
   * Config ID. Omitted when no config is set for the project.
   */
  id?: string | undefined;
  /**
   * ISO 8601 timestamp for the polling high-water mark. Omitted when no config is set.
   */
  lastPolledAt?: Date | undefined;
  /**
   * Organization the config belongs to.
   */
  organizationId: string;
  /**
   * Project the config belongs to.
   */
  projectId: string;
  /**
   * ISO 8601 timestamp of the most recent change. Omitted when no config is set.
   */
  updatedAt?: Date | undefined;
};

/** @internal */
export const CursorIntegrationConfig$inboundSchema: z.ZodMiniType<
  CursorIntegrationConfig,
  unknown
> = z.pipe(
  z.object({
    created_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    enabled: z.boolean(),
    has_api_key: z.boolean(),
    id: z.optional(z.string()),
    last_polled_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    organization_id: z.string(),
    project_id: z.string(),
    updated_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "has_api_key": "hasApiKey",
      "last_polled_at": "lastPolledAt",
      "organization_id": "organizationId",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function cursorIntegrationConfigFromJSON(
  jsonString: string,
): SafeParseResult<CursorIntegrationConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CursorIntegrationConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CursorIntegrationConfig' from JSON`,
  );
}
