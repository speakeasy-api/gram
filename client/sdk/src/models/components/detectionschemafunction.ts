/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One matcher/helper function available in a detection or scope CEL expression.
 */
export type DetectionSchemaFunction = {
  /**
   * Plain-English description of the matcher's behaviour, including span granularity.
   */
  description: string;
  /**
   * Function name (e.g. 'match', 'includes', 'get').
   */
  name: string;
  /**
   * Human-readable call signature (e.g. 'field.match(pattern: string) -> bool').
   */
  signature: string;
};

/** @internal */
export const DetectionSchemaFunction$inboundSchema: z.ZodMiniType<
  DetectionSchemaFunction,
  unknown
> = z.object({
  description: z.string(),
  name: z.string(),
  signature: z.string(),
});

export function detectionSchemaFunctionFromJSON(
  jsonString: string,
): SafeParseResult<DetectionSchemaFunction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DetectionSchemaFunction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DetectionSchemaFunction' from JSON`,
  );
}
