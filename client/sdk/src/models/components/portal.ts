/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { PortalServer, PortalServer$inboundSchema } from "./portalserver.js";

export type Portal = {
  /**
   * Resolved display name (override → project.name).
   */
  displayName: string;
  /**
   * Whether the portal is enabled.
   */
  enabled: boolean;
  /**
   * Resolved logo URL or empty when none.
   */
  logoUrl?: string | undefined;
  /**
   * The project's slug.
   */
  projectSlug: string;
  /**
   * Cards to render on the portal.
   */
  servers: Array<PortalServer>;
  /**
   * Tagline if set.
   */
  tagline?: string | undefined;
};

/** @internal */
export const Portal$inboundSchema: z.ZodMiniType<Portal, unknown> = z.pipe(
  z.object({
    display_name: z.string(),
    enabled: z._default(z.boolean(), false),
    logo_url: z.optional(z.string()),
    project_slug: z.string(),
    servers: z.array(PortalServer$inboundSchema),
    tagline: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
      "logo_url": "logoUrl",
      "project_slug": "projectSlug",
    });
  }),
);

export function portalFromJSON(
  jsonString: string,
): SafeParseResult<Portal, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Portal$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Portal' from JSON`,
  );
}
