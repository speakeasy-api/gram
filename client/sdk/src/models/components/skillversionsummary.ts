/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const SkillVersionSummaryState = {
  PendingReview: "pending_review",
  Active: "active",
  Superseded: "superseded",
} as const;
export type SkillVersionSummaryState = ClosedEnum<
  typeof SkillVersionSummaryState
>;

export type SkillVersionSummary = {
  assetFormat: string;
  authorName?: string | undefined;
  contentSha256: string;
  createdAt: Date;
  firstSeenAt?: Date | undefined;
  id: string;
  sizeBytes: number;
  state?: SkillVersionSummaryState | undefined;
};

/** @internal */
export const SkillVersionSummaryState$inboundSchema: z.ZodMiniEnum<
  typeof SkillVersionSummaryState
> = z.enum(SkillVersionSummaryState);

/** @internal */
export const SkillVersionSummary$inboundSchema: z.ZodMiniType<
  SkillVersionSummary,
  unknown
> = z.pipe(
  z.object({
    asset_format: z.string(),
    author_name: z.optional(z.string()),
    content_sha256: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    first_seen_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    id: z.string(),
    size_bytes: z.int(),
    state: z.optional(SkillVersionSummaryState$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "asset_format": "assetFormat",
      "author_name": "authorName",
      "content_sha256": "contentSha256",
      "created_at": "createdAt",
      "first_seen_at": "firstSeenAt",
      "size_bytes": "sizeBytes",
    });
  }),
);

export function skillVersionSummaryFromJSON(
  jsonString: string,
): SafeParseResult<SkillVersionSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SkillVersionSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SkillVersionSummary' from JSON`,
  );
}
