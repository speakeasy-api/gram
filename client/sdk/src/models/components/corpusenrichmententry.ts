/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CorpusEnrichmentEntry = {
  /**
   * Path of the file in the corpus
   */
  filePath: string;
  /**
   * Number of open drafts for this file
   */
  openDrafts: number;
};

/** @internal */
export const CorpusEnrichmentEntry$inboundSchema: z.ZodMiniType<
  CorpusEnrichmentEntry,
  unknown
> = z.pipe(
  z.object({
    file_path: z.string(),
    open_drafts: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "file_path": "filePath",
      "open_drafts": "openDrafts",
    });
  }),
);

export function corpusEnrichmentEntryFromJSON(
  jsonString: string,
): SafeParseResult<CorpusEnrichmentEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CorpusEnrichmentEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CorpusEnrichmentEntry' from JSON`,
  );
}
