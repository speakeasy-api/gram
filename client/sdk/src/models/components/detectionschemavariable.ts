/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One variable an author may reference in a detection or scope CEL expression.
 */
export type DetectionSchemaVariable = {
  /**
   * Plain-English description of what the variable holds.
   */
  description: string;
  /**
   * Variable name as written in CEL (e.g. 'content', 'tools').
   */
  name: string;
  /**
   * Descriptive type tag for the editor (e.g. 'field', 'list(tool)', 'string').
   */
  type: string;
};

/** @internal */
export const DetectionSchemaVariable$inboundSchema: z.ZodMiniType<
  DetectionSchemaVariable,
  unknown
> = z.object({
  description: z.string(),
  name: z.string(),
  type: z.string(),
});

export function detectionSchemaVariableFromJSON(
  jsonString: string,
): SafeParseResult<DetectionSchemaVariable, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DetectionSchemaVariable$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DetectionSchemaVariable' from JSON`,
  );
}
