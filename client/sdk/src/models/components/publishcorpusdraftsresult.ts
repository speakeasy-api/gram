/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PublishCorpusDraftsResult = {
  /**
   * The commit SHA of the published changes
   */
  commitSha: string;
};

/** @internal */
export const PublishCorpusDraftsResult$inboundSchema: z.ZodMiniType<
  PublishCorpusDraftsResult,
  unknown
> = z.pipe(
  z.object({
    commit_sha: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "commit_sha": "commitSha",
    });
  }),
);

export function publishCorpusDraftsResultFromJSON(
  jsonString: string,
): SafeParseResult<PublishCorpusDraftsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublishCorpusDraftsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublishCorpusDraftsResult' from JSON`,
  );
}
