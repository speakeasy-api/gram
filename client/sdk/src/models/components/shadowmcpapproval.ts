/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ShadowMCPApproval = {
  /**
   * When the approval was recorded.
   */
  approvedAt: Date;
  /**
   * User that recorded the approval.
   */
  approvedBy?: string | undefined;
  /**
   * The risk policy ID this approval is scoped to.
   */
  policyId: string;
  /**
   * Display name of the MCP server, when known.
   */
  serverName?: string | undefined;
  /**
   * The approved MCP server URL.
   */
  url: string;
};

/** @internal */
export const ShadowMCPApproval$inboundSchema: z.ZodMiniType<
  ShadowMCPApproval,
  unknown
> = z.pipe(
  z.object({
    approved_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    approved_by: z.optional(z.string()),
    policy_id: z.string(),
    server_name: z.optional(z.string()),
    url: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "approved_at": "approvedAt",
      "approved_by": "approvedBy",
      "policy_id": "policyId",
      "server_name": "serverName",
    });
  }),
);

export function shadowMCPApprovalFromJSON(
  jsonString: string,
): SafeParseResult<ShadowMCPApproval, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ShadowMCPApproval$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ShadowMCPApproval' from JSON`,
  );
}
