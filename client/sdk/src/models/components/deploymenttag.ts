/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DeploymentTag = {
  /**
   * The creation date of the tag.
   */
  createdAt: Date;
  /**
   * The ID of the deployment this tag currently points to. May be null if the deployment was deleted.
   */
  deploymentId?: string | undefined;
  /**
   * The ID of the deployment tag.
   */
  id: string;
  /**
   * The name of the tag (e.g., 'main', 'latest', 'v1.2.3').
   */
  name: string;
  /**
   * The ID of the project this tag belongs to.
   */
  projectId: string;
  /**
   * The last update date of the tag.
   */
  updatedAt: Date;
};

/** @internal */
export const DeploymentTag$inboundSchema: z.ZodMiniType<
  DeploymentTag,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    deployment_id: z.optional(z.string()),
    id: z.string(),
    name: z.string(),
    project_id: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "deployment_id": "deploymentId",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function deploymentTagFromJSON(
  jsonString: string,
): SafeParseResult<DeploymentTag, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeploymentTag$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeploymentTag' from JSON`,
  );
}
