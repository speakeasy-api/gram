/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A generic target narrowing for a policy bypass. Empty fields mean the whole policy.
 */
export type PolicyAccessTarget = {
  /**
   * Selector dimensions used for the bypass grant.
   */
  dimensions: { [k: string]: string };
  /**
   * Stable canonical target key.
   */
  key: string;
  /**
   * Target kind, such as shadow_mcp_server. Empty means whole policy.
   */
  kind: string;
  /**
   * Human-readable target label.
   */
  label: string;
};

/** @internal */
export const PolicyAccessTarget$inboundSchema: z.ZodMiniType<
  PolicyAccessTarget,
  unknown
> = z.object({
  dimensions: z.record(z.string(), z.string()),
  key: z.string(),
  kind: z.string(),
  label: z.string(),
});

export function policyAccessTargetFromJSON(
  jsonString: string,
): SafeParseResult<PolicyAccessTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyAccessTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyAccessTarget' from JSON`,
  );
}
