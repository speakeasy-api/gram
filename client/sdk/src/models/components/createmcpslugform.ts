/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Form for creating a new MCP slug. Platform-domain slugs (no custom_domain_id) must be prefixed with the organization slug.
 */
export type CreateMcpSlugForm = {
  /**
   * The ID of the custom domain to register the slug under. Omit for a platform-domain slug.
   */
  customDomainId?: string | undefined;
  /**
   * The ID of the MCP frontend this slug addresses
   */
  mcpFrontendId: string;
  /**
   * A url-friendly label (up to 128 characters) that addresses an MCP frontend through a slug-based URL. Platform-domain slugs (no custom domain) must be prefixed with the organization slug.
   */
  slug: string;
};

/** @internal */
export type CreateMcpSlugForm$Outbound = {
  custom_domain_id?: string | undefined;
  mcp_frontend_id: string;
  slug: string;
};

/** @internal */
export const CreateMcpSlugForm$outboundSchema: z.ZodMiniType<
  CreateMcpSlugForm$Outbound,
  CreateMcpSlugForm
> = z.pipe(
  z.object({
    customDomainId: z.optional(z.string()),
    mcpFrontendId: z.string(),
    slug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      customDomainId: "custom_domain_id",
      mcpFrontendId: "mcp_frontend_id",
    });
  }),
);

export function createMcpSlugFormToJSON(
  createMcpSlugForm: CreateMcpSlugForm,
): string {
  return JSON.stringify(
    CreateMcpSlugForm$outboundSchema.parse(createMcpSlugForm),
  );
}
