/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Source AI tool the event came from
 */
export const DispatchHookPayloadTool = {
  Cursor: "cursor",
  ClaudeCode: "claude_code",
  Codex: "codex",
} as const;
/**
 * Source AI tool the event came from
 */
export type DispatchHookPayloadTool = ClosedEnum<
  typeof DispatchHookPayloadTool
>;

/**
 * Agent-forwarded hook event: the device agent authenticates with its agent-scoped key and forwards a tool's hook event on behalf of a user.
 */
export type DispatchHookPayload = {
  /**
   * Optional originating hostname.
   */
  hookHostname?: string | undefined;
  /**
   * Optional per-invocation token reused across retries so a redelivered event is stored once.
   */
  idempotencyKey?: string | undefined;
  /**
   * The tool's raw hook event JSON, opaque to the transport.
   */
  payload: any;
  /**
   * Optional project override. When omitted, the event attributes to the org's default project; when set, it must name a project in the authenticated org. The override hook exists for future multi-project routing; the agent does not populate it today.
   */
  projectSlug?: string | undefined;
  /**
   * Source AI tool the event came from
   */
  tool: DispatchHookPayloadTool;
  /**
   * Email of the user the agent resolved and is vouching for, attributed within the authenticated org.
   */
  userEmail: string;
};

/** @internal */
export const DispatchHookPayloadTool$outboundSchema: z.ZodMiniEnum<
  typeof DispatchHookPayloadTool
> = z.enum(DispatchHookPayloadTool);

/** @internal */
export type DispatchHookPayload$Outbound = {
  hook_hostname?: string | undefined;
  idempotency_key?: string | undefined;
  payload: any;
  project_slug?: string | undefined;
  tool: string;
  user_email: string;
};

/** @internal */
export const DispatchHookPayload$outboundSchema: z.ZodMiniType<
  DispatchHookPayload$Outbound,
  DispatchHookPayload
> = z.pipe(
  z.object({
    hookHostname: z.optional(z.string()),
    idempotencyKey: z.optional(z.string()),
    payload: z.any(),
    projectSlug: z.optional(z.string()),
    tool: DispatchHookPayloadTool$outboundSchema,
    userEmail: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      hookHostname: "hook_hostname",
      idempotencyKey: "idempotency_key",
      projectSlug: "project_slug",
      userEmail: "user_email",
    });
  }),
);

export function dispatchHookPayloadToJSON(
  dispatchHookPayload: DispatchHookPayload,
): string {
  return JSON.stringify(
    DispatchHookPayload$outboundSchema.parse(dispatchHookPayload),
  );
}
