/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * An MCP slug: a url-friendly identifier that addresses an MCP frontend.
 */
export type McpSlug = {
  /**
   * When the MCP slug was created
   */
  createdAt: Date;
  /**
   * The ID of the custom domain this slug is registered under. Null for platform-domain slugs.
   */
  customDomainId?: string | undefined;
  /**
   * The ID of the MCP slug
   */
  id: string;
  /**
   * The ID of the MCP frontend this slug addresses
   */
  mcpFrontendId: string;
  /**
   * The project ID this MCP slug belongs to
   */
  projectId: string;
  /**
   * A url-friendly label (up to 128 characters) that addresses an MCP frontend through a slug-based URL. Platform-domain slugs (no custom domain) must be prefixed with the organization slug.
   */
  slug: string;
  /**
   * When the MCP slug was last updated
   */
  updatedAt: Date;
};

/** @internal */
export const McpSlug$inboundSchema: z.ZodMiniType<McpSlug, unknown> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    custom_domain_id: z.optional(z.string()),
    id: z.string(),
    mcp_frontend_id: z.string(),
    project_id: z.string(),
    slug: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "custom_domain_id": "customDomainId",
      "mcp_frontend_id": "mcpFrontendId",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function mcpSlugFromJSON(
  jsonString: string,
): SafeParseResult<McpSlug, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpSlug$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpSlug' from JSON`,
  );
}
