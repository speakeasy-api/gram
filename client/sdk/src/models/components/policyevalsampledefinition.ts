/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Sampling mode: auto (window/last-N, stratified) or manual (explicit pinned message ids).
 */
export const Mode = {
  Auto: "auto",
  Manual: "manual",
} as const;
/**
 * Sampling mode: auto (window/last-N, stratified) or manual (explicit pinned message ids).
 */
export type Mode = ClosedEnum<typeof Mode>;

export type PolicyEvalSampleDefinition = {
  /**
   * auto mode: cap to the most recent N sessions.
   */
  lastNSessions?: number | undefined;
  /**
   * auto mode: only consider messages created within this many days.
   */
  lookbackDays?: number | undefined;
  /**
   * Hard cap on the number of messages scanned (bounds run cost).
   */
  maxMessages: number;
  /**
   * manual mode: the explicit, pinned set of chat message ids to scan.
   */
  messageIds?: Array<string> | undefined;
  /**
   * Sampling mode: auto (window/last-N, stratified) or manual (explicit pinned message ids).
   */
  mode: Mode;
};

/** @internal */
export const Mode$inboundSchema: z.ZodMiniEnum<typeof Mode> = z.enum(Mode);
/** @internal */
export const Mode$outboundSchema: z.ZodMiniEnum<typeof Mode> =
  Mode$inboundSchema;

/** @internal */
export const PolicyEvalSampleDefinition$inboundSchema: z.ZodMiniType<
  PolicyEvalSampleDefinition,
  unknown
> = z.pipe(
  z.object({
    last_n_sessions: z.optional(z.int()),
    lookback_days: z.optional(z.int()),
    max_messages: z.int(),
    message_ids: z.optional(z.array(z.string())),
    mode: Mode$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "last_n_sessions": "lastNSessions",
      "lookback_days": "lookbackDays",
      "max_messages": "maxMessages",
      "message_ids": "messageIds",
    });
  }),
);
/** @internal */
export type PolicyEvalSampleDefinition$Outbound = {
  last_n_sessions?: number | undefined;
  lookback_days?: number | undefined;
  max_messages: number;
  message_ids?: Array<string> | undefined;
  mode: string;
};

/** @internal */
export const PolicyEvalSampleDefinition$outboundSchema: z.ZodMiniType<
  PolicyEvalSampleDefinition$Outbound,
  PolicyEvalSampleDefinition
> = z.pipe(
  z.object({
    lastNSessions: z.optional(z.int()),
    lookbackDays: z.optional(z.int()),
    maxMessages: z.int(),
    messageIds: z.optional(z.array(z.string())),
    mode: Mode$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      lastNSessions: "last_n_sessions",
      lookbackDays: "lookback_days",
      maxMessages: "max_messages",
      messageIds: "message_ids",
    });
  }),
);

export function policyEvalSampleDefinitionToJSON(
  policyEvalSampleDefinition: PolicyEvalSampleDefinition,
): string {
  return JSON.stringify(
    PolicyEvalSampleDefinition$outboundSchema.parse(policyEvalSampleDefinition),
  );
}
export function policyEvalSampleDefinitionFromJSON(
  jsonString: string,
): SafeParseResult<PolicyEvalSampleDefinition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyEvalSampleDefinition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyEvalSampleDefinition' from JSON`,
  );
}
