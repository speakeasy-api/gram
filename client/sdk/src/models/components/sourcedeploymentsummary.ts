/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SourceDeploymentSummary = {
  /**
   * The asset ID of this source in the deployment.
   */
  assetId: string;
  /**
   * The creation date of the deployment.
   */
  createdAt: Date;
  /**
   * The deployment ID.
   */
  id: string;
  /**
   * The status of the deployment.
   */
  status: string;
  /**
   * The number of tools generated from this source in the deployment.
   */
  toolCount: number;
};

/** @internal */
export const SourceDeploymentSummary$inboundSchema: z.ZodMiniType<
  SourceDeploymentSummary,
  unknown
> = z.pipe(
  z.object({
    asset_id: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    status: z.string(),
    tool_count: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "asset_id": "assetId",
      "created_at": "createdAt",
      "tool_count": "toolCount",
    });
  }),
);

export function sourceDeploymentSummaryFromJSON(
  jsonString: string,
): SafeParseResult<SourceDeploymentSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SourceDeploymentSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SourceDeploymentSummary' from JSON`,
  );
}
