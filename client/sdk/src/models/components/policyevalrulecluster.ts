/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PolicyEvalRuleCluster = {
  /**
   * Number of findings produced by this rule.
   */
  count: number;
  /**
   * Number of distinct messages this rule fired on.
   */
  distinctMessages: number;
  /**
   * Canonical rule id.
   */
  ruleId?: string | undefined;
  /**
   * Detection source (e.g. gitleaks, presidio, llm_judge, custom).
   */
  source: string;
};

/** @internal */
export const PolicyEvalRuleCluster$inboundSchema: z.ZodMiniType<
  PolicyEvalRuleCluster,
  unknown
> = z.pipe(
  z.object({
    count: z.int(),
    distinct_messages: z.int(),
    rule_id: z.optional(z.string()),
    source: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "distinct_messages": "distinctMessages",
      "rule_id": "ruleId",
    });
  }),
);

export function policyEvalRuleClusterFromJSON(
  jsonString: string,
): SafeParseResult<PolicyEvalRuleCluster, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyEvalRuleCluster$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyEvalRuleCluster' from JSON`,
  );
}
