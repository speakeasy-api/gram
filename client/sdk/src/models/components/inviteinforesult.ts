/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Current status of the invite
 */
export const InviteInfoResultStatus = {
  Pending: "pending",
  Accepted: "accepted",
  Expired: "expired",
  Cancelled: "cancelled",
} as const;
/**
 * Current status of the invite
 */
export type InviteInfoResultStatus = ClosedEnum<typeof InviteInfoResultStatus>;

export type InviteInfoResult = {
  /**
   * The email address the invite was sent to
   */
  email: string;
  /**
   * Display name of the user who sent the invite
   */
  inviterName: string;
  /**
   * Name of the organization
   */
  organizationName: string;
  /**
   * Current status of the invite
   */
  status: InviteInfoResultStatus;
};

/** @internal */
export const InviteInfoResultStatus$inboundSchema: z.ZodNativeEnum<
  typeof InviteInfoResultStatus
> = z.nativeEnum(InviteInfoResultStatus);

/** @internal */
export const InviteInfoResult$inboundSchema: z.ZodType<
  InviteInfoResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  email: z.string(),
  inviter_name: z.string(),
  organization_name: z.string(),
  status: InviteInfoResultStatus$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "inviter_name": "inviterName",
    "organization_name": "organizationName",
  });
});

export function inviteInfoResultFromJSON(
  jsonString: string,
): SafeParseResult<InviteInfoResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InviteInfoResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InviteInfoResult' from JSON`,
  );
}
