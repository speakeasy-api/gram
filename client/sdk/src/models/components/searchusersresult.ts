/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { UserSummary, UserSummary$inboundSchema } from "./usersummary.js";

/**
 * Result of searching user usage summaries
 */
export type SearchUsersResult = {
  /**
   * Whether telemetry is enabled for the organization
   */
  enabled: boolean;
  /**
   * Cursor for next page
   */
  nextCursor?: string | undefined;
  /**
   * List of user usage summaries
   */
  users: Array<UserSummary>;
};

/** @internal */
export const SearchUsersResult$inboundSchema: z.ZodType<
  SearchUsersResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  enabled: z.boolean(),
  next_cursor: z.string().optional(),
  users: z.array(UserSummary$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "next_cursor": "nextCursor",
  });
});

export function searchUsersResultFromJSON(
  jsonString: string,
): SafeParseResult<SearchUsersResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchUsersResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchUsersResult' from JSON`,
  );
}
