/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Current user's vote direction
 */
export const UserVote = {
  Up: "up",
  Down: "down",
} as const;
/**
 * Current user's vote direction
 */
export type UserVote = ClosedEnum<typeof UserVote>;

export type CorpusFeedbackResult = {
  /**
   * Number of downvotes
   */
  downvotes: number;
  /**
   * Feedback labels
   */
  labels: Array<string>;
  /**
   * Number of upvotes
   */
  upvotes: number;
  /**
   * Current user's vote direction
   */
  userVote?: UserVote | undefined;
};

/** @internal */
export const UserVote$inboundSchema: z.ZodMiniEnum<typeof UserVote> = z.enum(
  UserVote,
);

/** @internal */
export const CorpusFeedbackResult$inboundSchema: z.ZodMiniType<
  CorpusFeedbackResult,
  unknown
> = z.pipe(
  z.object({
    downvotes: z.int(),
    labels: z.array(z.string()),
    upvotes: z.int(),
    user_vote: z.optional(UserVote$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "user_vote": "userVote",
    });
  }),
);

export function corpusFeedbackResultFromJSON(
  jsonString: string,
): SafeParseResult<CorpusFeedbackResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CorpusFeedbackResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CorpusFeedbackResult' from JSON`,
  );
}
