/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PublishStatusResult = {
  /**
   * Whether GitHub publishing is configured on the server.
   */
  configured: boolean;
  /**
   * Whether this project has a GitHub connection.
   */
  connected: boolean;
  /**
   * URL-based Claude Code marketplace install URL — the value to pass to `/plugin marketplace add`. Present once a marketplace token has been minted, which happens automatically on the first publish.
   */
  marketplaceUrl?: string | undefined;
  /**
   * GitHub repo name, if connected.
   */
  repoName?: string | undefined;
  /**
   * GitHub repo owner, if connected.
   */
  repoOwner?: string | undefined;
  /**
   * Full GitHub repository URL, if connected.
   */
  repoUrl?: string | undefined;
};

/** @internal */
export const PublishStatusResult$inboundSchema: z.ZodMiniType<
  PublishStatusResult,
  unknown
> = z.pipe(
  z.object({
    configured: z.boolean(),
    connected: z.boolean(),
    marketplace_url: z.optional(z.string()),
    repo_name: z.optional(z.string()),
    repo_owner: z.optional(z.string()),
    repo_url: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "marketplace_url": "marketplaceUrl",
      "repo_name": "repoName",
      "repo_owner": "repoOwner",
      "repo_url": "repoUrl",
    });
  }),
);

export function publishStatusResultFromJSON(
  jsonString: string,
): SafeParseResult<PublishStatusResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublishStatusResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublishStatusResult' from JSON`,
  );
}
