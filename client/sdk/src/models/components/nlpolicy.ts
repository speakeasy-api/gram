/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * fail_open | fail_closed — judge error/timeout behavior in enforce mode.
 */
export const FailMode = {
  FailOpen: "fail_open",
  FailClosed: "fail_closed",
} as const;
/**
 * fail_open | fail_closed — judge error/timeout behavior in enforce mode.
 */
export type FailMode = ClosedEnum<typeof FailMode>;

/**
 * audit | enforce | disabled.
 */
export const Mode = {
  Audit: "audit",
  Enforce: "enforce",
  Disabled: "disabled",
} as const;
/**
 * audit | enforce | disabled.
 */
export type Mode = ClosedEnum<typeof Mode>;

export type NLPolicy = {
  /**
   * RFC3339 timestamp.
   */
  createdAt: Date;
  /**
   * Author-facing summary.
   */
  description?: string | undefined;
  /**
   * fail_open | fail_closed — judge error/timeout behavior in enforce mode.
   */
  failMode: FailMode;
  /**
   * The NL policy ID.
   */
  id: string;
  /**
   * audit | enforce | disabled.
   */
  mode: Mode;
  /**
   * Policy name.
   */
  name: string;
  /**
   * The natural-language judge prompt.
   */
  nlPrompt: string;
  /**
   * The project ID. Empty when org-wide.
   */
  projectId?: string | undefined;
  /**
   * Run inline on each tool call.
   */
  scopePerCall: boolean;
  /**
   * Run async over the rolling chat-session window.
   */
  scopeSession: boolean;
  /**
   * JSON-encoded static rule list (see spec §6 grammar).
   */
  staticRules: string;
  /**
   * RFC3339 timestamp.
   */
  updatedAt: Date;
  /**
   * Incremented on each update.
   */
  version: number;
};

/** @internal */
export const FailMode$inboundSchema: z.ZodMiniEnum<typeof FailMode> = z.enum(
  FailMode,
);

/** @internal */
export const Mode$inboundSchema: z.ZodMiniEnum<typeof Mode> = z.enum(Mode);

/** @internal */
export const NLPolicy$inboundSchema: z.ZodMiniType<NLPolicy, unknown> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    description: z.optional(z.string()),
    fail_mode: FailMode$inboundSchema,
    id: z.string(),
    mode: Mode$inboundSchema,
    name: z.string(),
    nl_prompt: z.string(),
    project_id: z.optional(z.string()),
    scope_per_call: z.boolean(),
    scope_session: z.boolean(),
    static_rules: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    version: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "fail_mode": "failMode",
      "nl_prompt": "nlPrompt",
      "project_id": "projectId",
      "scope_per_call": "scopePerCall",
      "scope_session": "scopeSession",
      "static_rules": "staticRules",
      "updated_at": "updatedAt",
    });
  }),
);

export function nlPolicyFromJSON(
  jsonString: string,
): SafeParseResult<NLPolicy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NLPolicy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NLPolicy' from JSON`,
  );
}
