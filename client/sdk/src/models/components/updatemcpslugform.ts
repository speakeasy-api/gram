/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Form for updating an MCP slug. This is a full-record replace: the custom_domain_id field omitted from the request becomes null on the stored record. Platform-domain slugs (no custom_domain_id) must be prefixed with the organization slug.
 */
export type UpdateMcpSlugForm = {
  /**
   * The ID of the custom domain to register the slug under. Omit to move the slug to a platform domain.
   */
  customDomainId?: string | undefined;
  /**
   * The ID of the MCP slug to update
   */
  id: string;
  /**
   * The ID of the MCP frontend this slug addresses
   */
  mcpFrontendId: string;
  /**
   * A url-friendly label (up to 128 characters) that addresses an MCP frontend through a slug-based URL. Platform-domain slugs (no custom domain) must be prefixed with the organization slug.
   */
  slug: string;
};

/** @internal */
export type UpdateMcpSlugForm$Outbound = {
  custom_domain_id?: string | undefined;
  id: string;
  mcp_frontend_id: string;
  slug: string;
};

/** @internal */
export const UpdateMcpSlugForm$outboundSchema: z.ZodMiniType<
  UpdateMcpSlugForm$Outbound,
  UpdateMcpSlugForm
> = z.pipe(
  z.object({
    customDomainId: z.optional(z.string()),
    id: z.string(),
    mcpFrontendId: z.string(),
    slug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      customDomainId: "custom_domain_id",
      mcpFrontendId: "mcp_frontend_id",
    });
  }),
);

export function updateMcpSlugFormToJSON(
  updateMcpSlugForm: UpdateMcpSlugForm,
): string {
  return JSON.stringify(
    UpdateMcpSlugForm$outboundSchema.parse(updateMcpSlugForm),
  );
}
