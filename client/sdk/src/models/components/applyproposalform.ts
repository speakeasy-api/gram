/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type ApplyProposalForm = {
  /**
   * If true, override staleness checks and apply anyway
   */
  force?: boolean | undefined;
  /**
   * The ID of the proposal to apply
   */
  proposalId: string;
};

/** @internal */
export type ApplyProposalForm$Outbound = {
  force?: boolean | undefined;
  proposal_id: string;
};

/** @internal */
export const ApplyProposalForm$outboundSchema: z.ZodMiniType<
  ApplyProposalForm$Outbound,
  ApplyProposalForm
> = z.pipe(
  z.object({
    force: z.optional(z.boolean()),
    proposalId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      proposalId: "proposal_id",
    });
  }),
);

export function applyProposalFormToJSON(
  applyProposalForm: ApplyProposalForm,
): string {
  return JSON.stringify(
    ApplyProposalForm$outboundSchema.parse(applyProposalForm),
  );
}
