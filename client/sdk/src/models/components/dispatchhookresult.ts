/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Normalized hook decision.
 */
export type DispatchHookResult = {
  /**
   * Agent-facing explanation for a deny.
   */
  agentMessage?: string | undefined;
  /**
   * Decision for blocking events: allow or deny.
   */
  permission?: string | undefined;
  /**
   * Human-facing explanation for a deny.
   */
  userMessage?: string | undefined;
};

/** @internal */
export const DispatchHookResult$inboundSchema: z.ZodMiniType<
  DispatchHookResult,
  unknown
> = z.pipe(
  z.object({
    agent_message: z.optional(z.string()),
    permission: z.optional(z.string()),
    user_message: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "agent_message": "agentMessage",
      "user_message": "userMessage",
    });
  }),
);

export function dispatchHookResultFromJSON(
  jsonString: string,
): SafeParseResult<DispatchHookResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DispatchHookResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DispatchHookResult' from JSON`,
  );
}
