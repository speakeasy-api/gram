/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CorpusSearchLogResult = {
  /**
   * Agent name
   */
  agent: string;
  /**
   * Search filters
   */
  filters: any;
  /**
   * Search log ID
   */
  id: string;
  /**
   * Search latency in milliseconds
   */
  latencyMs: number;
  /**
   * The project ID
   */
  projectId: string;
  /**
   * The search query
   */
  query: string;
  /**
   * Result count
   */
  resultCount: number;
  /**
   * Session ID
   */
  sessionId: string;
  /**
   * Search timestamp
   */
  timestamp: Date;
};

/** @internal */
export const CorpusSearchLogResult$inboundSchema: z.ZodMiniType<
  CorpusSearchLogResult,
  unknown
> = z.pipe(
  z.object({
    agent: z.string(),
    filters: z.any(),
    id: z.string(),
    latency_ms: z.number(),
    project_id: z.string(),
    query: z.string(),
    result_count: z.int(),
    session_id: z.string(),
    timestamp: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "latency_ms": "latencyMs",
      "project_id": "projectId",
      "result_count": "resultCount",
      "session_id": "sessionId",
    });
  }),
);

export function corpusSearchLogResultFromJSON(
  jsonString: string,
): SafeParseResult<CorpusSearchLogResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CorpusSearchLogResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CorpusSearchLogResult' from JSON`,
  );
}
