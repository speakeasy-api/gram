/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PublishPluginsResult = {
  /**
   * Git commit SHA of the push.
   */
  commitSha?: string | undefined;
  /**
   * Whether the publish succeeded.
   */
  published: boolean;
  /**
   * GitHub repository URL where plugins were published.
   */
  repoUrl: string;
};

/** @internal */
export const PublishPluginsResult$inboundSchema: z.ZodMiniType<
  PublishPluginsResult,
  unknown
> = z.pipe(
  z.object({
    commit_sha: z.optional(z.string()),
    published: z.boolean(),
    repo_url: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "commit_sha": "commitSha",
      "repo_url": "repoUrl",
    });
  }),
);

export function publishPluginsResultFromJSON(
  jsonString: string,
): SafeParseResult<PublishPluginsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublishPluginsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublishPluginsResult' from JSON`,
  );
}
