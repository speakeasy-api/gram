/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * static_rule | llm_judge | fail_mode | session_quarantine.
 */
export const DecidedBy = {
  StaticRule: "static_rule",
  LlmJudge: "llm_judge",
  FailMode: "fail_mode",
  SessionQuarantine: "session_quarantine",
} as const;
/**
 * static_rule | llm_judge | fail_mode | session_quarantine.
 */
export type DecidedBy = ClosedEnum<typeof DecidedBy>;

/**
 * ALLOW | BLOCK | JUDGE_ERROR.
 */
export const Decision = {
  Allow: "ALLOW",
  Block: "BLOCK",
  JudgeError: "JUDGE_ERROR",
} as const;
/**
 * ALLOW | BLOCK | JUDGE_ERROR.
 */
export type Decision = ClosedEnum<typeof Decision>;

/**
 * Snapshot of policy mode at decision time.
 */
export const NLPolicyDecisionMode = {
  Audit: "audit",
  Enforce: "enforce",
  Disabled: "disabled",
} as const;
/**
 * Snapshot of policy mode at decision time.
 */
export type NLPolicyDecisionMode = ClosedEnum<typeof NLPolicyDecisionMode>;

export type NLPolicyDecision = {
  /**
   * Source chat (optional).
   */
  chatId?: string | undefined;
  /**
   * RFC3339 timestamp.
   */
  createdAt: Date;
  /**
   * static_rule | llm_judge | fail_mode | session_quarantine.
   */
  decidedBy: DecidedBy;
  /**
   * ALLOW | BLOCK | JUDGE_ERROR.
   */
  decision: Decision;
  /**
   * True when mode=enforce AND decision=BLOCK.
   */
  enforced: boolean;
  /**
   * Decision row ID.
   */
  id: string;
  /**
   * Round-trip latency of the LLM call (when applicable).
   */
  judgeLatencyMs?: number | undefined;
  /**
   * Snapshot of policy mode at decision time.
   */
  mode: NLPolicyDecisionMode;
  /**
   * Policy that produced this decision.
   */
  nlPolicyId: string;
  /**
   * Policy version snapshot at decision time.
   */
  nlPolicyVersion: number;
  /**
   * Short human-readable reason.
   */
  reason?: string | undefined;
  /**
   * Source MCP session ID.
   */
  sessionId?: string | undefined;
  /**
   * Tool that was being called.
   */
  toolUrn: string;
};

/** @internal */
export const DecidedBy$inboundSchema: z.ZodMiniEnum<typeof DecidedBy> = z.enum(
  DecidedBy,
);

/** @internal */
export const Decision$inboundSchema: z.ZodMiniEnum<typeof Decision> = z.enum(
  Decision,
);

/** @internal */
export const NLPolicyDecisionMode$inboundSchema: z.ZodMiniEnum<
  typeof NLPolicyDecisionMode
> = z.enum(NLPolicyDecisionMode);

/** @internal */
export const NLPolicyDecision$inboundSchema: z.ZodMiniType<
  NLPolicyDecision,
  unknown
> = z.pipe(
  z.object({
    chat_id: z.optional(z.string()),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    decided_by: DecidedBy$inboundSchema,
    decision: Decision$inboundSchema,
    enforced: z.boolean(),
    id: z.string(),
    judge_latency_ms: z.optional(z.int()),
    mode: NLPolicyDecisionMode$inboundSchema,
    nl_policy_id: z.string(),
    nl_policy_version: z.int(),
    reason: z.optional(z.string()),
    session_id: z.optional(z.string()),
    tool_urn: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "chat_id": "chatId",
      "created_at": "createdAt",
      "decided_by": "decidedBy",
      "judge_latency_ms": "judgeLatencyMs",
      "nl_policy_id": "nlPolicyId",
      "nl_policy_version": "nlPolicyVersion",
      "session_id": "sessionId",
      "tool_urn": "toolUrn",
    });
  }),
);

export function nlPolicyDecisionFromJSON(
  jsonString: string,
): SafeParseResult<NLPolicyDecision, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NLPolicyDecision$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NLPolicyDecision' from JSON`,
  );
}
