/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Gram account tier.
 */
export const GramAccountType = {
  Free: "free",
  Pro: "pro",
  Enterprise: "enterprise",
} as const;
/**
 * Gram account tier.
 */
export type GramAccountType = ClosedEnum<typeof GramAccountType>;

export type OrganizationSummary = {
  createdAt: Date;
  /**
   * When the organization was disabled, if applicable.
   */
  disabledAt?: Date | undefined;
  /**
   * Gram account tier.
   */
  gramAccountType: GramAccountType;
  /**
   * Gram organization ID.
   */
  id: string;
  /**
   * Organization display name.
   */
  name: string;
  /**
   * Organization slug.
   */
  slug: string;
  updatedAt: Date;
  /**
   * Whether the organization is whitelisted.
   */
  whitelisted: boolean;
  /**
   * WorkOS organization ID, when linked.
   */
  workosId?: string | undefined;
};

/** @internal */
export const GramAccountType$inboundSchema: z.ZodMiniEnum<
  typeof GramAccountType
> = z.enum(GramAccountType);

/** @internal */
export const OrganizationSummary$inboundSchema: z.ZodMiniType<
  OrganizationSummary,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    disabled_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    gram_account_type: GramAccountType$inboundSchema,
    id: z.string(),
    name: z.string(),
    slug: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    whitelisted: z.boolean(),
    workos_id: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "disabled_at": "disabledAt",
      "gram_account_type": "gramAccountType",
      "updated_at": "updatedAt",
      "workos_id": "workosId",
    });
  }),
);

export function organizationSummaryFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationSummary' from JSON`,
  );
}
