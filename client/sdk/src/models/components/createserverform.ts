/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  HeaderInput,
  HeaderInput$Outbound,
  HeaderInput$outboundSchema,
} from "./headerinput.js";

/**
 * Form for creating a new remote MCP server
 */
export type CreateServerForm = {
  /**
   * Headers to send when proxying requests to the remote server
   */
  headers: Array<HeaderInput>;
  /**
   * The transport type for the remote MCP server (e.g. streamable-http)
   */
  transportType: string;
  /**
   * The URL of the remote MCP server
   */
  url: string;
};

/** @internal */
export type CreateServerForm$Outbound = {
  headers: Array<HeaderInput$Outbound>;
  transport_type: string;
  url: string;
};

/** @internal */
export const CreateServerForm$outboundSchema: z.ZodMiniType<
  CreateServerForm$Outbound,
  CreateServerForm
> = z.pipe(
  z.object({
    headers: z.array(HeaderInput$outboundSchema),
    transportType: z.string(),
    url: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      transportType: "transport_type",
    });
  }),
);

export function createServerFormToJSON(
  createServerForm: CreateServerForm,
): string {
  return JSON.stringify(
    CreateServerForm$outboundSchema.parse(createServerForm),
  );
}
