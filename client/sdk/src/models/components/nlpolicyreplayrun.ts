/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * pending | running | completed | failed.
 */
export const NLPolicyReplayRunStatus = {
  Pending: "pending",
  Running: "running",
  Completed: "completed",
  Failed: "failed",
} as const;
/**
 * pending | running | completed | failed.
 */
export type NLPolicyReplayRunStatus = ClosedEnum<
  typeof NLPolicyReplayRunStatus
>;

export type NLPolicyReplayRun = {
  /**
   * RFC3339 — null until completed.
   */
  completedAt?: Date | undefined;
  /**
   * JSON-encoded counts: {would_block, would_allow, judge_error}.
   */
  counts?: string | undefined;
  /**
   * Run ID.
   */
  id: string;
  /**
   * Policy under test.
   */
  nlPolicyId: string;
  /**
   * Policy version snapshot.
   */
  nlPolicyVersion: number;
  /**
   * JSON-encoded filter envelope.
   */
  sampleFilter: string;
  /**
   * RFC3339.
   */
  startedAt: Date;
  /**
   * pending | running | completed | failed.
   */
  status: NLPolicyReplayRunStatus;
};

/** @internal */
export const NLPolicyReplayRunStatus$inboundSchema: z.ZodMiniEnum<
  typeof NLPolicyReplayRunStatus
> = z.enum(NLPolicyReplayRunStatus);

/** @internal */
export const NLPolicyReplayRun$inboundSchema: z.ZodMiniType<
  NLPolicyReplayRun,
  unknown
> = z.pipe(
  z.object({
    completed_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    counts: z.optional(z.string()),
    id: z.string(),
    nl_policy_id: z.string(),
    nl_policy_version: z.int(),
    sample_filter: z.string(),
    started_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    status: NLPolicyReplayRunStatus$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "completed_at": "completedAt",
      "nl_policy_id": "nlPolicyId",
      "nl_policy_version": "nlPolicyVersion",
      "sample_filter": "sampleFilter",
      "started_at": "startedAt",
    });
  }),
);

export function nlPolicyReplayRunFromJSON(
  jsonString: string,
): SafeParseResult<NLPolicyReplayRun, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NLPolicyReplayRun$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NLPolicyReplayRun' from JSON`,
  );
}
