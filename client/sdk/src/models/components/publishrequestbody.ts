/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Visibility of the catalog
 */
export const PublishRequestBodyVisibility = {
  Public: "public",
  Private: "private",
} as const;
/**
 * Visibility of the catalog
 */
export type PublishRequestBodyVisibility = ClosedEnum<
  typeof PublishRequestBodyVisibility
>;

export type PublishRequestBody = {
  /**
   * Display name for the catalog
   */
  name: string;
  /**
   * URL-friendly identifier for the catalog
   */
  slug: string;
  /**
   * IDs of the toolsets to include
   */
  toolsetIds: Array<string>;
  /**
   * Visibility of the catalog
   */
  visibility?: PublishRequestBodyVisibility | undefined;
};

/** @internal */
export const PublishRequestBodyVisibility$outboundSchema: z.ZodMiniEnum<
  typeof PublishRequestBodyVisibility
> = z.enum(PublishRequestBodyVisibility);

/** @internal */
export type PublishRequestBody$Outbound = {
  name: string;
  slug: string;
  toolset_ids: Array<string>;
  visibility: string;
};

/** @internal */
export const PublishRequestBody$outboundSchema: z.ZodMiniType<
  PublishRequestBody$Outbound,
  PublishRequestBody
> = z.pipe(
  z.object({
    name: z.string(),
    slug: z.string(),
    toolsetIds: z.array(z.string()),
    visibility: z._default(
      PublishRequestBodyVisibility$outboundSchema,
      "private",
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      toolsetIds: "toolset_ids",
    });
  }),
);

export function publishRequestBodyToJSON(
  publishRequestBody: PublishRequestBody,
): string {
  return JSON.stringify(
    PublishRequestBody$outboundSchema.parse(publishRequestBody),
  );
}
