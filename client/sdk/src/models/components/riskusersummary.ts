/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RiskUserSummary = {
  /**
   * Number of chats with findings for this user.
   */
  chatsCount: number;
  /**
   * Number of findings for this user.
   */
  findingsCount: number;
  /**
   * When the most recent finding was detected.
   */
  latestDetected: Date;
  /**
   * The user identifier.
   */
  userId?: string | undefined;
};

/** @internal */
export const RiskUserSummary$inboundSchema: z.ZodMiniType<
  RiskUserSummary,
  unknown
> = z.pipe(
  z.object({
    chats_count: z.int(),
    findings_count: z.int(),
    latest_detected: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    user_id: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "chats_count": "chatsCount",
      "findings_count": "findingsCount",
      "latest_detected": "latestDetected",
      "user_id": "userId",
    });
  }),
);

export function riskUserSummaryFromJSON(
  jsonString: string,
): SafeParseResult<RiskUserSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskUserSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskUserSummary' from JSON`,
  );
}
