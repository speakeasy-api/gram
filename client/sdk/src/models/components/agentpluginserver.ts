/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Whether the agent should treat this server as required or optional.
 */
export const Policy = {
  Required: "required",
  Optional: "optional",
} as const;
/**
 * Whether the agent should treat this server as required or optional.
 */
export type Policy = ClosedEnum<typeof Policy>;

export type AgentPluginServer = {
  /**
   * Display name shown in the generated AI-tool config.
   */
  displayName: string;
  /**
   * True when the MCP server is publicly accessible and does not require a Gram credential.
   */
  isPublic: boolean;
  /**
   * Gram-hosted MCP URL the AI tool should connect to.
   */
  mcpUrl: string;
  /**
   * Whether the agent should treat this server as required or optional.
   */
  policy: Policy;
};

/** @internal */
export const Policy$inboundSchema: z.ZodMiniEnum<typeof Policy> = z.enum(
  Policy,
);

/** @internal */
export const AgentPluginServer$inboundSchema: z.ZodMiniType<
  AgentPluginServer,
  unknown
> = z.pipe(
  z.object({
    display_name: z.string(),
    is_public: z.boolean(),
    mcp_url: z.string(),
    policy: Policy$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
      "is_public": "isPublic",
      "mcp_url": "mcpUrl",
    });
  }),
);

export function agentPluginServerFromJSON(
  jsonString: string,
): SafeParseResult<AgentPluginServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentPluginServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentPluginServer' from JSON`,
  );
}
