/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Result of a legacy gram registration migration.
 */
export type MigrateLegacyGramRegistrationsResult = {
  /**
   * Number of user_session_clients newly inserted; already-migrated registrations count as zero.
   */
  migratedCount: number;
};

/** @internal */
export const MigrateLegacyGramRegistrationsResult$inboundSchema: z.ZodMiniType<
  MigrateLegacyGramRegistrationsResult,
  unknown
> = z.pipe(
  z.object({
    migrated_count: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "migrated_count": "migratedCount",
    });
  }),
);

export function migrateLegacyGramRegistrationsResultFromJSON(
  jsonString: string,
): SafeParseResult<MigrateLegacyGramRegistrationsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      MigrateLegacyGramRegistrationsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MigrateLegacyGramRegistrationsResult' from JSON`,
  );
}
