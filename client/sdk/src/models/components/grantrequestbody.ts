/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type GrantRequestBody = {
  /**
   * ID of the organization to grant access to
   */
  organizationId: string;
  /**
   * ID of the registry to grant access to
   */
  registryId: string;
};

/** @internal */
export type GrantRequestBody$Outbound = {
  organization_id: string;
  registry_id: string;
};

/** @internal */
export const GrantRequestBody$outboundSchema: z.ZodMiniType<
  GrantRequestBody$Outbound,
  GrantRequestBody
> = z.pipe(
  z.object({
    organizationId: z.string(),
    registryId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationId: "organization_id",
      registryId: "registry_id",
    });
  }),
);

export function grantRequestBodyToJSON(
  grantRequestBody: GrantRequestBody,
): string {
  return JSON.stringify(
    GrantRequestBody$outboundSchema.parse(grantRequestBody),
  );
}
