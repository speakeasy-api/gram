/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PortalServer = {
  /**
   * Server or toolset description.
   */
  description?: string | undefined;
  /**
   * URL of the per-server install page.
   */
  installUrl: string;
  /**
   * Server name.
   */
  name: string;
  /**
   * Endpoint slug.
   */
  slug: string;
  /**
   * Number of tools exposed by this server.
   */
  toolCount: number;
};

/** @internal */
export const PortalServer$inboundSchema: z.ZodMiniType<PortalServer, unknown> =
  z.pipe(
    z.object({
      description: z.optional(z.string()),
      install_url: z.string(),
      name: z.string(),
      slug: z.string(),
      tool_count: z.int(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "install_url": "installUrl",
        "tool_count": "toolCount",
      });
    }),
  );

export function portalServerFromJSON(
  jsonString: string,
): SafeParseResult<PortalServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PortalServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PortalServer' from JSON`,
  );
}
