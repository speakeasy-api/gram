/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OrganizationUser = {
  createdAt: Date;
  /**
   * Gram relationship row ID.
   */
  id: string;
  /**
   * Gram organization ID.
   */
  organizationId: string;
  updatedAt: Date;
  /**
   * Gram user ID.
   */
  userId: string;
  /**
   * WorkOS organization membership ID when known.
   */
  workosMembershipId?: string | undefined;
};

/** @internal */
export const OrganizationUser$inboundSchema: z.ZodMiniType<
  OrganizationUser,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    organization_id: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    user_id: z.string(),
    workos_membership_id: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "organization_id": "organizationId",
      "updated_at": "updatedAt",
      "user_id": "userId",
      "workos_membership_id": "workosMembershipId",
    });
  }),
);

export function organizationUserFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationUser, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationUser$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationUser' from JSON`,
  );
}
