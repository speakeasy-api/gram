/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PluginGitHubConnection = {
  createdAt: Date;
  id: string;
  /**
   * GitHub App installation ID.
   */
  installationId: number;
  /**
   * GitHub repo name.
   */
  repoName: string;
  /**
   * GitHub repo owner (org or user).
   */
  repoOwner: string;
};

/** @internal */
export const PluginGitHubConnection$inboundSchema: z.ZodMiniType<
  PluginGitHubConnection,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    installation_id: z.int(),
    repo_name: z.string(),
    repo_owner: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "installation_id": "installationId",
      "repo_name": "repoName",
      "repo_owner": "repoOwner",
    });
  }),
);

export function pluginGitHubConnectionFromJSON(
  jsonString: string,
): SafeParseResult<PluginGitHubConnection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PluginGitHubConnection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PluginGitHubConnection' from JSON`,
  );
}
