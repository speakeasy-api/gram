/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One member field available on an object-typed variable's element (e.g. 'name' on a 'tool').
 */
export type DetectionSchemaField = {
  /**
   * Plain-English description of what the field holds.
   */
  description: string;
  /**
   * Field name as written in CEL after the bind variable (e.g. 'name', 'server').
   */
  name: string;
  /**
   * Descriptive type tag for the editor (e.g. 'field').
   */
  type: string;
};

/** @internal */
export const DetectionSchemaField$inboundSchema: z.ZodMiniType<
  DetectionSchemaField,
  unknown
> = z.object({
  description: z.string(),
  name: z.string(),
  type: z.string(),
});

export function detectionSchemaFieldFromJSON(
  jsonString: string,
): SafeParseResult<DetectionSchemaField, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DetectionSchemaField$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DetectionSchemaField' from JSON`,
  );
}
