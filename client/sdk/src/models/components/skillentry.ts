/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SkillVersionSummary,
  SkillVersionSummary$inboundSchema,
} from "./skillversionsummary.js";

export type SkillEntry = {
  activeVersion?: SkillVersionSummary | undefined;
  createdAt: Date;
  description?: string | undefined;
  id: string;
  name: string;
  skillUuid?: string | undefined;
  slug: string;
  updatedAt: Date;
  versionCount: number;
};

/** @internal */
export const SkillEntry$inboundSchema: z.ZodMiniType<SkillEntry, unknown> = z
  .pipe(
    z.object({
      active_version: z.optional(SkillVersionSummary$inboundSchema),
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      description: z.optional(z.string()),
      id: z.string(),
      name: z.string(),
      skill_uuid: z.optional(z.string()),
      slug: z.string(),
      updated_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      version_count: z.int(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "active_version": "activeVersion",
        "created_at": "createdAt",
        "skill_uuid": "skillUuid",
        "updated_at": "updatedAt",
        "version_count": "versionCount",
      });
    }),
  );

export function skillEntryFromJSON(
  jsonString: string,
): SafeParseResult<SkillEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SkillEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SkillEntry' from JSON`,
  );
}
