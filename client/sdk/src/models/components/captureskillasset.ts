/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const CaptureSkillAssetKind = {
  Skill: "skill",
} as const;
export type CaptureSkillAssetKind = ClosedEnum<typeof CaptureSkillAssetKind>;

export type CaptureSkillAsset = {
  contentLength: number;
  contentType: string;
  createdAt: Date;
  id: string;
  kind: CaptureSkillAssetKind;
  sha256: string;
  updatedAt: Date;
};

/** @internal */
export const CaptureSkillAssetKind$inboundSchema: z.ZodMiniEnum<
  typeof CaptureSkillAssetKind
> = z.enum(CaptureSkillAssetKind);

/** @internal */
export const CaptureSkillAsset$inboundSchema: z.ZodMiniType<
  CaptureSkillAsset,
  unknown
> = z.pipe(
  z.object({
    content_length: z.int(),
    content_type: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    kind: CaptureSkillAssetKind$inboundSchema,
    sha256: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "content_length": "contentLength",
      "content_type": "contentType",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function captureSkillAssetFromJSON(
  jsonString: string,
): SafeParseResult<CaptureSkillAsset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CaptureSkillAsset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CaptureSkillAsset' from JSON`,
  );
}
