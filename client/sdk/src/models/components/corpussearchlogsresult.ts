/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  CorpusSearchLogResult,
  CorpusSearchLogResult$inboundSchema,
} from "./corpussearchlogresult.js";

export type CorpusSearchLogsResult = {
  /**
   * Search logs
   */
  logs: Array<CorpusSearchLogResult>;
  /**
   * Cursor for the next page
   */
  nextCursor?: string | undefined;
};

/** @internal */
export const CorpusSearchLogsResult$inboundSchema: z.ZodMiniType<
  CorpusSearchLogsResult,
  unknown
> = z.pipe(
  z.object({
    logs: z.array(CorpusSearchLogResult$inboundSchema),
    next_cursor: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function corpusSearchLogsResultFromJSON(
  jsonString: string,
): SafeParseResult<CorpusSearchLogsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CorpusSearchLogsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CorpusSearchLogsResult' from JSON`,
  );
}
