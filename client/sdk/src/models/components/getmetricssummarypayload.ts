/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Aggregation scope for metrics
 */
export const Scope = {
  Project: "project",
  Chat: "chat",
} as const;
/**
 * Aggregation scope for metrics
 */
export type Scope = ClosedEnum<typeof Scope>;

/**
 * Payload for getting metrics summary
 */
export type GetMetricsSummaryPayload = {
  /**
   * Chat/conversation ID (required when scope=chat)
   */
  chatId?: string | undefined;
  /**
   * Start time in ISO 8601 format
   */
  from: Date;
  /**
   * Aggregation scope for metrics
   */
  scope: Scope;
  /**
   * End time in ISO 8601 format
   */
  to: Date;
};

/** @internal */
export const Scope$outboundSchema: z.ZodNativeEnum<typeof Scope> = z.nativeEnum(
  Scope,
);

/** @internal */
export type GetMetricsSummaryPayload$Outbound = {
  chat_id?: string | undefined;
  from: string;
  scope: string;
  to: string;
};

/** @internal */
export const GetMetricsSummaryPayload$outboundSchema: z.ZodType<
  GetMetricsSummaryPayload$Outbound,
  z.ZodTypeDef,
  GetMetricsSummaryPayload
> = z.object({
  chatId: z.string().optional(),
  from: z.date().transform(v => v.toISOString()),
  scope: Scope$outboundSchema,
  to: z.date().transform(v => v.toISOString()),
}).transform((v) => {
  return remap$(v, {
    chatId: "chat_id",
  });
});

export function getMetricsSummaryPayloadToJSON(
  getMetricsSummaryPayload: GetMetricsSummaryPayload,
): string {
  return JSON.stringify(
    GetMetricsSummaryPayload$outboundSchema.parse(getMetricsSummaryPayload),
  );
}
