/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Whether this server is required or optional.
 */
export const PluginServerPolicy = {
  Required: "required",
  Optional: "optional",
} as const;
/**
 * Whether this server is required or optional.
 */
export type PluginServerPolicy = ClosedEnum<typeof PluginServerPolicy>;

export type PluginServer = {
  createdAt: Date;
  /**
   * Display name shown in generated plugin config.
   */
  displayName: string;
  /**
   * External MCP server URL.
   */
  externalUrl?: string | undefined;
  /**
   * Unique plugin server identifier.
   */
  id: string;
  /**
   * Whether this server is required or optional.
   */
  policy: PluginServerPolicy;
  /**
   * MCP registry ID, if this server is from a catalog.
   */
  registryId?: string | undefined;
  /**
   * Registry server specifier (e.g. io.modelcontextprotocol.anonymous/exa).
   */
  registryServerSpecifier?: string | undefined;
  /**
   * Ordering within the plugin.
   */
  sortOrder: number;
  /**
   * Gram toolset ID, if this server is a first-party toolset.
   */
  toolsetId?: string | undefined;
};

/** @internal */
export const PluginServerPolicy$inboundSchema: z.ZodMiniEnum<
  typeof PluginServerPolicy
> = z.enum(PluginServerPolicy);

/** @internal */
export const PluginServer$inboundSchema: z.ZodMiniType<PluginServer, unknown> =
  z.pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      display_name: z.string(),
      external_url: z.optional(z.string()),
      id: z.string(),
      policy: PluginServerPolicy$inboundSchema,
      registry_id: z.optional(z.string()),
      registry_server_specifier: z.optional(z.string()),
      sort_order: z.int(),
      toolset_id: z.optional(z.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "display_name": "displayName",
        "external_url": "externalUrl",
        "registry_id": "registryId",
        "registry_server_specifier": "registryServerSpecifier",
        "sort_order": "sortOrder",
        "toolset_id": "toolsetId",
      });
    }),
  );

export function pluginServerFromJSON(
  jsonString: string,
): SafeParseResult<PluginServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PluginServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PluginServer' from JSON`,
  );
}
