/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Message author.
 */
export const DashboardMessageRole = {
  User: "user",
  Assistant: "assistant",
} as const;
/**
 * Message author.
 */
export type DashboardMessageRole = ClosedEnum<typeof DashboardMessageRole>;

export type DashboardMessage = {
  /**
   * Message content (Markdown).
   */
  content: string;
  /**
   * RFC3339 creation timestamp.
   */
  createdAt: Date;
  /**
   * Message id.
   */
  id: string;
  /**
   * Message author.
   */
  role: DashboardMessageRole;
  /**
   * Monotonic cursor; pass the latest value as after_seq to poll for newer messages.
   */
  seq: number;
};

/** @internal */
export const DashboardMessageRole$inboundSchema: z.ZodMiniEnum<
  typeof DashboardMessageRole
> = z.enum(DashboardMessageRole);

/** @internal */
export const DashboardMessage$inboundSchema: z.ZodMiniType<
  DashboardMessage,
  unknown
> = z.pipe(
  z.object({
    content: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    role: DashboardMessageRole$inboundSchema,
    seq: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
    });
  }),
);

export function dashboardMessageFromJSON(
  jsonString: string,
): SafeParseResult<DashboardMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DashboardMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DashboardMessage' from JSON`,
  );
}
