/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PolicyAccessTarget,
  PolicyAccessTarget$inboundSchema,
} from "./policyaccesstarget.js";

/**
 * Request lifecycle status.
 */
export const PolicyAccessRequestStatus = {
  Requested: "requested",
  Approved: "approved",
  Denied: "denied",
} as const;
/**
 * Request lifecycle status.
 */
export type PolicyAccessRequestStatus = ClosedEnum<
  typeof PolicyAccessRequestStatus
>;

/**
 * A request for access through a risk policy that blocked a caller.
 */
export type PolicyAccessRequest = {
  createdAt: Date;
  decidedAt?: Date | undefined;
  /**
   * URN of the admin who decided.
   */
  decidedBy?: string | undefined;
  /**
   * Principals the bypass was granted to when approved.
   */
  grantedPrincipalUrns?: Array<string> | undefined;
  /**
   * Request ID.
   */
  id: string;
  /**
   * Optional requester note.
   */
  note?: string | undefined;
  /**
   * Organization the request belongs to.
   */
  organizationId: string;
  /**
   * The risk policy that blocked the caller.
   */
  policyId: string;
  /**
   * Policy display name.
   */
  policyName?: string | undefined;
  /**
   * Project the policy belongs to.
   */
  projectId: string;
  /**
   * Email of the blocked user.
   */
  requesterEmail?: string | undefined;
  /**
   * Gram user that was blocked.
   */
  requesterUserId?: string | undefined;
  /**
   * Request lifecycle status.
   */
  status: PolicyAccessRequestStatus;
  /**
   * A generic target narrowing for a policy bypass. Empty fields mean the whole policy.
   */
  target: PolicyAccessTarget;
  updatedAt: Date;
};

/** @internal */
export const PolicyAccessRequestStatus$inboundSchema: z.ZodMiniEnum<
  typeof PolicyAccessRequestStatus
> = z.enum(PolicyAccessRequestStatus);

/** @internal */
export const PolicyAccessRequest$inboundSchema: z.ZodMiniType<
  PolicyAccessRequest,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    decided_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    decided_by: z.optional(z.string()),
    granted_principal_urns: z.optional(z.array(z.string())),
    id: z.string(),
    note: z.optional(z.string()),
    organization_id: z.string(),
    policy_id: z.string(),
    policy_name: z.optional(z.string()),
    project_id: z.string(),
    requester_email: z.optional(z.string()),
    requester_user_id: z.optional(z.string()),
    status: PolicyAccessRequestStatus$inboundSchema,
    target: PolicyAccessTarget$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "decided_at": "decidedAt",
      "decided_by": "decidedBy",
      "granted_principal_urns": "grantedPrincipalUrns",
      "organization_id": "organizationId",
      "policy_id": "policyId",
      "policy_name": "policyName",
      "project_id": "projectId",
      "requester_email": "requesterEmail",
      "requester_user_id": "requesterUserId",
      "updated_at": "updatedAt",
    });
  }),
);

export function policyAccessRequestFromJSON(
  jsonString: string,
): SafeParseResult<PolicyAccessRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyAccessRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyAccessRequest' from JSON`,
  );
}
