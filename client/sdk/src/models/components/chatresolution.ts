/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Resolution information for a chat
 */
export type ChatResolution = {
  /**
   * When resolution was created
   */
  createdAt: Date;
  /**
   * Resolution ID
   */
  id: string;
  /**
   * Message IDs associated with this resolution
   */
  messageIds: Array<string>;
  /**
   * Resolution status
   */
  resolution: string;
  /**
   * Notes about the resolution
   */
  resolutionNotes: string;
  /**
   * Score 0-100
   */
  score: number;
  /**
   * User's intended goal
   */
  userGoal: string;
};

/** @internal */
export const ChatResolution$inboundSchema: z.ZodType<
  ChatResolution,
  z.ZodTypeDef,
  unknown
> = z.object({
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  id: z.string(),
  message_ids: z.array(z.string()),
  resolution: z.string(),
  resolution_notes: z.string(),
  score: z.number().int(),
  user_goal: z.string(),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "message_ids": "messageIds",
    "resolution_notes": "resolutionNotes",
    "user_goal": "userGoal",
  });
});

export function chatResolutionFromJSON(
  jsonString: string,
): SafeParseResult<ChatResolution, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ChatResolution$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChatResolution' from JSON`,
  );
}
