/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type SetSkillCaptureSettingsForm = {
  captureProjectSkills: boolean;
  captureUserSkills: boolean;
  enabled: boolean;
};

/** @internal */
export type SetSkillCaptureSettingsForm$Outbound = {
  capture_project_skills: boolean;
  capture_user_skills: boolean;
  enabled: boolean;
};

/** @internal */
export const SetSkillCaptureSettingsForm$outboundSchema: z.ZodMiniType<
  SetSkillCaptureSettingsForm$Outbound,
  SetSkillCaptureSettingsForm
> = z.pipe(
  z.object({
    captureProjectSkills: z.boolean(),
    captureUserSkills: z.boolean(),
    enabled: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      captureProjectSkills: "capture_project_skills",
      captureUserSkills: "capture_user_skills",
    });
  }),
);

export function setSkillCaptureSettingsFormToJSON(
  setSkillCaptureSettingsForm: SetSkillCaptureSettingsForm,
): string {
  return JSON.stringify(
    SetSkillCaptureSettingsForm$outboundSchema.parse(
      setSkillCaptureSettingsForm,
    ),
  );
}
