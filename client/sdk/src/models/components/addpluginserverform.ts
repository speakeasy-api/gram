/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

export const Policy = {
  Required: "required",
  Optional: "optional",
} as const;
export type Policy = ClosedEnum<typeof Policy>;

export type AddPluginServerForm = {
  /**
   * Display name for the server.
   */
  displayName: string;
  pluginId: string;
  policy?: Policy | undefined;
  sortOrder?: number | undefined;
  /**
   * Gram toolset ID for the MCP server.
   */
  toolsetId: string;
};

/** @internal */
export const Policy$outboundSchema: z.ZodMiniEnum<typeof Policy> = z.enum(
  Policy,
);

/** @internal */
export type AddPluginServerForm$Outbound = {
  display_name: string;
  plugin_id: string;
  policy: string;
  sort_order: number;
  toolset_id: string;
};

/** @internal */
export const AddPluginServerForm$outboundSchema: z.ZodMiniType<
  AddPluginServerForm$Outbound,
  AddPluginServerForm
> = z.pipe(
  z.object({
    displayName: z.string(),
    pluginId: z.string(),
    policy: z._default(Policy$outboundSchema, "required"),
    sortOrder: z._default(z.int(), 0),
    toolsetId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      displayName: "display_name",
      pluginId: "plugin_id",
      sortOrder: "sort_order",
      toolsetId: "toolset_id",
    });
  }),
);

export function addPluginServerFormToJSON(
  addPluginServerForm: AddPluginServerForm,
): string {
  return JSON.stringify(
    AddPluginServerForm$outboundSchema.parse(addPluginServerForm),
  );
}
