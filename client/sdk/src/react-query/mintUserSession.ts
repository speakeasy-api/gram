/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { userSessionsMint } from "../funcs/userSessionsMint.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type MintUserSessionMutationVariables = {
  request: operations.MintUserSessionRequest;
  security?: operations.MintUserSessionSecurity | undefined;
  options?: RequestOptions;
};

export type MintUserSessionMutationData =
  components.MintUserSessionResponseBody;

export type MintUserSessionMutationError =
  | errors.ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * mintUserSession userSessions
 *
 * @remarks
 * Mint a user_session on behalf of the authenticated dashboard user, bound to an issuer-gated audience: either a toolset (/mcp) or a remote MCP server (/x/mcp). Exactly one of toolset_id or mcp_server_id must be provided. The minted JWT matches the shape /token would emit after a successful OAuth dance, so the runtime MCP gateway validates it through the same path as a real MCP client's bearer.
 */
export function useMintUserSessionMutation(
  options?: MutationHookOptions<
    MintUserSessionMutationData,
    MintUserSessionMutationError,
    MintUserSessionMutationVariables
  >,
): UseMutationResult<
  MintUserSessionMutationData,
  MintUserSessionMutationError,
  MintUserSessionMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildMintUserSessionMutation(client, options),
    ...options,
  });
}

export function mutationKeyMintUserSession(): MutationKey {
  return ["@gram/client", "userSessions", "mint"];
}

export function buildMintUserSessionMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: MintUserSessionMutationVariables,
  ) => Promise<MintUserSessionMutationData>;
} {
  return {
    mutationKey: mutationKeyMintUserSession(),
    mutationFn: function mintUserSessionMutationFn({
      request,
      security,
      options,
    }): Promise<MintUserSessionMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(userSessionsMint(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
