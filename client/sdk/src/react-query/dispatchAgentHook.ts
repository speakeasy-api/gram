/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { hooksDispatch } from "../funcs/hooksDispatch.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type DispatchAgentHookMutationVariables = {
  request: operations.DispatchAgentHookRequest;
  security?: operations.DispatchAgentHookSecurity | undefined;
  options?: RequestOptions;
};

export type DispatchAgentHookMutationData = components.DispatchHookResult;

export type DispatchAgentHookMutationError =
  | errors.ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * dispatch hooks
 *
 * @remarks
 * Agent-forwarded hook dispatch. The device-agent daemon authenticates with its agent-scoped key and forwards a tool's hook event, vouching for the user's email. Unlike the per-tool endpoints (which take a hooks-scoped key + Gram-Project), the agent stays org-scoped: the event attributes to the org's default project unless an optional project_slug override is supplied. Fans out to the same per-tool event handling and returns a normalized decision.
 */
export function useDispatchAgentHookMutation(
  options?: MutationHookOptions<
    DispatchAgentHookMutationData,
    DispatchAgentHookMutationError,
    DispatchAgentHookMutationVariables
  >,
): UseMutationResult<
  DispatchAgentHookMutationData,
  DispatchAgentHookMutationError,
  DispatchAgentHookMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildDispatchAgentHookMutation(client, options),
    ...options,
  });
}

export function mutationKeyDispatchAgentHook(): MutationKey {
  return ["@gram/client", "hooks", "dispatch"];
}

export function buildDispatchAgentHookMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: DispatchAgentHookMutationVariables,
  ) => Promise<DispatchAgentHookMutationData>;
} {
  return {
    mutationKey: mutationKeyDispatchAgentHook(),
    mutationFn: function dispatchAgentHookMutationFn({
      request,
      security,
      options,
    }): Promise<DispatchAgentHookMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(hooksDispatch(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
