// The list the admin API accepts. An organization can still carry a value from
// outside it, so a select that renders this list has to keep the current value
// as an extra option.
export const ACCOUNT_TYPE_OPTIONS = ["free", "pro", "enterprise"];
