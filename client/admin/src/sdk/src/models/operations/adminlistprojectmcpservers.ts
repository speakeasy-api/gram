/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type AdminListProjectMcpServersRequest = {
  /**
   * Organization the project must belong to. A project outside it is reported as not found.
   */
  organizationId: string;
  /**
   * Project ID.
   */
  projectId: string;
};

/** @internal */
export type AdminListProjectMcpServersRequest$Outbound = {
  organization_id: string;
  project_id: string;
};

/** @internal */
export const AdminListProjectMcpServersRequest$outboundSchema: z.ZodMiniType<
  AdminListProjectMcpServersRequest$Outbound,
  AdminListProjectMcpServersRequest
> = z.pipe(
  z.object({
    organizationId: z.string(),
    projectId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationId: "organization_id",
      projectId: "project_id",
    });
  }),
);

export function adminListProjectMcpServersRequestToJSON(
  adminListProjectMcpServersRequest: AdminListProjectMcpServersRequest,
): string {
  return JSON.stringify(
    AdminListProjectMcpServersRequest$outboundSchema.parse(
      adminListProjectMcpServersRequest,
    ),
  );
}
