/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type AdminGetSpendBreakdownRequest = {
  /**
   * Organization ID or canonical slug.
   */
  organizationId: string;
  /**
   * Inclusive UTC midnight reporting boundary. Must be paired with to.
   */
  from?: Date | undefined;
  /**
   * Exclusive UTC midnight reporting boundary. Must be paired with from and no later than three calendar months after from, clamped to the target month's last day.
   */
  to?: Date | undefined;
};

/** @internal */
export type AdminGetSpendBreakdownRequest$Outbound = {
  organization_id: string;
  from?: string | undefined;
  to?: string | undefined;
};

/** @internal */
export const AdminGetSpendBreakdownRequest$outboundSchema: z.ZodMiniType<
  AdminGetSpendBreakdownRequest$Outbound,
  AdminGetSpendBreakdownRequest
> = z.pipe(
  z.object({
    organizationId: z.string(),
    from: z.optional(z.pipe(z.date(), z.transform(v => v.toISOString()))),
    to: z.optional(z.pipe(z.date(), z.transform(v => v.toISOString()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationId: "organization_id",
    });
  }),
);

export function adminGetSpendBreakdownRequestToJSON(
  adminGetSpendBreakdownRequest: AdminGetSpendBreakdownRequest,
): string {
  return JSON.stringify(
    AdminGetSpendBreakdownRequest$outboundSchema.parse(
      adminGetSpendBreakdownRequest,
    ),
  );
}
