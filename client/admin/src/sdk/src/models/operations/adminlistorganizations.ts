/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import {
  AdminListOrganizationsResult,
  AdminListOrganizationsResult$inboundSchema,
} from "../components/adminlistorganizationsresult.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AdminListOrganizationsRequest = {
  /**
   * Search term, trimmed of surrounding whitespace. Matches name and slug as a case-insensitive substring, with % and _ taken literally, and matches organization id and WorkOS id exactly, ignoring case. An id match also returns an organization that disabled_states or include_disabled would otherwise hide; it still respects account_type, account_types, trial_states and cursor.
   */
  q?: string | undefined;
  /**
   * Filter by a single gram_account_type (e.g. free, pro, payg, enterprise). Superseded by account_types, which it joins as one more member of the same set.
   */
  accountType?: string | undefined;
  /**
   * Match any of these gram_account_type values. Empty matches every account type. A value no organization carries matches nothing rather than failing the request.
   */
  accountTypes?: Array<string> | undefined;
  /**
   * Match any of running, ending_soon, expired, demoted, converted or none. Empty matches every trial state. An unrecognised value matches nothing rather than failing the request.
   */
  trialStates?: Array<string> | undefined;
  /**
   * Match any of active or disabled. Empty falls back to include_disabled. An unrecognised value matches nothing rather than failing the request.
   */
  disabledStates?: Array<string> | undefined;
  /**
   * Include organizations with disabled_at set. Defaults to false. Superseded by disabled_states, which overrides it outright when supplied.
   */
  includeDisabled?: boolean | undefined;
  /**
   * Pagination cursor: id of the last item from the previous page. Ignored when sort or page is supplied.
   */
  cursor?: string | undefined;
  /**
   * Page size (default 50, max 100).
   */
  limit?: number | undefined;
  /**
   * Column to sort by: name, slug, account_type, member_count, created_at, disabled_at or trial_ends_at. Any other value sorts by id. Supplying it selects offset paging.
   */
  sort?: string | undefined;
  /**
   * Sort direction, asc or desc, applied to the column named by sort. Any other value sorts ascending. On its own it does nothing: without sort there is no column to reverse, so it neither reorders the results nor selects offset paging.
   */
  direction?: string | undefined;
  /**
   * 1-based page number for offset paging (default 1). Supplying it selects offset paging.
   */
  page?: number | undefined;
};

export type AdminListOrganizationsResponse = {
  result: AdminListOrganizationsResult;
};

/** @internal */
export type AdminListOrganizationsRequest$Outbound = {
  q?: string | undefined;
  account_type?: string | undefined;
  account_types?: Array<string> | undefined;
  trial_states?: Array<string> | undefined;
  disabled_states?: Array<string> | undefined;
  include_disabled?: boolean | undefined;
  cursor?: string | undefined;
  limit?: number | undefined;
  sort?: string | undefined;
  direction?: string | undefined;
  page?: number | undefined;
};

/** @internal */
export const AdminListOrganizationsRequest$outboundSchema: z.ZodMiniType<
  AdminListOrganizationsRequest$Outbound,
  AdminListOrganizationsRequest
> = z.pipe(
  z.object({
    q: z.optional(z.string()),
    accountType: z.optional(z.string()),
    accountTypes: z.optional(z.array(z.string())),
    trialStates: z.optional(z.array(z.string())),
    disabledStates: z.optional(z.array(z.string())),
    includeDisabled: z.optional(z.boolean()),
    cursor: z.optional(z.string()),
    limit: z.optional(z.int()),
    sort: z.optional(z.string()),
    direction: z.optional(z.string()),
    page: z.optional(z.int()),
  }),
  z.transform((v) => {
    return remap$(v, {
      accountType: "account_type",
      accountTypes: "account_types",
      trialStates: "trial_states",
      disabledStates: "disabled_states",
      includeDisabled: "include_disabled",
    });
  }),
);

export function adminListOrganizationsRequestToJSON(
  adminListOrganizationsRequest: AdminListOrganizationsRequest,
): string {
  return JSON.stringify(
    AdminListOrganizationsRequest$outboundSchema.parse(
      adminListOrganizationsRequest,
    ),
  );
}

/** @internal */
export const AdminListOrganizationsResponse$inboundSchema: z.ZodMiniType<
  AdminListOrganizationsResponse,
  unknown
> = z.pipe(
  z.object({
    Result: AdminListOrganizationsResult$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "Result": "result",
    });
  }),
);

export function adminListOrganizationsResponseFromJSON(
  jsonString: string,
): SafeParseResult<AdminListOrganizationsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminListOrganizationsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminListOrganizationsResponse' from JSON`,
  );
}
