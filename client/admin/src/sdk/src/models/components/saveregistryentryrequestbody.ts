/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type SaveRegistryEntryRequestBody = {
  /**
   * Complete registry record JSON; at most 8388608 UTF-8 bytes (8 MiB), enforced by the server on incoming writes. Stored records remain readable for repair.
   */
  dataJson: string;
  id: string;
  updatedAt: string;
};

/** @internal */
export type SaveRegistryEntryRequestBody$Outbound = {
  data_json: string;
  id: string;
  updated_at: string;
};

/** @internal */
export const SaveRegistryEntryRequestBody$outboundSchema: z.ZodMiniType<
  SaveRegistryEntryRequestBody$Outbound,
  SaveRegistryEntryRequestBody
> = z.pipe(
  z.object({
    dataJson: z.string(),
    id: z.string(),
    updatedAt: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      dataJson: "data_json",
      updatedAt: "updated_at",
    });
  }),
);

export function saveRegistryEntryRequestBodyToJSON(
  saveRegistryEntryRequestBody: SaveRegistryEntryRequestBody,
): string {
  return JSON.stringify(
    SaveRegistryEntryRequestBody$outboundSchema.parse(
      saveRegistryEntryRequestBody,
    ),
  );
}
