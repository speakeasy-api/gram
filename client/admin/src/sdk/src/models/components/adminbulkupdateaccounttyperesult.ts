/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Outcome of a bulk account type change.
 */
export type AdminBulkUpdateAccountTypeResult = {
  /**
   * IDs from the request that matched no organization, deduplicated and in request order. Nothing was written for these.
   */
  missingIds: Array<string>;
  /**
   * IDs of the organizations whose account type was set. Order is unspecified: do not rely on it.
   */
  updatedIds: Array<string>;
};

/** @internal */
export const AdminBulkUpdateAccountTypeResult$inboundSchema: z.ZodMiniType<
  AdminBulkUpdateAccountTypeResult,
  unknown
> = z.pipe(
  z.object({
    missing_ids: z.array(z.string()),
    updated_ids: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "missing_ids": "missingIds",
      "updated_ids": "updatedIds",
    });
  }),
);

export function adminBulkUpdateAccountTypeResultFromJSON(
  jsonString: string,
): SafeParseResult<AdminBulkUpdateAccountTypeResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminBulkUpdateAccountTypeResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminBulkUpdateAccountTypeResult' from JSON`,
  );
}
