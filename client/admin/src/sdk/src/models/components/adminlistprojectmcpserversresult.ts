/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdminMcpServer,
  AdminMcpServer$inboundSchema,
} from "./adminmcpserver.js";

export type AdminListProjectMcpServersResult = {
  /**
   * The project's MCP servers, oldest first.
   */
  mcpServers: Array<AdminMcpServer>;
};

/** @internal */
export const AdminListProjectMcpServersResult$inboundSchema: z.ZodMiniType<
  AdminListProjectMcpServersResult,
  unknown
> = z.pipe(
  z.object({
    mcp_servers: z.array(AdminMcpServer$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "mcp_servers": "mcpServers",
    });
  }),
);

export function adminListProjectMcpServersResultFromJSON(
  jsonString: string,
): SafeParseResult<AdminListProjectMcpServersResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminListProjectMcpServersResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminListProjectMcpServersResult' from JSON`,
  );
}
