/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const Status = {
  Incomplete: "incomplete",
  IncompleteExpired: "incomplete_expired",
  Trialing: "trialing",
  Active: "active",
  PastDue: "past_due",
  Canceled: "canceled",
  Unpaid: "unpaid",
  Paused: "paused",
} as const;
export type Status = ClosedEnum<typeof Status>;

export type AdminStripeSubscription = {
  cancelAt?: Date | undefined;
  cancelAtPeriodEnd: boolean;
  canceledAt?: Date | undefined;
  currentPeriodEnd: Date;
  currentPeriodStart: Date;
  paymentFailed: boolean;
  status: Status;
  trialEnd?: Date | undefined;
  trialStart?: Date | undefined;
};

/** @internal */
export const Status$inboundSchema: z.ZodMiniEnum<typeof Status> = z.enum(
  Status,
);

/** @internal */
export const AdminStripeSubscription$inboundSchema: z.ZodMiniType<
  AdminStripeSubscription,
  unknown
> = z.pipe(
  z.object({
    cancel_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    cancel_at_period_end: z.boolean(),
    canceled_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    current_period_end: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    current_period_start: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    payment_failed: z.boolean(),
    status: Status$inboundSchema,
    trial_end: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    trial_start: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "cancel_at": "cancelAt",
      "cancel_at_period_end": "cancelAtPeriodEnd",
      "canceled_at": "canceledAt",
      "current_period_end": "currentPeriodEnd",
      "current_period_start": "currentPeriodStart",
      "payment_failed": "paymentFailed",
      "trial_end": "trialEnd",
      "trial_start": "trialStart",
    });
  }),
);

export function adminStripeSubscriptionFromJSON(
  jsonString: string,
): SafeParseResult<AdminStripeSubscription, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminStripeSubscription$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminStripeSubscription' from JSON`,
  );
}
