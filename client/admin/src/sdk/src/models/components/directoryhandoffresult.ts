/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DirectoryHandoff,
  DirectoryHandoff$inboundSchema,
} from "./directoryhandoff.js";

/**
 * WorkOS environment used by this server.
 */
export const WorkosEnvironment = {
  Development: "development",
  Production: "production",
  Unknown: "unknown",
} as const;
/**
 * WorkOS environment used by this server.
 */
export type WorkosEnvironment = ClosedEnum<typeof WorkosEnvironment>;

export type DirectoryHandoffResult = {
  /**
   * Non-secret directory handoff details stored for an organization.
   */
  handoff?: DirectoryHandoff | undefined;
  /**
   * WorkOS environment used by this server.
   */
  workosEnvironment: WorkosEnvironment;
};

/** @internal */
export const WorkosEnvironment$inboundSchema: z.ZodMiniEnum<
  typeof WorkosEnvironment
> = z.enum(WorkosEnvironment);

/** @internal */
export const DirectoryHandoffResult$inboundSchema: z.ZodMiniType<
  DirectoryHandoffResult,
  unknown
> = z.pipe(
  z.object({
    handoff: z.optional(DirectoryHandoff$inboundSchema),
    workos_environment: WorkosEnvironment$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "workos_environment": "workosEnvironment",
    });
  }),
);

export function directoryHandoffResultFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryHandoffResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryHandoffResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryHandoffResult' from JSON`,
  );
}
