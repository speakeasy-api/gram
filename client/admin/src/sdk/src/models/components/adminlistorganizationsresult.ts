/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdminOrganization,
  AdminOrganization$inboundSchema,
} from "./adminorganization.js";

export type AdminListOrganizationsResult = {
  /**
   * Cursor for the next page; empty when exhausted. Omitted in offset mode.
   */
  nextCursor?: string | undefined;
  /**
   * The page of organizations.
   */
  organizations: Array<AdminOrganization>;
  /**
   * Number of organizations matching the filters, before paging.
   */
  total: number;
};

/** @internal */
export const AdminListOrganizationsResult$inboundSchema: z.ZodMiniType<
  AdminListOrganizationsResult,
  unknown
> = z.pipe(
  z.object({
    next_cursor: z.optional(z.string()),
    organizations: z.array(AdminOrganization$inboundSchema),
    total: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function adminListOrganizationsResultFromJSON(
  jsonString: string,
): SafeParseResult<AdminListOrganizationsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminListOrganizationsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminListOrganizationsResult' from JSON`,
  );
}
