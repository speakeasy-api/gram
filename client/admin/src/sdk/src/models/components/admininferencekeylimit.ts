/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The configured monthly limit for one materialized platform-managed OpenRouter key.
 */
export type AdminInferenceKeyLimit = {
  keyType: string;
  monthlyCredits: number;
};

/** @internal */
export const AdminInferenceKeyLimit$inboundSchema: z.ZodMiniType<
  AdminInferenceKeyLimit,
  unknown
> = z.pipe(
  z.object({
    key_type: z.string(),
    monthly_credits: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "key_type": "keyType",
      "monthly_credits": "monthlyCredits",
    });
  }),
);

export function adminInferenceKeyLimitFromJSON(
  jsonString: string,
): SafeParseResult<AdminInferenceKeyLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminInferenceKeyLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminInferenceKeyLimit' from JSON`,
  );
}
