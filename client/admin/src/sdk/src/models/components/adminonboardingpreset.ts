/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AdminOnboardingPreset = {
  key: string;
  title: string;
  visibleTaskKeys: Array<string>;
};

/** @internal */
export const AdminOnboardingPreset$inboundSchema: z.ZodMiniType<
  AdminOnboardingPreset,
  unknown
> = z.pipe(
  z.object({
    key: z.string(),
    title: z.string(),
    visible_task_keys: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "visible_task_keys": "visibleTaskKeys",
    });
  }),
);

export function adminOnboardingPresetFromJSON(
  jsonString: string,
): SafeParseResult<AdminOnboardingPreset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminOnboardingPreset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminOnboardingPreset' from JSON`,
  );
}
