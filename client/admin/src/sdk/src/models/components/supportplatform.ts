/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SupportPlatform = {
  family: string;
  id: string;
  name: string;
  surface: string;
  vendor: string;
};

/** @internal */
export const SupportPlatform$inboundSchema: z.ZodMiniType<
  SupportPlatform,
  unknown
> = z.object({
  family: z.string(),
  id: z.string(),
  name: z.string(),
  surface: z.string(),
  vendor: z.string(),
});

export function supportPlatformFromJSON(
  jsonString: string,
): SafeParseResult<SupportPlatform, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportPlatform$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportPlatform' from JSON`,
  );
}
