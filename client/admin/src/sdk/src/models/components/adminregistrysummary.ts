/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdminRegistryIssue,
  AdminRegistryIssue$inboundSchema,
} from "./adminregistryissue.js";

export type AdminRegistrySummary = {
  id: string;
  issues: Array<AdminRegistryIssue>;
  name: string;
  published: boolean;
  updatedAt: string;
};

/** @internal */
export const AdminRegistrySummary$inboundSchema: z.ZodMiniType<
  AdminRegistrySummary,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    issues: z.array(AdminRegistryIssue$inboundSchema),
    name: z.string(),
    published: z.boolean(),
    updated_at: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "updated_at": "updatedAt",
    });
  }),
);

export function adminRegistrySummaryFromJSON(
  jsonString: string,
): SafeParseResult<AdminRegistrySummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminRegistrySummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminRegistrySummary' from JSON`,
  );
}
