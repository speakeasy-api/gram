/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdminOnboardingPreset,
  AdminOnboardingPreset$inboundSchema,
} from "./adminonboardingpreset.js";
import {
  AdminOnboardingTask,
  AdminOnboardingTask$inboundSchema,
} from "./adminonboardingtask.js";

/**
 * Absent for legacy organizations.
 */
export const Preset = {
  Gateway: "gateway",
  Security: "security",
} as const;
/**
 * Absent for legacy organizations.
 */
export type Preset = ClosedEnum<typeof Preset>;

export type AdminOnboardingConfiguration = {
  organizationId: string;
  /**
   * Absent for legacy organizations.
   */
  preset?: Preset | undefined;
  presets: Array<AdminOnboardingPreset>;
  tasks: Array<AdminOnboardingTask>;
};

/** @internal */
export const Preset$inboundSchema: z.ZodMiniEnum<typeof Preset> = z.enum(
  Preset,
);

/** @internal */
export const AdminOnboardingConfiguration$inboundSchema: z.ZodMiniType<
  AdminOnboardingConfiguration,
  unknown
> = z.pipe(
  z.object({
    organization_id: z.string(),
    preset: z.optional(Preset$inboundSchema),
    presets: z.array(AdminOnboardingPreset$inboundSchema),
    tasks: z.array(AdminOnboardingTask$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "organization_id": "organizationId",
    });
  }),
);

export function adminOnboardingConfigurationFromJSON(
  jsonString: string,
): SafeParseResult<AdminOnboardingConfiguration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminOnboardingConfiguration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminOnboardingConfiguration' from JSON`,
  );
}
