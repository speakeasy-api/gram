/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Organization member surfaced to admin operators.
 */
export type AdminOrganizationMember = {
  createdAt: Date;
  /**
   * User display name.
   */
  displayName: string;
  /**
   * User email address.
   */
  email: string;
  /**
   * User ID.
   */
  id: string;
  /**
   * The time the user last logged in, if any.
   */
  lastLogin?: Date | undefined;
  updatedAt: Date;
};

/** @internal */
export const AdminOrganizationMember$inboundSchema: z.ZodMiniType<
  AdminOrganizationMember,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    display_name: z.string(),
    email: z.string(),
    id: z.string(),
    last_login: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "display_name": "displayName",
      "last_login": "lastLogin",
      "updated_at": "updatedAt",
    });
  }),
);

export function adminOrganizationMemberFromJSON(
  jsonString: string,
): SafeParseResult<AdminOrganizationMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminOrganizationMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminOrganizationMember' from JSON`,
  );
}
