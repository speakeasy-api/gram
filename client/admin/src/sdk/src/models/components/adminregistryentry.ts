/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdminRegistryIssue,
  AdminRegistryIssue$inboundSchema,
} from "./adminregistryissue.js";

export type AdminRegistryEntry = {
  createdAt: string;
  /**
   * Complete lossless registry record JSON
   */
  dataJson: string;
  id: string;
  issues: Array<AdminRegistryIssue>;
  published: boolean;
  /**
   * Opaque write precondition; echo unchanged
   */
  updatedAt: string;
};

/** @internal */
export const AdminRegistryEntry$inboundSchema: z.ZodMiniType<
  AdminRegistryEntry,
  unknown
> = z.pipe(
  z.object({
    created_at: z.string(),
    data_json: z.string(),
    id: z.string(),
    issues: z.array(AdminRegistryIssue$inboundSchema),
    published: z.boolean(),
    updated_at: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "data_json": "dataJson",
      "updated_at": "updatedAt",
    });
  }),
);

export function adminRegistryEntryFromJSON(
  jsonString: string,
): SafeParseResult<AdminRegistryEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminRegistryEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminRegistryEntry' from JSON`,
  );
}
