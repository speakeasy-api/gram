/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Privacy-minimal result of recording an enterprise trial conversion.
 */
export type MarkEnterpriseTrialConvertedResult = {
  /**
   * The time at which the enterprise trial was recorded as converted.
   */
  convertedAt: Date;
  /**
   * The converted organization ID.
   */
  organizationId: string;
};

/** @internal */
export const MarkEnterpriseTrialConvertedResult$inboundSchema: z.ZodMiniType<
  MarkEnterpriseTrialConvertedResult,
  unknown
> = z.pipe(
  z.object({
    converted_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    organization_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "converted_at": "convertedAt",
      "organization_id": "organizationId",
    });
  }),
);

export function markEnterpriseTrialConvertedResultFromJSON(
  jsonString: string,
): SafeParseResult<MarkEnterpriseTrialConvertedResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      MarkEnterpriseTrialConvertedResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkEnterpriseTrialConvertedResult' from JSON`,
  );
}
