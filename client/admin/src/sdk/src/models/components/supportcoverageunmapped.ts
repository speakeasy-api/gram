/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A hook_source the surface fold did not recognize.
 */
export type SupportCoverageUnmapped = {
  /**
   * The raw, unrecognized hook_source.
   */
  hookSource: string;
  /**
   * Sessions observed under it inside the window.
   */
  sessions: number;
};

/** @internal */
export const SupportCoverageUnmapped$inboundSchema: z.ZodMiniType<
  SupportCoverageUnmapped,
  unknown
> = z.pipe(
  z.object({
    hook_source: z.string(),
    sessions: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "hook_source": "hookSource",
    });
  }),
);

export function supportCoverageUnmappedFromJSON(
  jsonString: string,
): SafeParseResult<SupportCoverageUnmapped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportCoverageUnmapped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportCoverageUnmapped' from JSON`,
  );
}
