/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AdminChatAnalysisSettings = {
  businessMemoryDailyCap: number;
  businessMemoryEnabled: boolean;
  isDefault: boolean;
  organizationId: string;
  workUnitsDailyCap: number;
  workUnitsEnabled: boolean;
};

/** @internal */
export const AdminChatAnalysisSettings$inboundSchema: z.ZodMiniType<
  AdminChatAnalysisSettings,
  unknown
> = z.pipe(
  z.object({
    business_memory_daily_cap: z.int(),
    business_memory_enabled: z.boolean(),
    is_default: z.boolean(),
    organization_id: z.string(),
    work_units_daily_cap: z.int(),
    work_units_enabled: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "business_memory_daily_cap": "businessMemoryDailyCap",
      "business_memory_enabled": "businessMemoryEnabled",
      "is_default": "isDefault",
      "organization_id": "organizationId",
      "work_units_daily_cap": "workUnitsDailyCap",
      "work_units_enabled": "workUnitsEnabled",
    });
  }),
);

export function adminChatAnalysisSettingsFromJSON(
  jsonString: string,
): SafeParseResult<AdminChatAnalysisSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminChatAnalysisSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminChatAnalysisSettings' from JSON`,
  );
}
