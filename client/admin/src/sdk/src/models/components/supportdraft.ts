/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SupportFact,
  SupportFact$inboundSchema,
  SupportFact$Outbound,
  SupportFact$outboundSchema,
} from "./supportfact.js";
import {
  SupportMapping,
  SupportMapping$inboundSchema,
  SupportMapping$Outbound,
  SupportMapping$outboundSchema,
} from "./supportmapping.js";

export const Accounts = {
  Supported: "supported",
  Unsupported: "unsupported",
  Unknown: "unknown",
} as const;
export type Accounts = ClosedEnum<typeof Accounts>;

export type SupportDraft = {
  accounts: { [k: string]: { [k: string]: Accounts } };
  mappings: { [k: string]: SupportMapping };
  references: { [k: string]: { [k: string]: SupportFact } };
};

/** @internal */
export const Accounts$inboundSchema: z.ZodMiniEnum<typeof Accounts> = z.enum(
  Accounts,
);
/** @internal */
export const Accounts$outboundSchema: z.ZodMiniEnum<typeof Accounts> =
  Accounts$inboundSchema;

/** @internal */
export const SupportDraft$inboundSchema: z.ZodMiniType<SupportDraft, unknown> =
  z.object({
    accounts: z.record(
      z.string(),
      z.record(z.string(), Accounts$inboundSchema),
    ),
    mappings: z.record(z.string(), SupportMapping$inboundSchema),
    references: z.record(
      z.string(),
      z.record(z.string(), SupportFact$inboundSchema),
    ),
  });
/** @internal */
export type SupportDraft$Outbound = {
  accounts: { [k: string]: { [k: string]: string } };
  mappings: { [k: string]: SupportMapping$Outbound };
  references: { [k: string]: { [k: string]: SupportFact$Outbound } };
};

/** @internal */
export const SupportDraft$outboundSchema: z.ZodMiniType<
  SupportDraft$Outbound,
  SupportDraft
> = z.object({
  accounts: z.record(z.string(), z.record(z.string(), Accounts$outboundSchema)),
  mappings: z.record(z.string(), SupportMapping$outboundSchema),
  references: z.record(
    z.string(),
    z.record(z.string(), SupportFact$outboundSchema),
  ),
});

export function supportDraftToJSON(supportDraft: SupportDraft): string {
  return JSON.stringify(SupportDraft$outboundSchema.parse(supportDraft));
}
export function supportDraftFromJSON(
  jsonString: string,
): SafeParseResult<SupportDraft, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportDraft$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportDraft' from JSON`,
  );
}
