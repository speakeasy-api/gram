/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Non-secret directory handoff details stored for an organization.
 */
export type DirectoryHandoff = {
  /**
   * Organization receiving the directory handoff.
   */
  organizationId: string;
  /**
   * SCIM base URL supplied to the customer administrator.
   */
  scimBaseUrl: string;
  /**
   * Email of the Speakeasy operator who last stored the handoff.
   */
  setBy: string;
  /**
   * First eight hexadecimal characters of the SCIM token SHA-256 digest.
   */
  tokenFingerprint: string;
  updatedAt: Date;
  /**
   * Live WorkOS directory ID, when discovered.
   */
  workosDirectoryId?: string | undefined;
  /**
   * Live WorkOS directory state, when discovered.
   */
  workosDirectoryState?: string | undefined;
};

/** @internal */
export const DirectoryHandoff$inboundSchema: z.ZodMiniType<
  DirectoryHandoff,
  unknown
> = z.pipe(
  z.object({
    organization_id: z.string(),
    scim_base_url: z.string(),
    set_by: z.string(),
    token_fingerprint: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    workos_directory_id: z.optional(z.string()),
    workos_directory_state: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "organization_id": "organizationId",
      "scim_base_url": "scimBaseUrl",
      "set_by": "setBy",
      "token_fingerprint": "tokenFingerprint",
      "updated_at": "updatedAt",
      "workos_directory_id": "workosDirectoryId",
      "workos_directory_state": "workosDirectoryState",
    });
  }),
);

export function directoryHandoffFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryHandoff, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryHandoff$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryHandoff' from JSON`,
  );
}
