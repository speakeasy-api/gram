/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { AuditLog, AuditLog$inboundSchema } from "./auditlog.js";

export type AdminListOrganizationActivityResult = {
  /**
   * List of organization activity.
   */
  logs: Array<AuditLog>;
  /**
   * Cursor for the next page of results.
   */
  nextCursor?: string | undefined;
};

/** @internal */
export const AdminListOrganizationActivityResult$inboundSchema: z.ZodMiniType<
  AdminListOrganizationActivityResult,
  unknown
> = z.pipe(
  z.object({
    logs: z.array(AuditLog$inboundSchema),
    next_cursor: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function adminListOrganizationActivityResultFromJSON(
  jsonString: string,
): SafeParseResult<AdminListOrganizationActivityResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      AdminListOrganizationActivityResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminListOrganizationActivityResult' from JSON`,
  );
}
