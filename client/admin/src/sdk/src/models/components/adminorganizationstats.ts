/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Platform-wide organization counts surfaced above the admin organizations list.
 */
export type AdminOrganizationStats = {
  /**
   * Organizations created in the last 7 days, whatever their current status.
   */
  createdLast7Days: number;
  /**
   * Organizations on a paid account type (payg or enterprise), disabled ones included.
   */
  customers: number;
  /**
   * Customers created in the last 7 days, whatever their current status.
   */
  customersCreatedLast7Days: number;
  /**
   * Organizations with disabled_at set.
   */
  disabled: number;
  /**
   * Organizations disabled in the last 7 days.
   */
  disabledLast7Days: number;
  /**
   * Every organization on the platform, disabled ones included.
   */
  total: number;
  /**
   * Organizations whose trial_state is ending_soon.
   */
  trialsEndingSoon: number;
};

/** @internal */
export const AdminOrganizationStats$inboundSchema: z.ZodMiniType<
  AdminOrganizationStats,
  unknown
> = z.pipe(
  z.object({
    created_last_7_days: z.int(),
    customers: z.int(),
    customers_created_last_7_days: z.int(),
    disabled: z.int(),
    disabled_last_7_days: z.int(),
    total: z.int(),
    trials_ending_soon: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_last_7_days": "createdLast7Days",
      "customers_created_last_7_days": "customersCreatedLast7Days",
      "disabled_last_7_days": "disabledLast7Days",
      "trials_ending_soon": "trialsEndingSoon",
    });
  }),
);

export function adminOrganizationStatsFromJSON(
  jsonString: string,
): SafeParseResult<AdminOrganizationStats, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminOrganizationStats$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminOrganizationStats' from JSON`,
  );
}
