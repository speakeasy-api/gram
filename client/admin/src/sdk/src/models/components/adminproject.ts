/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Project summary surfaced to admin operators.
 */
export type AdminProject = {
  /**
   * The creation date of the project.
   */
  createdAt: Date;
  /**
   * The ID of the project
   */
  id: string;
  /**
   * Number of MCP servers in the project, counting both toolset-backed servers and mcp_servers rows.
   */
  mcpServerCount: number;
  /**
   * The name of the project
   */
  name: string;
  /**
   * The slug of the project
   */
  slug: string;
  /**
   * The last update date of the project.
   */
  updatedAt: Date;
};

/** @internal */
export const AdminProject$inboundSchema: z.ZodMiniType<AdminProject, unknown> =
  z.pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      id: z.string(),
      mcp_server_count: z.int(),
      name: z.string(),
      slug: z.string(),
      updated_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "mcp_server_count": "mcpServerCount",
        "updated_at": "updatedAt",
      });
    }),
  );

export function adminProjectFromJSON(
  jsonString: string,
): SafeParseResult<AdminProject, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminProject$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminProject' from JSON`,
  );
}
