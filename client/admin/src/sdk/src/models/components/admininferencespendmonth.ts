/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AdminInferenceSpendMonth = {
  /**
   * Exclusive end of the UTC calendar month.
   */
  periodEnd: RFCDate;
  periodStart: RFCDate;
  spendUsd: string;
};

/** @internal */
export const AdminInferenceSpendMonth$inboundSchema: z.ZodMiniType<
  AdminInferenceSpendMonth,
  unknown
> = z.pipe(
  z.object({
    period_end: z.pipe(z.string(), z.transform(v => new RFCDate(v))),
    period_start: z.pipe(z.string(), z.transform(v => new RFCDate(v))),
    spend_usd: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "period_end": "periodEnd",
      "period_start": "periodStart",
      "spend_usd": "spendUsd",
    });
  }),
);

export function adminInferenceSpendMonthFromJSON(
  jsonString: string,
): SafeParseResult<AdminInferenceSpendMonth, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminInferenceSpendMonth$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminInferenceSpendMonth' from JSON`,
  );
}
