/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type CreateRegistryEntryRequestBody = {
  /**
   * Complete registry record JSON; at most 8388608 UTF-8 bytes (8 MiB), enforced by the server on incoming writes. Stored records remain readable for repair.
   */
  dataJson: string;
};

/** @internal */
export type CreateRegistryEntryRequestBody$Outbound = {
  data_json: string;
};

/** @internal */
export const CreateRegistryEntryRequestBody$outboundSchema: z.ZodMiniType<
  CreateRegistryEntryRequestBody$Outbound,
  CreateRegistryEntryRequestBody
> = z.pipe(
  z.object({
    dataJson: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      dataJson: "data_json",
    });
  }),
);

export function createRegistryEntryRequestBodyToJSON(
  createRegistryEntryRequestBody: CreateRegistryEntryRequestBody,
): string {
  return JSON.stringify(
    CreateRegistryEntryRequestBody$outboundSchema.parse(
      createRegistryEntryRequestBody,
    ),
  );
}
