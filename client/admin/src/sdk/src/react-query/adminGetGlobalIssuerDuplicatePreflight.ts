/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import { AdminGetGlobalIssuerDuplicatePreflightRequest } from "../models/operations/admingetglobalissuerduplicatepreflight.js";
import { useGramContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  AdminGetGlobalIssuerDuplicatePreflightQueryData,
  buildAdminGetGlobalIssuerDuplicatePreflightQuery,
  prefetchAdminGetGlobalIssuerDuplicatePreflight,
  queryKeyAdminGetGlobalIssuerDuplicatePreflight,
} from "./adminGetGlobalIssuerDuplicatePreflight.core.js";
export {
  type AdminGetGlobalIssuerDuplicatePreflightQueryData,
  buildAdminGetGlobalIssuerDuplicatePreflightQuery,
  prefetchAdminGetGlobalIssuerDuplicatePreflight,
  queryKeyAdminGetGlobalIssuerDuplicatePreflight,
};

export type AdminGetGlobalIssuerDuplicatePreflightQueryError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * getGlobalIssuerDuplicatePreflight admin
 *
 * @remarks
 * Report the global remote_session_issuers that already describe an upstream issuer URL, so the catalog create and edit forms can warn before curating a second entry for the same authorization server. Requires platform admin.
 *
 * Scoped to the global partition only. Tenant issuers naming the same URL are deliberately not reported here — listGlobalIssuerConvergenceCandidates is the surface for those, and it is keyed on a global issuer that already exists.
 *
 * The global tier is unique on slug but not on issuer, so nothing prevents a duplicate catalog entry and this warning is the only thing that will catch one. Advisory all the same: it never blocks the write. Matching uses the same canonicalization as the tenant-facing preflights, and an unparseable URL returns no matches rather than an error.
 */
export function useAdminGetGlobalIssuerDuplicatePreflight(
  request?: AdminGetGlobalIssuerDuplicatePreflightRequest | undefined,
  options?: QueryHookOptions<
    AdminGetGlobalIssuerDuplicatePreflightQueryData,
    AdminGetGlobalIssuerDuplicatePreflightQueryError
  >,
): UseQueryResult<
  AdminGetGlobalIssuerDuplicatePreflightQueryData,
  AdminGetGlobalIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useQuery({
    ...buildAdminGetGlobalIssuerDuplicatePreflightQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * getGlobalIssuerDuplicatePreflight admin
 *
 * @remarks
 * Report the global remote_session_issuers that already describe an upstream issuer URL, so the catalog create and edit forms can warn before curating a second entry for the same authorization server. Requires platform admin.
 *
 * Scoped to the global partition only. Tenant issuers naming the same URL are deliberately not reported here — listGlobalIssuerConvergenceCandidates is the surface for those, and it is keyed on a global issuer that already exists.
 *
 * The global tier is unique on slug but not on issuer, so nothing prevents a duplicate catalog entry and this warning is the only thing that will catch one. Advisory all the same: it never blocks the write. Matching uses the same canonicalization as the tenant-facing preflights, and an unparseable URL returns no matches rather than an error.
 */
export function useAdminGetGlobalIssuerDuplicatePreflightSuspense(
  request?: AdminGetGlobalIssuerDuplicatePreflightRequest | undefined,
  options?: SuspenseQueryHookOptions<
    AdminGetGlobalIssuerDuplicatePreflightQueryData,
    AdminGetGlobalIssuerDuplicatePreflightQueryError
  >,
): UseSuspenseQueryResult<
  AdminGetGlobalIssuerDuplicatePreflightQueryData,
  AdminGetGlobalIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildAdminGetGlobalIssuerDuplicatePreflightQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

export function setAdminGetGlobalIssuerDuplicatePreflightData(
  client: QueryClient,
  queryKeyBase: [parameters: { issuer?: string | undefined }],
  data: AdminGetGlobalIssuerDuplicatePreflightQueryData,
): AdminGetGlobalIssuerDuplicatePreflightQueryData | undefined {
  const key = queryKeyAdminGetGlobalIssuerDuplicatePreflight(...queryKeyBase);

  return client.setQueryData<AdminGetGlobalIssuerDuplicatePreflightQueryData>(
    key,
    data,
  );
}

export function invalidateAdminGetGlobalIssuerDuplicatePreflight(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<[parameters: { issuer?: string | undefined }]>,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/admin-client",
      "admin",
      "getGlobalIssuerDuplicatePreflight",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllAdminGetGlobalIssuerDuplicatePreflight(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/admin-client",
      "admin",
      "getGlobalIssuerDuplicatePreflight",
    ],
  });
}
