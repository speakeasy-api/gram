/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PlatformMCPConnection,
  PlatformMCPConnection$inboundSchema,
} from "./platformmcpconnection.js";

/**
 * Current package admission evaluation.
 */
export const Admission = {
  Enabled: "enabled",
  Disabled: "disabled",
  Indeterminate: "indeterminate",
} as const;
/**
 * Current package admission evaluation.
 */
export type Admission = ClosedEnum<typeof Admission>;

/**
 * Bounded lifecycle state reason.
 */
export const ReasonCode = {
  Eligible: "eligible",
  DefaultProjectMissing: "default_project_missing",
  MarketplaceUnpublished: "marketplace_unpublished",
  GateDisabled: "gate_disabled",
  StatusIndeterminate: "status_indeterminate",
  AuthorizedAwaitingDiscovery: "authorized_awaiting_discovery",
  Ready: "ready",
} as const;
/**
 * Bounded lifecycle state reason.
 */
export type ReasonCode = ClosedEnum<typeof ReasonCode>;

export type PlatformMCPLifecycle = {
  /**
   * Current package admission evaluation.
   */
  admission: Admission;
  /**
   * Whether the organization has an active Platform MCP connection.
   */
  authorized: boolean;
  /**
   * Active Platform MCP connections, each scoped to this organization and usable only to revoke within it.
   */
  connections?: Array<PlatformMCPConnection> | undefined;
  /**
   * Literal default project identifier when it exists.
   */
  defaultProjectId?: string | undefined;
  /**
   * Whether the literal default project has a published marketplace repository.
   */
  marketplacePublished: boolean;
  /**
   * Whether at least one active connection completed authenticated tools/list discovery.
   */
  ready: boolean;
  /**
   * Bounded lifecycle state reason.
   */
  reasonCode: ReasonCode;
};

/** @internal */
export const Admission$inboundSchema: z.ZodMiniEnum<typeof Admission> = z.enum(
  Admission,
);

/** @internal */
export const ReasonCode$inboundSchema: z.ZodMiniEnum<typeof ReasonCode> = z
  .enum(ReasonCode);

/** @internal */
export const PlatformMCPLifecycle$inboundSchema: z.ZodMiniType<
  PlatformMCPLifecycle,
  unknown
> = z.pipe(
  z.object({
    admission: Admission$inboundSchema,
    authorized: z.boolean(),
    connections: z.optional(z.array(PlatformMCPConnection$inboundSchema)),
    default_project_id: z.optional(z.string()),
    marketplace_published: z.boolean(),
    ready: z.boolean(),
    reason_code: ReasonCode$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "default_project_id": "defaultProjectId",
      "marketplace_published": "marketplacePublished",
      "reason_code": "reasonCode",
    });
  }),
);

export function platformMCPLifecycleFromJSON(
  jsonString: string,
): SafeParseResult<PlatformMCPLifecycle, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformMCPLifecycle$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformMCPLifecycle' from JSON`,
  );
}
