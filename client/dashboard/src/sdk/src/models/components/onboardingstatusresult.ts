/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OnboardingStatusResult = {
  /**
   * Whether the organization has at least one linked directory sync in WorkOS.
   */
  dsyncConfigured: boolean;
  /**
   * Whether the organization has at least one active SSO connection in WorkOS.
   */
  ssoConfigured: boolean;
};

/** @internal */
export const OnboardingStatusResult$inboundSchema: z.ZodMiniType<
  OnboardingStatusResult,
  unknown
> = z.pipe(
  z.object({
    dsync_configured: z.boolean(),
    sso_configured: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "dsync_configured": "dsyncConfigured",
      "sso_configured": "ssoConfigured",
    });
  }),
);

export function onboardingStatusResultFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingStatusResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingStatusResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingStatusResult' from JSON`,
  );
}
