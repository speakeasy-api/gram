/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A named, saved question against a catalog dataset, kept with the builder state it was built with. Belongs to a project and is visible to everyone in the organization.
 */
export type ExploreQuery = {
  createdAt: Date;
  /**
   * Who saved it, when known
   */
  createdByUserId?: string | undefined;
  /**
   * The catalog dataset the query asks
   */
  dataset: string;
  id: string;
  /**
   * Present when the saved spec no longer compiles against the catalog, naming what is wrong, so a catalog change fails visibly rather than returning wrong numbers.
   */
  invalidReason?: string | undefined;
  /**
   * Display name. Not unique: two people saving the same name is normal.
   */
  name: string;
  organizationId: string;
  projectId: string;
  /**
   * Builder state: chart type, window, dimensions, measures, filters, grain, sort and limit. The client interprets it; the server validates the parts the catalog knows.
   */
  spec: { [k: string]: any };
  updatedAt: Date;
};

/** @internal */
export const ExploreQuery$inboundSchema: z.ZodMiniType<ExploreQuery, unknown> =
  z.pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      created_by_user_id: z.optional(z.string()),
      dataset: z.string(),
      id: z.string(),
      invalid_reason: z.optional(z.string()),
      name: z.string(),
      organization_id: z.string(),
      project_id: z.string(),
      spec: z.record(z.string(), z.any()),
      updated_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "created_by_user_id": "createdByUserId",
        "invalid_reason": "invalidReason",
        "organization_id": "organizationId",
        "project_id": "projectId",
        "updated_at": "updatedAt",
      });
    }),
  );

export function exploreQueryFromJSON(
  jsonString: string,
): SafeParseResult<ExploreQuery, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExploreQuery$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExploreQuery' from JSON`,
  );
}
