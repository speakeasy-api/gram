/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExploreCalculation,
  ExploreCalculation$inboundSchema,
} from "./explorecalculation.js";
import { ExploreFilter, ExploreFilter$inboundSchema } from "./explorefilter.js";
import {
  ExploreGroupExpression,
  ExploreGroupExpression$inboundSchema,
} from "./exploregroupexpression.js";

/**
 * How the dashboard renders the results
 */
export const ExploreSavedQueryChartType = {
  Line: "line",
  Bar: "bar",
  Area: "area",
  Table: "table",
  Number: "number",
} as const;
/**
 * How the dashboard renders the results
 */
export type ExploreSavedQueryChartType = ClosedEnum<
  typeof ExploreSavedQueryChartType
>;

/**
 * Semantic dataset to query
 */
export const ExploreSavedQueryDataset = {
  Events: "events",
  TurnUsage: "turn_usage",
  UserUsage: "user_usage",
} as const;
/**
 * Semantic dataset to query
 */
export type ExploreSavedQueryDataset = ClosedEnum<
  typeof ExploreSavedQueryDataset
>;

/**
 * Relative time window the query runs over
 */
export const ExploreSavedQueryWindow = {
  Oneh: "1h",
  TwentyFourh: "24h",
  Sevend: "7d",
  Thirtyd: "30d",
  Ninetyd: "90d",
} as const;
/**
 * Relative time window the query runs over
 */
export type ExploreSavedQueryWindow = ClosedEnum<
  typeof ExploreSavedQueryWindow
>;

/**
 * A saved dataset-and-calculations query with a chart type and relative window.
 */
export type ExploreSavedQuery = {
  /**
   * Aggregate op(column) calculations to compute over the dataset
   */
  calculations: Array<ExploreCalculation>;
  /**
   * How the dashboard renders the results
   */
  chartType: ExploreSavedQueryChartType;
  /**
   * Creation time in ISO 8601 format
   */
  createdAt: Date;
  /**
   * Semantic dataset to query
   */
  dataset: ExploreSavedQueryDataset;
  /**
   * Filter predicates on canonical fields. All are ANDed together.
   */
  filters: Array<ExploreFilter>;
  /**
   * Timeseries bucket width in seconds. Zero or omitted aggregates the whole range into a table.
   */
  granularitySeconds: number;
  /**
   * Dimensions to break results down by, in order. Empty returns a single aggregate group.
   */
  groupBy: Array<string>;
  /**
   * Named conditional grouping axes evaluated independently on canonical rows. Each produces true and false groups without filtering rows.
   */
  groupExpressions: Array<ExploreGroupExpression>;
  /**
   * Saved query ID
   */
  id: string;
  /**
   * Keep at most this many table rows after ranking. Zero keeps everything.
   */
  limit: number;
  /**
   * Display name of the saved query
   */
  name: string;
  /**
   * Calculation used to rank table rows, referenced by canonical name such as SUM(input_tokens). Must be requested by the query and is ignored for timeseries.
   */
  sortBy: string;
  /**
   * Sort descending when sort_by is set.
   */
  sortDesc: boolean;
  /**
   * Last update time in ISO 8601 format
   */
  updatedAt: Date;
  /**
   * Relative time window the query runs over
   */
  window: ExploreSavedQueryWindow;
};

/** @internal */
export const ExploreSavedQueryChartType$inboundSchema: z.ZodMiniEnum<
  typeof ExploreSavedQueryChartType
> = z.enum(ExploreSavedQueryChartType);

/** @internal */
export const ExploreSavedQueryDataset$inboundSchema: z.ZodMiniEnum<
  typeof ExploreSavedQueryDataset
> = z.enum(ExploreSavedQueryDataset);

/** @internal */
export const ExploreSavedQueryWindow$inboundSchema: z.ZodMiniEnum<
  typeof ExploreSavedQueryWindow
> = z.enum(ExploreSavedQueryWindow);

/** @internal */
export const ExploreSavedQuery$inboundSchema: z.ZodMiniType<
  ExploreSavedQuery,
  unknown
> = z.pipe(
  z.object({
    calculations: z.array(ExploreCalculation$inboundSchema),
    chart_type: ExploreSavedQueryChartType$inboundSchema,
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    dataset: ExploreSavedQueryDataset$inboundSchema,
    filters: z.array(ExploreFilter$inboundSchema),
    granularity_seconds: z.int(),
    group_by: z.array(z.string()),
    group_expressions: z.array(ExploreGroupExpression$inboundSchema),
    id: z.string(),
    limit: z._default(z.int(), 0),
    name: z.string(),
    sort_by: z.string(),
    sort_desc: z._default(z.boolean(), true),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    window: ExploreSavedQueryWindow$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "chart_type": "chartType",
      "created_at": "createdAt",
      "granularity_seconds": "granularitySeconds",
      "group_by": "groupBy",
      "group_expressions": "groupExpressions",
      "sort_by": "sortBy",
      "sort_desc": "sortDesc",
      "updated_at": "updatedAt",
    });
  }),
);

export function exploreSavedQueryFromJSON(
  jsonString: string,
): SafeParseResult<ExploreSavedQuery, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExploreSavedQuery$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExploreSavedQuery' from JSON`,
  );
}
