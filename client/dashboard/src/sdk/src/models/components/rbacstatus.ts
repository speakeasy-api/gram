/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RBACStatus = {
  /**
   * Whether RBAC enforcement is currently enabled for this organization.
   */
  rbacEnabled: boolean;
};

/** @internal */
export const RBACStatus$inboundSchema: z.ZodMiniType<RBACStatus, unknown> = z
  .pipe(
    z.object({
      rbac_enabled: z.boolean(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "rbac_enabled": "rbacEnabled",
      });
    }),
  );

export function rbacStatusFromJSON(
  jsonString: string,
): SafeParseResult<RBACStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RBACStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RBACStatus' from JSON`,
  );
}
