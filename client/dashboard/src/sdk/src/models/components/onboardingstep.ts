/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Dashboard area where the work happens
 */
export const OnboardingStepDestination = {
  Plugins: "plugins",
  Integrations: "integrations",
  Policies: "policies",
  Mcp: "mcp",
  Devices: "devices",
  Settings: "settings",
} as const;
/**
 * Dashboard area where the work happens
 */
export type OnboardingStepDestination = ClosedEnum<
  typeof OnboardingStepDestination
>;

/**
 * One recommended action, verified against real evidence.
 */
export type OnboardingStep = {
  /**
   * What to do and why it is the quickest path
   */
  description: string;
  /**
   * Dashboard area where the work happens
   */
  destination: OnboardingStepDestination;
  /**
   * What must be observed in the last 30 days for the step to count as verified
   */
  evidence: string;
  /**
   * Product this step covers. Omitted for steps that cover the whole organization.
   */
  productSlug?: string | undefined;
  /**
   * Stable step key, unique within an organization's plan
   */
  slug: string;
  /**
   * Coverage technique this step sets up
   */
  techniqueSlug: string;
  /**
   * Short imperative title
   */
  title: string;
  /**
   * When the step's evidence was last confirmed
   */
  verifiedAt?: Date | undefined;
};

/** @internal */
export const OnboardingStepDestination$inboundSchema: z.ZodMiniEnum<
  typeof OnboardingStepDestination
> = z.enum(OnboardingStepDestination);

/** @internal */
export const OnboardingStep$inboundSchema: z.ZodMiniType<
  OnboardingStep,
  unknown
> = z.pipe(
  z.object({
    description: z.string(),
    destination: OnboardingStepDestination$inboundSchema,
    evidence: z.string(),
    product_slug: z.optional(z.string()),
    slug: z.string(),
    technique_slug: z.string(),
    title: z.string(),
    verified_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "product_slug": "productSlug",
      "technique_slug": "techniqueSlug",
      "verified_at": "verifiedAt",
    });
  }),
);

export function onboardingStepFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingStep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingStep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingStep' from JSON`,
  );
}
