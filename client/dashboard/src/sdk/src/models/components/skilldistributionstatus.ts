/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Sync receipt rollups for a skill's active distribution.
 */
export type SkillDistributionStatus = {
  /**
   * Receipts that could not apply because the filesystem is read-only.
   */
  degraded: number;
  /**
   * Applied receipts materialized at the target version.
   */
  live: number;
  /**
   * The version targeted by the active distribution.
   */
  resolvedVersionId: string;
  /**
   * Receipts skipped because another installation won a conflict.
   */
  shadowed: number;
  /**
   * The distributed skill ID.
   */
  skillId: string;
  /**
   * Applied receipts materialized at another or unknown version.
   */
  stale: number;
};

/** @internal */
export const SkillDistributionStatus$inboundSchema: z.ZodMiniType<
  SkillDistributionStatus,
  unknown
> = z.pipe(
  z.object({
    degraded: z.int(),
    live: z.int(),
    resolved_version_id: z.string(),
    shadowed: z.int(),
    skill_id: z.string(),
    stale: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "resolved_version_id": "resolvedVersionId",
      "skill_id": "skillId",
    });
  }),
);

export function skillDistributionStatusFromJSON(
  jsonString: string,
): SafeParseResult<SkillDistributionStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SkillDistributionStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SkillDistributionStatus' from JSON`,
  );
}
