/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type IdentityProviderClaim = {
  carriesAccess: boolean;
  name: string;
  purpose: string;
};

/** @internal */
export const IdentityProviderClaim$inboundSchema: z.ZodMiniType<
  IdentityProviderClaim,
  unknown
> = z.pipe(
  z.object({
    carries_access: z.boolean(),
    name: z.string(),
    purpose: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "carries_access": "carriesAccess",
    });
  }),
);

export function identityProviderClaimFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderClaim, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderClaim$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderClaim' from JSON`,
  );
}
