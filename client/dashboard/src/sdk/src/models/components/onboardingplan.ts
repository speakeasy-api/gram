/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A plan a provider sells. It applies to every product of that provider.
 */
export type OnboardingPlan = {
  /**
   * Display name
   */
  name: string;
  /**
   * Stable plan key, e.g. anthropic-enterprise
   */
  slug: string;
};

/** @internal */
export const OnboardingPlan$inboundSchema: z.ZodMiniType<
  OnboardingPlan,
  unknown
> = z.object({
  name: z.string(),
  slug: z.string(),
});

export function onboardingPlanFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingPlan, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingPlan$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingPlan' from JSON`,
  );
}
