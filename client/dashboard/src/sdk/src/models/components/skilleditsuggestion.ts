/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The suggestion state.
 */
export const SkillEditSuggestionStatus = {
  Open: "open",
  Approved: "approved",
  Dismissed: "dismissed",
  Superseded: "superseded",
} as const;
/**
 * The suggestion state.
 */
export type SkillEditSuggestionStatus = ClosedEnum<
  typeof SkillEditSuggestionStatus
>;

/**
 * A proposed edit to an existing project skill.
 */
export type SkillEditSuggestion = {
  /**
   * When the suggestion was approved, when present.
   */
  approvedAt?: Date | undefined;
  /**
   * The user that approved the suggestion, when present.
   */
  approvedByUserId?: string | undefined;
  /**
   * The version the suggestion was generated from.
   */
  baseVersionId: string;
  /**
   * When the suggestion was created.
   */
  createdAt: Date;
  /**
   * Feedback records considered by the suggestion.
   */
  feedbackCount: number;
  /**
   * The suggestion ID.
   */
  id: string;
  /**
   * The complete proposed SKILL.md content.
   */
  proposedContent: string;
  /**
   * Why the edit was proposed.
   */
  rationale: string;
  /**
   * The version created by approval, when present.
   */
  resultingVersionId?: string | undefined;
  /**
   * Scored sessions considered by the suggestion.
   */
  scoredSessionCount: number;
  /**
   * The user-facing skill name.
   */
  skillDisplayName: string;
  /**
   * The skill targeted by the suggestion.
   */
  skillId: string;
  /**
   * The canonical skill name.
   */
  skillName: string;
  /**
   * The suggestion state.
   */
  status: SkillEditSuggestionStatus;
  /**
   * When the suggestion was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export const SkillEditSuggestionStatus$inboundSchema: z.ZodMiniEnum<
  typeof SkillEditSuggestionStatus
> = z.enum(SkillEditSuggestionStatus);

/** @internal */
export const SkillEditSuggestion$inboundSchema: z.ZodMiniType<
  SkillEditSuggestion,
  unknown
> = z.pipe(
  z.object({
    approved_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    approved_by_user_id: z.optional(z.string()),
    base_version_id: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    feedback_count: z.int(),
    id: z.string(),
    proposed_content: z.string(),
    rationale: z.string(),
    resulting_version_id: z.optional(z.string()),
    scored_session_count: z.int(),
    skill_display_name: z.string(),
    skill_id: z.string(),
    skill_name: z.string(),
    status: SkillEditSuggestionStatus$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "approved_at": "approvedAt",
      "approved_by_user_id": "approvedByUserId",
      "base_version_id": "baseVersionId",
      "created_at": "createdAt",
      "feedback_count": "feedbackCount",
      "proposed_content": "proposedContent",
      "resulting_version_id": "resultingVersionId",
      "scored_session_count": "scoredSessionCount",
      "skill_display_name": "skillDisplayName",
      "skill_id": "skillId",
      "skill_name": "skillName",
      "updated_at": "updatedAt",
    });
  }),
);

export function skillEditSuggestionFromJSON(
  jsonString: string,
): SafeParseResult<SkillEditSuggestion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SkillEditSuggestion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SkillEditSuggestion' from JSON`,
  );
}
