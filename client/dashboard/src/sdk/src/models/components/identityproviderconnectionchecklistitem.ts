/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One step the organization administrator completes in the identity provider's console. Ordered; keys are stable across responses.
 */
export type IdentityProviderConnectionChecklistItem = {
  /**
   * What to do in the console, including any value copied from this connection.
   */
  description: string;
  /**
   * Stable step identifier.
   */
  key: string;
  /**
   * Short step title.
   */
  title: string;
};

/** @internal */
export const IdentityProviderConnectionChecklistItem$inboundSchema:
  z.ZodMiniType<IdentityProviderConnectionChecklistItem, unknown> = z.object({
    description: z.string(),
    key: z.string(),
    title: z.string(),
  });

export function identityProviderConnectionChecklistItemFromJSON(
  jsonString: string,
): SafeParseResult<
  IdentityProviderConnectionChecklistItem,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      IdentityProviderConnectionChecklistItem$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'IdentityProviderConnectionChecklistItem' from JSON`,
  );
}
