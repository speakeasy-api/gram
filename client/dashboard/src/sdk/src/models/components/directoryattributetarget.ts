/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DirectoryAttributeTarget = {
  /**
   * Identity-provider attribute key, such as department or job_title.
   */
  key: string;
  /**
   * Number of active directory members with this attribute value.
   */
  memberCount: number;
  /**
   * Canonical principal URN for this attribute value.
   */
  principalUrn: string;
  /**
   * Identity-provider attribute value.
   */
  value: string;
};

/** @internal */
export const DirectoryAttributeTarget$inboundSchema: z.ZodMiniType<
  DirectoryAttributeTarget,
  unknown
> = z.pipe(
  z.object({
    key: z.string(),
    member_count: z.int(),
    principal_urn: z.string(),
    value: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "member_count": "memberCount",
      "principal_urn": "principalUrn",
    });
  }),
);

export function directoryAttributeTargetFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryAttributeTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryAttributeTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryAttributeTarget' from JSON`,
  );
}
