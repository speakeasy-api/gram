/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Operator the dimension admits, per describe
 */
export const Operator = {
  Equals: "equals",
  In: "in",
} as const;
/**
 * Operator the dimension admits, per describe
 */
export type Operator = ClosedEnum<typeof Operator>;

/**
 * A filter on a dimension. All filters are ANDed.
 */
export type AnalyticsFilter = {
  /**
   * Dimension to filter on
   */
  field: string;
  /**
   * Operator the dimension admits, per describe
   */
  operator: Operator;
  /**
   * equals takes exactly one value; in matches any of them.
   */
  values: Array<string>;
};

/** @internal */
export const Operator$outboundSchema: z.ZodMiniEnum<typeof Operator> = z.enum(
  Operator,
);

/** @internal */
export type AnalyticsFilter$Outbound = {
  field: string;
  operator: string;
  values: Array<string>;
};

/** @internal */
export const AnalyticsFilter$outboundSchema: z.ZodMiniType<
  AnalyticsFilter$Outbound,
  AnalyticsFilter
> = z.object({
  field: z.string(),
  operator: Operator$outboundSchema,
  values: z.array(z.string()),
});

export function analyticsFilterToJSON(
  analyticsFilter: AnalyticsFilter,
): string {
  return JSON.stringify(AnalyticsFilter$outboundSchema.parse(analyticsFilter));
}
