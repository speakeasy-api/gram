/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { ExploreRow, ExploreRow$inboundSchema } from "./explorerow.js";

/**
 * Semantic dataset that answered the query
 */
export const ExploreQueryResultDataset = {
  Events: "events",
  TurnUsage: "turn_usage",
  UserUsage: "user_usage",
} as const;
/**
 * Semantic dataset that answered the query
 */
export type ExploreQueryResultDataset = ClosedEnum<
  typeof ExploreQueryResultDataset
>;

/**
 * Result of an Explore query.
 */
export type ExploreQueryResult = {
  /**
   * Canonical names of the calculations answered, in request order
   */
  calculations: Array<string>;
  /**
   * Semantic dataset that answered the query
   */
  dataset: ExploreQueryResultDataset;
  /**
   * Timeseries bucket width. Zero for table queries.
   */
  granularitySeconds: number;
  /**
   * Group-by dimensions answered, in request order
   */
  groupBy: Array<string>;
  /**
   * Rows ordered by bucket and group for timeseries, or by sort_by for tables
   */
  rows: Array<ExploreRow>;
};

/** @internal */
export const ExploreQueryResultDataset$inboundSchema: z.ZodMiniEnum<
  typeof ExploreQueryResultDataset
> = z.enum(ExploreQueryResultDataset);

/** @internal */
export const ExploreQueryResult$inboundSchema: z.ZodMiniType<
  ExploreQueryResult,
  unknown
> = z.pipe(
  z.object({
    calculations: z.array(z.string()),
    dataset: ExploreQueryResultDataset$inboundSchema,
    granularity_seconds: z.int(),
    group_by: z.array(z.string()),
    rows: z.array(ExploreRow$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "granularity_seconds": "granularitySeconds",
      "group_by": "groupBy",
    });
  }),
);

export function exploreQueryResultFromJSON(
  jsonString: string,
): SafeParseResult<ExploreQueryResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExploreQueryResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExploreQueryResult' from JSON`,
  );
}
