/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A product surface the platform can cover.
 */
export type OnboardingProduct = {
  /**
   * Display name
   */
  name: string;
  /**
   * Stable product key
   */
  slug: string;
  /**
   * hook_source ids whose events belong to this product
   */
  sourceIds: Array<string>;
};

/** @internal */
export const OnboardingProduct$inboundSchema: z.ZodMiniType<
  OnboardingProduct,
  unknown
> = z.pipe(
  z.object({
    name: z.string(),
    slug: z.string(),
    source_ids: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "source_ids": "sourceIds",
    });
  }),
);

export function onboardingProductFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingProduct' from JSON`,
  );
}
