/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingPlan,
  OnboardingPlan$inboundSchema,
} from "./onboardingplan.js";

/**
 * A product surface the platform can cover.
 */
export type OnboardingProduct = {
  /**
   * Display name
   */
  name: string;
  /**
   * Plans an admin can declare for this product. Empty when the product has no plan.
   */
  plans: Array<OnboardingPlan>;
  /**
   * Stable product key
   */
  slug: string;
  /**
   * hook_source ids whose events belong to this product
   */
  sourceIds: Array<string>;
  /**
   * Vendor that sells the product
   */
  vendor: string;
};

/** @internal */
export const OnboardingProduct$inboundSchema: z.ZodMiniType<
  OnboardingProduct,
  unknown
> = z.pipe(
  z.object({
    name: z.string(),
    plans: z.array(OnboardingPlan$inboundSchema),
    slug: z.string(),
    source_ids: z.array(z.string()),
    vendor: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "source_ids": "sourceIds",
    });
  }),
);

export function onboardingProductFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingProduct' from JSON`,
  );
}
