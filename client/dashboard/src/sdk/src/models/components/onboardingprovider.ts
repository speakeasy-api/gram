/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingPlan,
  OnboardingPlan$inboundSchema,
} from "./onboardingplan.js";
import {
  OnboardingProduct,
  OnboardingProduct$inboundSchema,
} from "./onboardingproduct.js";

/**
 * A vendor whose products the platform can cover, with the plans it sells and the products it makes.
 */
export type OnboardingProvider = {
  /**
   * Display name
   */
  name: string;
  /**
   * Plans an admin can declare for this provider. Empty when the provider has no plans.
   */
  plans: Array<OnboardingPlan>;
  /**
   * Products of this provider, in display order
   */
  products: Array<OnboardingProduct>;
  /**
   * Stable provider key
   */
  slug: string;
};

/** @internal */
export const OnboardingProvider$inboundSchema: z.ZodMiniType<
  OnboardingProvider,
  unknown
> = z.object({
  name: z.string(),
  plans: z.array(OnboardingPlan$inboundSchema),
  products: z.array(OnboardingProduct$inboundSchema),
  slug: z.string(),
});

export function onboardingProviderFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingProvider, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingProvider$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingProvider' from JSON`,
  );
}
