/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  InferenceSpendMonthKey,
  InferenceSpendMonthKey$inboundSchema,
} from "./inferencespendmonthkey.js";

export type InferenceSpendMonth = {
  /**
   * Whether this is the current UTC calendar month
   */
  current: boolean;
  /**
   * Spend by platform-managed key type. Keys with no recorded days in the month are omitted.
   */
  keySpend: Array<InferenceSpendMonthKey>;
  /**
   * End of the UTC calendar month (exclusive)
   */
  monthEnd: Date;
  /**
   * Start of the UTC calendar month
   */
  monthStart: Date;
  /**
   * Most recent completed durable UTC spend day included in this month
   */
  recordedThrough?: RFCDate | undefined;
  /**
   * Exact durable inference spend in USD through recorded_through
   */
  spendUsd: string;
};

/** @internal */
export const InferenceSpendMonth$inboundSchema: z.ZodMiniType<
  InferenceSpendMonth,
  unknown
> = z.pipe(
  z.object({
    current: z.boolean(),
    key_spend: z.array(InferenceSpendMonthKey$inboundSchema),
    month_end: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    month_start: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    recorded_through: z.optional(
      z.pipe(z.string(), z.transform(v => new RFCDate(v))),
    ),
    spend_usd: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "key_spend": "keySpend",
      "month_end": "monthEnd",
      "month_start": "monthStart",
      "recorded_through": "recordedThrough",
      "spend_usd": "spendUsd",
    });
  }),
);

export function inferenceSpendMonthFromJSON(
  jsonString: string,
): SafeParseResult<InferenceSpendMonth, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InferenceSpendMonth$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InferenceSpendMonth' from JSON`,
  );
}
