/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A fixed choice offered by the wizard.
 */
export type OnboardingOption = {
  /**
   * Display name
   */
  name: string;
  /**
   * Stable key
   */
  slug: string;
};

/** @internal */
export const OnboardingOption$inboundSchema: z.ZodMiniType<
  OnboardingOption,
  unknown
> = z.object({
  name: z.string(),
  slug: z.string(),
});

export function onboardingOptionFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingOption, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingOption$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingOption' from JSON`,
  );
}
