/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Authorization status, independent of directory sync.
 */
export const SlackDirectoryConnectionStatus = {
  Connected: "connected",
  Disconnected: "disconnected",
  ReconnectRequired: "reconnect_required",
} as const;
/**
 * Authorization status, independent of directory sync.
 */
export type SlackDirectoryConnectionStatus = ClosedEnum<
  typeof SlackDirectoryConnectionStatus
>;

/**
 * An organization Slack workspace authorization. Authorization does not indicate directory sync or invocation protection.
 */
export type SlackDirectoryConnection = {
  /**
   * When credentials were removed.
   */
  disconnectedAt?: Date | undefined;
  /**
   * Connection generation used to reject stale changes.
   */
  generation: string;
  /**
   * Permissions granted by Slack.
   */
  grantedScopes: Array<string>;
  /**
   * Durable organization workspace connection ID.
   */
  id: string;
  /**
   * Bounded authorization failure code, when known.
   */
  lastErrorCode?: string | undefined;
  /**
   * Authorization status, independent of directory sync.
   */
  status: SlackDirectoryConnectionStatus;
  /**
   * Last connection change timestamp.
   */
  updatedAt: Date;
  /**
   * Verified Slack workspace ID.
   */
  workspaceId: string;
  /**
   * Workspace display name from Slack.
   */
  workspaceName: string;
};

/** @internal */
export const SlackDirectoryConnectionStatus$inboundSchema: z.ZodMiniEnum<
  typeof SlackDirectoryConnectionStatus
> = z.enum(SlackDirectoryConnectionStatus);

/** @internal */
export const SlackDirectoryConnection$inboundSchema: z.ZodMiniType<
  SlackDirectoryConnection,
  unknown
> = z.pipe(
  z.object({
    disconnected_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    generation: z.string(),
    granted_scopes: z.array(z.string()),
    id: z.string(),
    last_error_code: z.optional(z.string()),
    status: SlackDirectoryConnectionStatus$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    workspace_id: z.string(),
    workspace_name: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "disconnected_at": "disconnectedAt",
      "granted_scopes": "grantedScopes",
      "last_error_code": "lastErrorCode",
      "updated_at": "updatedAt",
      "workspace_id": "workspaceId",
      "workspace_name": "workspaceName",
    });
  }),
);

export function slackDirectoryConnectionFromJSON(
  jsonString: string,
): SafeParseResult<SlackDirectoryConnection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SlackDirectoryConnection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SlackDirectoryConnection' from JSON`,
  );
}
