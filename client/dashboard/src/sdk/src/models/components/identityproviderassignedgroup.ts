/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * An identity provider group assigned directly to an application.
 */
export type IdentityProviderAssignedGroup = {
  /**
   * Group display name.
   */
  name: string;
  /**
   * Provider-assigned group identifier.
   */
  sourceGroupId: string;
};

/** @internal */
export const IdentityProviderAssignedGroup$inboundSchema: z.ZodMiniType<
  IdentityProviderAssignedGroup,
  unknown
> = z.pipe(
  z.object({
    name: z.string(),
    source_group_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "source_group_id": "sourceGroupId",
    });
  }),
);

export function identityProviderAssignedGroupFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderAssignedGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderAssignedGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderAssignedGroup' from JSON`,
  );
}
