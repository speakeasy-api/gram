/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * How access was granted: rbac (direct grant or role), plugin (plugin assignment), or both.
 */
export const AccessSource = {
  Rbac: "rbac",
  Plugin: "plugin",
  Both: "both",
} as const;
/**
 * How access was granted: rbac (direct grant or role), plugin (plugin assignment), or both.
 */
export type AccessSource = ClosedEnum<typeof AccessSource>;

/**
 * An MCP server accessible to an identity.
 */
export type AccessibleMCPServer = {
  /**
   * How access was granted: rbac (direct grant or role), plugin (plugin assignment), or both.
   */
  accessSource: AccessSource;
  /**
   * Unique server identifier.
   */
  id: string;
  /**
   * Display name of the server.
   */
  name: string;
  /**
   * Name of the plugin that grants access, when access_source is plugin or both.
   */
  pluginName?: string | undefined;
  /**
   * Project the server belongs to.
   */
  projectId: string;
  /**
   * Slug of the project the server belongs to.
   */
  projectSlug: string;
  /**
   * URL-safe server slug.
   */
  slug: string;
};

/** @internal */
export const AccessSource$inboundSchema: z.ZodMiniEnum<typeof AccessSource> = z
  .enum(AccessSource);

/** @internal */
export const AccessibleMCPServer$inboundSchema: z.ZodMiniType<
  AccessibleMCPServer,
  unknown
> = z.pipe(
  z.object({
    access_source: AccessSource$inboundSchema,
    id: z.string(),
    name: z.string(),
    plugin_name: z.optional(z.string()),
    project_id: z.string(),
    project_slug: z.string(),
    slug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_source": "accessSource",
      "plugin_name": "pluginName",
      "project_id": "projectId",
      "project_slug": "projectSlug",
    });
  }),
);

export function accessibleMCPServerFromJSON(
  jsonString: string,
): SafeParseResult<AccessibleMCPServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AccessibleMCPServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AccessibleMCPServer' from JSON`,
  );
}
