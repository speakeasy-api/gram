/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Authoritative impact summary for deleting a remote_session_issuer: how many clients reference it and the names of the MCP servers those clients are attached to.
 */
export type OrganizationIssuerDeletePreflight = {
  /**
   * Number of non-deleted remote_session_clients registered with this issuer.
   */
  clientCount: number;
  /**
   * Display names of MCP servers attached to this issuer's clients.
   */
  mcpServerNames: Array<string>;
};

/** @internal */
export const OrganizationIssuerDeletePreflight$inboundSchema: z.ZodMiniType<
  OrganizationIssuerDeletePreflight,
  unknown
> = z.pipe(
  z.object({
    client_count: z.int(),
    mcp_server_names: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "client_count": "clientCount",
      "mcp_server_names": "mcpServerNames",
    });
  }),
);

export function organizationIssuerDeletePreflightFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationIssuerDeletePreflight, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationIssuerDeletePreflight$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationIssuerDeletePreflight' from JSON`,
  );
}
