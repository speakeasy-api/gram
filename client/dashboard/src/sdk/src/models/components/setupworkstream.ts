/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Canonical onboarding workstream. Task keys define task display order; the containing workstreams array defines workstream display order.
 */
export type SetupWorkstream = {
  /**
   * Stable workstream identifier used for assignment.
   */
  id: string;
  /**
   * Ordered task keys available in this response. Hidden tasks are included only in authorized responses.
   */
  taskKeys: Array<string>;
  /**
   * Workstream display title.
   */
  title: string;
};

/** @internal */
export const SetupWorkstream$inboundSchema: z.ZodMiniType<
  SetupWorkstream,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    task_keys: z.array(z.string()),
    title: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "task_keys": "taskKeys",
    });
  }),
);

export function setupWorkstreamFromJSON(
  jsonString: string,
): SafeParseResult<SetupWorkstream, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SetupWorkstream$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SetupWorkstream' from JSON`,
  );
}
