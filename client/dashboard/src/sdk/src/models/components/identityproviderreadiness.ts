/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  IdentityProviderReadinessCheck,
  IdentityProviderReadinessCheck$inboundSchema,
} from "./identityproviderreadinesscheck.js";

/**
 * Identity provider supported by the guided setup.
 */
export const IdentityProviderReadinessProvider = {
  Okta: "okta",
} as const;
/**
 * Identity provider supported by the guided setup.
 */
export type IdentityProviderReadinessProvider = ClosedEnum<
  typeof IdentityProviderReadinessProvider
>;

export type IdentityProviderReadiness = {
  checkedAt: Date;
  /**
   * Independent readiness checks in evaluation order.
   */
  checks: Array<IdentityProviderReadinessCheck>;
  /**
   * Whether guided setup is available.
   */
  eligible: boolean;
  /**
   * Identity provider supported by the guided setup.
   */
  provider: IdentityProviderReadinessProvider;
};

/** @internal */
export const IdentityProviderReadinessProvider$inboundSchema: z.ZodMiniEnum<
  typeof IdentityProviderReadinessProvider
> = z.enum(IdentityProviderReadinessProvider);

/** @internal */
export const IdentityProviderReadiness$inboundSchema: z.ZodMiniType<
  IdentityProviderReadiness,
  unknown
> = z.pipe(
  z.object({
    checked_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    checks: z.array(IdentityProviderReadinessCheck$inboundSchema),
    eligible: z.boolean(),
    provider: IdentityProviderReadinessProvider$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "checked_at": "checkedAt",
    });
  }),
);

export function identityProviderReadinessFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderReadiness, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderReadiness$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderReadiness' from JSON`,
  );
}
