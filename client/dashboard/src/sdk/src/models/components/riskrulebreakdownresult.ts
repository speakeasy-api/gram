/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RiskRuleBreakdownEntry,
  RiskRuleBreakdownEntry$inboundSchema,
} from "./riskrulebreakdownentry.js";

export type RiskRuleBreakdownResult = {
  /**
   * Category the breakdown is scoped to.
   */
  category: string;
  /**
   * Inclusive start of the window used.
   */
  from: Date;
  /**
   * Rules in this category, ordered by finding count descending.
   */
  rules: Array<RiskRuleBreakdownEntry>;
  /**
   * Exclusive end of the window used.
   */
  to: Date;
  /**
   * Total findings across all rules in this category and window.
   */
  total: number;
};

/** @internal */
export const RiskRuleBreakdownResult$inboundSchema: z.ZodMiniType<
  RiskRuleBreakdownResult,
  unknown
> = z.object({
  category: z.string(),
  from: z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
  rules: z.array(RiskRuleBreakdownEntry$inboundSchema),
  to: z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
  total: z.int(),
});

export function riskRuleBreakdownResultFromJSON(
  jsonString: string,
): SafeParseResult<RiskRuleBreakdownResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskRuleBreakdownResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskRuleBreakdownResult' from JSON`,
  );
}
