/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One recorded change to the catalog. The highest revision is the list_version agents echo on scan receipts.
 */
export type AiScanCatalogRevision = {
  /**
   * What changed: seed, upsert, enable, disable, or delete.
   */
  action: string;
  actorEmail?: string | undefined;
  actorUserId?: string | undefined;
  createdAt: Date;
  reason?: string | undefined;
  revision: number;
  /**
   * Target state after the change, absent on deletion.
   */
  targetAfter?: any | undefined;
  /**
   * Target state before the change, absent on creation.
   */
  targetBefore?: any | undefined;
  targetId: string;
};

/** @internal */
export const AiScanCatalogRevision$inboundSchema: z.ZodMiniType<
  AiScanCatalogRevision,
  unknown
> = z.pipe(
  z.object({
    action: z.string(),
    actor_email: z.optional(z.string()),
    actor_user_id: z.optional(z.string()),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    reason: z.optional(z.string()),
    revision: z.int(),
    target_after: z.optional(z.any()),
    target_before: z.optional(z.any()),
    target_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "actor_email": "actorEmail",
      "actor_user_id": "actorUserId",
      "created_at": "createdAt",
      "target_after": "targetAfter",
      "target_before": "targetBefore",
      "target_id": "targetId",
    });
  }),
);

export function aiScanCatalogRevisionFromJSON(
  jsonString: string,
): SafeParseResult<AiScanCatalogRevision, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AiScanCatalogRevision$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AiScanCatalogRevision' from JSON`,
  );
}
