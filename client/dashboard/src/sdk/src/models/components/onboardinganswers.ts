/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingSelectedProvider,
  OnboardingSelectedProvider$inboundSchema,
} from "./onboardingselectedprovider.js";

/**
 * MDM vendor the organization uses
 */
export const MdmVendor = {
  Jamf: "jamf",
  Intune: "intune",
  Iru: "iru",
  None: "none",
} as const;
/**
 * MDM vendor the organization uses
 */
export type MdmVendor = ClosedEnum<typeof MdmVendor>;

/**
 * The single use case the admin picked. Omitted until the admin picks one.
 */
export const UseCase = {
  Observability: "observability",
  CostTracking: "cost-tracking",
  Security: "security",
  McpGateway: "mcp-gateway",
} as const;
/**
 * The single use case the admin picked. Omitted until the admin picks one.
 */
export type UseCase = ClosedEnum<typeof UseCase>;

/**
 * What an organization admin told us during onboarding.
 */
export type OnboardingAnswers = {
  /**
   * Set once every step for the use case has been verified
   */
  completedAt?: Date | undefined;
  /**
   * MDM vendor the organization uses
   */
  mdmVendor: MdmVendor;
  /**
   * Products the organization uses. Each belongs to one of the selected providers.
   */
  productSlugs: Array<string>;
  /**
   * Providers the organization uses, each with the plan it is on
   */
  providers: Array<OnboardingSelectedProvider>;
  updatedAt: Date;
  /**
   * The single use case the admin picked. Omitted until the admin picks one.
   */
  useCase?: UseCase | undefined;
};

/** @internal */
export const MdmVendor$inboundSchema: z.ZodMiniEnum<typeof MdmVendor> = z.enum(
  MdmVendor,
);

/** @internal */
export const UseCase$inboundSchema: z.ZodMiniEnum<typeof UseCase> = z.enum(
  UseCase,
);

/** @internal */
export const OnboardingAnswers$inboundSchema: z.ZodMiniType<
  OnboardingAnswers,
  unknown
> = z.pipe(
  z.object({
    completed_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    mdm_vendor: MdmVendor$inboundSchema,
    product_slugs: z.array(z.string()),
    providers: z.array(OnboardingSelectedProvider$inboundSchema),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    use_case: z.optional(UseCase$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "completed_at": "completedAt",
      "mdm_vendor": "mdmVendor",
      "product_slugs": "productSlugs",
      "updated_at": "updatedAt",
      "use_case": "useCase",
    });
  }),
);

export function onboardingAnswersFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingAnswers, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingAnswers$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingAnswers' from JSON`,
  );
}
