/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  ToolActivityCall,
  ToolActivityCall$Outbound,
  ToolActivityCall$outboundSchema,
} from "./toolactivitycall.js";

export type SummarizeToolActivityRequestBody = {
  /**
   * True while the tools are still running (produces a present-tense label); false once they have completed (past tense).
   */
  inProgress?: boolean | undefined;
  /**
   * The most recent tool calls in the current turn, in order (the service summarizes at most 20).
   */
  toolCalls: Array<ToolActivityCall>;
  /**
   * The user prompt that initiated the turn, used to ground the summary in the user's intent. Only the first 2000 characters inform the summary.
   */
  userMessage?: string | undefined;
};

/** @internal */
export type SummarizeToolActivityRequestBody$Outbound = {
  in_progress: boolean;
  tool_calls: Array<ToolActivityCall$Outbound>;
  user_message?: string | undefined;
};

/** @internal */
export const SummarizeToolActivityRequestBody$outboundSchema: z.ZodMiniType<
  SummarizeToolActivityRequestBody$Outbound,
  SummarizeToolActivityRequestBody
> = z.pipe(
  z.object({
    inProgress: z._default(z.boolean(), false),
    toolCalls: z.array(ToolActivityCall$outboundSchema),
    userMessage: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      inProgress: "in_progress",
      toolCalls: "tool_calls",
      userMessage: "user_message",
    });
  }),
);

export function summarizeToolActivityRequestBodyToJSON(
  summarizeToolActivityRequestBody: SummarizeToolActivityRequestBody,
): string {
  return JSON.stringify(
    SummarizeToolActivityRequestBody$outboundSchema.parse(
      summarizeToolActivityRequestBody,
    ),
  );
}
