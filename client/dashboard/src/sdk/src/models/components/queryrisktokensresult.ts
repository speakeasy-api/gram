/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RiskTokensPoint,
  RiskTokensPoint$inboundSchema,
} from "./risktokenspoint.js";

/**
 * Result of the token-by-risk breakdown query
 */
export type QueryRiskTokensResult = {
  /**
   * Timeseries bucket width in seconds. Always 86400 — the source aggregate is bucketed daily.
   */
  intervalSeconds: number;
  /**
   * Gap-filled daily buckets in ascending time order
   */
  points: Array<RiskTokensPoint>;
};

/** @internal */
export const QueryRiskTokensResult$inboundSchema: z.ZodMiniType<
  QueryRiskTokensResult,
  unknown
> = z.pipe(
  z.object({
    interval_seconds: z.int(),
    points: z.array(RiskTokensPoint$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_seconds": "intervalSeconds",
    });
  }),
);

export function queryRiskTokensResultFromJSON(
  jsonString: string,
): SafeParseResult<QueryRiskTokensResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QueryRiskTokensResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QueryRiskTokensResult' from JSON`,
  );
}
