/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One UTC day of token usage split by risk involvement
 */
export type RiskTokensPoint = {
  /**
   * Bucket start time in Unix nanoseconds (string for JS precision)
   */
  bucketTimeUnixNano: string;
  /**
   * Tokens from sessions with at least one active risk finding created in the query window
   */
  riskyTokens: number;
  /**
   * All session tokens in the bucket
   */
  totalTokens: number;
};

/** @internal */
export const RiskTokensPoint$inboundSchema: z.ZodMiniType<
  RiskTokensPoint,
  unknown
> = z.pipe(
  z.object({
    bucket_time_unix_nano: z.string(),
    risky_tokens: z.int(),
    total_tokens: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "bucket_time_unix_nano": "bucketTimeUnixNano",
      "risky_tokens": "riskyTokens",
      "total_tokens": "totalTokens",
    });
  }),
);

export function riskTokensPointFromJSON(
  jsonString: string,
): SafeParseResult<RiskTokensPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskTokensPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskTokensPoint' from JSON`,
  );
}
