/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Icons advertised by a remote MCP server in its initialize response
 */
export type DiscoverServerIconsResult = {
  /**
   * Absolute icon source URLs, in the order the server advertised them. Empty when the server is unreachable, requires auth, or advertises no icons.
   */
  icons: Array<string>;
};

/** @internal */
export const DiscoverServerIconsResult$inboundSchema: z.ZodMiniType<
  DiscoverServerIconsResult,
  unknown
> = z.object({
  icons: z.array(z.string()),
});

export function discoverServerIconsResultFromJSON(
  jsonString: string,
): SafeParseResult<DiscoverServerIconsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DiscoverServerIconsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DiscoverServerIconsResult' from JSON`,
  );
}
