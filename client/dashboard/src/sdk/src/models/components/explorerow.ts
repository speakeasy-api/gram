/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One result row of an Explore query.
 */
export type ExploreRow = {
  /**
   * Timeseries bucket start in ISO 8601. Empty for whole-range table queries.
   */
  bucket: string;
  /**
   * Group values parallel to group_by. Empty string means the canonical dimension is empty.
   */
  group: Array<string>;
  /**
   * Values keyed by canonical calculation name, such as SUM(input_tokens).
   */
  values: { [k: string]: number };
};

/** @internal */
export const ExploreRow$inboundSchema: z.ZodMiniType<ExploreRow, unknown> = z
  .object({
    bucket: z.string(),
    group: z.array(z.string()),
    values: z.record(z.string(), z.number()),
  });

export function exploreRowFromJSON(
  jsonString: string,
): SafeParseResult<ExploreRow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExploreRow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExploreRow' from JSON`,
  );
}
