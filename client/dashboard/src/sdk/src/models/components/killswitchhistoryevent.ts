/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  KillswitchSchedule,
  KillswitchSchedule$inboundSchema,
} from "./killswitchschedule.js";
import {
  KillswitchScope,
  KillswitchScope$inboundSchema,
} from "./killswitchscope.js";

export const Action = {
  Created: "created",
  Edited: "edited",
  Lifted: "lifted",
  Expired: "expired",
  Restored: "restored",
} as const;
export type Action = ClosedEnum<typeof Action>;

export const KillswitchHistoryEventStatus = {
  Active: "active",
  Scheduled: "scheduled",
  Expired: "expired",
  Lifted: "lifted",
} as const;
export type KillswitchHistoryEventStatus = ClosedEnum<
  typeof KillswitchHistoryEventStatus
>;

export type KillswitchHistoryEvent = {
  action: Action;
  actorDisplayName?: string | undefined;
  actorUserId?: string | undefined;
  changedAt: Date;
  externalNote: string;
  internalNote: string;
  schedule: KillswitchSchedule;
  scope: KillswitchScope;
  sequence: number;
  status: KillswitchHistoryEventStatus;
  version: number;
};

/** @internal */
export const Action$inboundSchema: z.ZodMiniEnum<typeof Action> = z.enum(
  Action,
);

/** @internal */
export const KillswitchHistoryEventStatus$inboundSchema: z.ZodMiniEnum<
  typeof KillswitchHistoryEventStatus
> = z.enum(KillswitchHistoryEventStatus);

/** @internal */
export const KillswitchHistoryEvent$inboundSchema: z.ZodMiniType<
  KillswitchHistoryEvent,
  unknown
> = z.pipe(
  z.object({
    action: Action$inboundSchema,
    actor_display_name: z.optional(z.string()),
    actor_user_id: z.optional(z.string()),
    changed_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    external_note: z.string(),
    internal_note: z.string(),
    schedule: KillswitchSchedule$inboundSchema,
    scope: KillswitchScope$inboundSchema,
    sequence: z.int(),
    status: KillswitchHistoryEventStatus$inboundSchema,
    version: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "actor_display_name": "actorDisplayName",
      "actor_user_id": "actorUserId",
      "changed_at": "changedAt",
      "external_note": "externalNote",
      "internal_note": "internalNote",
    });
  }),
);

export function killswitchHistoryEventFromJSON(
  jsonString: string,
): SafeParseResult<KillswitchHistoryEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KillswitchHistoryEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KillswitchHistoryEvent' from JSON`,
  );
}
