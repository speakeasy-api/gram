/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type MintFrozenGatewaySessionResponseBody = {
  /**
   * Gateway bearer token.
   */
  accessToken: string;
  /**
   * Lifetime in seconds.
   */
  expiresIn: number;
  /**
   * Number of approved tools in this frozen connection.
   */
  frozenToolCount: number;
};

/** @internal */
export const MintFrozenGatewaySessionResponseBody$inboundSchema: z.ZodMiniType<
  MintFrozenGatewaySessionResponseBody,
  unknown
> = z.pipe(
  z.object({
    access_token: z.string(),
    expires_in: z.int(),
    frozen_tool_count: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_token": "accessToken",
      "expires_in": "expiresIn",
      "frozen_tool_count": "frozenToolCount",
    });
  }),
);

export function mintFrozenGatewaySessionResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<MintFrozenGatewaySessionResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      MintFrozenGatewaySessionResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MintFrozenGatewaySessionResponseBody' from JSON`,
  );
}
