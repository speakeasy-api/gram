/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export const Mode = {
  MultiLabel: "multi_label",
  Exclusive: "exclusive",
  OrderedScore: "ordered_score",
} as const;
/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export type Mode = ClosedEnum<typeof Mode>;

export type CreateSigintSensorForm = {
  /**
   * Optional description; empty stores no value
   */
  description?: string | undefined;
  /**
   * Optional classification instructions; empty stores no value
   */
  instructions?: string | undefined;
  /**
   * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
   */
  mode: Mode;
  /**
   * Display name; trimmed before enforcing the 1 to 200 character limit
   */
  name: string;
  /**
   * Initial ordered signal IDs. Omitted and [] both create an empty sensor.
   */
  signalIds?: Array<string> | undefined;
};

/** @internal */
export const Mode$outboundSchema: z.ZodMiniEnum<typeof Mode> = z.enum(Mode);

/** @internal */
export type CreateSigintSensorForm$Outbound = {
  description?: string | undefined;
  instructions?: string | undefined;
  mode: string;
  name: string;
  signal_ids?: Array<string> | undefined;
};

/** @internal */
export const CreateSigintSensorForm$outboundSchema: z.ZodMiniType<
  CreateSigintSensorForm$Outbound,
  CreateSigintSensorForm
> = z.pipe(
  z.object({
    description: z.optional(z.string()),
    instructions: z.optional(z.string()),
    mode: Mode$outboundSchema,
    name: z.string(),
    signalIds: z.optional(z.array(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      signalIds: "signal_ids",
    });
  }),
);

export function createSigintSensorFormToJSON(
  createSigintSensorForm: CreateSigintSensorForm,
): string {
  return JSON.stringify(
    CreateSigintSensorForm$outboundSchema.parse(createSigintSensorForm),
  );
}
