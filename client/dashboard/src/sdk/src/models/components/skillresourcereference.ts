/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The optional directory the Agent Skills specification reserves for this path.
 */
export const SkillResourceReferenceKind = {
  Script: "script",
  Reference: "reference",
  Asset: "asset",
  Other: "other",
} as const;
/**
 * The optional directory the Agent Skills specification reserves for this path.
 */
export type SkillResourceReferenceKind = ClosedEnum<
  typeof SkillResourceReferenceKind
>;

/**
 * A supporting file a SKILL.md points at, named relative to the skill directory root.
 */
export type SkillResourceReference = {
  /**
   * The optional directory the Agent Skills specification reserves for this path.
   */
  kind: SkillResourceReferenceKind;
  /**
   * The referenced path, relative to the skill directory root.
   */
  path: string;
};

/** @internal */
export const SkillResourceReferenceKind$inboundSchema: z.ZodMiniEnum<
  typeof SkillResourceReferenceKind
> = z.enum(SkillResourceReferenceKind);

/** @internal */
export const SkillResourceReference$inboundSchema: z.ZodMiniType<
  SkillResourceReference,
  unknown
> = z.object({
  kind: SkillResourceReferenceKind$inboundSchema,
  path: z.string(),
});

export function skillResourceReferenceFromJSON(
  jsonString: string,
): SafeParseResult<SkillResourceReference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SkillResourceReference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SkillResourceReference' from JSON`,
  );
}
