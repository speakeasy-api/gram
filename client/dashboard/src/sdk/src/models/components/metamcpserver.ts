/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A meta MCP server: an aggregate server fronting an explicitly managed set of MCP servers. URL addressability lives on its MCP endpoints.
 */
export type MetaMcpServer = {
  /**
   * When the meta MCP server was created
   */
  createdAt: Date;
  /**
   * The ID of the meta MCP server
   */
  id: string;
  /**
   * The display name of the meta MCP server
   */
  name: string;
  /**
   * The organization this meta MCP server belongs to
   */
  organizationId: string;
  /**
   * The project this meta MCP server belongs to
   */
  projectId: string;
  /**
   * When the meta MCP server was last updated
   */
  updatedAt: Date;
  /**
   * The ID of the user session issuer used to authenticate callers. Null when no issuer is attached.
   */
  userSessionIssuerId?: string | undefined;
};

/** @internal */
export const MetaMcpServer$inboundSchema: z.ZodMiniType<
  MetaMcpServer,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    name: z.string(),
    organization_id: z.string(),
    project_id: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    user_session_issuer_id: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "organization_id": "organizationId",
      "project_id": "projectId",
      "updated_at": "updatedAt",
      "user_session_issuer_id": "userSessionIssuerId",
    });
  }),
);

export function metaMcpServerFromJSON(
  jsonString: string,
): SafeParseResult<MetaMcpServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetaMcpServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetaMcpServer' from JSON`,
  );
}
