/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  AIScanMatch,
  AIScanMatch$Outbound,
  AIScanMatch$outboundSchema,
} from "./aiscanmatch.js";

export type ReportAIScanRequestBody = {
  /**
   * Number of matches reported by the agent. Echoed into the scan receipt as reported.
   */
  matchCount: number;
  /**
   * Detection targets the scan matched. Empty when the device came back clean; the report still lands as a scan receipt.
   */
  matches: Array<AIScanMatch>;
  /**
   * When the agent completed the scan.
   */
  scanCompletedAt: Date;
  /**
   * When the agent started the scan.
   */
  scanStartedAt: Date;
  /**
   * Version of the target list compiled into the agent binary that ran the scan. Echoed into the scan receipt as reported.
   */
  targetListVersion: number;
};

/** @internal */
export type ReportAIScanRequestBody$Outbound = {
  match_count: number;
  matches: Array<AIScanMatch$Outbound>;
  scan_completed_at: string;
  scan_started_at: string;
  target_list_version: number;
};

/** @internal */
export const ReportAIScanRequestBody$outboundSchema: z.ZodMiniType<
  ReportAIScanRequestBody$Outbound,
  ReportAIScanRequestBody
> = z.pipe(
  z.object({
    matchCount: z.int(),
    matches: z.array(AIScanMatch$outboundSchema),
    scanCompletedAt: z.pipe(z.date(), z.transform(v => v.toISOString())),
    scanStartedAt: z.pipe(z.date(), z.transform(v => v.toISOString())),
    targetListVersion: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      matchCount: "match_count",
      scanCompletedAt: "scan_completed_at",
      scanStartedAt: "scan_started_at",
      targetListVersion: "target_list_version",
    });
  }),
);

export function reportAIScanRequestBodyToJSON(
  reportAIScanRequestBody: ReportAIScanRequestBody,
): string {
  return JSON.stringify(
    ReportAIScanRequestBody$outboundSchema.parse(reportAIScanRequestBody),
  );
}
