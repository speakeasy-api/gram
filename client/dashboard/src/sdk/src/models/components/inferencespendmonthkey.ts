/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The platform-managed inference function
 */
export const InferenceSpendMonthKeyKeyType = {
  Chat: "chat",
  Internal: "internal",
} as const;
/**
 * The platform-managed inference function
 */
export type InferenceSpendMonthKeyKeyType = ClosedEnum<
  typeof InferenceSpendMonthKeyKeyType
>;

export type InferenceSpendMonthKey = {
  /**
   * The platform-managed inference function
   */
  keyType: InferenceSpendMonthKeyKeyType;
  /**
   * Exact durable spend in USD for this key through recorded_through
   */
  spendUsd: string;
};

/** @internal */
export const InferenceSpendMonthKeyKeyType$inboundSchema: z.ZodMiniEnum<
  typeof InferenceSpendMonthKeyKeyType
> = z.enum(InferenceSpendMonthKeyKeyType);

/** @internal */
export const InferenceSpendMonthKey$inboundSchema: z.ZodMiniType<
  InferenceSpendMonthKey,
  unknown
> = z.pipe(
  z.object({
    key_type: InferenceSpendMonthKeyKeyType$inboundSchema,
    spend_usd: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "key_type": "keyType",
      "spend_usd": "spendUsd",
    });
  }),
);

export function inferenceSpendMonthKeyFromJSON(
  jsonString: string,
): SafeParseResult<InferenceSpendMonthKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InferenceSpendMonthKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InferenceSpendMonthKey' from JSON`,
  );
}
