/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OnboardingSelectedProduct = {
  /**
   * Slug of one of the product's plans. Omitted only when the product has no plans.
   */
  planSlug?: string | undefined;
  /**
   * Slug of a product from the reference data
   */
  productSlug: string;
};

/** @internal */
export const OnboardingSelectedProduct$inboundSchema: z.ZodMiniType<
  OnboardingSelectedProduct,
  unknown
> = z.pipe(
  z.object({
    plan_slug: z.optional(z.string()),
    product_slug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_slug": "planSlug",
      "product_slug": "productSlug",
    });
  }),
);
/** @internal */
export type OnboardingSelectedProduct$Outbound = {
  plan_slug?: string | undefined;
  product_slug: string;
};

/** @internal */
export const OnboardingSelectedProduct$outboundSchema: z.ZodMiniType<
  OnboardingSelectedProduct$Outbound,
  OnboardingSelectedProduct
> = z.pipe(
  z.object({
    planSlug: z.optional(z.string()),
    productSlug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      planSlug: "plan_slug",
      productSlug: "product_slug",
    });
  }),
);

export function onboardingSelectedProductToJSON(
  onboardingSelectedProduct: OnboardingSelectedProduct,
): string {
  return JSON.stringify(
    OnboardingSelectedProduct$outboundSchema.parse(onboardingSelectedProduct),
  );
}
export function onboardingSelectedProductFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingSelectedProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingSelectedProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingSelectedProduct' from JSON`,
  );
}
