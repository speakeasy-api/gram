/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  IdentityProviderApplication,
  IdentityProviderApplication$inboundSchema,
} from "./identityproviderapplication.js";

/**
 * A capped live application inventory from the identity provider.
 */
export type ListIdentityProviderApplicationsResult = {
  applicationCount: number;
  applications: Array<IdentityProviderApplication>;
  detail: string;
  readAt: Date;
  truncated: boolean;
};

/** @internal */
export const ListIdentityProviderApplicationsResult$inboundSchema:
  z.ZodMiniType<ListIdentityProviderApplicationsResult, unknown> = z.pipe(
    z.object({
      application_count: z.int(),
      applications: z.array(IdentityProviderApplication$inboundSchema),
      detail: z.string(),
      read_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      truncated: z.boolean(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "application_count": "applicationCount",
        "read_at": "readAt",
      });
    }),
  );

export function listIdentityProviderApplicationsResultFromJSON(
  jsonString: string,
): SafeParseResult<ListIdentityProviderApplicationsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListIdentityProviderApplicationsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListIdentityProviderApplicationsResult' from JSON`,
  );
}
