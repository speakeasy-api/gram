/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export const UpdateSigintSensorFormMode = {
  MultiLabel: "multi_label",
  Exclusive: "exclusive",
  OrderedScore: "ordered_score",
} as const;
/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export type UpdateSigintSensorFormMode = ClosedEnum<
  typeof UpdateSigintSensorFormMode
>;

export type UpdateSigintSensorForm = {
  /**
   * Replacement description; empty clears it
   */
  description?: string | undefined;
  /**
   * Sensor ID
   */
  id: string;
  /**
   * Replacement instructions; empty clears it
   */
  instructions?: string | undefined;
  /**
   * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
   */
  mode?: UpdateSigintSensorFormMode | undefined;
  /**
   * Replacement display name; trimmed before enforcing the 1 to 200 character limit
   */
  name?: string | undefined;
  /**
   * Authoritative ordered signal IDs. Omit or send null to preserve; send [] to clear.
   */
  signalIds?: Array<string> | undefined;
};

/** @internal */
export const UpdateSigintSensorFormMode$outboundSchema: z.ZodMiniEnum<
  typeof UpdateSigintSensorFormMode
> = z.enum(UpdateSigintSensorFormMode);

/** @internal */
export type UpdateSigintSensorForm$Outbound = {
  description?: string | undefined;
  id: string;
  instructions?: string | undefined;
  mode?: string | undefined;
  name?: string | undefined;
  signal_ids?: Array<string> | undefined;
};

/** @internal */
export const UpdateSigintSensorForm$outboundSchema: z.ZodMiniType<
  UpdateSigintSensorForm$Outbound,
  UpdateSigintSensorForm
> = z.pipe(
  z.object({
    description: z.optional(z.string()),
    id: z.string(),
    instructions: z.optional(z.string()),
    mode: z.optional(UpdateSigintSensorFormMode$outboundSchema),
    name: z.optional(z.string()),
    signalIds: z.optional(z.array(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      signalIds: "signal_ids",
    });
  }),
);

export function updateSigintSensorFormToJSON(
  updateSigintSensorForm: UpdateSigintSensorForm,
): string {
  return JSON.stringify(
    UpdateSigintSensorForm$outboundSchema.parse(updateSigintSensorForm),
  );
}
