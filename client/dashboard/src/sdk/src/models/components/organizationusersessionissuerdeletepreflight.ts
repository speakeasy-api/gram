/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OrganizationUserSessionIssuerReference,
  OrganizationUserSessionIssuerReference$inboundSchema,
} from "./organizationusersessionissuerreference.js";

/**
 * Authoritative impact summary for deleting an organization-owned user_session_issuer.
 */
export type OrganizationUserSessionIssuerDeletePreflight = {
  /**
   * True when no live MCP server or toolset references the issuer.
   */
  canDelete: boolean;
  /**
   * Number of non-deleted user_session_clients registered with the issuer.
   */
  clientCount: number;
  /**
   * Number of non-deleted, unexpired user_sessions issued by the issuer.
   */
  liveSessionCount: number;
  /**
   * Live MCP servers that block deletion.
   */
  mcpServers: Array<OrganizationUserSessionIssuerReference>;
  /**
   * Live toolsets that block deletion.
   */
  toolsets: Array<OrganizationUserSessionIssuerReference>;
};

/** @internal */
export const OrganizationUserSessionIssuerDeletePreflight$inboundSchema:
  z.ZodMiniType<OrganizationUserSessionIssuerDeletePreflight, unknown> = z.pipe(
    z.object({
      can_delete: z.boolean(),
      client_count: z.int(),
      live_session_count: z.int(),
      mcp_servers: z.array(
        OrganizationUserSessionIssuerReference$inboundSchema,
      ),
      toolsets: z.array(OrganizationUserSessionIssuerReference$inboundSchema),
    }),
    z.transform((v) => {
      return remap$(v, {
        "can_delete": "canDelete",
        "client_count": "clientCount",
        "live_session_count": "liveSessionCount",
        "mcp_servers": "mcpServers",
      });
    }),
  );

export function organizationUserSessionIssuerDeletePreflightFromJSON(
  jsonString: string,
): SafeParseResult<
  OrganizationUserSessionIssuerDeletePreflight,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      OrganizationUserSessionIssuerDeletePreflight$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'OrganizationUserSessionIssuerDeletePreflight' from JSON`,
  );
}
