/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Organization package admission: enabled, disabled, or indeterminate.
 */
export const Admission = {
  Enabled: "enabled",
  Disabled: "disabled",
  Indeterminate: "indeterminate",
} as const;
/**
 * Organization package admission: enabled, disabled, or indeterminate.
 */
export type Admission = ClosedEnum<typeof Admission>;

/**
 * Platform package freshness: current, stale, missing, unavailable, or indeterminate.
 */
export const Freshness = {
  Current: "current",
  Stale: "stale",
  Missing: "missing",
  Unavailable: "unavailable",
  Indeterminate: "indeterminate",
} as const;
/**
 * Platform package freshness: current, stale, missing, unavailable, or indeterminate.
 */
export type Freshness = ClosedEnum<typeof Freshness>;

export type PlatformMCPPackageStatusResult = {
  /**
   * Organization package admission: enabled, disabled, or indeterminate.
   */
  admission: Admission;
  /**
   * Deterministic portable Agent Plugins direct-download ZIP filename.
   */
  agentPluginFilename: string;
  /**
   * Whether organization admission currently permits installing the package.
   */
  available: boolean;
  /**
   * Literal default project that owns the canonical organization marketplace, when present.
   */
  canonicalProjectSlug?: string | undefined;
  /**
   * Deterministic Claude direct-download ZIP filename.
   */
  claudeFilename: string;
  /**
   * Whether keyless direct downloads are currently admitted.
   */
  directDownloadAvailable: boolean;
  /**
   * Platform package freshness: current, stale, missing, unavailable, or indeterminate.
   */
  freshness: Freshness;
  /**
   * Whether the canonical default project has a published GitHub marketplace.
   */
  marketplaceConnected: boolean;
  /**
   * Effective name of the canonical marketplace, when its default project is present.
   */
  marketplaceName?: string | undefined;
  /**
   * Git URL used by supported clients to register the canonical marketplace.
   */
  marketplaceUrl?: string | undefined;
  /**
   * Fixed Platform MCP package identity.
   */
  packageName: string;
  /**
   * Whether the last successful canonical publish recorded the Platform package fingerprint.
   */
  packagePresent: boolean;
  /**
   * Whether an organization admin can publish or repair the canonical package now.
   */
  repairAllowed: boolean;
  /**
   * Canonical GitHub repository URL.
   */
  repoUrl?: string | undefined;
};

/** @internal */
export const Admission$inboundSchema: z.ZodMiniEnum<typeof Admission> = z.enum(
  Admission,
);

/** @internal */
export const Freshness$inboundSchema: z.ZodMiniEnum<typeof Freshness> = z.enum(
  Freshness,
);

/** @internal */
export const PlatformMCPPackageStatusResult$inboundSchema: z.ZodMiniType<
  PlatformMCPPackageStatusResult,
  unknown
> = z.pipe(
  z.object({
    admission: Admission$inboundSchema,
    agent_plugin_filename: z.string(),
    available: z.boolean(),
    canonical_project_slug: z.optional(z.string()),
    claude_filename: z.string(),
    direct_download_available: z.boolean(),
    freshness: Freshness$inboundSchema,
    marketplace_connected: z.boolean(),
    marketplace_name: z.optional(z.string()),
    marketplace_url: z.optional(z.string()),
    package_name: z.string(),
    package_present: z.boolean(),
    repair_allowed: z.boolean(),
    repo_url: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "agent_plugin_filename": "agentPluginFilename",
      "canonical_project_slug": "canonicalProjectSlug",
      "claude_filename": "claudeFilename",
      "direct_download_available": "directDownloadAvailable",
      "marketplace_connected": "marketplaceConnected",
      "marketplace_name": "marketplaceName",
      "marketplace_url": "marketplaceUrl",
      "package_name": "packageName",
      "package_present": "packagePresent",
      "repair_allowed": "repairAllowed",
      "repo_url": "repoUrl",
    });
  }),
);

export function platformMCPPackageStatusResultFromJSON(
  jsonString: string,
): SafeParseResult<PlatformMCPPackageStatusResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformMCPPackageStatusResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformMCPPackageStatusResult' from JSON`,
  );
}
