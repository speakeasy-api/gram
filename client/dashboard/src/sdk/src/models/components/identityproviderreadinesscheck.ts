/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Stable readiness check key.
 */
export const IdentityProviderReadinessCheckKey = {
  WorkosOrganizationLinked: "workos_organization_linked",
  WorkosDomainVerified: "workos_domain_verified",
  DirectoryHandoffStored: "directory_handoff_stored",
  WorkosDirectoryCreated: "workos_directory_created",
  ConnectionsApiAvailable: "connections_api_available",
  SsoFeatureEnabled: "sso_feature_enabled",
  ScimFeatureEnabled: "scim_feature_enabled",
} as const;
/**
 * Stable readiness check key.
 */
export type IdentityProviderReadinessCheckKey = ClosedEnum<
  typeof IdentityProviderReadinessCheckKey
>;

/**
 * Team responsible for the check.
 */
export const Owner = {
  PlatformAdmin: "platform_admin",
  Customer: "customer",
  Speakeasy: "speakeasy",
} as const;
/**
 * Team responsible for the check.
 */
export type Owner = ClosedEnum<typeof Owner>;

export type IdentityProviderReadinessCheck = {
  checkedAt: Date;
  /**
   * Customer-safe explanation of the result.
   */
  detail: string;
  /**
   * Stable readiness check key.
   */
  key: IdentityProviderReadinessCheckKey;
  /**
   * Whether the readiness check passed.
   */
  ok: boolean;
  /**
   * Team responsible for the check.
   */
  owner: Owner;
  /**
   * Staff action needed to make the check pass.
   */
  remedy: string;
};

/** @internal */
export const IdentityProviderReadinessCheckKey$inboundSchema: z.ZodMiniEnum<
  typeof IdentityProviderReadinessCheckKey
> = z.enum(IdentityProviderReadinessCheckKey);

/** @internal */
export const Owner$inboundSchema: z.ZodMiniEnum<typeof Owner> = z.enum(Owner);

/** @internal */
export const IdentityProviderReadinessCheck$inboundSchema: z.ZodMiniType<
  IdentityProviderReadinessCheck,
  unknown
> = z.pipe(
  z.object({
    checked_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    detail: z.string(),
    key: IdentityProviderReadinessCheckKey$inboundSchema,
    ok: z.boolean(),
    owner: Owner$inboundSchema,
    remedy: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "checked_at": "checkedAt",
    });
  }),
);

export function identityProviderReadinessCheckFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderReadinessCheck, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderReadinessCheck$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderReadinessCheck' from JSON`,
  );
}
