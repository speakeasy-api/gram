/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The GitHub repository visibility.
 */
export const FetchedGitHubRepositoryVisibility = {
  Public: "public",
} as const;
/**
 * The GitHub repository visibility.
 */
export type FetchedGitHubRepositoryVisibility = ClosedEnum<
  typeof FetchedGitHubRepositoryVisibility
>;

/**
 * The public GitHub repository snapshot used to discover skills.
 */
export type FetchedGitHubRepository = {
  /**
   * The immutable commit scanned for skills.
   */
  commitSha: string;
  /**
   * The repository default branch.
   */
  defaultBranch: string;
  /**
   * The repository in owner/name form.
   */
  fullName: string;
  /**
   * The canonical GitHub repository URL.
   */
  url: string;
  /**
   * The GitHub repository visibility.
   */
  visibility: FetchedGitHubRepositoryVisibility;
};

/** @internal */
export const FetchedGitHubRepositoryVisibility$inboundSchema: z.ZodMiniEnum<
  typeof FetchedGitHubRepositoryVisibility
> = z.enum(FetchedGitHubRepositoryVisibility);

/** @internal */
export const FetchedGitHubRepository$inboundSchema: z.ZodMiniType<
  FetchedGitHubRepository,
  unknown
> = z.pipe(
  z.object({
    commit_sha: z.string(),
    default_branch: z.string(),
    full_name: z.string(),
    url: z.string(),
    visibility: FetchedGitHubRepositoryVisibility$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "commit_sha": "commitSha",
      "default_branch": "defaultBranch",
      "full_name": "fullName",
    });
  }),
);

export function fetchedGitHubRepositoryFromJSON(
  jsonString: string,
): SafeParseResult<FetchedGitHubRepository, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FetchedGitHubRepository$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FetchedGitHubRepository' from JSON`,
  );
}
