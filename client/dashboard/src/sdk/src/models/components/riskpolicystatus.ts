/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Workflow state: running, sleeping, or not_started.
 */
export const WorkflowStatus = {
  Running: "running",
  Sleeping: "sleeping",
  NotStarted: "not_started",
} as const;
/**
 * Workflow state: running, sleeping, or not_started.
 */
export type WorkflowStatus = ClosedEnum<typeof WorkflowStatus>;

export type RiskPolicyStatus = {
  /**
   * Messages analyzed at the current policy version.
   */
  analyzedMessages: number;
  /**
   * Number of findings at the current policy version.
   */
  findingsCount: number;
  /**
   * Messages not yet analyzed.
   */
  pendingMessages: number;
  /**
   * The risk policy ID.
   */
  policyId: string;
  /**
   * Current policy version.
   */
  policyVersion: number;
  /**
   * Total messages in the project.
   */
  totalMessages: number;
  /**
   * Workflow state: running, sleeping, or not_started.
   */
  workflowStatus: WorkflowStatus;
};

/** @internal */
export const WorkflowStatus$inboundSchema: z.ZodMiniEnum<
  typeof WorkflowStatus
> = z.enum(WorkflowStatus);

/** @internal */
export const RiskPolicyStatus$inboundSchema: z.ZodMiniType<
  RiskPolicyStatus,
  unknown
> = z.pipe(
  z.object({
    analyzed_messages: z.int(),
    findings_count: z.int(),
    pending_messages: z.int(),
    policy_id: z.string(),
    policy_version: z.int(),
    total_messages: z.int(),
    workflow_status: WorkflowStatus$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "analyzed_messages": "analyzedMessages",
      "findings_count": "findingsCount",
      "pending_messages": "pendingMessages",
      "policy_id": "policyId",
      "policy_version": "policyVersion",
      "total_messages": "totalMessages",
      "workflow_status": "workflowStatus",
    });
  }),
);

export function riskPolicyStatusFromJSON(
  jsonString: string,
): SafeParseResult<RiskPolicyStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskPolicyStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskPolicyStatus' from JSON`,
  );
}
