/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export const SigintSensorMode = {
  MultiLabel: "multi_label",
  Exclusive: "exclusive",
  OrderedScore: "ordered_score",
} as const;
/**
 * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
 */
export type SigintSensorMode = ClosedEnum<typeof SigintSensorMode>;

/**
 * A project-scoped sensor with ordered live references to custom signals. signal_ids is always present, including when empty.
 */
export type SigintSensor = {
  /**
   * Creation time
   */
  createdAt: Date;
  /**
   * Optional author-facing description
   */
  description?: string | undefined;
  /**
   * Sensor ID
   */
  id: string;
  /**
   * Optional framing instructions for classification
   */
  instructions?: string | undefined;
  /**
   * How a sensor interprets its ordered signals: independently applicable labels, an exclusive choice, or ordered score levels.
   */
  mode: SigintSensorMode;
  /**
   * Display name
   */
  name: string;
  /**
   * Owning project ID
   */
  projectId: string;
  /**
   * Ordered signal IDs. In ordered_score mode the zero-based array index is the score level.
   */
  signalIds: Array<string>;
  /**
   * Last update time
   */
  updatedAt: Date;
};

/** @internal */
export const SigintSensorMode$inboundSchema: z.ZodMiniEnum<
  typeof SigintSensorMode
> = z.enum(SigintSensorMode);

/** @internal */
export const SigintSensor$inboundSchema: z.ZodMiniType<SigintSensor, unknown> =
  z.pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      description: z.optional(z.string()),
      id: z.string(),
      instructions: z.optional(z.string()),
      mode: SigintSensorMode$inboundSchema,
      name: z.string(),
      project_id: z.string(),
      signal_ids: z.array(z.string()),
      updated_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "project_id": "projectId",
        "signal_ids": "signalIds",
        "updated_at": "updatedAt",
      });
    }),
  );

export function sigintSensorFromJSON(
  jsonString: string,
): SafeParseResult<SigintSensor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SigintSensor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SigintSensor' from JSON`,
  );
}
