/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A compact summary of the toolset backing a toolset-backed MCP server, sufficient to render a listing card without a separate toolsets fetch.
 */
export type McpServerToolsetSummary = {
  /**
   * The ID of the backing toolset
   */
  id: string;
  /**
   * The name of the backing toolset
   */
  name: string;
  /**
   * The registry specifier the toolset was installed from, when it originated from the catalog.
   */
  originRegistrySpecifier?: string | undefined;
  /**
   * The slug of the backing toolset
   */
  slug: string;
  /**
   * The number of tools in the toolset's latest version
   */
  toolCount: number;
  /**
   * The tool URNs in the toolset's latest version
   */
  toolUrns: Array<string>;
};

/** @internal */
export const McpServerToolsetSummary$inboundSchema: z.ZodMiniType<
  McpServerToolsetSummary,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    name: z.string(),
    origin_registry_specifier: z.optional(z.string()),
    slug: z.string(),
    tool_count: z.int(),
    tool_urns: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "origin_registry_specifier": "originRegistrySpecifier",
      "tool_count": "toolCount",
      "tool_urns": "toolUrns",
    });
  }),
);

export function mcpServerToolsetSummaryFromJSON(
  jsonString: string,
): SafeParseResult<McpServerToolsetSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpServerToolsetSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpServerToolsetSummary' from JSON`,
  );
}
