/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Observed account type.
 */
export const MemberType = {
  Person: "person",
  Guest: "guest",
  SingleChannelGuest: "single_channel_guest",
  Bot: "bot",
  Unknown: "unknown",
} as const;
/**
 * Observed account type.
 */
export type MemberType = ClosedEnum<typeof MemberType>;

/**
 * Observed account state.
 */
export const SlackDirectoryMemberStatus = {
  Active: "active",
  Deactivated: "deactivated",
  Invited: "invited",
  Unknown: "unknown",
} as const;
/**
 * Observed account state.
 */
export type SlackDirectoryMemberStatus = ClosedEnum<
  typeof SlackDirectoryMemberStatus
>;

export type SlackDirectoryMember = {
  /**
   * Connection that observed this workspace member.
   */
  connectionId: string;
  /**
   * Optional observed display name.
   */
  displayName?: string | undefined;
  /**
   * Observed email; never evidence of a confirmed Gram identity.
   */
  email?: string | undefined;
  /**
   * Durable membership ID.
   */
  id: string;
  /**
   * Latest published observation.
   */
  lastSeenAt: Date;
  /**
   * Observed account type.
   */
  memberType: MemberType;
  /**
   * Whether this row was present in its workspace's last complete snapshot.
   */
  observedInLastSync: boolean;
  /**
   * Slack workspace user ID.
   */
  slackUserId: string;
  /**
   * Observed account state.
   */
  status: SlackDirectoryMemberStatus;
  /**
   * Verified member workspace.
   */
  workspaceId: string;
  /**
   * Workspace display name.
   */
  workspaceName: string;
};

/** @internal */
export const MemberType$inboundSchema: z.ZodMiniEnum<typeof MemberType> = z
  .enum(MemberType);

/** @internal */
export const SlackDirectoryMemberStatus$inboundSchema: z.ZodMiniEnum<
  typeof SlackDirectoryMemberStatus
> = z.enum(SlackDirectoryMemberStatus);

/** @internal */
export const SlackDirectoryMember$inboundSchema: z.ZodMiniType<
  SlackDirectoryMember,
  unknown
> = z.pipe(
  z.object({
    connection_id: z.string(),
    display_name: z.optional(z.string()),
    email: z.optional(z.string()),
    id: z.string(),
    last_seen_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    member_type: MemberType$inboundSchema,
    observed_in_last_sync: z.boolean(),
    slack_user_id: z.string(),
    status: SlackDirectoryMemberStatus$inboundSchema,
    workspace_id: z.string(),
    workspace_name: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "connection_id": "connectionId",
      "display_name": "displayName",
      "last_seen_at": "lastSeenAt",
      "member_type": "memberType",
      "observed_in_last_sync": "observedInLastSync",
      "slack_user_id": "slackUserId",
      "workspace_id": "workspaceId",
      "workspace_name": "workspaceName",
    });
  }),
);

export function slackDirectoryMemberFromJSON(
  jsonString: string,
): SafeParseResult<SlackDirectoryMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SlackDirectoryMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SlackDirectoryMember' from JSON`,
  );
}
