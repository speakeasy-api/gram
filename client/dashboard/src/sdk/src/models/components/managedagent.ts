/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AgentPermissions,
  AgentPermissions$inboundSchema,
} from "./agentpermissions.js";

export const Lifecycle = {
  Active: "active",
  Suspended: "suspended",
  Revoked: "revoked",
} as const;
export type Lifecycle = ClosedEnum<typeof Lifecycle>;

export type ManagedAgent = {
  createdAt: Date;
  id: string;
  lifecycle: Lifecycle;
  name: string;
  ownerUserId: string;
  permissions: AgentPermissions;
  updatedAt: Date;
};

/** @internal */
export const Lifecycle$inboundSchema: z.ZodMiniEnum<typeof Lifecycle> = z.enum(
  Lifecycle,
);

/** @internal */
export const ManagedAgent$inboundSchema: z.ZodMiniType<ManagedAgent, unknown> =
  z.pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      id: z.string(),
      lifecycle: Lifecycle$inboundSchema,
      name: z.string(),
      owner_user_id: z.string(),
      permissions: AgentPermissions$inboundSchema,
      updated_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "owner_user_id": "ownerUserId",
        "updated_at": "updatedAt",
      });
    }),
  );

export function managedAgentFromJSON(
  jsonString: string,
): SafeParseResult<ManagedAgent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ManagedAgent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ManagedAgent' from JSON`,
  );
}
