/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SyncSkillUpdate,
  SyncSkillUpdate$inboundSchema,
} from "./syncskillupdate.js";

/**
 * The deterministic delta from the submitted complete snapshot to the user's current visible distributions.
 */
export type SyncSkillsResult = {
  /**
   * Installed Gram-managed skill names no longer visible to this user.
   */
  removals: Array<string>;
  /**
   * New or changed skill manifests to write.
   */
  updates: Array<SyncSkillUpdate>;
};

/** @internal */
export const SyncSkillsResult$inboundSchema: z.ZodMiniType<
  SyncSkillsResult,
  unknown
> = z.object({
  removals: z.array(z.string()),
  updates: z.array(SyncSkillUpdate$inboundSchema),
});

export function syncSkillsResultFromJSON(
  jsonString: string,
): SafeParseResult<SyncSkillsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncSkillsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncSkillsResult' from JSON`,
  );
}
