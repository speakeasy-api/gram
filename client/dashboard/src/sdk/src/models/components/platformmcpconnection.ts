/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PlatformMCPConnection = {
  /**
   * When this connection was authorized.
   */
  authorizedAt?: Date | undefined;
  /**
   * Opaque active Platform MCP connection identifier, usable only to revoke within this organization.
   */
  id: string;
  /**
   * Whether authenticated tools/list completed for this connection generation.
   */
  ready: boolean;
  /**
   * When this connection was most recently reauthorized.
   */
  reauthorizedAt?: Date | undefined;
};

/** @internal */
export const PlatformMCPConnection$inboundSchema: z.ZodMiniType<
  PlatformMCPConnection,
  unknown
> = z.pipe(
  z.object({
    authorized_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    id: z.string(),
    ready: z.boolean(),
    reauthorized_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "authorized_at": "authorizedAt",
      "reauthorized_at": "reauthorizedAt",
    });
  }),
);

export function platformMCPConnectionFromJSON(
  jsonString: string,
): SafeParseResult<PlatformMCPConnection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformMCPConnection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformMCPConnection' from JSON`,
  );
}
