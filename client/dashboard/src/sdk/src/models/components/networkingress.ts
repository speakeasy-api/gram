/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type NetworkIngress = {
  /**
   * Whether a join auth key is currently stored. The key itself is never returned.
   */
  authKeyConfigured: boolean;
  /**
   * When the node's current connection was established.
   */
  connectedSince?: Date | undefined;
  /**
   * When the network ingress was created.
   */
  createdAt: Date;
  /**
   * The kind of credential configured for joining the customer's network. One of: auth_key, oauth_client.
   */
  credentialKind: string;
  /**
   * The DNS name the node is reachable at on the customer network, if known
   */
  dnsName?: string | undefined;
  /**
   * Whether the ingress node should be running
   */
  enabled: boolean;
  /**
   * The device hostname the node advertises on the customer's network
   */
  hostname: string;
  /**
   * The ID of the network ingress
   */
  id: string;
  /**
   * Whether requests without an attributable network identity are rejected at the gateway
   */
  identityRequired: boolean;
  /**
   * The most recent node error, if any
   */
  lastError?: string | undefined;
  /**
   * When the node last reported in.
   */
  lastSeenAt?: Date | undefined;
  /**
   * The name of the customer network the node last joined, if known
   */
  networkName?: string | undefined;
  /**
   * Whether an OAuth client credential is currently stored. The secret itself is never returned.
   */
  oauthClientConfigured: boolean;
  /**
   * The ID of the organization this ingress belongs to
   */
  organizationId: string;
  /**
   * Whether public access is disabled: MCP traffic is rejected on the platform host and the org's custom domain
   */
  privateNetworkOnly: boolean;
  /**
   * The overlay network technology serving this ingress. Only 'tailscale' is currently supported.
   */
  provider: string;
  /**
   * The latest observed node status. One of: pending, connecting, online, offline, error, disabled.
   */
  status: string;
  /**
   * ACL tags the node advertises on the customer's network
   */
  tags: Array<string>;
  /**
   * When the network ingress was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export const NetworkIngress$inboundSchema: z.ZodMiniType<
  NetworkIngress,
  unknown
> = z.pipe(
  z.object({
    auth_key_configured: z.boolean(),
    connected_since: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    credential_kind: z.string(),
    dns_name: z.optional(z.string()),
    enabled: z.boolean(),
    hostname: z.string(),
    id: z.string(),
    identity_required: z.boolean(),
    last_error: z.optional(z.string()),
    last_seen_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    network_name: z.optional(z.string()),
    oauth_client_configured: z.boolean(),
    organization_id: z.string(),
    private_network_only: z.boolean(),
    provider: z.string(),
    status: z.string(),
    tags: z.array(z.string()),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "auth_key_configured": "authKeyConfigured",
      "connected_since": "connectedSince",
      "created_at": "createdAt",
      "credential_kind": "credentialKind",
      "dns_name": "dnsName",
      "identity_required": "identityRequired",
      "last_error": "lastError",
      "last_seen_at": "lastSeenAt",
      "network_name": "networkName",
      "oauth_client_configured": "oauthClientConfigured",
      "organization_id": "organizationId",
      "private_network_only": "privateNetworkOnly",
      "updated_at": "updatedAt",
    });
  }),
);

export function networkIngressFromJSON(
  jsonString: string,
): SafeParseResult<NetworkIngress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NetworkIngress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NetworkIngress' from JSON`,
  );
}
