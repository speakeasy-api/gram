/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type KillswitchAgentBadge = {
  affected: boolean;
  affectedNow: boolean;
  agentId: string;
  scheduled: boolean;
};

/** @internal */
export const KillswitchAgentBadge$inboundSchema: z.ZodMiniType<
  KillswitchAgentBadge,
  unknown
> = z.pipe(
  z.object({
    affected: z.boolean(),
    affected_now: z.boolean(),
    agent_id: z.string(),
    scheduled: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "affected_now": "affectedNow",
      "agent_id": "agentId",
    });
  }),
);

export function killswitchAgentBadgeFromJSON(
  jsonString: string,
): SafeParseResult<KillswitchAgentBadge, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KillswitchAgentBadge$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KillswitchAgentBadge' from JSON`,
  );
}
