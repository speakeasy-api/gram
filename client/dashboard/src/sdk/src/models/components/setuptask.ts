/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SetupTaskAssignee,
  SetupTaskAssignee$inboundSchema,
} from "./setuptaskassignee.js";

/**
 * Effective task status.
 */
export const SetupTaskStatus = {
  Todo: "todo",
  InProgress: "in_progress",
  AwaitingSupport: "awaiting_support",
  Done: "done",
} as const;
/**
 * Effective task status.
 */
export type SetupTaskStatus = ClosedEnum<typeof SetupTaskStatus>;

export type SetupTask = {
  assignee?: SetupTaskAssignee | undefined;
  /**
   * Incomplete prerequisite task keys.
   */
  blockedBy: Array<string>;
  /**
   * Whether current organization facts force the effective status to done. This field is read-only.
   */
  completedByFact: boolean;
  /**
   * Task description.
   */
  description: string;
  /**
   * Whether a platform administrator hid the task.
   */
  hidden: boolean;
  /**
   * Stable code-owned task key.
   */
  key: string;
  /**
   * Effective task status.
   */
  status: SetupTaskStatus;
  /**
   * Task title.
   */
  title: string;
};

/** @internal */
export const SetupTaskStatus$inboundSchema: z.ZodMiniEnum<
  typeof SetupTaskStatus
> = z.enum(SetupTaskStatus);

/** @internal */
export const SetupTask$inboundSchema: z.ZodMiniType<SetupTask, unknown> = z
  .pipe(
    z.object({
      assignee: z.optional(SetupTaskAssignee$inboundSchema),
      blocked_by: z.array(z.string()),
      completed_by_fact: z.boolean(),
      description: z.string(),
      hidden: z.boolean(),
      key: z.string(),
      status: SetupTaskStatus$inboundSchema,
      title: z.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "blocked_by": "blockedBy",
        "completed_by_fact": "completedByFact",
      });
    }),
  );

export function setupTaskFromJSON(
  jsonString: string,
): SafeParseResult<SetupTask, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SetupTask$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SetupTask' from JSON`,
  );
}
