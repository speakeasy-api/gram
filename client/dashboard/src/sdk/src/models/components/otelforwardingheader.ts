/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * HTTP header forwarded with each OTEL payload.
 */
export type OtelForwardingHeader = {
  /**
   * Whether a non-empty value is currently stored for this header. Always false on write-only operations.
   */
  hasValue: boolean;
  /**
   * Header name.
   */
  name: string;
};

/** @internal */
export const OtelForwardingHeader$inboundSchema: z.ZodMiniType<
  OtelForwardingHeader,
  unknown
> = z.pipe(
  z.object({
    has_value: z.boolean(),
    name: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "has_value": "hasValue",
    });
  }),
);

export function otelForwardingHeaderFromJSON(
  jsonString: string,
): SafeParseResult<OtelForwardingHeader, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OtelForwardingHeader$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OtelForwardingHeader' from JSON`,
  );
}
