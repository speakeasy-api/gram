/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SupportCoverageCell,
  SupportCoverageCell$inboundSchema,
} from "./supportcoveragecell.js";
import {
  SupportCoverageUnmapped,
  SupportCoverageUnmapped$inboundSchema,
} from "./supportcoverageunmapped.js";

/**
 * Observed support coverage for the caller's organization over a fixed observation window.
 */
export type SupportCoverageResult = {
  /**
   * One cell per (capability, surface) pair. Always fully populated, so clients never infer a missing pair as unsupported.
   */
  cells: Array<SupportCoverageCell>;
  /**
   * RFC3339 start of the observation window.
   */
  from: Date;
  /**
   * RFC3339 end of the observation window.
   */
  to: Date;
  /**
   * Activity whose hook_source folded to no surface. Non-empty means the matrix is not showing everything the org did.
   */
  unmapped: Array<SupportCoverageUnmapped>;
  /**
   * Length of the observation window in days.
   */
  windowDays: number;
};

/** @internal */
export const SupportCoverageResult$inboundSchema: z.ZodMiniType<
  SupportCoverageResult,
  unknown
> = z.pipe(
  z.object({
    cells: z.array(SupportCoverageCell$inboundSchema),
    from: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    to: z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    unmapped: z.array(SupportCoverageUnmapped$inboundSchema),
    window_days: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "window_days": "windowDays",
    });
  }),
);

export function supportCoverageResultFromJSON(
  jsonString: string,
): SafeParseResult<SupportCoverageResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportCoverageResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportCoverageResult' from JSON`,
  );
}
