/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GatewayReviewedTool = {
  /**
   * Full MCP tool definition as formatted JSON text, preserving exact schema numbers for human review.
   */
  definition: string;
  /**
   * Fingerprint of the full definition and routing identity.
   */
  fingerprint: string;
  /**
   * Qualified tool name.
   */
  name: string;
};

/** @internal */
export const GatewayReviewedTool$inboundSchema: z.ZodMiniType<
  GatewayReviewedTool,
  unknown
> = z.object({
  definition: z.string(),
  fingerprint: z.string(),
  name: z.string(),
});

export function gatewayReviewedToolFromJSON(
  jsonString: string,
): SafeParseResult<GatewayReviewedTool, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GatewayReviewedTool$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GatewayReviewedTool' from JSON`,
  );
}
