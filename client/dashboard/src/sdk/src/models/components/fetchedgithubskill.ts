/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SkillValidationError,
  SkillValidationError$inboundSchema,
} from "./skillvalidationerror.js";

/**
 * A parsed SKILL.md fetched from a public GitHub repository.
 */
export type FetchedGitHubSkill = {
  /**
   * The exact SKILL.md content fetched from GitHub.
   */
  content: string;
  /**
   * The optional skill description.
   */
  description?: string | undefined;
  /**
   * The skill name as written in frontmatter.
   */
  displayName: string;
  /**
   * The normalized skill name.
   */
  name: string;
  /**
   * The repository-relative SKILL.md path.
   */
  path: string;
  /**
   * Whether the manifest conforms to the Agent Skills specification.
   */
  specValid: boolean;
  /**
   * Specification validation problems.
   */
  validationErrors: Array<SkillValidationError>;
};

/** @internal */
export const FetchedGitHubSkill$inboundSchema: z.ZodMiniType<
  FetchedGitHubSkill,
  unknown
> = z.pipe(
  z.object({
    content: z.string(),
    description: z.optional(z.string()),
    display_name: z.string(),
    name: z.string(),
    path: z.string(),
    spec_valid: z.boolean(),
    validation_errors: z.array(SkillValidationError$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
      "spec_valid": "specValid",
      "validation_errors": "validationErrors",
    });
  }),
);

export function fetchedGitHubSkillFromJSON(
  jsonString: string,
): SafeParseResult<FetchedGitHubSkill, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FetchedGitHubSkill$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FetchedGitHubSkill' from JSON`,
  );
}
