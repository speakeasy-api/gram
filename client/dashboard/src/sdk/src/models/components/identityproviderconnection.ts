/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  IdentityProviderVerifyEvidence,
  IdentityProviderVerifyEvidence$inboundSchema,
} from "./identityproviderverifyevidence.js";

export const IdentityProviderConnectionKind = {
  Okta: "okta",
} as const;
export type IdentityProviderConnectionKind = ClosedEnum<
  typeof IdentityProviderConnectionKind
>;

export const IdentityProviderConnectionStatus = {
  Pending: "pending",
  AwaitingVerification: "awaiting_verification",
  Active: "active",
  Failed: "failed",
} as const;
export type IdentityProviderConnectionStatus = ClosedEnum<
  typeof IdentityProviderConnectionStatus
>;

/**
 * An organization identity provider connection. Credentials and private signing material are never returned.
 */
export type IdentityProviderConnection = {
  capabilities: Array<string>;
  /**
   * Identifier of the configured identity provider application.
   */
  clientId?: string | undefined;
  createdAt: Date;
  displayName?: string | undefined;
  grantedScopes: Array<string>;
  id: string;
  /**
   * Absolute URL of the Speakeasy-hosted public JSON Web Key Set.
   */
  jwksUrl: string;
  kind: IdentityProviderConnectionKind;
  lastVerifiedAt?: Date | undefined;
  /**
   * RFC 7638 thumbprint of the active signing key.
   */
  signingKeyKid: string;
  status: IdentityProviderConnectionStatus;
  statusDetail?: string | undefined;
  tenantIdentifier: string;
  updatedAt: Date;
  verifyEvidence?: IdentityProviderVerifyEvidence | undefined;
};

/** @internal */
export const IdentityProviderConnectionKind$inboundSchema: z.ZodMiniEnum<
  typeof IdentityProviderConnectionKind
> = z.enum(IdentityProviderConnectionKind);

/** @internal */
export const IdentityProviderConnectionStatus$inboundSchema: z.ZodMiniEnum<
  typeof IdentityProviderConnectionStatus
> = z.enum(IdentityProviderConnectionStatus);

/** @internal */
export const IdentityProviderConnection$inboundSchema: z.ZodMiniType<
  IdentityProviderConnection,
  unknown
> = z.pipe(
  z.object({
    capabilities: z.array(z.string()),
    client_id: z.optional(z.string()),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    display_name: z.optional(z.string()),
    granted_scopes: z.array(z.string()),
    id: z.string(),
    jwks_url: z.string(),
    kind: IdentityProviderConnectionKind$inboundSchema,
    last_verified_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    signing_key_kid: z.string(),
    status: IdentityProviderConnectionStatus$inboundSchema,
    status_detail: z.optional(z.string()),
    tenant_identifier: z.string(),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    verify_evidence: z.optional(IdentityProviderVerifyEvidence$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "client_id": "clientId",
      "created_at": "createdAt",
      "display_name": "displayName",
      "granted_scopes": "grantedScopes",
      "jwks_url": "jwksUrl",
      "last_verified_at": "lastVerifiedAt",
      "signing_key_kid": "signingKeyKid",
      "status_detail": "statusDetail",
      "tenant_identifier": "tenantIdentifier",
      "updated_at": "updatedAt",
      "verify_evidence": "verifyEvidence",
    });
  }),
);

export function identityProviderConnectionFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderConnection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderConnection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderConnection' from JSON`,
  );
}
