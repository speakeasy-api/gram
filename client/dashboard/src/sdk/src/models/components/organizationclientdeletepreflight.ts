/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Authoritative impact summary for deleting a remote_session_client: how many sessions it holds and the names of the MCP servers it is attached to.
 */
export type OrganizationClientDeletePreflight = {
  /**
   * Display names of MCP servers this client is attached to.
   */
  mcpServerNames: Array<string>;
  /**
   * Number of non-deleted remote_sessions minted against this client.
   */
  sessionCount: number;
};

/** @internal */
export const OrganizationClientDeletePreflight$inboundSchema: z.ZodMiniType<
  OrganizationClientDeletePreflight,
  unknown
> = z.pipe(
  z.object({
    mcp_server_names: z.array(z.string()),
    session_count: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "mcp_server_names": "mcpServerNames",
      "session_count": "sessionCount",
    });
  }),
);

export function organizationClientDeletePreflightFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationClientDeletePreflight, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationClientDeletePreflight$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationClientDeletePreflight' from JSON`,
  );
}
