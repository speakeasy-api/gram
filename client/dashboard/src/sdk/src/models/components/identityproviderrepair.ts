/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type IdentityProviderRepair = {
  deepLink?: string | undefined;
  fallbackAvailable: boolean;
  instructions: Array<string>;
  title: string;
};

/** @internal */
export const IdentityProviderRepair$inboundSchema: z.ZodMiniType<
  IdentityProviderRepair,
  unknown
> = z.pipe(
  z.object({
    deep_link: z.optional(z.string()),
    fallback_available: z.boolean(),
    instructions: z.array(z.string()),
    title: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "deep_link": "deepLink",
      "fallback_available": "fallbackAvailable",
    });
  }),
);

export function identityProviderRepairFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderRepair, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderRepair$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderRepair' from JSON`,
  );
}
