/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type TriggerRequestBody = {
  /**
   * Start of the message window (inclusive).
   */
  from: Date;
  /**
   * The project whose messages to re-scan.
   */
  projectId: string;
  /**
   * The risk policy to scan against. Must be enabled.
   */
  riskPolicyId: string;
  /**
   * End of the message window (exclusive). Defaults to now.
   */
  to?: Date | undefined;
};

/** @internal */
export type TriggerRequestBody$Outbound = {
  from: string;
  project_id: string;
  risk_policy_id: string;
  to?: string | undefined;
};

/** @internal */
export const TriggerRequestBody$outboundSchema: z.ZodMiniType<
  TriggerRequestBody$Outbound,
  TriggerRequestBody
> = z.pipe(
  z.object({
    from: z.pipe(z.date(), z.transform(v => v.toISOString())),
    projectId: z.string(),
    riskPolicyId: z.string(),
    to: z.optional(z.pipe(z.date(), z.transform(v => v.toISOString()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      projectId: "project_id",
      riskPolicyId: "risk_policy_id",
    });
  }),
);

export function triggerRequestBodyToJSON(
  triggerRequestBody: TriggerRequestBody,
): string {
  return JSON.stringify(
    TriggerRequestBody$outboundSchema.parse(triggerRequestBody),
  );
}
