/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A SKILL.md candidate that was excluded from the results.
 */
export type GitHubSkillIssue = {
  /**
   * A safe explanation of why the manifest was excluded.
   */
  message: string;
  /**
   * The repository-relative candidate path.
   */
  path: string;
};

/** @internal */
export const GitHubSkillIssue$inboundSchema: z.ZodMiniType<
  GitHubSkillIssue,
  unknown
> = z.object({
  message: z.string(),
  path: z.string(),
});

export function gitHubSkillIssueFromJSON(
  jsonString: string,
): SafeParseResult<GitHubSkillIssue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GitHubSkillIssue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GitHubSkillIssue' from JSON`,
  );
}
