/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingAnswers,
  OnboardingAnswers$inboundSchema,
} from "./onboardinganswers.js";
import {
  OnboardingStep,
  OnboardingStep$inboundSchema,
} from "./onboardingstep.js";

/**
 * Where an organization is in onboarding.
 */
export type OnboardingState = {
  /**
   * What an organization admin told us during onboarding.
   */
  answers?: OnboardingAnswers | undefined;
  /**
   * True once the selected use case is covered
   */
  done: boolean;
  /**
   * One recommended action, verified against real evidence.
   */
  nextStep?: OnboardingStep | undefined;
  /**
   * Every step planned for the answers, in order, with verification state
   */
  steps: Array<OnboardingStep>;
};

/** @internal */
export const OnboardingState$inboundSchema: z.ZodMiniType<
  OnboardingState,
  unknown
> = z.pipe(
  z.object({
    answers: z.optional(OnboardingAnswers$inboundSchema),
    done: z.boolean(),
    next_step: z.optional(OnboardingStep$inboundSchema),
    steps: z.array(OnboardingStep$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_step": "nextStep",
    });
  }),
);

export function onboardingStateFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingState, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingState$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingState' from JSON`,
  );
}
