/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Evidence used to produce the match.
 */
export const Basis = {
  Name: "name",
  CatalogIdentifier: "catalog_identifier",
  SignOnDomain: "sign_on_domain",
} as const;
/**
 * Evidence used to produce the match.
 */
export type Basis = ClosedEnum<typeof Basis>;

/**
 * Confidence in the catalogue match.
 */
export const Confidence = {
  Exact: "exact",
  Likely: "likely",
  None: "none",
} as const;
/**
 * Confidence in the catalogue match.
 */
export type Confidence = ClosedEnum<typeof Confidence>;

/**
 * A name-based match between an identity provider application and the Speakeasy MCP server catalogue.
 */
export type IdentityProviderApplicationMatch = {
  /**
   * Evidence used to produce the match.
   */
  basis: Basis;
  /**
   * Catalogue entry reference.
   */
  catalogRef: string;
  /**
   * Confidence in the catalogue match.
   */
  confidence: Confidence;
  /**
   * Catalogue entry name.
   */
  name: string;
  /**
   * Opaque catalogue provider key.
   */
  providerKey: string;
  /**
   * Streamable HTTP endpoint from the inspected catalogue entry.
   */
  remoteUrl: string;
};

/** @internal */
export const Basis$inboundSchema: z.ZodMiniEnum<typeof Basis> = z.enum(Basis);

/** @internal */
export const Confidence$inboundSchema: z.ZodMiniEnum<typeof Confidence> = z
  .enum(Confidence);

/** @internal */
export const IdentityProviderApplicationMatch$inboundSchema: z.ZodMiniType<
  IdentityProviderApplicationMatch,
  unknown
> = z.pipe(
  z.object({
    basis: Basis$inboundSchema,
    catalog_ref: z.string(),
    confidence: Confidence$inboundSchema,
    name: z.string(),
    provider_key: z.string(),
    remote_url: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "catalog_ref": "catalogRef",
      "provider_key": "providerKey",
      "remote_url": "remoteUrl",
    });
  }),
);

export function identityProviderApplicationMatchFromJSON(
  jsonString: string,
): SafeParseResult<IdentityProviderApplicationMatch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IdentityProviderApplicationMatch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IdentityProviderApplicationMatch' from JSON`,
  );
}
