/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingOption,
  OnboardingOption$inboundSchema,
} from "./onboardingoption.js";
import {
  OnboardingProduct,
  OnboardingProduct$inboundSchema,
} from "./onboardingproduct.js";

/**
 * Everything the wizard needs to render its questions.
 */
export type OnboardingReferenceData = {
  /**
   * MDM vendors the admin can declare
   */
  mdmVendors: Array<OnboardingOption>;
  /**
   * Products the admin can pick, in display order
   */
  products: Array<OnboardingProduct>;
  /**
   * Use cases the admin can pick exactly one of
   */
  useCases: Array<OnboardingOption>;
};

/** @internal */
export const OnboardingReferenceData$inboundSchema: z.ZodMiniType<
  OnboardingReferenceData,
  unknown
> = z.pipe(
  z.object({
    mdm_vendors: z.array(OnboardingOption$inboundSchema),
    products: z.array(OnboardingProduct$inboundSchema),
    use_cases: z.array(OnboardingOption$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "mdm_vendors": "mdmVendors",
      "use_cases": "useCases",
    });
  }),
);

export function onboardingReferenceDataFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingReferenceData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingReferenceData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingReferenceData' from JSON`,
  );
}
