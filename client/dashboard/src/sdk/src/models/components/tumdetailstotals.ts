/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Whole-range totals for the billing usage details
 */
export type TumDetailsTotals = {
  /**
   * Billed input tokens
   */
  inputTokens: number;
  /**
   * Billed output tokens
   */
  outputTokens: number;
  /**
   * Billed tokens under management
   */
  totalTokens: number;
};

/** @internal */
export const TumDetailsTotals$inboundSchema: z.ZodMiniType<
  TumDetailsTotals,
  unknown
> = z.pipe(
  z.object({
    input_tokens: z.int(),
    output_tokens: z.int(),
    total_tokens: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "input_tokens": "inputTokens",
      "output_tokens": "outputTokens",
      "total_tokens": "totalTokens",
    });
  }),
);

export function tumDetailsTotalsFromJSON(
  jsonString: string,
): SafeParseResult<TumDetailsTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TumDetailsTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TumDetailsTotals' from JSON`,
  );
}
