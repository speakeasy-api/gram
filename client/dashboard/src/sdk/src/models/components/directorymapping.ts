/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { RoleGrant, RoleGrant$inboundSchema } from "./rolegrant.js";

/**
 * Whether this mapping targets a directory group or an identity-provider attribute.
 */
export const Kind = {
  Group: "group",
  Attribute: "attribute",
} as const;
/**
 * Whether this mapping targets a directory group or an identity-provider attribute.
 */
export type Kind = ClosedEnum<typeof Kind>;

export type DirectoryMapping = {
  /**
   * Identity-provider attribute key when kind is attribute.
   */
  attributeKey?: string | undefined;
  /**
   * Identity-provider attribute value when kind is attribute.
   */
  attributeValue?: string | undefined;
  /**
   * Scope grants assigned to this directory principal.
   */
  grants: Array<RoleGrant>;
  /**
   * Directory group ID when kind is group.
   */
  groupId?: string | undefined;
  /**
   * Directory group display name when kind is group.
   */
  groupName?: string | undefined;
  /**
   * Whether this mapping targets a directory group or an identity-provider attribute.
   */
  kind: Kind;
  /**
   * Number of active directory members that currently match this mapping.
   */
  memberCount: number;
  /**
   * Directory group or attribute principal URN.
   */
  principalUrn: string;
};

/** @internal */
export const Kind$inboundSchema: z.ZodMiniEnum<typeof Kind> = z.enum(Kind);

/** @internal */
export const DirectoryMapping$inboundSchema: z.ZodMiniType<
  DirectoryMapping,
  unknown
> = z.pipe(
  z.object({
    attribute_key: z.optional(z.string()),
    attribute_value: z.optional(z.string()),
    grants: z.array(RoleGrant$inboundSchema),
    group_id: z.optional(z.string()),
    group_name: z.optional(z.string()),
    kind: Kind$inboundSchema,
    member_count: z.int(),
    principal_urn: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "attribute_key": "attributeKey",
      "attribute_value": "attributeValue",
      "group_id": "groupId",
      "group_name": "groupName",
      "member_count": "memberCount",
      "principal_urn": "principalUrn",
    });
  }),
);

export function directoryMappingFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryMapping, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryMapping$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryMapping' from JSON`,
  );
}
