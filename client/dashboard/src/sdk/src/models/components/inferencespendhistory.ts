/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  InferenceSpendMonth,
  InferenceSpendMonth$inboundSchema,
} from "./inferencespendmonth.js";

export type InferenceSpendHistory = {
  /**
   * UTC calendar months with recorded spend, newest first. The current month is always present.
   */
  months: Array<InferenceSpendMonth>;
};

/** @internal */
export const InferenceSpendHistory$inboundSchema: z.ZodMiniType<
  InferenceSpendHistory,
  unknown
> = z.object({
  months: z.array(InferenceSpendMonth$inboundSchema),
});

export function inferenceSpendHistoryFromJSON(
  jsonString: string,
): SafeParseResult<InferenceSpendHistory, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InferenceSpendHistory$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InferenceSpendHistory' from JSON`,
  );
}
