/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Where the challenge got to.
 */
export const RiskPolicyChallengeStatus = {
  Challenged: "challenged",
  Acknowledged: "acknowledged",
  Declined: "declined",
} as const;
/**
 * Where the challenge got to.
 */
export type RiskPolicyChallengeStatus = ClosedEnum<
  typeof RiskPolicyChallengeStatus
>;

/**
 * One warn/challenge lifecycle record: a warn-action policy matched, and the user was asked to acknowledge before proceeding.
 */
export type RiskPolicyChallenge = {
  /**
   * When the user acknowledged, if they did.
   */
  acknowledgedAt?: Date | undefined;
  /**
   * When the warning was issued.
   */
  challengedAt: Date;
  /**
   * Log-safe description of what was flagged. Never the matched value itself.
   */
  entity?: string | undefined;
  /**
   * When the acknowledgement stops suppressing re-challenge.
   */
  expiresAt?: Date | undefined;
  /**
   * Challenge identifier.
   */
  id: string;
  /**
   * Display name of the policy that issued the warning.
   */
  policyName?: string | undefined;
  /**
   * The policy that issued the warning.
   */
  riskPolicyId: string;
  /**
   * The rule that matched.
   */
  ruleId?: string | undefined;
  /**
   * Where the challenge got to.
   */
  status: RiskPolicyChallengeStatus;
  /**
   * The tool the challenge applies to. Absent for a non-tool challenge.
   */
  toolName?: string | undefined;
  /**
   * The user identifier the challenge was recorded against.
   */
  userId: string;
};

/** @internal */
export const RiskPolicyChallengeStatus$inboundSchema: z.ZodMiniEnum<
  typeof RiskPolicyChallengeStatus
> = z.enum(RiskPolicyChallengeStatus);

/** @internal */
export const RiskPolicyChallenge$inboundSchema: z.ZodMiniType<
  RiskPolicyChallenge,
  unknown
> = z.pipe(
  z.object({
    acknowledged_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    challenged_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    entity: z.optional(z.string()),
    expires_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    id: z.string(),
    policy_name: z.optional(z.string()),
    risk_policy_id: z.string(),
    rule_id: z.optional(z.string()),
    status: RiskPolicyChallengeStatus$inboundSchema,
    tool_name: z.optional(z.string()),
    user_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "acknowledged_at": "acknowledgedAt",
      "challenged_at": "challengedAt",
      "expires_at": "expiresAt",
      "policy_name": "policyName",
      "risk_policy_id": "riskPolicyId",
      "rule_id": "ruleId",
      "tool_name": "toolName",
      "user_id": "userId",
    });
  }),
);

export function riskPolicyChallengeFromJSON(
  jsonString: string,
): SafeParseResult<RiskPolicyChallenge, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RiskPolicyChallenge$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RiskPolicyChallenge' from JSON`,
  );
}
