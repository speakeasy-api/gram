/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RotatedObservabilityKey = {
  /**
   * The API key ID.
   */
  id: string;
  /**
   * The recognizable prefix of the previous key.
   */
  keyPrefix: string;
  /**
   * The API key name.
   */
  name: string;
};

/** @internal */
export const RotatedObservabilityKey$inboundSchema: z.ZodMiniType<
  RotatedObservabilityKey,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    key_prefix: z.string(),
    name: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "key_prefix": "keyPrefix",
    });
  }),
);

export function rotatedObservabilityKeyFromJSON(
  jsonString: string,
): SafeParseResult<RotatedObservabilityKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RotatedObservabilityKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RotatedObservabilityKey' from JSON`,
  );
}
