/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Progress counters for an ad-hoc risk analysis run.
 */
export type AdhocRiskAnalysisProgress = {
  /**
   * Analysis batches that completed successfully.
   */
  batchesCompleted: number;
  /**
   * Analysis batches that failed after retries.
   */
  batchesFailed: number;
  /**
   * Messages dispatched for scanning so far.
   */
  dispatchedMessages: number;
  /**
   * Risk findings recorded so far.
   */
  findings: number;
  /**
   * Number of policies being scanned.
   */
  policies: number;
  /**
   * Messages scanned across all completed batches.
   */
  processedMessages: number;
  /**
   * Total messages in the requested time window.
   */
  totalMessages: number;
};

/** @internal */
export const AdhocRiskAnalysisProgress$inboundSchema: z.ZodMiniType<
  AdhocRiskAnalysisProgress,
  unknown
> = z.pipe(
  z.object({
    batches_completed: z.int(),
    batches_failed: z.int(),
    dispatched_messages: z.int(),
    findings: z.int(),
    policies: z.int(),
    processed_messages: z.int(),
    total_messages: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "batches_completed": "batchesCompleted",
      "batches_failed": "batchesFailed",
      "dispatched_messages": "dispatchedMessages",
      "processed_messages": "processedMessages",
      "total_messages": "totalMessages",
    });
  }),
);

export function adhocRiskAnalysisProgressFromJSON(
  jsonString: string,
): SafeParseResult<AdhocRiskAnalysisProgress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdhocRiskAnalysisProgress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdhocRiskAnalysisProgress' from JSON`,
  );
}
