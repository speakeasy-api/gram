/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OnboardingSelectedProvider = {
  /**
   * Slug of one of the provider's plans. Omitted only when the provider has no plans.
   */
  planSlug?: string | undefined;
  /**
   * Slug of a provider from the reference data
   */
  providerSlug: string;
};

/** @internal */
export const OnboardingSelectedProvider$inboundSchema: z.ZodMiniType<
  OnboardingSelectedProvider,
  unknown
> = z.pipe(
  z.object({
    plan_slug: z.optional(z.string()),
    provider_slug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_slug": "planSlug",
      "provider_slug": "providerSlug",
    });
  }),
);
/** @internal */
export type OnboardingSelectedProvider$Outbound = {
  plan_slug?: string | undefined;
  provider_slug: string;
};

/** @internal */
export const OnboardingSelectedProvider$outboundSchema: z.ZodMiniType<
  OnboardingSelectedProvider$Outbound,
  OnboardingSelectedProvider
> = z.pipe(
  z.object({
    planSlug: z.optional(z.string()),
    providerSlug: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      planSlug: "plan_slug",
      providerSlug: "provider_slug",
    });
  }),
);

export function onboardingSelectedProviderToJSON(
  onboardingSelectedProvider: OnboardingSelectedProvider,
): string {
  return JSON.stringify(
    OnboardingSelectedProvider$outboundSchema.parse(onboardingSelectedProvider),
  );
}
export function onboardingSelectedProviderFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingSelectedProvider, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingSelectedProvider$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingSelectedProvider' from JSON`,
  );
}
