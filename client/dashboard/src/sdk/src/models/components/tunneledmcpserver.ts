/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Derived live connection status for a tunneled MCP server source
 */
export const ConnectionStatus = {
  Connected: "connected",
  Inactive: "inactive",
  NeverConnected: "never_connected",
} as const;
/**
 * Derived live connection status for a tunneled MCP server source
 */
export type ConnectionStatus = ClosedEnum<typeof ConnectionStatus>;

/**
 * Stored lifecycle status for a tunneled MCP server source
 */
export const TunneledMcpServerStatus = {
  Created: "created",
  Active: "active",
  Revoked: "revoked",
} as const;
/**
 * Stored lifecycle status for a tunneled MCP server source
 */
export type TunneledMcpServerStatus = ClosedEnum<
  typeof TunneledMcpServerStatus
>;

/**
 * A customer-hosted MCP server connected through a tunnel
 */
export type TunneledMcpServer = {
  /**
   * Number of active tunnel connections currently visible in Redis
   */
  activeConnectionCount: number;
  /**
   * Total MCP consumer sessions currently pinned across active tunnel connections
   */
  activeConsumerSessionCount: number;
  /**
   * Most recent agent version reported by the tunnel
   */
  agentVersion?: string | undefined;
  /**
   * Whether the owner has consented to serving this source through a public, anonymous MCP endpoint
   */
  allowPublic: boolean;
  /**
   * Derived live connection status for a tunneled MCP server source
   */
  connectionStatus: ConnectionStatus;
  /**
   * When the tunneled MCP server source was created
   */
  createdAt: Date;
  /**
   * The ID of the tunneled MCP server
   */
  id: string;
  /**
   * Non-secret prefix of the tunnel key
   */
  keyPrefix: string;
  /**
   * Most recent persisted heartbeat timestamp
   */
  lastSeenAt?: Date | undefined;
  /**
   * Human-readable name for the tunneled MCP server
   */
  name: string;
  /**
   * The project ID this tunneled MCP server belongs to
   */
  projectId: string;
  /**
   * Stored lifecycle status for a tunneled MCP server source
   */
  status: TunneledMcpServerStatus;
  /**
   * When the tunneled MCP server source was last updated
   */
  updatedAt: Date;
};

/** @internal */
export const ConnectionStatus$inboundSchema: z.ZodMiniEnum<
  typeof ConnectionStatus
> = z.enum(ConnectionStatus);

/** @internal */
export const TunneledMcpServerStatus$inboundSchema: z.ZodMiniEnum<
  typeof TunneledMcpServerStatus
> = z.enum(TunneledMcpServerStatus);

/** @internal */
export const TunneledMcpServer$inboundSchema: z.ZodMiniType<
  TunneledMcpServer,
  unknown
> = z.pipe(
  z.object({
    active_connection_count: z.int(),
    active_consumer_session_count: z.int(),
    agent_version: z.optional(z.string()),
    allow_public: z.boolean(),
    connection_status: ConnectionStatus$inboundSchema,
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    id: z.string(),
    key_prefix: z.string(),
    last_seen_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    name: z.string(),
    project_id: z.string(),
    status: TunneledMcpServerStatus$inboundSchema,
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "active_connection_count": "activeConnectionCount",
      "active_consumer_session_count": "activeConsumerSessionCount",
      "agent_version": "agentVersion",
      "allow_public": "allowPublic",
      "connection_status": "connectionStatus",
      "created_at": "createdAt",
      "key_prefix": "keyPrefix",
      "last_seen_at": "lastSeenAt",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function tunneledMcpServerFromJSON(
  jsonString: string,
): SafeParseResult<TunneledMcpServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TunneledMcpServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TunneledMcpServer' from JSON`,
  );
}
