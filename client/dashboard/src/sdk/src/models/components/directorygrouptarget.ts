/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DirectoryGroupTarget = {
  /**
   * Directory group ID.
   */
  id: string;
  /**
   * Number of active directory members in this group.
   */
  memberCount: number;
  /**
   * Directory group display name.
   */
  name: string;
  /**
   * Canonical principal URN for this group.
   */
  principalUrn: string;
};

/** @internal */
export const DirectoryGroupTarget$inboundSchema: z.ZodMiniType<
  DirectoryGroupTarget,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    member_count: z.int(),
    name: z.string(),
    principal_urn: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "member_count": "memberCount",
      "principal_urn": "principalUrn",
    });
  }),
);

export function directoryGroupTargetFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryGroupTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryGroupTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryGroupTarget' from JSON`,
  );
}
