/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OnboardingStep,
  OnboardingStep$inboundSchema,
} from "./onboardingstep.js";

export const OnboardingUseCaseStatusUseCase = {
  Observability: "observability",
  CostTracking: "cost-tracking",
  Security: "security",
  McpGateway: "mcp-gateway",
} as const;
export type OnboardingUseCaseStatusUseCase = ClosedEnum<
  typeof OnboardingUseCaseStatusUseCase
>;

export type OnboardingUseCaseStatus = {
  /**
   * One recommended action, verified against real evidence.
   */
  nextStep?: OnboardingStep | undefined;
  /**
   * Whether this is the use case the admin picked
   */
  selected: boolean;
  useCase: OnboardingUseCaseStatusUseCase;
  /**
   * Whether the organization has evidence for this use case in the last 30 days
   */
  verified: boolean;
};

/** @internal */
export const OnboardingUseCaseStatusUseCase$inboundSchema: z.ZodMiniEnum<
  typeof OnboardingUseCaseStatusUseCase
> = z.enum(OnboardingUseCaseStatusUseCase);

/** @internal */
export const OnboardingUseCaseStatus$inboundSchema: z.ZodMiniType<
  OnboardingUseCaseStatus,
  unknown
> = z.pipe(
  z.object({
    next_step: z.optional(OnboardingStep$inboundSchema),
    selected: z.boolean(),
    use_case: OnboardingUseCaseStatusUseCase$inboundSchema,
    verified: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_step": "nextStep",
      "use_case": "useCase",
    });
  }),
);

export function onboardingUseCaseStatusFromJSON(
  jsonString: string,
): SafeParseResult<OnboardingUseCaseStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnboardingUseCaseStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnboardingUseCaseStatus' from JSON`,
  );
}
