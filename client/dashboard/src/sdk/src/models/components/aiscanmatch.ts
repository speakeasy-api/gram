/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * One AI detection target a device-agent scan matched.
 */
export type AIScanMatch = {
  /**
   * Target category the agent scanned under: harness or local_model. The server catalog's category wins for targets it knows; this is what gets stored for the rest.
   */
  category: string;
  /**
   * What the scan observed: installed or running.
   */
  signal: string;
  /**
   * Identifier of the matched target from the agent's compiled-in list (e.g. claude-code, ollama). Stored as reported: an agent binary can ship a newer target list than the server catalog knows.
   */
  targetId: string;
  /**
   * Installed version, when the scan could read one statically (e.g. from the app bundle's Info.plist).
   */
  version?: string | undefined;
};

/** @internal */
export type AIScanMatch$Outbound = {
  category: string;
  signal: string;
  target_id: string;
  version?: string | undefined;
};

/** @internal */
export const AIScanMatch$outboundSchema: z.ZodMiniType<
  AIScanMatch$Outbound,
  AIScanMatch
> = z.pipe(
  z.object({
    category: z.string(),
    signal: z.string(),
    targetId: z.string(),
    version: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      targetId: "target_id",
    });
  }),
);

export function aiScanMatchToJSON(aiScanMatch: AIScanMatch): string {
  return JSON.stringify(AIScanMatch$outboundSchema.parse(aiScanMatch));
}
