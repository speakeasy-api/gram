/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A pass-through (unproxied) MCP server configuration
 */
export type PassthroughMcpServer = {
  /**
   * When the pass-through MCP server was created
   */
  createdAt: Date;
  /**
   * Optional description shown alongside the server.
   */
  description?: string | undefined;
  /**
   * The ID of the pass-through MCP server
   */
  id: string;
  /**
   * Optional human-readable name for the pass-through MCP server
   */
  name?: string | undefined;
  /**
   * The project ID this pass-through MCP server belongs to
   */
  projectId: string;
  /**
   * URL-friendly slug derived from the URL and ID.
   */
  slug?: string | undefined;
  /**
   * When the pass-through MCP server was last updated
   */
  updatedAt: Date;
  /**
   * The URL of the vendor's MCP server
   */
  url: string;
};

/** @internal */
export const PassthroughMcpServer$inboundSchema: z.ZodMiniType<
  PassthroughMcpServer,
  unknown
> = z.pipe(
  z.object({
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    description: z.optional(z.string()),
    id: z.string(),
    name: z.optional(z.string()),
    project_id: z.string(),
    slug: z.optional(z.string()),
    updated_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    url: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "project_id": "projectId",
      "updated_at": "updatedAt",
    });
  }),
);

export function passthroughMcpServerFromJSON(
  jsonString: string,
): SafeParseResult<PassthroughMcpServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PassthroughMcpServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PassthroughMcpServer' from JSON`,
  );
}
