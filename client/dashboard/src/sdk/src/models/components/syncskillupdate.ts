/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A skill manifest the caller should write to local managed state.
 */
export type SyncSkillUpdate = {
  /**
   * The complete SKILL.md content to write.
   */
  content: string;
  /**
   * The optional description from the resolved manifest version.
   */
  description?: string | undefined;
  /**
   * The normalized skill name.
   */
  name: string;
  /**
   * The SHA-256 digest of content.
   */
  rawSha256: string;
};

/** @internal */
export const SyncSkillUpdate$inboundSchema: z.ZodMiniType<
  SyncSkillUpdate,
  unknown
> = z.pipe(
  z.object({
    content: z.string(),
    description: z.optional(z.string()),
    name: z.string(),
    raw_sha256: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "raw_sha256": "rawSha256",
    });
  }),
);

export function syncSkillUpdateFromJSON(
  jsonString: string,
): SafeParseResult<SyncSkillUpdate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncSkillUpdate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncSkillUpdate' from JSON`,
  );
}
