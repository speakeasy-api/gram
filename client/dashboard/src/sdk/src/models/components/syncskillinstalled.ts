/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * A Gram-managed skill currently present on the caller's machine.
 */
export type SyncSkillInstalled = {
  /**
   * The normalized skill name.
   */
  name: string;
  /**
   * The SHA-256 digest of the exact local SKILL.md content.
   */
  rawSha256: string;
};

/** @internal */
export type SyncSkillInstalled$Outbound = {
  name: string;
  raw_sha256: string;
};

/** @internal */
export const SyncSkillInstalled$outboundSchema: z.ZodMiniType<
  SyncSkillInstalled$Outbound,
  SyncSkillInstalled
> = z.pipe(
  z.object({
    name: z.string(),
    rawSha256: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      rawSha256: "raw_sha256",
    });
  }),
);

export function syncSkillInstalledToJSON(
  syncSkillInstalled: SyncSkillInstalled,
): string {
  return JSON.stringify(
    SyncSkillInstalled$outboundSchema.parse(syncSkillInstalled),
  );
}
