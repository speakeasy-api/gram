/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdhocRiskAnalysisProgress,
  AdhocRiskAnalysisProgress$inboundSchema,
} from "./adhocriskanalysisprogress.js";

/**
 * Status of a project's most recent ad-hoc risk analysis run.
 */
export type AdhocRiskAnalysisStatus = {
  /**
   * When the run finished, if it has.
   */
  closedAt?: Date | undefined;
  /**
   * Progress counters for an ad-hoc risk analysis run.
   */
  progress?: AdhocRiskAnalysisProgress | undefined;
  /**
   * When the run started.
   */
  startedAt?: Date | undefined;
  /**
   * One of: none (no run ever triggered), running, completed, failed, canceled, terminated, timed_out.
   */
  status: string;
  /**
   * The Temporal workflow id of the run; empty when status is none.
   */
  workflowId: string;
};

/** @internal */
export const AdhocRiskAnalysisStatus$inboundSchema: z.ZodMiniType<
  AdhocRiskAnalysisStatus,
  unknown
> = z.pipe(
  z.object({
    closed_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    progress: z.optional(AdhocRiskAnalysisProgress$inboundSchema),
    started_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    status: z.string(),
    workflow_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "closed_at": "closedAt",
      "started_at": "startedAt",
      "workflow_id": "workflowId",
    });
  }),
);

export function adhocRiskAnalysisStatusFromJSON(
  jsonString: string,
): SafeParseResult<AdhocRiskAnalysisStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdhocRiskAnalysisStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdhocRiskAnalysisStatus' from JSON`,
  );
}
