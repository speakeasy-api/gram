/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The recorded organization decision behind state. Absent rows read as unreviewed, so every tool has one.
 */
export const AIToolAccessSummaryDecision = {
  Unreviewed: "unreviewed",
  Approved: "approved",
  Blocked: "blocked",
} as const;
/**
 * The recorded organization decision behind state. Absent rows read as unreviewed, so every tool has one.
 */
export type AIToolAccessSummaryDecision = ClosedEnum<
  typeof AIToolAccessSummaryDecision
>;

/**
 * The decision as a table renders it: allowed and blocked are the recorded organization decision, unreviewed means nobody has decided and nothing is enforced.
 */
export const AIToolAccessSummaryState = {
  Allowed: "allowed",
  Blocked: "blocked",
  Unreviewed: "unreviewed",
} as const;
/**
 * The decision as a table renders it: allowed and blocked are the recorded organization decision, unreviewed means nobody has decided and nothing is enforced.
 */
export type AIToolAccessSummaryState = ClosedEnum<
  typeof AIToolAccessSummaryState
>;

/**
 * The enforcement verdict for one detected AI tool, computed server-side so a client renders wording without re-deriving it. state shares its vocabulary with the shadow MCP inventory's access summary, so one column describes both halves of the Shadow AI section.
 */
export type AIToolAccessSummary = {
  /**
   * When the decision was last recorded.
   */
  decidedAt?: Date | undefined;
  /**
   * URN of the admin who last recorded the decision.
   */
  decidedBy?: string | undefined;
  /**
   * The recorded organization decision behind state. Absent rows read as unreviewed, so every tool has one.
   */
  decision: AIToolAccessSummaryDecision;
  /**
   * Whether a block on this tool would actually reach the gateway. False when the tool is linked only by a self-reported client name, or by nothing at all: the decision is recorded honestly and enforces nothing. Surfaced so an admin is never told a tool is blocked when it is not.
   */
  enforceable: boolean;
  /**
   * Why the decision was made, when an admin gave a reason.
   */
  rationale?: string | undefined;
  /**
   * The decision as a table renders it: allowed and blocked are the recorded organization decision, unreviewed means nobody has decided and nothing is enforced.
   */
  state: AIToolAccessSummaryState;
};

/** @internal */
export const AIToolAccessSummaryDecision$inboundSchema: z.ZodMiniEnum<
  typeof AIToolAccessSummaryDecision
> = z.enum(AIToolAccessSummaryDecision);

/** @internal */
export const AIToolAccessSummaryState$inboundSchema: z.ZodMiniEnum<
  typeof AIToolAccessSummaryState
> = z.enum(AIToolAccessSummaryState);

/** @internal */
export const AIToolAccessSummary$inboundSchema: z.ZodMiniType<
  AIToolAccessSummary,
  unknown
> = z.pipe(
  z.object({
    decided_at: z.optional(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    decided_by: z.optional(z.string()),
    decision: AIToolAccessSummaryDecision$inboundSchema,
    enforceable: z.boolean(),
    rationale: z.optional(z.string()),
    state: AIToolAccessSummaryState$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "decided_at": "decidedAt",
      "decided_by": "decidedBy",
    });
  }),
);

export function aiToolAccessSummaryFromJSON(
  jsonString: string,
): SafeParseResult<AIToolAccessSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AIToolAccessSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AIToolAccessSummary' from JSON`,
  );
}
