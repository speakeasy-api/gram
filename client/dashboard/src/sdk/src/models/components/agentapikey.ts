/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AgentAPIKey = {
  createdAt: Date;
  expiresAt?: Date | undefined;
  id: string;
  lastAccessedAt?: Date | undefined;
  name: string;
};

/** @internal */
export const AgentAPIKey$inboundSchema: z.ZodMiniType<AgentAPIKey, unknown> = z
  .pipe(
    z.object({
      created_at: z.pipe(
        z.iso.datetime({ offset: true }),
        z.transform(v => new Date(v)),
      ),
      expires_at: z.optional(
        z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
      ),
      id: z.string(),
      last_accessed_at: z.optional(
        z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
      ),
      name: z.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "expires_at": "expiresAt",
        "last_accessed_at": "lastAccessedAt",
      });
    }),
  );

export function agentAPIKeyFromJSON(
  jsonString: string,
): SafeParseResult<AgentAPIKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentAPIKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentAPIKey' from JSON`,
  );
}
