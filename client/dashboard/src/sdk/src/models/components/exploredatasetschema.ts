/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExploreFieldMeta,
  ExploreFieldMeta$inboundSchema,
} from "./explorefieldmeta.js";

/**
 * Customer-facing dataset category
 */
export const Category = {
  Event: "event",
  Usage: "usage",
} as const;
/**
 * Customer-facing dataset category
 */
export type Category = ClosedEnum<typeof Category>;

/**
 * Stable dataset ID
 */
export const Name = {
  Events: "events",
  TurnUsage: "turn_usage",
  UserUsage: "user_usage",
} as const;
/**
 * Stable dataset ID
 */
export type Name = ClosedEnum<typeof Name>;

/**
 * One labeled semantic dataset and its typed fields.
 */
export type ExploreDatasetSchema = {
  /**
   * Customer-facing dataset category
   */
  category: Category;
  /**
   * What the dataset contains
   */
  description: string;
  /**
   * Typed fields in display order
   */
  fields: Array<ExploreFieldMeta>;
  /**
   * What one canonical row represents
   */
  grain: string;
  /**
   * Customer-facing display name
   */
  label: string;
  /**
   * Stable dataset ID
   */
  name: Name;
};

/** @internal */
export const Category$inboundSchema: z.ZodMiniEnum<typeof Category> = z.enum(
  Category,
);

/** @internal */
export const Name$inboundSchema: z.ZodMiniEnum<typeof Name> = z.enum(Name);

/** @internal */
export const ExploreDatasetSchema$inboundSchema: z.ZodMiniType<
  ExploreDatasetSchema,
  unknown
> = z.object({
  category: Category$inboundSchema,
  description: z.string(),
  fields: z.array(ExploreFieldMeta$inboundSchema),
  grain: z.string(),
  label: z.string(),
  name: Name$inboundSchema,
});

export function exploreDatasetSchemaFromJSON(
  jsonString: string,
): SafeParseResult<ExploreDatasetSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExploreDatasetSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExploreDatasetSchema' from JSON`,
  );
}
