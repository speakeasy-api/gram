/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const ContractVersion = {
  LitellmActingPrincipalV1: "litellm-acting-principal.v1",
} as const;
export type ContractVersion = ClosedEnum<typeof ContractVersion>;

/**
 * A short-lived Gram-signed acting-principal assertion for one managed LiteLLM inference invocation.
 */
export type LitellmActingPrincipalResult = {
  assertion: string;
  contractVersion: ContractVersion;
  expiresIn: number;
  invocationId: string;
};

/** @internal */
export const ContractVersion$inboundSchema: z.ZodMiniEnum<
  typeof ContractVersion
> = z.enum(ContractVersion);

/** @internal */
export const LitellmActingPrincipalResult$inboundSchema: z.ZodMiniType<
  LitellmActingPrincipalResult,
  unknown
> = z.pipe(
  z.object({
    assertion: z.string(),
    contract_version: ContractVersion$inboundSchema,
    expires_in: z.int(),
    invocation_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "contract_version": "contractVersion",
      "expires_in": "expiresIn",
      "invocation_id": "invocationId",
    });
  }),
);

export function litellmActingPrincipalResultFromJSON(
  jsonString: string,
): SafeParseResult<LitellmActingPrincipalResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LitellmActingPrincipalResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LitellmActingPrincipalResult' from JSON`,
  );
}
