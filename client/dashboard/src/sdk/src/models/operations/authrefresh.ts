/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AuthRefreshResponse = {
  headers: { [k: string]: Array<string> };
};

/** @internal */
export const AuthRefreshResponse$inboundSchema: z.ZodMiniType<
  AuthRefreshResponse,
  unknown
> = z.pipe(
  z.object({
    Headers: z._default(z.record(z.string(), z.array(z.string())), {}),
  }),
  z.transform((v) => {
    return remap$(v, {
      "Headers": "headers",
    });
  }),
);

export function authRefreshResponseFromJSON(
  jsonString: string,
): SafeParseResult<AuthRefreshResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AuthRefreshResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AuthRefreshResponse' from JSON`,
  );
}
