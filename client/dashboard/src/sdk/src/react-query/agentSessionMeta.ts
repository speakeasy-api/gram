/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  GetAgentSessionMetaRequest,
  GetAgentSessionMetaSecurity,
} from "../models/operations/getagentsessionmeta.js";
import { useGramContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  AgentSessionMetaQueryData,
  buildAgentSessionMetaQuery,
  prefetchAgentSessionMeta,
  queryKeyAgentSessionMeta,
} from "./agentSessionMeta.core.js";
export {
  type AgentSessionMetaQueryData,
  buildAgentSessionMetaQuery,
  prefetchAgentSessionMeta,
  queryKeyAgentSessionMeta,
};

export type AgentSessionMetaQueryError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * getSessionMeta agent
 *
 * @remarks
 * Resolve display metadata (Gram chat id, generated title, last activity) for captured agent sessions the calling user owns. Used by the device agent's session picker to overlay server-generated titles on locally discovered transcripts; unknown or non-owned session ids are silently omitted, so the picker degrades gracefully. Requires a per-user key: the fleet-shared org install key is refused because session metadata is per-user data.
 */
export function useAgentSessionMeta(
  request: GetAgentSessionMetaRequest,
  security?: GetAgentSessionMetaSecurity | undefined,
  options?: QueryHookOptions<
    AgentSessionMetaQueryData,
    AgentSessionMetaQueryError
  >,
): UseQueryResult<AgentSessionMetaQueryData, AgentSessionMetaQueryError> {
  const client = useGramContext();
  return useQuery({
    ...buildAgentSessionMetaQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

/**
 * getSessionMeta agent
 *
 * @remarks
 * Resolve display metadata (Gram chat id, generated title, last activity) for captured agent sessions the calling user owns. Used by the device agent's session picker to overlay server-generated titles on locally discovered transcripts; unknown or non-owned session ids are silently omitted, so the picker degrades gracefully. Requires a per-user key: the fleet-shared org install key is refused because session metadata is per-user data.
 */
export function useAgentSessionMetaSuspense(
  request: GetAgentSessionMetaRequest,
  security?: GetAgentSessionMetaSecurity | undefined,
  options?: SuspenseQueryHookOptions<
    AgentSessionMetaQueryData,
    AgentSessionMetaQueryError
  >,
): UseSuspenseQueryResult<
  AgentSessionMetaQueryData,
  AgentSessionMetaQueryError
> {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildAgentSessionMetaQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

export function setAgentSessionMetaData(
  client: QueryClient,
  queryKeyBase: [
    parameters: { sessionIds: Array<string>; gramKey?: string | undefined },
  ],
  data: AgentSessionMetaQueryData,
): AgentSessionMetaQueryData | undefined {
  const key = queryKeyAgentSessionMeta(...queryKeyBase);

  return client.setQueryData<AgentSessionMetaQueryData>(key, data);
}

export function invalidateAgentSessionMeta(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: { sessionIds: Array<string>; gramKey?: string | undefined }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "agent", "getSessionMeta", ...queryKeyBase],
  });
}

export function invalidateAllAgentSessionMeta(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "agent", "getSessionMeta"],
  });
}
