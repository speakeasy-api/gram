/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { assistantsInterruptTurn } from "../funcs/assistantsInterruptTurn.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import { InterruptTurnResult } from "../models/components/interruptturnresult.js";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  InterruptAssistantTurnRequest,
  InterruptAssistantTurnSecurity,
} from "../models/operations/interruptassistantturn.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type InterruptAssistantTurnMutationVariables = {
  request: InterruptAssistantTurnRequest;
  security?: InterruptAssistantTurnSecurity | undefined;
  options?: RequestOptions;
};

export type InterruptAssistantTurnMutationData = InterruptTurnResult;

export type InterruptAssistantTurnMutationError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * interruptTurn assistants
 *
 * @remarks
 * Stop whatever a conversation is currently generating. Cancels turns still queued on the conversation's thread and interrupts the turn in flight on the assistant runtime, so the reply stops where it is rather than finishing in the background. Idempotent and safe to call when nothing is running: `stopped` is false when the reply had already finished.
 */
export function useInterruptAssistantTurnMutation(
  options?: MutationHookOptions<
    InterruptAssistantTurnMutationData,
    InterruptAssistantTurnMutationError,
    InterruptAssistantTurnMutationVariables
  >,
): UseMutationResult<
  InterruptAssistantTurnMutationData,
  InterruptAssistantTurnMutationError,
  InterruptAssistantTurnMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildInterruptAssistantTurnMutation(client, options),
    ...options,
  });
}

export function mutationKeyInterruptAssistantTurn(): MutationKey {
  return ["@gram/client", "assistants", "interruptTurn"];
}

export function buildInterruptAssistantTurnMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: InterruptAssistantTurnMutationVariables,
  ) => Promise<InterruptAssistantTurnMutationData>;
} {
  return {
    mutationKey: mutationKeyInterruptAssistantTurn(),
    mutationFn: function interruptAssistantTurnMutationFn({
      request,
      security,
      options,
    }): Promise<InterruptAssistantTurnMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(assistantsInterruptTurn(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
