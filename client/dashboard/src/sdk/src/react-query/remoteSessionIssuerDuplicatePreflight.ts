/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  GetRemoteSessionIssuerDuplicatePreflightRequest,
  GetRemoteSessionIssuerDuplicatePreflightSecurity,
} from "../models/operations/getremotesessionissuerduplicatepreflight.js";
import { useGramContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  buildRemoteSessionIssuerDuplicatePreflightQuery,
  prefetchRemoteSessionIssuerDuplicatePreflight,
  queryKeyRemoteSessionIssuerDuplicatePreflight,
  RemoteSessionIssuerDuplicatePreflightQueryData,
} from "./remoteSessionIssuerDuplicatePreflight.core.js";
export {
  buildRemoteSessionIssuerDuplicatePreflightQuery,
  prefetchRemoteSessionIssuerDuplicatePreflight,
  queryKeyRemoteSessionIssuerDuplicatePreflight,
  type RemoteSessionIssuerDuplicatePreflightQueryData,
};

export type RemoteSessionIssuerDuplicatePreflightQueryError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * getRemoteSessionIssuerDuplicatePreflight remoteSessionIssuers
 *
 * @remarks
 * Report the existing remote_session_issuers that already describe an upstream issuer URL, so a create or edit form can warn before it duplicates one. Covers this project's own issuers plus those inherited from the organization and the platform catalog.
 *
 * Advisory only. Duplicating an issuer URL is legitimate — a project may want its own record so it can attach different documentation, branding or scopes — so nothing here blocks a write, and no lock is taken. A create that races another create still produces two records, which is a supported state.
 *
 * Matching uses the same canonicalization as getRemoteSessionIssuer. A URL that cannot be parsed as an issuer identifier returns no matches rather than an error, because a partially typed URL is the normal state of a form field.
 */
export function useRemoteSessionIssuerDuplicatePreflight(
  request?: GetRemoteSessionIssuerDuplicatePreflightRequest | undefined,
  security?: GetRemoteSessionIssuerDuplicatePreflightSecurity | undefined,
  options?: QueryHookOptions<
    RemoteSessionIssuerDuplicatePreflightQueryData,
    RemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseQueryResult<
  RemoteSessionIssuerDuplicatePreflightQueryData,
  RemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useQuery({
    ...buildRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

/**
 * getRemoteSessionIssuerDuplicatePreflight remoteSessionIssuers
 *
 * @remarks
 * Report the existing remote_session_issuers that already describe an upstream issuer URL, so a create or edit form can warn before it duplicates one. Covers this project's own issuers plus those inherited from the organization and the platform catalog.
 *
 * Advisory only. Duplicating an issuer URL is legitimate — a project may want its own record so it can attach different documentation, branding or scopes — so nothing here blocks a write, and no lock is taken. A create that races another create still produces two records, which is a supported state.
 *
 * Matching uses the same canonicalization as getRemoteSessionIssuer. A URL that cannot be parsed as an issuer identifier returns no matches rather than an error, because a partially typed URL is the normal state of a form field.
 */
export function useRemoteSessionIssuerDuplicatePreflightSuspense(
  request?: GetRemoteSessionIssuerDuplicatePreflightRequest | undefined,
  security?: GetRemoteSessionIssuerDuplicatePreflightSecurity | undefined,
  options?: SuspenseQueryHookOptions<
    RemoteSessionIssuerDuplicatePreflightQueryData,
    RemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseSuspenseQueryResult<
  RemoteSessionIssuerDuplicatePreflightQueryData,
  RemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

export function setRemoteSessionIssuerDuplicatePreflightData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
      gramKey?: string | undefined;
      gramProject?: string | undefined;
    },
  ],
  data: RemoteSessionIssuerDuplicatePreflightQueryData,
): RemoteSessionIssuerDuplicatePreflightQueryData | undefined {
  const key = queryKeyRemoteSessionIssuerDuplicatePreflight(...queryKeyBase);

  return client.setQueryData<RemoteSessionIssuerDuplicatePreflightQueryData>(
    key,
    data,
  );
}

export function invalidateRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
      gramKey?: string | undefined;
      gramProject?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/client",
      "remoteSessionIssuers",
      "getDuplicatePreflight",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gram/client", "remoteSessionIssuers", "getDuplicatePreflight"],
  });
}
