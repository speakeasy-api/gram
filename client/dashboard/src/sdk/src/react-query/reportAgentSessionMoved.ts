/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { agentReportSessionMoved } from "../funcs/agentReportSessionMoved.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  ReportAgentSessionMovedRequest,
  ReportAgentSessionMovedSecurity,
} from "../models/operations/reportagentsessionmoved.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type ReportAgentSessionMovedMutationVariables = {
  request: ReportAgentSessionMovedRequest;
  security?: ReportAgentSessionMovedSecurity | undefined;
  options?: RequestOptions;
};

export type ReportAgentSessionMovedMutationData = void;

export type ReportAgentSessionMovedMutationError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * reportSessionMoved agent
 *
 * @remarks
 * Record that a captured agent session was moved to another harness on a device (session portability). Carries no session content — only the session identity, the target harness, and device attribution — and lands as a chat_session:move audit event so organizations retain governance visibility over local-first moves. Accepts both the per-user key and the org install key (with a vouched email), mirroring getPlugins, because fleet devices must be able to report moves. Fire-and-forget from the agent's perspective: the daemon must never fail a move because this call failed.
 */
export function useReportAgentSessionMovedMutation(
  options?: MutationHookOptions<
    ReportAgentSessionMovedMutationData,
    ReportAgentSessionMovedMutationError,
    ReportAgentSessionMovedMutationVariables
  >,
): UseMutationResult<
  ReportAgentSessionMovedMutationData,
  ReportAgentSessionMovedMutationError,
  ReportAgentSessionMovedMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildReportAgentSessionMovedMutation(client, options),
    ...options,
  });
}

export function mutationKeyReportAgentSessionMoved(): MutationKey {
  return ["@gram/client", "agent", "reportSessionMoved"];
}

export function buildReportAgentSessionMovedMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: ReportAgentSessionMovedMutationVariables,
  ) => Promise<ReportAgentSessionMovedMutationData>;
} {
  return {
    mutationKey: mutationKeyReportAgentSessionMoved(),
    mutationFn: function reportAgentSessionMovedMutationFn({
      request,
      security,
      options,
    }): Promise<ReportAgentSessionMovedMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(agentReportSessionMoved(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
