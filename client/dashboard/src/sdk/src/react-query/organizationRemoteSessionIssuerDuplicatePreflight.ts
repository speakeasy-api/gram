/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  GetOrganizationRemoteSessionIssuerDuplicatePreflightRequest,
  GetOrganizationRemoteSessionIssuerDuplicatePreflightSecurity,
} from "../models/operations/getorganizationremotesessionissuerduplicatepreflight.js";
import { useGramContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  buildOrganizationRemoteSessionIssuerDuplicatePreflightQuery,
  OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
  prefetchOrganizationRemoteSessionIssuerDuplicatePreflight,
  queryKeyOrganizationRemoteSessionIssuerDuplicatePreflight,
} from "./organizationRemoteSessionIssuerDuplicatePreflight.core.js";
export {
  buildOrganizationRemoteSessionIssuerDuplicatePreflightQuery,
  type OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
  prefetchOrganizationRemoteSessionIssuerDuplicatePreflight,
  queryKeyOrganizationRemoteSessionIssuerDuplicatePreflight,
};

export type OrganizationRemoteSessionIssuerDuplicatePreflightQueryError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * getIssuerDuplicatePreflight organizationRemoteSessionIssuers
 *
 * @remarks
 * Report the existing remote_session_issuers that already describe an upstream issuer URL, so a create or edit form can warn before it duplicates one. Requires org:read.
 *
 * Covers every issuer in the caller's organization — organization-level and project-specific alike — plus the platform catalog. The project-specific rows are the point: an organization administrator about to add an organization-level issuer most needs to know that several of their projects already configured the same URL separately, because those are exactly the records migrateIssuer can consolidate. The answer does not depend on whether the issuer being created is organization-level or project-scoped; an org administrator holds org:read either way.
 *
 * Advisory only. Duplicating an issuer URL is legitimate, so nothing here blocks a write and no lock is taken. Matching uses the same canonicalization as getRemoteSessionIssuer, and a URL that cannot be parsed as an issuer identifier returns no matches rather than an error.
 */
export function useOrganizationRemoteSessionIssuerDuplicatePreflight(
  request?:
    | GetOrganizationRemoteSessionIssuerDuplicatePreflightRequest
    | undefined,
  security?:
    | GetOrganizationRemoteSessionIssuerDuplicatePreflightSecurity
    | undefined,
  options?: QueryHookOptions<
    OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
    OrganizationRemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseQueryResult<
  OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
  OrganizationRemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useQuery({
    ...buildOrganizationRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

/**
 * getIssuerDuplicatePreflight organizationRemoteSessionIssuers
 *
 * @remarks
 * Report the existing remote_session_issuers that already describe an upstream issuer URL, so a create or edit form can warn before it duplicates one. Requires org:read.
 *
 * Covers every issuer in the caller's organization — organization-level and project-specific alike — plus the platform catalog. The project-specific rows are the point: an organization administrator about to add an organization-level issuer most needs to know that several of their projects already configured the same URL separately, because those are exactly the records migrateIssuer can consolidate. The answer does not depend on whether the issuer being created is organization-level or project-scoped; an org administrator holds org:read either way.
 *
 * Advisory only. Duplicating an issuer URL is legitimate, so nothing here blocks a write and no lock is taken. Matching uses the same canonicalization as getRemoteSessionIssuer, and a URL that cannot be parsed as an issuer identifier returns no matches rather than an error.
 */
export function useOrganizationRemoteSessionIssuerDuplicatePreflightSuspense(
  request?:
    | GetOrganizationRemoteSessionIssuerDuplicatePreflightRequest
    | undefined,
  security?:
    | GetOrganizationRemoteSessionIssuerDuplicatePreflightSecurity
    | undefined,
  options?: SuspenseQueryHookOptions<
    OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
    OrganizationRemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseSuspenseQueryResult<
  OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
  OrganizationRemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildOrganizationRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

export function setOrganizationRemoteSessionIssuerDuplicatePreflightData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
      gramKey?: string | undefined;
    },
  ],
  data: OrganizationRemoteSessionIssuerDuplicatePreflightQueryData,
): OrganizationRemoteSessionIssuerDuplicatePreflightQueryData | undefined {
  const key = queryKeyOrganizationRemoteSessionIssuerDuplicatePreflight(
    ...queryKeyBase,
  );

  return client.setQueryData<
    OrganizationRemoteSessionIssuerDuplicatePreflightQueryData
  >(key, data);
}

export function invalidateOrganizationRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
      gramKey?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/client",
      "organizationRemoteSessionIssuers",
      "getDuplicatePreflight",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllOrganizationRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/client",
      "organizationRemoteSessionIssuers",
      "getDuplicatePreflight",
    ],
  });
}
