/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { remoteSessionsCommitServerIdentityConfiguration } from "../funcs/remoteSessionsCommitServerIdentityConfiguration.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import { CommitServerIdentityConfigurationResult } from "../models/components/commitserveridentityconfigurationresult.js";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  CommitServerIdentityConfigurationRequest,
  CommitServerIdentityConfigurationSecurity,
} from "../models/operations/commitserveridentityconfiguration.js";
import { unwrapAsync } from "../types/fp.js";
import { useGramContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type CommitServerIdentityConfigurationMutationVariables = {
  request: CommitServerIdentityConfigurationRequest;
  security?: CommitServerIdentityConfigurationSecurity | undefined;
  options?: RequestOptions;
};

export type CommitServerIdentityConfigurationMutationData =
  CommitServerIdentityConfigurationResult;

export type CommitServerIdentityConfigurationMutationError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * commitServerIdentityConfiguration remoteSessions
 *
 * @remarks
 * Atomically configure identity for a Remote MCP-backed MCP server. The complete plan selects or creates a project Remote Identity Provider and links an existing client, creates a manual client, or automatically prefers CIMD over DCR. Existing-client linking requires mcp:write on the target and on every other MCP server sharing its user session issuer, because the client binding is keyed by issuer; creating a provider or client additionally requires project:write. Unsupported automatic registration returns manual_setup_required without changing local state.
 */
export function useCommitServerIdentityConfigurationMutation(
  options?: MutationHookOptions<
    CommitServerIdentityConfigurationMutationData,
    CommitServerIdentityConfigurationMutationError,
    CommitServerIdentityConfigurationMutationVariables
  >,
): UseMutationResult<
  CommitServerIdentityConfigurationMutationData,
  CommitServerIdentityConfigurationMutationError,
  CommitServerIdentityConfigurationMutationVariables
> {
  const client = useGramContext();
  return useMutation({
    ...buildCommitServerIdentityConfigurationMutation(client, options),
    ...options,
  });
}

export function mutationKeyCommitServerIdentityConfiguration(): MutationKey {
  return [
    "@gram/client",
    "remoteSessions",
    "commitServerIdentityConfiguration",
  ];
}

export function buildCommitServerIdentityConfigurationMutation(
  client$: GramCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: CommitServerIdentityConfigurationMutationVariables,
  ) => Promise<CommitServerIdentityConfigurationMutationData>;
} {
  return {
    mutationKey: mutationKeyCommitServerIdentityConfiguration(),
    mutationFn: function commitServerIdentityConfigurationMutationFn({
      request,
      security,
      options,
    }): Promise<CommitServerIdentityConfigurationMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(remoteSessionsCommitServerIdentityConfiguration(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}
