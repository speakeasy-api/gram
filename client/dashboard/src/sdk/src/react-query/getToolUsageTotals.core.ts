/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  QueryClient,
  QueryFunctionContext,
  QueryKey,
} from "@tanstack/react-query";
import { GramCore } from "../core.js";
import { telemetryGetToolUsageTotals } from "../funcs/telemetryGetToolUsageTotals.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import { GetToolUsageTotalsResult } from "../models/components/gettoolusagetotalsresult.js";
import {
  GetToolUsageTotalsRequest,
  GetToolUsageTotalsSecurity,
} from "../models/operations/gettoolusagetotals.js";
import { unwrapAsync } from "../types/fp.js";
export type GetToolUsageTotalsQueryData = GetToolUsageTotalsResult;

export function prefetchGetToolUsageTotals(
  queryClient: QueryClient,
  client$: GramCore,
  request: GetToolUsageTotalsRequest,
  security?: GetToolUsageTotalsSecurity | undefined,
  options?: RequestOptions,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildGetToolUsageTotalsQuery(
      client$,
      request,
      security,
      options,
    ),
  });
}

export function buildGetToolUsageTotalsQuery(
  client$: GramCore,
  request: GetToolUsageTotalsRequest,
  security?: GetToolUsageTotalsSecurity | undefined,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<GetToolUsageTotalsQueryData>;
} {
  return {
    queryKey: queryKeyGetToolUsageTotals({
      gramKey: request.gramKey,
      gramSession: request.gramSession,
      gramProject: request.gramProject,
    }),
    queryFn: async function getToolUsageTotalsQueryFn(
      ctx,
    ): Promise<GetToolUsageTotalsQueryData> {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };

      return unwrapAsync(telemetryGetToolUsageTotals(
        client$,
        request,
        security,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyGetToolUsageTotals(
  parameters: {
    gramKey?: string | undefined;
    gramSession?: string | undefined;
    gramProject?: string | undefined;
  },
): QueryKey {
  return ["@gram/client", "telemetry", "getToolUsageTotals", parameters];
}
