/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GramError } from "../models/errors/gramerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ServiceError } from "../models/errors/serviceerror.js";
import {
  GetGlobalRemoteSessionIssuerDuplicatePreflightRequest,
  GetGlobalRemoteSessionIssuerDuplicatePreflightSecurity,
} from "../models/operations/getglobalremotesessionissuerduplicatepreflight.js";
import { useGramContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  buildGlobalRemoteSessionIssuerDuplicatePreflightQuery,
  GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
  prefetchGlobalRemoteSessionIssuerDuplicatePreflight,
  queryKeyGlobalRemoteSessionIssuerDuplicatePreflight,
} from "./globalRemoteSessionIssuerDuplicatePreflight.core.js";
export {
  buildGlobalRemoteSessionIssuerDuplicatePreflightQuery,
  type GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
  prefetchGlobalRemoteSessionIssuerDuplicatePreflight,
  queryKeyGlobalRemoteSessionIssuerDuplicatePreflight,
};

export type GlobalRemoteSessionIssuerDuplicatePreflightQueryError =
  | ServiceError
  | GramError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * getGlobalIssuerDuplicatePreflight adminRemoteSessions
 *
 * @remarks
 * Report the global remote_session_issuers that already describe an upstream issuer URL, so the catalog create and edit forms can warn before curating a second entry for the same authorization server. Requires platform admin.
 *
 * Scoped to the global partition only. Tenant issuers naming the same URL are deliberately not reported here — listGlobalIssuerConvergenceCandidates is the surface for those, and it is keyed on a global issuer that already exists.
 *
 * The global tier is unique on slug but not on issuer, so nothing prevents a duplicate catalog entry and this warning is the only thing that will catch one. Advisory all the same: it never blocks the write. Matching uses the same canonicalization as the tenant-facing preflights, and an unparseable URL returns no matches rather than an error.
 */
export function useGlobalRemoteSessionIssuerDuplicatePreflight(
  request?: GetGlobalRemoteSessionIssuerDuplicatePreflightRequest | undefined,
  security?: GetGlobalRemoteSessionIssuerDuplicatePreflightSecurity | undefined,
  options?: QueryHookOptions<
    GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
    GlobalRemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseQueryResult<
  GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
  GlobalRemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useQuery({
    ...buildGlobalRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

/**
 * getGlobalIssuerDuplicatePreflight adminRemoteSessions
 *
 * @remarks
 * Report the global remote_session_issuers that already describe an upstream issuer URL, so the catalog create and edit forms can warn before curating a second entry for the same authorization server. Requires platform admin.
 *
 * Scoped to the global partition only. Tenant issuers naming the same URL are deliberately not reported here — listGlobalIssuerConvergenceCandidates is the surface for those, and it is keyed on a global issuer that already exists.
 *
 * The global tier is unique on slug but not on issuer, so nothing prevents a duplicate catalog entry and this warning is the only thing that will catch one. Advisory all the same: it never blocks the write. Matching uses the same canonicalization as the tenant-facing preflights, and an unparseable URL returns no matches rather than an error.
 */
export function useGlobalRemoteSessionIssuerDuplicatePreflightSuspense(
  request?: GetGlobalRemoteSessionIssuerDuplicatePreflightRequest | undefined,
  security?: GetGlobalRemoteSessionIssuerDuplicatePreflightSecurity | undefined,
  options?: SuspenseQueryHookOptions<
    GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
    GlobalRemoteSessionIssuerDuplicatePreflightQueryError
  >,
): UseSuspenseQueryResult<
  GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
  GlobalRemoteSessionIssuerDuplicatePreflightQueryError
> {
  const client = useGramContext();
  return useSuspenseQuery({
    ...buildGlobalRemoteSessionIssuerDuplicatePreflightQuery(
      client,
      request,
      security,
      options,
    ),
    ...options,
  });
}

export function setGlobalRemoteSessionIssuerDuplicatePreflightData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
    },
  ],
  data: GlobalRemoteSessionIssuerDuplicatePreflightQueryData,
): GlobalRemoteSessionIssuerDuplicatePreflightQueryData | undefined {
  const key = queryKeyGlobalRemoteSessionIssuerDuplicatePreflight(
    ...queryKeyBase,
  );

  return client.setQueryData<
    GlobalRemoteSessionIssuerDuplicatePreflightQueryData
  >(key, data);
}

export function invalidateGlobalRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      issuer?: string | undefined;
      gramSession?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/client",
      "adminRemoteSessions",
      "getGlobalIssuerDuplicatePreflight",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllGlobalRemoteSessionIssuerDuplicatePreflight(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gram/client",
      "adminRemoteSessions",
      "getGlobalIssuerDuplicatePreflight",
    ],
  });
}
