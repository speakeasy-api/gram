// DEAD CODE — slated for deletion (`git rm`).
//
// The Monaco-backed query editor was superseded by the structured, no-DSL
// ConditionBuilder (./condition-builder.tsx). Nothing imports this module any
// more. Left as an empty stub only because the agent can't delete files; remove
// it from the repo.
export {};
