// DEAD CODE — slated for deletion (`git rm`).
//
// This legacy autocomplete query input was already unused (superseded first by
// the Monaco editor, now by the structured ConditionBuilder in
// ./condition-builder.tsx). Left as an empty stub only because the agent can't
// delete files; remove it from the repo.
export {};
