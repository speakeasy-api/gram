// Code generated by goa v3.25.3, DO NOT EDIT.
//
// litellm service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package litellm

import (
	"context"

	litellmviews "github.com/speakeasy-api/gram/server/gen/litellm/views"
	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Receives LiteLLM Generic Guardrail callbacks and OpenTelemetry exports.
type Service interface {
	// Evaluates and captures a LiteLLM model request before it reaches the
	// provider.
	Ingest(context.Context, *IngestPayload) (res *LitellmIngestResult, err error)
	// Accepts LiteLLM OTLP trace exports. Send the standard OTLP JSON
	// ExportTraceServiceRequest shape shown here with application/json, or the
	// binary opentelemetry.proto.collector.trace.v1.ExportTraceServiceRequest with
	// application/x-protobuf or application/protobuf. Content-Encoding may be gzip.
	Traces(context.Context, *TracesPayload) (err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "litellm"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [2]string{"ingest", "traces"}

// IngestPayload is the payload type of the litellm service ingest method.
type IngestPayload struct {
	ApikeyToken                      *string
	ProjectSlugInput                 *string
	InputType                        string
	RequestData                      *LiteLLMRequestData
	Texts                            []string
	Images                           []string
	Tools                            []any
	ToolCalls                        []any
	StructuredMessages               []*LiteLLMStructuredMessage
	RequestHeaders                   map[string]string
	LitellmCallID                    *string
	LitellmTraceID                   *string
	LitellmVersion                   *string
	Model                            *string
	AdditionalProviderSpecificParams map[string]any
}

type LiteLLMGuardrailAction string

// Sanitized metadata bound to the LiteLLM virtual key and request.
type LiteLLMRequestData struct {
	UserAPIKeyHash      *string
	UserAPIKeyAlias     *string
	UserAPIKeyUserID    *string
	UserAPIKeyUserEmail *string
	UserAPIKeyTeamID    *string
	UserAPIKeyTeamAlias *string
	UserAPIKeyEndUserID *string
	UserAPIKeyOrgID     *string
}

// A message supplied to LiteLLM for the guarded model request.
type LiteLLMStructuredMessage struct {
	Role    string
	Content any
}

// LitellmIngestResult is the result type of the litellm service ingest method.
type LitellmIngestResult struct {
	Action              LiteLLMGuardrailAction
	BlockedReason       *string
	Texts               []string
	Images              []string
	Tools               []any
	StreamHoldbackChars []int
}

// TracesPayload is the payload type of the litellm service traces method.
type TracesPayload struct {
	ApikeyToken      *string
	ProjectSlugInput *string
	// Standard OTLP ResourceSpans objects. OTLP integer fields use their canonical
	// decimal-string JSON representation and trace/span IDs use lowercase
	// hexadecimal strings.
	ResourceSpans []any
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}

// MakeRequestTooLarge builds a goa.ServiceError from an error.
func MakeRequestTooLarge(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "request_too_large", false, false, false)
}

// NewLitellmIngestResult initializes result type LitellmIngestResult from
// viewed result type LitellmIngestResult.
func NewLitellmIngestResult(vres *litellmviews.LitellmIngestResult) *LitellmIngestResult {
	return newLitellmIngestResult(vres.Projected)
}

// NewViewedLitellmIngestResult initializes viewed result type
// LitellmIngestResult from result type LitellmIngestResult using the given
// view.
func NewViewedLitellmIngestResult(res *LitellmIngestResult, view string) *litellmviews.LitellmIngestResult {
	p := newLitellmIngestResultView(res)
	return &litellmviews.LitellmIngestResult{Projected: p, View: "default"}
}

// newLitellmIngestResult converts projected type LitellmIngestResult to
// service type LitellmIngestResult.
func newLitellmIngestResult(vres *litellmviews.LitellmIngestResultView) *LitellmIngestResult {
	res := &LitellmIngestResult{
		BlockedReason: vres.BlockedReason,
	}
	if vres.Action != nil {
		res.Action = LiteLLMGuardrailAction(*vres.Action)
	}
	if vres.Texts != nil {
		res.Texts = make([]string, len(vres.Texts))
		for i, val := range vres.Texts {
			res.Texts[i] = val
		}
	}
	if vres.Images != nil {
		res.Images = make([]string, len(vres.Images))
		for i, val := range vres.Images {
			res.Images[i] = val
		}
	}
	if vres.Tools != nil {
		res.Tools = make([]any, len(vres.Tools))
		for i, val := range vres.Tools {
			res.Tools[i] = val
		}
	}
	if vres.StreamHoldbackChars != nil {
		res.StreamHoldbackChars = make([]int, len(vres.StreamHoldbackChars))
		for i, val := range vres.StreamHoldbackChars {
			res.StreamHoldbackChars[i] = val
		}
	}
	return res
}

// newLitellmIngestResultView projects result type LitellmIngestResult to
// projected type LitellmIngestResultView using the "default" view.
func newLitellmIngestResultView(res *LitellmIngestResult) *litellmviews.LitellmIngestResultView {
	vres := &litellmviews.LitellmIngestResultView{
		BlockedReason: res.BlockedReason,
	}
	action := litellmviews.LiteLLMGuardrailActionView(res.Action)
	vres.Action = &action
	if res.Texts != nil {
		vres.Texts = make([]string, len(res.Texts))
		for i, val := range res.Texts {
			vres.Texts[i] = val
		}
	}
	if res.Images != nil {
		vres.Images = make([]string, len(res.Images))
		for i, val := range res.Images {
			vres.Images[i] = val
		}
	}
	if res.Tools != nil {
		vres.Tools = make([]any, len(res.Tools))
		for i, val := range res.Tools {
			vres.Tools[i] = val
		}
	}
	if res.StreamHoldbackChars != nil {
		vres.StreamHoldbackChars = make([]int, len(res.StreamHoldbackChars))
		for i, val := range res.StreamHoldbackChars {
			vres.StreamHoldbackChars[i] = val
		}
	}
	return vres
}
