// Code generated by goa v3.25.3, DO NOT EDIT.
//
// User types
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package types

// An entry in Gram's curated CIMD preset catalog.
type CimdClientPreset struct {
	// Stable identifier for the publishing vendor. Not unique — a vendor may
	// publish several client documents.
	VendorKey string
	// Human-readable client name.
	DisplayName string
	// The https URL this client presents as its client_id. Usually exact; when
	// is_pattern is true this is a wildcard pattern rather than a literal URL.
	ClientIDMetadataURI string
	// True when client_id_metadata_uri contains a `*` wildcard, matching a whole
	// namespace of client_ids rather than one URL. Used for vendors that mint a
	// separate metadata document per connector or install, so their client_ids
	// cannot be enumerated. A `*` stands for exactly one path segment and never
	// widens the host.
	IsPattern bool
	// Whether presets-mode issuers currently admit this entry. Disabled entries
	// are listed so operators can see that Gram knows about the vendor.
	Enabled bool
}
