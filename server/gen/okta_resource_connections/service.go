// Code generated by goa v3.25.3, DO NOT EDIT.
//
// oktaResourceConnections service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package oktaresourceconnections

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Okta resource connections per MCP server: what the organization
// administrator still has to do in the identity provider console, what they
// confirmed, and what the exchange path observed. Advisory only; the exchange
// path never consults it.
type Service interface {
	// List the Okta resource connection and derived state of every eligible MCP
	// server. Requires org:admin and a live identity provider connection.
	List(context.Context, *ListPayload) (res *ListOktaResourceConnectionsResult, err error)
	// Record that the administrator created the agent-to-resource connections for
	// these servers in the identity provider console, with the audience each
	// resource app is configured with. Servers that share an upstream share one
	// Okta resource connection. Confirmation is the administrator's word;
	// Speakeasy cannot check it. The connection must be verified or degraded.
	// Requires org:admin and the okta-connections rollout.
	Confirm(context.Context, *ConfirmPayload) (res *ConfirmOktaResourceConnectionsResult, err error)
	// Delete the Okta resource connection for this server's upstream so it, and
	// every server sharing that upstream, shows as pending again. Requires
	// org:admin.
	Reset(context.Context, *ResetPayload) (res *OktaResourceConnectionServer, err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "oktaResourceConnections"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [3]string{"list", "confirm", "reset"}

// ConfirmOktaResourceConnectionsResult is the result type of the
// oktaResourceConnections service confirm method.
type ConfirmOktaResourceConnectionsResult struct {
	// The confirmed servers with their new state.
	Servers []*OktaResourceConnectionServer
}

// ConfirmPayload is the payload type of the oktaResourceConnections service
// confirm method.
type ConfirmPayload struct {
	SessionToken *string
	// Servers to confirm with their configured values.
	Connections []*OktaResourceConnectionConfirmation
}

// ListOktaResourceConnectionsResult is the result type of the
// oktaResourceConnections service list method.
type ListOktaResourceConnectionsResult struct {
	// Pending servers first, then by name. By default only servers with a step
	// left; include_all adds connected and not-applicable ones.
	Servers []*OktaResourceConnectionServer
	// Servers that still need a step.
	PendingCount int
	// Eligible servers before filtering.
	TotalCount int
	// Servers left out because their authorization server metadata has not been
	// fetched yet.
	UndiscoveredCount int
	// Whether the connection has an AI agent recorded; every server needs one
	// first.
	AgentRecorded bool
	// The organization's identity provider connection.
	ConnectionID *string
	// Link to the add-resource-connection form of the AI agent in the identity
	// provider console. Omitted until an agent is recorded or when the org uses a
	// custom domain.
	DeepLink *string
}

// ListPayload is the payload type of the oktaResourceConnections service list
// method.
type ListPayload struct {
	SessionToken *string
	// Include connected and not-applicable servers.
	IncludeAll bool
}

// One server the administrator connected in the identity provider console,
// with the values they configured there.
type OktaResourceConnectionConfirmation struct {
	// Server whose upstream was connected.
	McpServerID string
	// The issuer URL entered on the resource app's Cross App Access settings; the
	// identity assertion audience. Must be an https URL without query or fragment.
	Audience string
	// The identity provider app instance picked for this upstream, from the
	// applications snapshot.
	OktaApplicationID *string
}

// OktaResourceConnectionServer is the result type of the
// oktaResourceConnections service reset method.
type OktaResourceConnectionServer struct {
	// MCP server ID.
	McpServerID string
	// Project the server belongs to.
	ProjectID   string
	ProjectSlug string
	// Server display name; admin-editable, never a key.
	ServerName string
	ServerSlug string
	// Derived readiness. not_applicable: see not_applicable_reason; needs_agent:
	// no AI agent recorded on the connection; needs_connection: the
	// agent-to-resource connection has not been confirmed for this server's
	// upstream; connected: the administrator confirmed it. Whether the exchange
	// works is not known here.
	State string
	// no_idjag: the server's authorization server metadata does not advertise the
	// identity assertion grant.
	NotApplicableReason *string
	// Whether the administrator still has a step to do for this server.
	Pending bool
	// The upstream authorization server ID. Together with the resource indicator,
	// identifies the shared readiness confirmation; independent of the confirmed
	// identity assertion audience.
	IssuerID *string
	// The resource indicator to enter on the connection: the server's RFC 9728
	// resource identifier when known, otherwise its URL.
	ResourceIndicator string
	// Speakeasy's client ID at the server's authorization server. Omitted when no
	// client is bound yet.
	ClientID *string
	// bound: an explicit identity chaining binding selects the client; single: the
	// one client attached to the authorization server; ambiguous: more than one
	// candidate and no single binding; missing: no client registered yet.
	ClientBinding string
	// Scopes Speakeasy requests at the resource.
	Scopes []string
	// Link to the add-resource-connection form of the AI agent in the identity
	// provider console. Omitted until an agent is recorded or when the org uses a
	// custom domain.
	DeepLink *string
	// The resource app's Cross App Access issuer URL as configured in the identity
	// provider; the audience of the identity assertion. Present once confirmed.
	Audience *string
	// The identity provider app instance the administrator picked for this
	// upstream, when recorded.
	OktaApplicationID *string
	// Label of that app instance in the latest applications snapshot; omitted when
	// the instance is no longer there.
	OktaApplicationLabel *string
	// When the administrator confirmed the connection for this server's upstream.
	ConfirmedAt *string
}

// ResetPayload is the payload type of the oktaResourceConnections service
// reset method.
type ResetPayload struct {
	SessionToken *string
	// Server to reset.
	McpServerID string
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}

// MakeFailedPrecondition builds a goa.ServiceError from an error.
func MakeFailedPrecondition(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "failed_precondition", false, false, false)
}
