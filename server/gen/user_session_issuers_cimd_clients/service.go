// Code generated by goa v3.25.3, DO NOT EDIT.
//
// userSessionIssuersCimdClients service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package usersessionissuerscimdclients

import (
	"context"

	types "github.com/speakeasy-api/gram/server/gen/types"
	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Manage the CIMD (OAuth Client ID Metadata Document) clients a
// user_session_issuer admits: the read-only preset catalog Gram curates, plus
// per-issuer custom document URLs.
type Service interface {
	// List Gram's curated CIMD preset catalog. Issuers whose admission mode is
	// 'presets' — the default — admit every enabled entry here automatically, with
	// no per-issuer configuration. The catalog is global and contains no tenant
	// data.
	ListPresets(context.Context, *ListPresetsPayload) (res *ListCimdClientPresetsResult, err error)
	// Allow an additional CIMD document URL on a user_session_issuer, beyond the
	// preset catalog. The URL is validated for
	// draft-ietf-oauth-client-id-metadata-document-02 §3 syntax and rejected
	// outright when malformed. The document itself is deliberately NOT fetched
	// here: a vendor's host being briefly unreachable must not block
	// configuration, and an advisory warning nobody can act on is not worth an
	// outbound request on every write. Call verifyURL first to check that the
	// document is reachable and valid.
	CreateUserSessionIssuerCimdClient(context.Context, *CreateUserSessionIssuerCimdClientPayload) (res *CreateUserSessionIssuerCimdClientResult, err error)
	// Check that a CIMD document URL is reachable and spec-compliant, without
	// saving anything. A pre-flight for create: the same fetch and validation the
	// authorization server performs, reported in full so an operator can fix the
	// URL before adding it. Every probe outcome is a 200 with verified true or
	// false — errors are reserved for a malformed request, missing authorization,
	// or an exceeded rate limit. Rate limited per project, since this is the one
	// endpoint that makes Gram fetch a caller-chosen URL.
	VerifyURL(context.Context, *VerifyURLPayload) (res *VerifyCimdURLResult, err error)
	// List the custom CIMD document URLs configured on a user_session_issuer. Does
	// not include the preset catalog — call listPresets for that.
	ListUserSessionIssuerCimdClients(context.Context, *ListUserSessionIssuerCimdClientsPayload) (res *ListUserSessionIssuerCimdClientsResult, err error)
	// Get a single custom CIMD document URL entry by id.
	GetUserSessionIssuerCimdClient(context.Context, *GetUserSessionIssuerCimdClientPayload) (res *types.UserSessionIssuerCimdClient, err error)
	// Remove a custom CIMD document URL from a user_session_issuer. New
	// authorization requests from that client are denied immediately; sessions
	// already issued to it are unaffected and continue until they expire.
	DeleteUserSessionIssuerCimdClient(context.Context, *DeleteUserSessionIssuerCimdClientPayload) (err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "userSessionIssuersCimdClients"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [6]string{"listPresets", "createUserSessionIssuerCimdClient", "verifyURL", "listUserSessionIssuerCimdClients", "getUserSessionIssuerCimdClient", "deleteUserSessionIssuerCimdClient"}

// CreateUserSessionIssuerCimdClientPayload is the payload type of the
// userSessionIssuersCimdClients service createUserSessionIssuerCimdClient
// method.
type CreateUserSessionIssuerCimdClientPayload struct {
	SessionToken     *string
	ApikeyToken      *string
	ProjectSlugInput *string
	// The user_session_issuer the URL is allowed on.
	UserSessionIssuerID string
	// The exact https URL the client presents as its client_id. Matched byte for
	// byte at authorization time — the spec forbids normalization, so this must be
	// the vendor's published URL exactly.
	ClientIDMetadataURI string
}

// CreateUserSessionIssuerCimdClientResult is the result type of the
// userSessionIssuersCimdClients service createUserSessionIssuerCimdClient
// method.
type CreateUserSessionIssuerCimdClientResult struct {
	Client *types.UserSessionIssuerCimdClient
}

// DeleteUserSessionIssuerCimdClientPayload is the payload type of the
// userSessionIssuersCimdClients service deleteUserSessionIssuerCimdClient
// method.
type DeleteUserSessionIssuerCimdClientPayload struct {
	// The user_session_issuer_cimd_client id.
	ID               string
	SessionToken     *string
	ApikeyToken      *string
	ProjectSlugInput *string
}

// GetUserSessionIssuerCimdClientPayload is the payload type of the
// userSessionIssuersCimdClients service getUserSessionIssuerCimdClient method.
type GetUserSessionIssuerCimdClientPayload struct {
	// The user_session_issuer_cimd_client id.
	ID               string
	SessionToken     *string
	ApikeyToken      *string
	ProjectSlugInput *string
}

// ListCimdClientPresetsResult is the result type of the
// userSessionIssuersCimdClients service listPresets method.
type ListCimdClientPresetsResult struct {
	Items []*types.CimdClientPreset
}

// ListPresetsPayload is the payload type of the userSessionIssuersCimdClients
// service listPresets method.
type ListPresetsPayload struct {
	SessionToken     *string
	ApikeyToken      *string
	ProjectSlugInput *string
}

// ListUserSessionIssuerCimdClientsPayload is the payload type of the
// userSessionIssuersCimdClients service listUserSessionIssuerCimdClients
// method.
type ListUserSessionIssuerCimdClientsPayload struct {
	// The user_session_issuer whose custom CIMD clients are listed.
	UserSessionIssuerID string
	// Pagination cursor: id of the last item from the previous page.
	Cursor *string
	// Page size (default 50, max 100).
	Limit            *int
	SessionToken     *string
	ApikeyToken      *string
	ProjectSlugInput *string
}

// ListUserSessionIssuerCimdClientsResult is the result type of the
// userSessionIssuersCimdClients service listUserSessionIssuerCimdClients
// method.
type ListUserSessionIssuerCimdClientsResult struct {
	Items []*types.UserSessionIssuerCimdClient
	// Cursor for the next page; empty when exhausted.
	NextCursor *string
}

// VerifyCimdURLResult is the result type of the userSessionIssuersCimdClients
// service verifyURL method.
type VerifyCimdURLResult struct {
	// True when the document was fetched and passed every check the authorization
	// server applies. A client presenting this URL will not be rejected for its
	// document.
	Verified bool
	// Why the probe ended as it did.
	Outcome string
	// Status the document endpoint returned; omitted when no response was received.
	HTTPStatus *int
	// Stable machine label for the rule that rejected the document, e.g.
	// client_id_mismatch. Set only for invalid_url and invalid_document.
	Reason *string
	// Human-readable explanation, safe to display to the operator.
	Detail string
	// The document's client_name, set only when verified. Lets an operator confirm
	// the URL names the client they intended.
	ClientName *string
}

// VerifyURLPayload is the payload type of the userSessionIssuersCimdClients
// service verifyURL method.
type VerifyURLPayload struct {
	// The https URL to probe.
	ClientIDMetadataURI string
	SessionToken        *string
	ApikeyToken         *string
	ProjectSlugInput    *string
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}
