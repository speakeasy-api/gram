// Code generated by goa v3.25.3, DO NOT EDIT.
//
// adminOpenRouterKeys service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package adminopenrouterkeys

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Platform-admin management of per-organization platform OpenRouter keys.
// Speakeasy-staff only; every method requires the platform-admin flag.
type Service interface {
	// List every organization's platform OpenRouter keys. Requires platform admin.
	ListKeys(context.Context, *ListKeysPayload) (res *ListKeysResult, err error)
	// Fetch an organization's live credit usage from OpenRouter for one key.
	// Requires platform admin.
	GetKeyUsage(context.Context, *GetKeyUsagePayload) (res *GetKeyUsageResult, err error)
	// Lock down an organization's platform OpenRouter key, upstream and locally.
	// Requires platform admin.
	DisableKey(context.Context, *DisableKeyPayload) (res *AdminOpenRouterKey, err error)
	// Reinstate a disabled platform OpenRouter key, upstream and locally, keeping
	// its recorded credit ceiling. Requires platform admin.
	EnableKey(context.Context, *EnableKeyPayload) (res *AdminOpenRouterKey, err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "adminOpenRouterKeys"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [4]string{"listKeys", "getKeyUsage", "disableKey", "enableKey"}

// AdminOpenRouterKey is the result type of the adminOpenRouterKeys service
// disableKey method.
type AdminOpenRouterKey struct {
	// Organization that owns the key.
	OrganizationID string
	// Display name of the owning organization.
	OrganizationName string
	// Slug of the owning organization.
	OrganizationSlug string
	// The organization's Gram account type (e.g. free, pro, enterprise).
	GramAccountType string
	// Which upstream key this row provisions: 'chat' pays for customer-facing
	// completions, 'internal' pays for platform-initiated LLM usage.
	KeyType string
	// Monthly credit ceiling last mirrored from OpenRouter.
	MonthlyCredits int64
	// Whether the key is locked down (refused locally and disabled upstream).
	Disabled bool
	// Administrative view of every reason currently disabling the key. Values are
	// open-ended for forward compatibility.
	DisableCauses []string
	// When the key row was created.
	CreatedAt string
	// When the key row was last updated.
	UpdatedAt string
}

// DisableKeyPayload is the payload type of the adminOpenRouterKeys service
// disableKey method.
type DisableKeyPayload struct {
	SessionToken *string
	// Organization that owns the key.
	OrganizationID string
	// Key type to disable.
	KeyType string
}

// EnableKeyPayload is the payload type of the adminOpenRouterKeys service
// enableKey method.
type EnableKeyPayload struct {
	SessionToken *string
	// Organization that owns the key.
	OrganizationID string
	// Key type to enable.
	KeyType string
}

// GetKeyUsagePayload is the payload type of the adminOpenRouterKeys service
// getKeyUsage method.
type GetKeyUsagePayload struct {
	SessionToken *string
	// Organization that owns the key.
	OrganizationID string
	// Key type to inspect.
	KeyType string
}

// Live usage as reported by OpenRouter at request time.
type GetKeyUsageResult struct {
	// Credits spent this month, rounded to cents.
	CreditsUsed float64
	// Monthly credit ceiling recorded locally.
	MonthlyCredits int64
	// Monthly limit as configured upstream; absent when OpenRouter reports an
	// unlimited key.
	UpstreamLimit *int64
}

// ListKeysPayload is the payload type of the adminOpenRouterKeys service
// listKeys method.
type ListKeysPayload struct {
	SessionToken *string
}

// ListKeysResult is the result type of the adminOpenRouterKeys service
// listKeys method.
type ListKeysResult struct {
	// Every live platform OpenRouter key, ordered by organization slug then key
	// type.
	Keys []*AdminOpenRouterKey
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}
