// Code generated by goa v3.25.3, DO NOT EDIT.
//
// platformMcp service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package platformmcp

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Session-authenticated onboarding and lifecycle projection for the
// organization-level Gram Platform MCP.
type Service interface {
	// Get the current user's safe Platform MCP onboarding projection for the
	// active organization.
	GetOnboarding(context.Context, *GetOnboardingPayload) (res *PlatformMCPOnboardingState, err error)
	// Create or resume the current user's durable Platform MCP onboarding workflow.
	StartOnboarding(context.Context, *StartOnboardingPayload) (res *PlatformMCPOnboardingState, err error)
	// Record a selected manual-install client family for the current user's
	// Platform MCP workflow.
	RecordInstallIntent(context.Context, *RecordInstallIntentPayload) (res *PlatformMCPOnboardingState, err error)
	// Record that the user copied the displayed Platform MCP configuration or
	// completed an equivalent supported agent-setup action.
	RecordAgentConfigurationCopied(context.Context, *RecordAgentConfigurationCopiedPayload) (res *PlatformMCPOnboardingState, err error)
	// Return the secure setup continuation for the workflow-bound registration.
	// Browser Catalogue registrations return their existing same-origin dashboard
	// Inspect page; the local fixture returns a one-time handoff for its synthetic
	// provider setup endpoint.
	StartOnboardingSetup(context.Context, *StartOnboardingSetupPayload) (res *PlatformMCPOnboardingSetupHandoff, err error)
	// Force a rate-limited authenticated readiness recheck for the workflow-bound
	// local registration.
	RecheckOnboardingReadiness(context.Context, *RecheckOnboardingReadinessPayload) (res *PlatformMCPOnboardingState, err error)
	// Attach the workflow-bound ready local MCP to the selected project's existing
	// Default plugin. The caller supplies only the selected project slug and its
	// opaque server-issued version token.
	DistributeOnboardingCandidate(context.Context, *DistributeOnboardingCandidatePayload) (res *PlatformMCPOnboardingState, err error)
	// Remove only the workflow-bound MCP from the selected project's existing
	// Default plugin. Registration, readiness, connection, and prior evidence
	// remain intact.
	RemoveOnboardingDistribution(context.Context, *RemoveOnboardingDistributionPayload) (res *PlatformMCPOnboardingState, err error)
	// Retry in-memory local package publication for the workflow-bound
	// distribution without changing its attachment or version.
	RepairOnboardingPublication(context.Context, *RepairOnboardingPublicationPayload) (res *PlatformMCPOnboardingState, err error)
	// Dismiss the optional current user's Platform MCP onboarding workflow without
	// changing organization setup or project resources.
	DismissOnboarding(context.Context, *DismissOnboardingPayload) (err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "platformMcp"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [10]string{"getOnboarding", "startOnboarding", "recordInstallIntent", "recordAgentConfigurationCopied", "startOnboardingSetup", "recheckOnboardingReadiness", "distributeOnboardingCandidate", "removeOnboardingDistribution", "repairOnboardingPublication", "dismissOnboarding"}

// DismissOnboardingPayload is the payload type of the platformMcp service
// dismissOnboarding method.
type DismissOnboardingPayload struct {
	SessionToken *string
}

// DistributeOnboardingCandidatePayload is the payload type of the platformMcp
// service distributeOnboardingCandidate method.
type DistributeOnboardingCandidatePayload struct {
	// Exact workflow-selected project slug.
	ProjectSlug string
	// Opaque current distribution version token from getOnboarding.
	ExpectedVersion string
	SessionToken    *string
}

// GetOnboardingPayload is the payload type of the platformMcp service
// getOnboarding method.
type GetOnboardingPayload struct {
	SessionToken *string
}

// PlatformMCPOnboardingSetupHandoff is the result type of the platformMcp
// service startOnboardingSetup method.
type PlatformMCPOnboardingSetupHandoff struct {
	// One-time opaque local-fixture setup handoff.
	Handoff *string
	// Server-owned same-origin dashboard Inspect URL for the persisted browser
	// Catalogue registration.
	DashboardSetupURL *string
}

// PlatformMCPOnboardingState is the result type of the platformMcp service
// getOnboarding method.
type PlatformMCPOnboardingState struct {
	// Whether the active organization currently passes the Platform MCP capability
	// and rollout gates.
	Enabled bool
	// Current server-derived onboarding stage.
	Stage string
	// Canonical Platform MCP endpoint URL supplied by the server.
	McpURL string
	// Whether the authenticated user has an active onboarding workflow in this
	// organization.
	WorkflowActive bool
	// Selected manual-install client family when a workflow is active.
	ClientFamily string
	// Whether the user copied Platform MCP configuration or completed an
	// equivalent supported agent-setup action in this workflow.
	AgentConfigurationCopied bool
	// Whether the authenticated user has a current authorized Platform MCP
	// connection.
	ConnectionAuthorized bool
	// Bounded authorization state for the authenticated user's latest Platform MCP
	// connection.
	ConnectionAuthState string
	// Bounded reason why interactive Platform MCP authorization is required again.
	ReauthorizationReason string
	// Whether the authenticated user's current connection has completed
	// authenticated discovery.
	ConnectionReady bool
	// Whether the active workflow observed a successful reviewed MCP Catalog
	// search through Platform MCP.
	CatalogExplored bool
	// Display name of the workflow-bound project, when one has been selected.
	SelectedProjectName string
	// Slug of the workflow-bound project, when one has been selected.
	SelectedProjectSlug string
	// Whether the server-owned local candidate is registered for the selected
	// project.
	RegistrationComplete bool
	// Bounded current readiness state for the selected registration.
	ReadinessState string
	// Whether selected-registration readiness is currently fresh.
	ReadinessFreshness string
	// Bounded lifecycle state for the selected MCP's existing-Default-plugin
	// distribution.
	DistributionState string
	// Whether the selected MCP is currently attached to the selected project's
	// existing Default plugin.
	DistributionAttached bool
	// Whether Platform MCP successfully added the workflow-bound MCP to the
	// selected project's existing Default plugin.
	DistributionToolSucceeded bool
	// Whether Platform MCP observed a successful ready status check for the
	// workflow-bound MCP.
	ReadinessVerified bool
	// Bounded package-publication state for the selected project's Default plugin
	// after a distribution change.
	DistributionPublicationState string
	// Whether the server observed a successful normal Remote MCP tool call for the
	// currently attached selected distribution version.
	SelectedUseVerified bool
	// Opaque server-issued version token required by distribute, remove, or
	// publication-repair mutations. It binds the selected project and
	// authenticated user without exposing internal IDs or counters.
	DistributionExpectedVersion string
	// A bounded next action when the workflow cannot progress.
	RepairAction string
}

// RecheckOnboardingReadinessPayload is the payload type of the platformMcp
// service recheckOnboardingReadiness method.
type RecheckOnboardingReadinessPayload struct {
	SessionToken *string
}

// RecordAgentConfigurationCopiedPayload is the payload type of the platformMcp
// service recordAgentConfigurationCopied method.
type RecordAgentConfigurationCopiedPayload struct {
	SessionToken *string
}

// RecordInstallIntentPayload is the payload type of the platformMcp service
// recordInstallIntent method.
type RecordInstallIntentPayload struct {
	// Manual-install client family.
	ClientFamily string
	SessionToken *string
}

// RemoveOnboardingDistributionPayload is the payload type of the platformMcp
// service removeOnboardingDistribution method.
type RemoveOnboardingDistributionPayload struct {
	// Exact workflow-selected project slug.
	ProjectSlug string
	// Opaque current distribution version token from getOnboarding.
	ExpectedVersion string
	SessionToken    *string
}

// RepairOnboardingPublicationPayload is the payload type of the platformMcp
// service repairOnboardingPublication method.
type RepairOnboardingPublicationPayload struct {
	// Exact workflow-selected project slug.
	ProjectSlug string
	// Opaque current distribution version token from getOnboarding.
	ExpectedVersion string
	SessionToken    *string
}

// StartOnboardingPayload is the payload type of the platformMcp service
// startOnboarding method.
type StartOnboardingPayload struct {
	SessionToken *string
}

// StartOnboardingSetupPayload is the payload type of the platformMcp service
// startOnboardingSetup method.
type StartOnboardingSetupPayload struct {
	SessionToken *string
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}
