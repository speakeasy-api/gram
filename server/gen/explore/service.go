// Code generated by goa v3.25.3, DO NOT EDIT.
//
// explore service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/design

package explore

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Authority-aware ad-hoc analytics over canonical event and usage datasets,
// with saved queries.
type Service interface {
	// Describe the labeled semantic datasets and typed fields available to Explore.
	Meta(context.Context, *MetaPayload) (res *ExploreMetaResult, err error)
	// Run an organization-scoped query over one semantic dataset. Observations are
	// canonicalized by field authority before filters, grouping, and calculations.
	Query(context.Context, *QueryPayload) (res *ExploreQueryResult, err error)
	// List a semantic dataset dimension's most frequent canonical values inside a
	// time window.
	DimensionValues(context.Context, *DimensionValuesPayload) (res *DimensionValuesResult, err error)
	// List the organization's saved Explore queries.
	ListSavedQueries(context.Context, *ListSavedQueriesPayload) (res *ListSavedQueriesResult, err error)
	// Save a dataset-and-calculations query to the organization's dashboard.
	CreateSavedQuery(context.Context, *CreateSavedQueryPayload) (res *ExploreSavedQuery, err error)
	// Update a saved dataset-and-calculations query in place.
	UpdateSavedQuery(context.Context, *UpdateSavedQueryPayload) (res *ExploreSavedQuery, err error)
	// Delete a saved Explore query.
	DeleteSavedQuery(context.Context, *DeleteSavedQueryPayload) (err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// APIKeyAuth implements the authorization logic for the APIKey security scheme.
	APIKeyAuth(ctx context.Context, key string, schema *security.APIKeyScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "explore"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [7]string{"meta", "query", "dimensionValues", "listSavedQueries", "createSavedQuery", "updateSavedQuery", "deleteSavedQuery"}

// CreateSavedQueryPayload is the payload type of the explore service
// createSavedQuery method.
type CreateSavedQueryPayload struct {
	// Display name of the saved query
	Name string
	// How the dashboard renders the results
	ChartType string
	// Relative time window the query runs over
	Window string
	// Semantic dataset to query
	Dataset string
	// Aggregate op(column) calculations to compute over the dataset
	Calculations []*ExploreCalculation
	// Dimensions to break results down by, in order. Empty returns a single
	// aggregate group.
	GroupBy []string
	// Named conditional grouping axes evaluated independently on canonical rows.
	// Each produces true and false groups without filtering rows.
	GroupExpressions []*ExploreGroupExpression
	// Filter predicates on canonical fields. All are ANDed together.
	Filters []*ExploreFilter
	// Timeseries bucket width in seconds. Zero or omitted aggregates the whole
	// range into a table.
	GranularitySeconds *int64
	// Calculation used to rank table rows, referenced by canonical name such as
	// SUM(input_tokens). Must be requested by the query and is ignored for
	// timeseries.
	SortBy *string
	// Sort descending when sort_by is set.
	SortDesc bool
	// Keep at most this many table rows after ranking. Zero keeps everything.
	Limit        int
	SessionToken *string
}

// DeleteSavedQueryPayload is the payload type of the explore service
// deleteSavedQuery method.
type DeleteSavedQueryPayload struct {
	// Saved query ID
	ID           string
	SessionToken *string
}

// DimensionValuesPayload is the payload type of the explore service
// dimensionValues method.
type DimensionValuesPayload struct {
	// Semantic dataset carrying the dimension
	Dataset string
	// Dimension to list values for
	Dimension string
	// Start time in ISO 8601 format
	From string
	// End time in ISO 8601 format
	To           string
	SessionToken *string
}

// DimensionValuesResult is the result type of the explore service
// dimensionValues method.
type DimensionValuesResult struct {
	// Distinct non-empty values, most frequent first, capped at 50
	Values []string
}

// One aggregate operation over a semantic dataset field.
type ExploreCalculation struct {
	// Aggregate operation
	Op string
	// Target field. Omit for COUNT.
	Column *string
}

// One labeled semantic dataset and its typed fields.
type ExploreDatasetSchema struct {
	// Stable dataset ID
	Name string
	// Customer-facing display name
	Label string
	// Customer-facing dataset category
	Category string
	// What the dataset contains
	Description string
	// What one canonical row represents
	Grain string
	// Typed fields in display order
	Fields []*ExploreFieldMeta
}

// One labeled, typed field of a semantic dataset.
type ExploreFieldMeta struct {
	// Stable field name
	Name string
	// Customer-facing display name
	Label string
	// Value type
	Type string
	// Field role
	Role string
	// Measure unit, or empty for dimensions
	Unit string
	// What the field carries
	Description string
	// Row-filter operators legal on the field
	FilterOps []string
}

// One filter predicate over a canonical dataset field.
type ExploreFilter struct {
	// Field to filter on: a dimension, or a measure for numeric operators
	Dimension string
	// Filter operator. String fields take in, not_in, contains, or exists.
	// Measures take eq, neq, gt, gte, lt, or lte.
	Op string
	// Operator operands. Numeric comparisons and contains use the first value.
	// Exists ignores values.
	Values []string
}

// One named conditional grouping axis evaluated over a canonical dataset field.
type ExploreGroupExpression struct {
	// Customer-facing name returned in the result group_by tuple
	Name string
	// Field whose value is tested
	Dimension string
	// Type-appropriate predicate operator. String fields take in, not_in,
	// contains, or exists. Measures take eq, neq, gt, gte, lt, or lte.
	Op string
	// Predicate operands. Numeric comparisons and contains use the first value.
	// Exists ignores values.
	Values []string
}

// ExploreMetaResult is the result type of the explore service meta method.
type ExploreMetaResult struct {
	// Labeled semantic dataset schemas in display order
	Datasets []*ExploreDatasetSchema
}

// ExploreQueryResult is the result type of the explore service query method.
type ExploreQueryResult struct {
	// Canonical names of the calculations answered, in request order
	Calculations []string
	// Semantic dataset that answered the query
	Dataset string
	// Group-by dimensions answered, in request order
	GroupBy []string
	// Timeseries bucket width. Zero for table queries.
	GranularitySeconds int64
	// Rows ordered by bucket and group for timeseries, or by sort_by for tables
	Rows []*ExploreRow
}

// One result row of an Explore query.
type ExploreRow struct {
	// Timeseries bucket start in ISO 8601. Empty for whole-range table queries.
	Bucket string
	// Group values parallel to group_by. Empty string means the canonical
	// dimension is empty.
	Group []string
	// Values keyed by canonical calculation name, such as SUM(input_tokens).
	Values map[string]float64
}

// ExploreSavedQuery is the result type of the explore service createSavedQuery
// method.
type ExploreSavedQuery struct {
	// Saved query ID
	ID string
	// Display name of the saved query
	Name string
	// How the dashboard renders the results
	ChartType string
	// Relative time window the query runs over
	Window string
	// Semantic dataset to query
	Dataset string
	// Aggregate op(column) calculations to compute over the dataset
	Calculations []*ExploreCalculation
	// Dimensions to break results down by, in order. Empty returns a single
	// aggregate group.
	GroupBy []string
	// Named conditional grouping axes evaluated independently on canonical rows.
	// Each produces true and false groups without filtering rows.
	GroupExpressions []*ExploreGroupExpression
	// Filter predicates on canonical fields. All are ANDed together.
	Filters []*ExploreFilter
	// Timeseries bucket width in seconds. Zero or omitted aggregates the whole
	// range into a table.
	GranularitySeconds int64
	// Calculation used to rank table rows, referenced by canonical name such as
	// SUM(input_tokens). Must be requested by the query and is ignored for
	// timeseries.
	SortBy string
	// Sort descending when sort_by is set.
	SortDesc bool
	// Keep at most this many table rows after ranking. Zero keeps everything.
	Limit int
	// Creation time in ISO 8601 format
	CreatedAt string
	// Last update time in ISO 8601 format
	UpdatedAt string
}

// ListSavedQueriesPayload is the payload type of the explore service
// listSavedQueries method.
type ListSavedQueriesPayload struct {
	SessionToken *string
}

// ListSavedQueriesResult is the result type of the explore service
// listSavedQueries method.
type ListSavedQueriesResult struct {
	// Saved queries, most recently updated first
	Queries []*ExploreSavedQuery
}

// MetaPayload is the payload type of the explore service meta method.
type MetaPayload struct {
	SessionToken *string
}

// QueryPayload is the payload type of the explore service query method.
type QueryPayload struct {
	// Start time in ISO 8601 format
	From string
	// End time in ISO 8601 format
	To string
	// Semantic dataset to query
	Dataset string
	// Aggregate op(column) calculations to compute over the dataset
	Calculations []*ExploreCalculation
	// Dimensions to break results down by, in order. Empty returns a single
	// aggregate group.
	GroupBy []string
	// Named conditional grouping axes evaluated independently on canonical rows.
	// Each produces true and false groups without filtering rows.
	GroupExpressions []*ExploreGroupExpression
	// Filter predicates on canonical fields. All are ANDed together.
	Filters []*ExploreFilter
	// Timeseries bucket width in seconds. Zero or omitted aggregates the whole
	// range into a table.
	GranularitySeconds *int64
	// Calculation used to rank table rows, referenced by canonical name such as
	// SUM(input_tokens). Must be requested by the query and is ignored for
	// timeseries.
	SortBy *string
	// Sort descending when sort_by is set.
	SortDesc bool
	// Keep at most this many table rows after ranking. Zero keeps everything.
	Limit        int
	SessionToken *string
}

// UpdateSavedQueryPayload is the payload type of the explore service
// updateSavedQuery method.
type UpdateSavedQueryPayload struct {
	// Saved query ID
	ID string
	// Display name of the saved query
	Name string
	// How the dashboard renders the results
	ChartType string
	// Relative time window the query runs over
	Window string
	// Semantic dataset to query
	Dataset string
	// Aggregate op(column) calculations to compute over the dataset
	Calculations []*ExploreCalculation
	// Dimensions to break results down by, in order. Empty returns a single
	// aggregate group.
	GroupBy []string
	// Named conditional grouping axes evaluated independently on canonical rows.
	// Each produces true and false groups without filtering rows.
	GroupExpressions []*ExploreGroupExpression
	// Filter predicates on canonical fields. All are ANDed together.
	Filters []*ExploreFilter
	// Timeseries bucket width in seconds. Zero or omitted aggregates the whole
	// range into a table.
	GranularitySeconds *int64
	// Calculation used to rank table rows, referenced by canonical name such as
	// SUM(input_tokens). Must be requested by the query and is ignored for
	// timeseries.
	SortBy *string
	// Sort descending when sort_by is set.
	SortDesc bool
	// Keep at most this many table rows after ranking. Zero keeps everything.
	Limit        int
	SessionToken *string
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeForbidden builds a goa.ServiceError from an error.
func MakeForbidden(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "forbidden", false, false, false)
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}

// MakeUnsupportedMedia builds a goa.ServiceError from an error.
func MakeUnsupportedMedia(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unsupported_media", false, false, false)
}

// MakeInvalid builds a goa.ServiceError from an error.
func MakeInvalid(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invalid", false, false, false)
}

// MakeInvariantViolation builds a goa.ServiceError from an error.
func MakeInvariantViolation(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "invariant_violation", false, false, true)
}

// MakeUnexpected builds a goa.ServiceError from an error.
func MakeUnexpected(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unexpected", false, false, true)
}

// MakeGatewayError builds a goa.ServiceError from an error.
func MakeGatewayError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "gateway_error", false, false, true)
}
