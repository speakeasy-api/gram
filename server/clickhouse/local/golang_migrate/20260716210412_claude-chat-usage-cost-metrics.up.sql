-- Superseded before deployment by 20260717043657_claude-chat-read-dedup.up.sql,
-- which retains Claude Chat events for read-time deduplication instead of
-- adding them to a row-summing materialized view.
SELECT 1;
