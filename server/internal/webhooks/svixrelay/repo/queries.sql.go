// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package repo

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getWebhookEnabledOrg = `-- name: GetWebhookEnabledOrg :one
SELECT id, svix_app_id
FROM organization_metadata
WHERE id = $1
  AND webhooks_enabled IS TRUE
  AND svix_app_id IS NOT NULL
  AND svix_app_id <> ''
  AND disabled_at IS NULL
`

type GetWebhookEnabledOrgRow struct {
	ID        string
	SvixAppID pgtype.Text
}

// Whether one organization is eligible for webhook delivery. Cached per
// organization for a short TTL, so the common case — an event for an
// organization that has never enabled webhooks — costs one query per
// organization per interval rather than one per message.
func (q *Queries) GetWebhookEnabledOrg(ctx context.Context, id string) (GetWebhookEnabledOrgRow, error) {
	row := q.db.QueryRow(ctx, getWebhookEnabledOrg, id)
	var i GetWebhookEnabledOrgRow
	err := row.Scan(&i.ID, &i.SvixAppID)
	return i, err
}
