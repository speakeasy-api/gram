// Code generated by goa v3.25.3, DO NOT EDIT.
//
// organizationRoles service
//
// Command:
// $ goa gen github.com/speakeasy-api/gram/server/internal/devidp/design -o
// internal/devidp

package organizationroles

import (
	"context"
)

// Dev-idp organization-role CRUD. Mirrors the WorkOS authorization-role
// surface (idp-design.md §7.1,
// /authorization/organizations/{id}/roles[/{slug}]). Keyed on
// `(organization_id, slug)` since slug is the natural identifier; the
// underlying row's UUID is also returned for completeness. Permanently
// unauthenticated.
type Service interface {
	// Create a role on an organization.
	Create(context.Context, *CreatePayload) (res *OrganizationRole, err error)
	// Patch a role's name and/or description by (organization_id, slug).
	Update(context.Context, *UpdatePayload) (res *OrganizationRole, err error)
	// List every role on an organization. No pagination — roles are a small
	// per-org set.
	List(context.Context, *ListPayload) (res *ListOrganizationRolesResult, err error)
	// Delete a role from an organization by (organization_id, slug).
	Delete(context.Context, *DeletePayload) (err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "gram-dev-idp"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "organizationRoles"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [4]string{"create", "update", "list", "delete"}

// CreatePayload is the payload type of the organizationRoles service create
// method.
type CreatePayload struct {
	// Organization UUID.
	OrganizationID string
	// Role slug — unique within the org.
	Slug string
	// Display name.
	Name string
	// Free-form description.
	Description *string
}

// DeletePayload is the payload type of the organizationRoles service delete
// method.
type DeletePayload struct {
	// Organization UUID.
	OrganizationID string
	// Role slug.
	Slug string
}

// ListOrganizationRolesResult is the result type of the organizationRoles
// service list method.
type ListOrganizationRolesResult struct {
	// Roles for the organization.
	Items []*OrganizationRole
}

// ListPayload is the payload type of the organizationRoles service list method.
type ListPayload struct {
	// Organization UUID.
	OrganizationID string
}

// OrganizationRole is the result type of the organizationRoles service create
// method.
type OrganizationRole struct {
	// Role UUID.
	ID string
	// Organization UUID.
	OrganizationID string
	// Role slug — unique within the organization.
	Slug string
	// Display name.
	Name string
	// Free-form description; empty string when unset.
	Description string
	CreatedAt   string
	UpdatedAt   string
}

// UpdatePayload is the payload type of the organizationRoles service update
// method.
type UpdatePayload struct {
	// Organization UUID.
	OrganizationID string
	// Role slug.
	Slug        string
	Name        *string
	Description *string
}
