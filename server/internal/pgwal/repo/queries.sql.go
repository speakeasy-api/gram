// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package repo

import (
	"context"
)

const currentWALLSN = `-- name: CurrentWALLSN :one
SELECT pg_current_wal_lsn()::text AS lsn
`

// Returns the primary's current WAL insert LSN in text form.
func (q *Queries) CurrentWALLSN(ctx context.Context) (string, error) {
	row := q.db.QueryRow(ctx, currentWALLSN)
	var lsn string
	err := row.Scan(&lsn)
	return lsn, err
}

const wALReplayCaughtUp = `-- name: WALReplayCaughtUp :one
SELECT COALESCE(pg_last_wal_replay_lsn() >= CAST($1 AS text)::pg_lsn, TRUE)::boolean AS caught_up
`

// True once this connection's replayed WAL LSN has reached target_lsn. On a
// primary (pg_last_wal_replay_lsn() IS NULL) this always returns true.
// target_lsn is cast text -> pg_lsn so sqlc types the parameter as a Go string
// (pg_lsn has no Go/pgx mapping) while Postgres does the comparison as pg_lsn.
func (q *Queries) WALReplayCaughtUp(ctx context.Context, targetLsn string) (bool, error) {
	row := q.db.QueryRow(ctx, wALReplayCaughtUp, targetLsn)
	var caught_up bool
	err := row.Scan(&caught_up)
	return caught_up, err
}
